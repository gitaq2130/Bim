# BuildTwin — CLAUDE.md

> 이 파일은 BuildTwin 프로젝트의 단일 진실 원천(Single Source of Truth)이다.
> 모든 에이전트(사람·Claude 서브에이전트)는 작업 전에 이 문서를 읽고 따른다.
> 이 문서와 충돌하는 결정은 반드시 `docs/adr/`에 ADR로 남겨야 유효하다.

---

## 0. 프로젝트 정의 (변경 금지)

BuildTwin은 건설 PM/CM 회사를 위한 AI PM 플랫폼이다. 핵심은 3D 뷰어가 아니라 **"계획 상태 / 실제 상태 / 전문가 판단 상태 / 승인 상태"를 객체 단위로 비교하는 데이터 구조**다.

MVP 범위(이 범위 밖은 구현하지 않는다):

1. **도면 업로드·인식**: IFC(1순위), DWG/DXF(2D), RVT(3순위 — 아래 제약 참고)를 업로드하면 객체(기둥·보·슬래브·벽·덕트·배관·케이블트레이·외장패널)를 추출해 DB에 저장한다.
2. **2D↔3D 상호작용**: 3D 객체를 클릭하면 대응하는 2D 도면 위치가 하이라이트되고, 2D 도면에서 영역/객체를 선택하면 3D가 해당 객체로 이동·하이라이트된다. 3D 모델에서 층별 평면 단면을 자동 생성해 2D와 겹쳐 볼 수 있어야 한다.
3. **현장 스캔 비교**: 포인트클라우드(E57/LAS/PLY)를 업로드하면 BIM 좌표계에 정합하고, 객체별로 `미시공 / 시공중 / 완료추정 / 위치불일치 / 확인불가(가림)`를 판정한다.
4. **후공정 확인**: 공정표(CSV/XML/P6·MS Project export)를 객체와 연결하고, 각 작업의 **Work Readiness Score**(선행공정·검측·자재·도면승인·간섭)를 계산해 다음 착수 가능 작업과 차단 원인을 제시한다.
5. **3중 검증**: 시공사 신고(작업일보 입력) / 물리적 증거(스캔) / 시스템 논리(BIM 수량·선후행·자재 입출고)가 불일치하면 자동 확정을 막고 CM 확인 요청을 생성한다.

핵심 원칙:

- 스캔 AI는 **"완료 추정"까지만** 판정한다. "확정 완료"는 반드시 사람(CM) 승인 액션을 거친다.
- 모든 객체는 IFC GlobalId를 1차 키로 쓰고, 2D 엔티티·공정 Activity·자재·스캔 판정은 이 키에 매달린다.
- 모든 판정에는 `confidence`(0~1)와 `evidence`(근거 파일·좌표·규칙 ID)를 남긴다.

기술 제약(반드시 지킬 것):

- **RVT 파일은 네이티브 파싱 불가.** Revit 없이 서버에서 열 수 없다. 선택지는 ① 사용자에게 IFC 내보내기 요구 ② Autodesk Platform Services(APS) Model Derivative API로 변환 ③ Revit 애드인(pyRevit/C#)으로 IFC+메타데이터 내보내기. MVP는 ①+②로 가고, ③은 ADR로 남긴다.
- DWG는 ODA(Open Design Alliance) 라이선스 없이 직접 파싱하기 어렵다. MVP는 **DXF 우선**, DWG는 ODA File Converter 또는 APS로 DXF 변환 후 처리한다.
- 포인트클라우드 정합은 자동 ICP만 믿지 않는다. 현장 기준점(최소 3점) 또는 마커(AprilTag/QR) 좌표를 사용자가 입력하는 경로를 반드시 둔다.

---

## 1. 기술 스택 (기본값 — `architect`가 ADR로 바꿀 수 있음)

| 영역 | 기본 선택 | 이유 |
|---|---|---|
| Backend | Python 3.12, FastAPI, SQLAlchemy, PostgreSQL + PostGIS | 3D 공간 쿼리, IfcOpenShell·Open3D가 Python |
| BIM 파싱 | IfcOpenShell, ezdxf | IFC 기하·속성 추출, DXF 엔티티 추출 |
| 3D 뷰어 | xeokit-sdk 또는 web-ifc + three.js | 브라우저 IFC 렌더, 객체 ID 기반 선택 |
| 2D 뷰어 | SVG/Canvas (DXF→SVG 변환) | 엔티티 단위 이벤트 바인딩 |
| 포인트클라우드 | Open3D (정합), potree 또는 three.js PointsMaterial (뷰) | |
| 공정 최적화 | OR-Tools (CP-SAT) | 선후행 제약·자원 제약 |
| Frontend | React + TypeScript + Vite | |
| 작업큐 | Celery + Redis | 파싱·정합은 비동기 |
| 저장소 | MinIO(S3 호환) | 원본 파일·스캔 |
| 테스트 | pytest, Playwright | |

---

## 2. 디렉터리 구조와 소유 에이전트

```
buildtwin/
├── CLAUDE.md                      # (architect) 이 문서
├── Makefile                       # (qa) make dev / make test / make lint
├── .claude/agents/                # (architect) 서브에이전트 정의
├── apps/web/                      # (frontend) React+TS+Vite 앱
│   └── src/
│       ├── viewer3d/              # (viewer-3d) IFC 3D 뷰어 모듈
│       ├── viewer2d/              # (viewer-2d) DXF→SVG 2D 뷰어 모듈
│       ├── sync/                  # (sync-2d3d, 클라이언트 파트) 뷰어 이벤트 브로커·selection 슬라이스
│       └── ...                    # (frontend) 화면·스토어·API 클라이언트 — 뷰어는 index 재수출만 import
├── services/
│   ├── ingest/                    # (bim-ingest) IFC/DXF/RVT → 객체·엔티티 추출
│   ├── sync/                      # (sync-2d3d) 2D↔3D 매핑, 뷰어 이벤트 브로커
│   ├── scan/                      # (reality-capture) 포인트클라우드 정합·객체 판정
│   ├── progress/                  # (progress-engine) 공정표·상태기계·Readiness·3중 검증
│   ├── knowledge/                 # (knowledge) 규칙 엔진·사례 DB·전문가 검토 로그
│   ├── api/                       # (api) FastAPI 라우터·인증·업로드·작업 폴링 — 도메인 로직 금지(서비스 호출만)
│   └── common/                    # (architect) 공용 인프라: celery_app.py, safe_expr.py(knowledge가 구현·유지)
├── packages/core/
│   ├── models/                    # (architect) 공용 데이터 모델(SQLAlchemy/Pydantic)
│   ├── db.py                      # (architect) 엔진·세션
│   └── settings.py                # (architect) 환경 설정(.env)
├── rules/                         # (knowledge) 판단 규칙 YAML, verification.yaml
├── config/                        # 파일별 담당은 config/README.md 참조(readiness/resources/wbs=progress-engine, scan=reality-capture, sync=sync-2d3d, ingest=bim-ingest)
├── docs/
│   ├── adr/                       # (architect) Architecture Decision Records
│   ├── plans/                     # (architect) 작업 분배 계획 `NNNN-*.md` (형식은 .claude/agents/architect.md 출력 계약)
│   ├── glossary.md                # (모두 등록 / architect 승인) 도메인 용어집
│   └── api.md                     # (api) OpenAPI에서 자동 생성
└── tests/                         # (qa) 단위·통합·E2E, fixtures/, metrics.json
    └── fixtures/                  # (qa) 샘플 IFC/DXF/E57
```

소유 규칙:

- 각 에이전트는 **자기 담당 디렉터리만** 수정한다. 밖을 고쳐야 하면 `architect`에게 먼저 제안하고, `architect`가 계획에 명시한 경우에만 수정한다.
- `packages/core/models/`는 `architect`가 소유하되, 구현 에이전트는 필드 추가를 **제안**할 수 있다(직접 수정 금지).
- `docs/glossary.md`는 누구나 항목을 **추가**할 수 있으나 기존 항목 변경은 `architect` 승인이 필요하다.
- **`packages/core/models/` 를 건드리는 변경은 같은 PR 안에서 `architect` 단독 커밋으로 뗀다**(주석
  한 줄이어도). `reviewer` 체크 3 은 "`packages/core/models/` 수정은 `architect` 커밋이 아니면 FAIL"인데
  (`.claude/agents/reviewer.md`), 한 커밋에 여러 소유가 섞이면 그 판정이 **커밋 경계**가 아니라 **커밋
  본문·계획의 명시 허가를 읽는 일**이 된다(2026-09-04 `e798910` 이 이 디렉터리와 `apps/web/`·`tests/` 를
  한 커밋에 섞었다). 하필 §6-1 이 **이름 붙은 블라인드 스팟**으로 지목한 디렉터리라, 여기만은 감사를
  기계적으로 만들 값이 있다.
  - *역방향 확인 — 이 관례가 밀어내는 것.* 모델 필드 변경과 그 소비자 수정은 **원자적으로 머지**돼야
    HEAD 가 빨개지지 않는다. 그래서 요구하는 것은 **PR 분리가 아니라 커밋 분리**다 — 한 PR 안에서
    커밋만 떼면 원자성은 그대로이고 소유 감사만 기계적이 된다. 커밋을 뗄 수 없는 변경(같은 파일의 같은
    줄을 함께 고쳐야 하는 경우)은 **그 이유를 커밋 본문에 적는다** — 예외를 없애는 규칙이 아니라 예외를
    **보이게** 하는 규칙이다.

---

## 3. 공통 규칙

1. **커밋 전 `make test` 통과.** 실패하는 테스트가 있으면 커밋하지 않는다. 테스트를 skip/disable해서 통과시키는 것은 금지.
2. **새 도메인 개념은 `docs/glossary.md`에 한국어+영어로 등록**한다. 코드 식별자·API 필드명·UI 라벨은 glossary의 영어 표기를 그대로 쓴다.
3. **판정 로직에는 항상 `confidence`(float 0~1)와 `evidence`(근거) 필드**가 있어야 한다. 스캔 판정, 2D↔3D 매핑, Activity↔객체 매핑, 규칙 엔진 판정, 3중 검증 모두 해당. `evidence`는 최소 `{source_file, coordinates | bbox, rule_id | method}` 구조.
4. **외부 API 키·시크릿은 `.env`에만** 둔다. 코드·YAML·테스트 픽스처에 하드코딩 금지. `.env.example`에 키 이름만 나열한다.
5. **한국어 주석 허용, 식별자는 영어.** 변수·함수·클래스·테이블·컬럼·API 경로는 모두 영어. 주석·docstring·커밋 메시지·문서는 한국어 가능.
6. **좌표계 변환은 하드코딩 금지.** 원점·회전·스케일·EPSG는 항상 `CoordinateSystem` 객체(`packages/core/models/`)로 전달하고, 값은 DB 또는 사용자 입력에서 온다.
7. **상태 전이는 반드시 `actor`(system/contractor/cm)와 `evidence`를 기록**한다. ADR 0001의 상태기계 밖 전이는 코드로 존재해서는 안 된다.
8. **"확정(CONFIRMED)" 상태는 `actor == cm`인 전이로만 도달**한다. `system` actor가 CONFIRMED로 전이하는 코드 경로가 있으면 `reviewer`가 즉시 반려한다. **역할→actor 매핑은 `contractor→contractor`, `cm→cm`뿐이다.** `admin`은 프로젝트·사용자 관리 역할이며 확정·검측 승인·검토요청 처리 권한이 없다(ADR 0001 §4-1). `client`는 조회 전용.
11. **API 계층(`services/api`)에는 도메인 규칙을 두지 않는다.** 재업로드·orphan 규칙은 `services/ingest/persistence.py`, 매핑 생명주기·검토요청 해소는 `services/sync`, 검측 ReviewRequest 생성·종료는 `services/progress/state_machine.py`가 소유한다. API는 이를 호출만 한다.
12. **API 필드명은 snake_case(glossary 영어 표기 그대로)**. 뷰어 TS 타입도 서버 계약 필드는 같은 표기를 쓴다(`global_id`, `coordinate_system`).
9. **비동기 작업(파싱·정합·판정)은 Celery 태스크**로 발행하고, API는 `job_id`를 돌려준 뒤 상태 폴링 엔드포인트로 진행률을 제공한다.
10. **MVP 범위 밖 기능은 구현하지 않는다.** 필요하면 `docs/adr/`에 "Deferred" ADR을 남기고 끝낸다.

---

## 4. 에이전트 호출 규약

기능 요청은 항상 다음 순서로 흐른다:

```
architect  →  담당 에이전트(1개 이상)  →  qa  →  reviewer
```

- **architect**: 요청을 받으면 "어느 에이전트가, 어떤 인터페이스(입출력 계약)로, 어떤 순서로" 구현할지 계획을 쓴다. 데이터 모델 변경이 필요하면 `packages/core/models/`와 ADR을 먼저 고친다. 직접 구현 코드는 쓰지 않는다.
- **담당 에이전트**: architect의 계획과 자기 파일의 입출력 계약대로 구현한다. 담당 디렉터리 밖은 건드리지 않는다.
- **qa**: 담당 에이전트의 "완료 조건"을 테스트로 강제한다. 픽스처가 없으면 만든다. `tests/metrics.json` 기준치를 갱신하거나 하락을 잡는다.
- **reviewer**: 5가지 체크(무승인 확정 경로 / confidence·evidence 누락 / 디렉터리 밖 수정 / 좌표계 하드코딩 / glossary 불일치)를 수행하고, 하나라도 걸리면 승인하지 않고 담당 에이전트에게 돌려보낸다. 직접 수정하지 않는다.

에이전트 목록(`.claude/agents/`):

| 에이전트 | 모델 | 담당 |
|---|---|---|
| `architect` | opus | 설계·ADR·데이터 모델·작업 분배 |
| `bim-ingest` | sonnet | `services/ingest/` |
| `viewer-3d` | sonnet | `apps/web/src/viewer3d/` |
| `viewer-2d` | sonnet | `apps/web/src/viewer2d/` |
| `sync-2d3d` | sonnet | `services/sync/` |
| `reality-capture` | sonnet | `services/scan/` |
| `progress-engine` | sonnet | `services/progress/`, `config/` |
| `knowledge` | sonnet | `services/knowledge/`, `rules/` |
| `api` | sonnet | `services/api/`, `docs/api.md` |
| `frontend` | sonnet | `apps/web/` (뷰어 디렉터리 제외) |
| `qa` | sonnet | `tests/`, `.github/workflows/`, `Makefile` |
| `reviewer` | opus | 머지 전 리뷰(수정 권한 없음) |

호출 예시:

```
@architect 스캔 판정 결과를 객체 상세 API에 노출하려고 해. 계획 세워줘.
@bim-ingest architect 계획대로 IFC 파서 구현해줘.
@qa bim-ingest 완료 조건을 pytest로 붙여줘.
@reviewer services/ingest 변경분 리뷰해줘.
```

---

## 5. 개발 명령

```
make dev    # API + Celery worker + web dev server (docker compose)
make test   # pytest + vitest + (선택) playwright
make lint   # ruff + mypy + eslint + tsc --noEmit
```

Claude Code는 `buildtwin/` 디렉터리에서 실행한다(`.claude/agents/`가 이 위치 기준으로 로드됨).

---

## 6. 계획·ADR·검증을 쓸 때 반복된 실패 (0007·0008·0009 + 2026-09-04 사이클 실측)

> **이 절이 왜 CLAUDE.md 에 있는가(architect 판단, 2026-09-04).** 아래 네 패턴은 최근 사이클들에서
> **각각 여러 건씩** 반복됐고, **전부 하류(구현·qa·리뷰어)가 실행으로 잡았다.** 즉 계획을 쓰는 시점에는 아무도
> 못 보는 종류의 오류다. 그래서 사건을 겪은 ADR 안에 두면 안 된다 — ADR 0009 §5-4 에 묻으면 다음
> 사이클은 그 ADR 을 열 이유가 없어 같은 함정에 다시 빠진다. 별도 문서로 빼는 것도 같은 이유로 기각했다
> (링크되지 않은 문서는 읽히지 않는다 — 이 저장소의 "job 경고는 아무도 되돌아보지 않는다"와 같은 실패다).
> **모든 에이전트가 작업 전에 반드시 읽는 문서는 이 파일 하나뿐이므로**, 규칙은 여기 두고 사건의 전모는
> 각 ADR·계획에 남긴다. 아래 각 항목은 **근거 → 규칙** 순으로 적는다: 규칙만 남기면 다음 사람이 그것을
> 재량으로 읽고 건너뛴다.

이 프로젝트의 지배적 실패 모드는 **"조용히 죽는 것"**(예외 없음·테스트 전부 통과·화면 정상·이력만 사라짐)
이고, 아래 넷은 그 실패가 **계획·ADR 단계에서 만들어지는** 네 가지 경로다.

### 6-1. 전수 목록의 **생성 기준**이 곧 그 목록의 한계다

| 회차 | 어디 | 조건절·기준 | 밀려난 것 | 관측값 |
|---|---|---|---|---|
| 1 | 0008 계획 | `grep "session.get(...)"` 하나 | 시그니처가 바뀐 호출부 / Celery 잡 안에서 삼켜지는 예외 | 두 경로가 통째로 목록 밖 |
| 2 | 0009 계획 §1-a | 기준은 잘 적었으나 블라인드 스팟 1(시트명 변경)을 **적어 두기만 하고 태우지 않음** | 그 경로의 **관측 가능성**(폭발 반경은 같다) | `orphaned=0`·`fingerprint_changed=False` — 초판 탐지 규칙이 이 경로에서 전부 침묵 |
| 3 | 0009 계획 §7 | 반증 목록("결함이 있어도 통과하는 단언")을 **실측 없이 생각으로** | — | `mapping_count == 6` 이라 적었는데 정상 코드에서 **4** |
| 4 | 0009 ADR 개정 2 | `cause` 정본 자리 목록의 기준 = **"값이 흐르는 세 층"**(생산 `services/ingest` · 소비 `services/progress` · 화면 `apps/web`) | **내 것이지만 지금 열고 있지 않은 파일** | `packages/core/models/review.py` 머리 주석이 옛 `cause` 이름에 남은 채 커밋됨(주석이라 어떤 테스트도 실패시키지 않는다) |
| 5 | 0009 ADR 재심 | 같은 자리의 목록 기준 = 실제로 친 `grep -rn "moved=9" services/ apps/`(**내가 고치는 파일 + 남에게 넘길 트리**) | 같은 칸 | **같은 파일이 두 사이클 연속** 전수 목록 밖. 두 기준 모두 **작업 분배의 모양**이라 그 칸이 없다 |
| 6 | 계획 0001 백로그(계획 0004 가 고침) | "무효화 접두사가 **목록 키**를 덮는가" | 그 기준에는 **상세 방향을 볼 칸이 없다** — 기준은 범위뿐 아니라 **방향**도 가진다 | `useCreateDailyReport` — 작업일보 제출 후 화면 목록 `REPORTED` / 상세 `PLANNED`(실측) |
| 7 | 계획 0004 §후속 1 | 낡은 주석을 잡는 `make lint` 한 줄 = **시제 표현** grep | 값·개수가 낡은 주장(현재형으로 적힌 거짓) | 세 번째 자리도 같은 디렉터리였다: `state.py:94` "그 후속이 오기 전까지는…" 이 같은 8커밋 범위의 `3f358db` 로, 같은 docstring 의 "핸들러는 … 다섯뿐"이 그 여섯 번째 등록으로 거짓이 됨 |
| 8 | 계획 0005 §2-a(2026-09-05) | 루트 grep 은 옳았다(§6-1 대로) — 거른 것은 **그 뒤의 분류 기준**: "값으로 쓰이거나 **값 집합을 선언하는** 자리" | 값을 선언하지 않고 **런타임 비교로 소비**하는 자리 | `ReviewsPage.tsx` 는 그 grep 출력에 있었는데(히트 6) 표에서 빠졌다. 거기 `g.cause === "row_replaced"` 가 `notice strong` 과 "가장 먼저 확인" 배지를 켠다 — 개명이 닿지 않으면 가장 위험한 경위의 강조가 조용히 꺼진다 |

**규칙.** 전수 목록·반증 목록·영향 범위 표를 쓸 때는 ① **어떤 기준으로 만들었는지**를 목록 옆에 적고
② **그 기준이 놓치는 것**을 함께 적고 ③ **적어 둔 블라인드 스팟을 최소 한 건은 실제로 태워 본다.**
**그리고 목록은 저장소 루트에서 만든다**(`grep -rn <문자열> .`) — 소유·계층·"넘길 트리"로 **경로를
먼저 좁히지 않는다.** 소유는 목록을 만든 **뒤에** 나눈다. 경로를 먼저 좁히면 좁힌 기준이 곧 목록의
한계이고, 그 한계는 **관측된 두 번 다 자기가 지금 보고 있지 않은 자기 파일** 쪽으로 생겼다(4·5회차).
**관계를 세는 목록은 곱으로 만들고**(모든 무효화 × 모든 키) 두 방향을 각각 칸으로 둔다 —
"A 가 B 를 덮는가"로 만든 목록은 **"B 가 A 를 덮는가"를 영원히 묻지 못한다**(6회차).

  - *역방향 확인 — 이 새 기준이 놓치는 것(§6-1 ②를 새 기준에 대해 답한다).* 축을 경로에서 **문자 그대로의
    문자열**로 옮겼을 뿐이므로, 같은 값의 **표기 변종**은 여전히 밖에 있다(`moved=9` 는 잡지만
    `moved = 9`·`moved: 9`·`이동 9건`·`9쌍`·"아홉"은 못 잡는다). 값 하나를 셀 때는 문자열 하나가 아니라
    **그 값이 쓰이는 표기 집합**을 세고, 세지 못한 표기를 목록 옆에 적는다. (2026-09-04 실측: `moved=9` 의
    그런 변종은 이 저장소에 **0건**이라 이번엔 실해가 없었다 — 그러나 기준의 한계는 **결과가 아니라
    기준에서** 판단한다. 0건이었다는 사실은 기준을 정당화하지 않는다.)
  - *역방향 확인 — "언제나"라고 쓰지 않는 이유.* 표본은 **2건**뿐이다(4·5회차 — 둘 다
    `packages/core/models/review.py`). 두 번 다 같은 칸이었다는 것이 이 문장의 전부이고, **다른 방향으로
    한계가 생기지 않는다는 뜻이 아니다.** 그러므로 이 문장을 "그쪽만 확인하면 된다"로 읽지 말고,
    새 목록마다 §6-1 ②("그 기준이 놓치는 것")를 **처음부터 다시** 답한다.

**이름 붙은 블라인드 스팟: `packages/core/models/` 의 주석.** `architect` 소유인데 `docs/` 도 아니고
남에게 넘길 트리도 아니라 두 종류의 목록 모두에서 빠졌고(**세 사이클 연속** — 위 표 4·5·7), 주석이라
CI 도 잡지 못한다. **이 디렉터리의 주석이 자기 파일 밖에 대해 하는 모든 단정을 명시적으로 확인한다** —
값만이 아니다. **시제 표현**("아직/전까지/후속이 오면")은 "다음 커밋이 나를 거짓으로 만든다"는
뜻이므로 여기 주석에 쓰지 않는다 — 사실만 현재형으로. **개수도 세지 않는다** — 세는 대신 그 주석이
실제로 기대는 **부재**를 적는다.
**부재 단정은 시제 표현의 변장일 수 있다**(2026-09-05, 같은 자리 네 번째 관측): "지금 X 가 없다" +
같은 계획이 X 를 만들기로 했다 = "아직 X 가 없다"이고, 그 문장은 다음 커밋이 거짓으로 만든다
(`review.py` 가 "이 저장소에 그 감사가 없다"고 적었고 두 커밋 뒤 작업 8 이 그 감사를 만들었다).
부재를 적을 때 **그것을 메우는 작업이 같은 사이클에 있는지** 본다.
  - *역방향 확인 — "자기 파일 밖에"가 미는 것.* 같은 파일 안을 가리키는 단정("바로 위 줄이 선례다")은
    이 문장 밖이다. 관측된 3회차가 모두 파일 밖 단정이라 이렇게 좁혔을 뿐이고, **파일 안 단정이 낡지
    않는다는 뜻이 아니다** — 줄이 옮겨지면 같이 낡는다.
*새 항목을 만들지 않는다*: §6-5 는 "**새로운 종류**"에 항목을 요구하는데 규칙은 §6-4 1 이, 자리는 §6-1 이
이미 갖고 있다. 없던 것은 규칙이 아니라 **기계적 감사**이고 산문으로 닫히지 않는다 —
`make lint` 형태와 "셋 중 하나만 잡는다"는 역방향 확인을 `docs/plans/0004-*.md` §후속 1 에서 qa 로 넘긴다.
"놓칠 수 있다"고 적는 것은 커버리지가 아니다. 그리고 **다른 절을 가리켜 확인을 갈음하지 않는다** —
"→ §7 V4 가 이 구멍을 덮는다"는 문장은 V4 를 열어 확인하기 전에는 쓰면 안 된다(0009 계획이 그렇게 적었고
V4 에는 그 케이스가 없었다).

### 6-2. 검증 시나리오는 **결함을 못 잡도록** 세워질 수 있다

- 0008 계획 S4: 같은 파일을 양쪽에 올려 누수가 나도 점수가 **산술적으로 동일**했다 → 결함이 있는데 통과.
- 0009 계획 §7 V3b: 병합 충돌을 **사람 판단이 없는 프로젝트**로 세워 `identity_drift_review_id is None`
  이 **정답으로 고정**됐다 → 결함 있는 코드와 옳은 코드가 그 시나리오에서 **구별 불가능**했고, 그래서
  "병합은 절대 CM 큐에 올라가지 않는다"는 blocker 가 사이클 끝까지 살아남았다. 음성 대조군 N1~N3 도
  전부 한쪽 축(*moved*)의 것이라 병합 축은 양성에서도 음성에서도 검증되지 않았다.

**규칙.** 시나리오를 쓸 때마다 스스로에게 묻는다: **"이 단언의 기대값을, 결함 있는 코드가 그대로
만족하는가?"** 그렇다면 그 시나리오는 검증이 아니라 장식이다. 구체적으로 —

1. **양성 케이스는 "그 결함이 있으면 값이 달라지는" 상태로 세운다.** 판정 결과가 결함 유무와 무관하게
   같은 값(`None`·`0`·빈 목록)으로 고정되는 조건(사람 판단이 없는 프로젝트, 빈 입력, 동일 파일 등)을
   양성 케이스의 전제로 쓰지 않는다.
2. **탐지를 검증할 때는 발화의 *결과*까지 값이 갈리게 세운다**(경고가 떴는가 → 큐에 올라갔는가).
3. **음성 대조군을 한 축에만 몰지 않는다.** 판정 경로가 둘 이상이면 각 축마다 양성·음성을 세운다.
4. **두 사실이 함께여야 의미가 있으면 함께 단언한다**(0009 V5: "승인 상태가 뒤집혔다" + "같은 적재가
   그것을 큐에 올렸다" — 하나만 고정하면 다른 하나가 사라져도 초록이다).

### 6-3. 조건을 **좁게** 적으면 가장 위험한 변종이 표 밖으로 나간다

재발 9건. **1~6 은 전부 같은 조건절 계열**이고, **7~9 는 같은 실패를 조건절 밖에서** 반복한 것이다
(7 = 근거를 말하는 문서 넷과 그 근거를 반박하는 응답 문자열, 8 = 한 문서 안 두 표의 축 불일치,
9 = 8 을 피하려고 축을 **바꾼** 계획에서 같은 무주공산이 다른 자리에 난 것).
근거는 표로 둔다 — 회차가 이 규칙의 무게다.

| 회차 | 어디 | 조건절·기준 | 밀려난 것 | 관측값 |
|---|---|---|---|---|
| 1 | 0009 ADR §5-2 초판 | "**고아**가 된 문서에 사람 판단이 있으면" | `merge_overwritten`(행이 살아 있고 내용만 바뀐다) | 영원히 큐에 닿지 못함 |
| 2 | 같은 표 | 두 `doc_id` 가 하나의 "**새**" `doc_id` 로 수렴 | 운영의 실제 병합은 **이미 있던** id 로 수렴 | 가장 위험한 변종이 표 밖 |
| 3 | ① 0009 계획 §3-e 3 → ② ADR §5-2 **개정 1**(= 1·2 를 고친 개정) | ① 범위를 **`drift.moved` 로** 좁힘 / ② "**한 적재 안에서** 두 행이 같은 `doc_id` 로 수렴" | ① 병합 경로 전부 / ② 사명 변경 주의 정상 운영(별칭표 통합 + 옛 법인명 행 이탈)은 두 행을 한 적재에 두지 않는다 | ① 구현이 계획대로 했는데 병합 경로가 **구조적으로 결과에 들어갈 수 없었다** / ② `drawing_approval` 0.0→1.0(미승인 도면 위 착수 가능)인데 **경고 0건·검토요청 0건**. 초판이 고친 blocker 와 데이터 모양·결과가 같다 (§5-5) |
| 4 | ADR §5-2 **(바-2) 대조표**(= 3 을 막으려고 §6-3 을 이행해 만든 그 표) | "판단이 걸리지 않은 드리프트도 **경고까지는** 발화한다" | 경고 게이트가 `moved` 와 `lost_decisions and not merged` 둘뿐 → (나)·(다) 축은 사람 판단이 있을 때만 관측 | 적재 전체가 침묵(`identity_drift=None`, 경고 0, 요청 0) |
| 5 | ADR §5-2 **(바-3) 재검증 표**(= 4 를 막으려고 (바-2) 10행을 다시 태운 그 표) | 머리말 "각 줄은 **참조가 아니라** 이번 재심에서 실행한 값" | 10행 중 **6행 한 칸**이 "(위 표 6행)"이라는 가리킴 — 하필 4의 거짓이 있던 그 행 | 거짓이 아니라 **미검증**. 고친 것은 한 칸 |
| 6 | 계획 0001 백로그(2026-09-04, 계획 0004 가 고침) | "무효화 접두사가 **목록 키**를 덮는가" | 그 기준에는 **상세 방향을 볼 칸이 없다** → `useCreateDailyReport` 의 반대 방향 결함 | 작업일보 제출 후 화면 목록 `REPORTED` / 상세 `PLANNED`(실측) |
| 7 | ADR 0011·`errors.py`·glossary·`ErrorBox.tsx` **네 곳**(2026-09-04) | "전이는 허용 표에 있으므로 '수행할 수 없습니다'는 거짓" — code 를 가른 **유일한 근거** | 그 근거를 **응답이 싣는 `detail` 자신**에 대해서는 아무도 묻지 않았다(부모 포맷을 그대로 상속) | `code: revocation_reason_required` 인데 `detail: "CONFIRMED -> MISMATCH by cm **not allowed**. …"`(실측). 근거를 말하는 문서 넷과 그 근거를 반박하는 문자열 하나가 **같은 커밋 범위** |
| 8 | 계획 0004 자신(2026-09-04, 최초 커밋부터) | 작업 분배 표를 **"ADR 규칙 하나당 한 행"** 축으로 만듦 | 시나리오 표의 **V8~V10**(그 축에 받을 칸이 없다)과 ADR 이 규칙으로 적지 않은 **오류 표현 계층** | 작업 7 입력 "V1~V7" ↔ 같은 파일 40줄 아래 시나리오 표 V10 까지. V8~V10 무주공산 → api 가 qa 소유 `tests/integration/test_18_…` 을 씀(형식 체크 3 FAIL) |
| 9 | 계획 0005 자신(2026-09-05) — **8 을 피하려고** 작업 분배 축을 "한 소유가 한 커밋으로 끝낼 수 있는 단위"로 바꿈 | 그 축에도 **"내 작업이 남의 파일을 낡게 만드는 자리"** 칸이 없다 | 작업 7 이 거짓으로 만든 "정본은 어디인가" 주석 셋(frontend·qa 소유) | 담당이 셋을 찾아 보고했으나 소유가 남이라 못 고쳤고 계획에도 칸이 없었다 → architect 가 `d0a0e88` 로 남의 트리 둘을 만졌다. **축을 바꾸면 무주공산은 없어지지 않고 자리를 옮긴다** |

**1~6 회차에서 배운 것 셋.** ① **규칙을 아는 것으로는 부족하다** — 3회차는 이 §6-3 을 쓴 사람이 그 규칙을
쓴 **바로 다음 개정**에서 저질렀다. 초판의 한정어를 지우는 데 주의를 다 쓰고, **그 자리를 메우려고 새로
쓴 문장에는 같은 확인을 하지 않았다.** ② **규칙과 그 위반은 같은 커밋에서 함께 태어날 수 있다** —
아래 "표의 각 칸을 …" 규칙과 그것을 어긴 표가 **둘 다 `685cbc5`** 다(`git log -S` 로 확인).
"방금 이 규칙을 썼다"는 사실의 방어력은 **0** 이고, 잡은 것은 바깥에서 칸을 하나씩 읽은 리뷰어뿐이다.
③ 그 요구는 **검증 대상 표**가 아니라 **지금 쓰고 있는 표**에 걸린다. 그래서 §6-3 은 **점검표**가 아니라
**산출물**을 요구하는 규칙이어야 한다:

- **한정어마다 역방향 확인을 문서에 한 줄씩 적는다**(머릿속으로 하지 않는다 — 안 적으면 안 한 것이다).
  적는 자리는 그 조건절 바로 아래다. 새로 **쓴** 조건절도 예외가 아니다(개정 1 이 정확히 여기서 실패했다).
- **그 확인을 실행으로 태우고 결과를 표로 남긴다.** 새 조건이 무엇을 잡고 무엇을 놓치는지를,
  **옛 조건의 결과와 나란히** 적는다. ADR 0009 §5-2 (바)가 그 형식이고, 그 표가 실제로 한 건을 잡아냈다 —
  리뷰어 제안대로 조건을 갈아치웠으면 **고친 자리에서 옛 조건이 잡던 다른 경로를 잃을 뻔했다**(P13).
  즉 역방향 확인은 "새 조건이 놓치는 것"뿐 아니라 **"옛 조건이 잡던 것"** 도 물어야 한다.
- **표의 각 칸을 다른 절 참조로 갈음하면 그 행은 검증되지 않은 것이다**(2026-09-04 네 번째 재발이
  가르친 것). (바-2) 6행의 "실제로 어떻게 되는가" 칸에 적힌 것은 실측값이 아니라 "((사) 표)"라는
  **가리킴**이었고, 가리킨 (사) 표도 같은 경로에서 코드와 달랐다 — **서로를 가리키는 두 줄은 서로의
  근거가 될 수 없다.** §6-1 의 "다른 절을 가리켜 확인을 갈음하지 않는다"가 **§6-3 이 요구한 산출물
  안에서** 깨졌다. 표를 **만드는 것**과 표의 각 칸을 **태우는 것**은 다른 일이다: 칸마다 실행값이나
  코드 인용 중 하나가 그 자리에 있어야 하고, 문서 안의 다른 줄은 둘 중 어느 것도 아니다.
  **그리고 이 요구는 "검증 대상 표"가 아니라 "지금 쓰고 있는 표"에 걸린다**(2026-09-04 **다섯 번째**
  재발이 가르친 것). (바-3)은 (바-2)를 재검증하려고 만든 표인데, 나는 이 규칙을 **(바-2)의 칸**에 대한
  요구로만 읽고 **(바-3)의 칸**에는 적용하지 않았다 — 그래서 (바-2)의 여섯 번째 칸이 참조로 갈음됐던
  자리가, (바-3)에서도 정확히 여섯 번째 칸에서 참조로 갈음됐다. 겹수를 세지 말고 **표를 하나 쓸 때마다**
  그 표의 칸을 태운다. 자기 검증에는 재귀가 있고, **"방금 이 규칙을 썼다"는 그 재귀를 멈춰 주지 않는다.**
- **같은 문서 안에 이미 반박이 적혀 있어도 결론은 그것을 따라가지 못한다 — 인접 절과 교차 확인한다.**
  위 네 번째 재발에서, 틀린 표의 **26줄 위**(같은 커밋에서 쓴 "놓치는 것" 4)가 이미
  "…사람 판단이 없으면 `lost_decisions` 가 비어 게이트가 닫힌다"라고 정확히 반대를 적고 있었다.
  §6-3 이 이미 "근거는 문서 안에 다 있었고 결론이 근거를 따라가지 못했다"(§3 ↔ §5-2)를 적었는데
  **그 실패가 같은 문서의 이웃한 두 절 사이에서 다시 일어났다.** 그러므로 표의 한 행을 채울 때,
  그 행이 다루는 값(게이트·조건·발화)을 **같은 문서에서 다르게 말하는 줄이 있는지 먼저 찾는다.**
  있으면 둘 중 하나는 틀렸다 — 실행으로 가른다.
- **조건을 바꾸면 그 결과를 소비하는 게이트·문구도 같은 PR 에서 확인한다.** 개정 2 의 판정은
  `if moved or merged` 게이트 뒤에 있었고, 판정만 고쳤으면 **고친 blocker 가 게이트에서 다시 조용히
  삼켜졌다**(실측). 조건절 하나를 고칠 때 "이 값을 누가 읽는가"를 끝까지 따라간다.

**규칙.** 조건절에 들어가는 **모든 한정어**(고아·새·최초·직접·해당 등)에 대해 "이 단어를 빼면 무엇이 더
들어오는가 / 이 단어 때문에 무엇이 빠지는가"를 한 번씩 답한다. 특히 **같은 문서가 이미 최악이라고 분류해
둔 경로**(위험도 표, 실패 모드 표)가 그 조건절에 걸리는지 **역방향으로** 확인한다 — 0009 는 §3 에서
병합을 "복구 불가 / 미승인 도면 위에서 착수 가능"으로 분류해 놓고 §5-2 에서 그것을 경고로 강등했다.
근거는 문서 안에 다 있었고 **결론이 근거를 따라가지 못했다.**

### 6-4. 문구 정확도를 미루면 **다음 문구가 같은 거짓을 갖고 태어난다**

- 0009 ADR §Deferred 2 초판: `document_possibly_renamed` 문구 정정을 미루면서 "§5-2 가 두 경우를
  분리하면 이 문구는 진짜 rename 에만 붙는다"고 적었다 → **그 §5-2 가 만든 새 검토요청 제목이 똑같은
  종류의 거짓을 갖고 태어났다**(고아가 아닌 것을 고아라 하고, 없는 새 `doc_id` 위에서 다시 확정하라 하고,
  0건 이동을 보고했다). 미룬 판단이 **한 사이클도 버티지 못했다.**
- 앞선 사이클: 존재한 적 없는 되돌리기 엔드포인트를 약속한 승인 다이얼로그 문구를, 웹 테스트 169건이
  **계약으로 고정**한 채 전원 통과했다.

**규칙.** 이 저장소에서 사용자에게 보이는 문구(검토요청 제목, 경고 메시지, 블로커 사유, 다이얼로그)는
장식이 아니라 **CM 이 다음 행동을 고르는 유일한 입력**이다. 부정확한 문구는 "나중에 다듬을 표현"이 아니라
**작동하지 않는 안전 장치**다. 따라서 —

1. **사실과 다른 문구는 그것을 만든 사이클이 고친다.** 문구 정정을 Deferred 로 미루는 근거로 "다음 변경이
   자연히 좁혀 준다"를 쓰지 않는다.
2. **문구가 "무엇이 일어났는가"를 여러 경위에 걸쳐 말해야 하면, 경위를 기계 판독 값으로 실어 보낸다**
   (0009 `lost_decisions[].cause`). 소비자가 **산문을 부분 문자열로 되읽어 분류하게 두지 않는다**
   (`Blocker.kind` 도입으로 이미 한 번 걷어낸 패턴이다). 모르는 값은 **모른다고 적고**, 가장 흔한 값으로
   떨어뜨리는 폴백을 두지 않는다.
3. **문구 테스트는 문장을 통째로 베끼지 말고 "그 상황에서 참일 수 없는 말이 없다"를 단언한다** — 문장을
   베끼면 거짓 문구가 계약이 된다.

### 6-5. 적용 지점

| 누가 | 언제 | 무엇을 |
|---|---|---|
| `architect` | 계획·ADR 을 쓸 때 | §6-1 생성 기준·블라인드 스팟 명시 + 한 건 실측(**전수 목록은 저장소 루트에서 만들고 소유는 그 뒤에 나눈다 — `packages/core/models/` 주석을 명시 확인**), §6-3 한정어 역방향 확인을 **문서에 적고 실행 표로 남기기**(새로 쓴 조건절 포함, “옛 조건이 잡던 것”까지, **각 칸을 참조가 아니라 실행값·코드 인용으로 채우고 같은 문서의 인접 절과 교차 확인 — 이 요구는 검증 대상 표가 아니라 지금 쓰는 표에 걸린다: 재검증 표도 예외가 아니다**), §6-4 문구 정정을 Deferred 로 넘기지 않기 |
| `qa` | 검증 시나리오를 붙일 때 | §6-2 네 항목. 기대값이 결함 코드에서도 같은 시나리오는 **반려 사유**다 |
| `reviewer` | 머지 전 | 계획·ADR 이 §6-1·§6-3 을 지켰는지(**대조표의 칸이 다른 절 참조로 갈음돼 있으면 그 행은 미검증으로 본다**), 새 사용자 문구가 §6-4 를 지켰는지 |

**이 절을 갱신하는 규칙.** 항목을 지우지 않는다. 새 사이클에서 같은 패턴이 재발하면 근거를 **추가**한다
— 재발 횟수가 그 규칙의 무게다. 새로운 종류의 반복이 두 사이클 이상에서 관측되면 항목을 추가한다.

**압축 규칙(architect, 2026-09-04 추가).** 위 규칙이 단조 증가만 허용해 §6 은 CLAUDE.md 의 절반을
넘었다(2026-09-04 관측 51%, 2026-09-05 실측 줄 52.7% · 문자 58.6%). **읽히지 않는 안전 장치는 작동하지 않는 안전 장치**(§6-4)라는 판단은 이 절 자신에게도 걸린다.
그래서 **근거는 압축하되 규칙은 압축하지 않는다.**

- **규칙**(작업 시점에 행동을 바꾸는 문장) — 한 글자도 지우지 않는다.
- **근거**(그 규칙이 왜 있는지 말하는 문장) — `회차/어디/조건절·기준/밀려난 것/관측값` 표 한 행으로
  압축한다. 회차와 관측값은 남긴다(무게와 반증 가능성이 거기 있다). 서사는 남기지 않는다.
- **압축 커밋은 "없어진 옛 불릿 ↔ 새 표 행" 대응을 훑고, 대응되지 않는 관측값이 있으면 행을 나눈다**
  (2026-09-04 추가). 실측: 위 한 줄이 "회차와 관측값은 남긴다"고 적은 **바로 그 압축**에서 §6-3 3행이
  두 자리를 합치며 앞쪽의 한정어·관측값만 잃었다. 잡은 것은 리뷰어다.
- **한 번에 한 절씩.** 리뷰어가 압축된 절을 읽고 통과시킨 뒤 다음 절로 간다 — 5회차가 가르친 대로
  "방금 이 규칙을 썼다"의 방어력은 0이고, 잡은 것은 언제나 바깥에서 읽은 리뷰어였다.
  (압축분: `685cbc5` = §6-3 재발 서사. `docs/plans/0005-*.md` §과제 3 = §6-1. §6-2·§6-4 는 다음 사이클.)
  - *재심 판단(2026-09-04): 그 사이클에는 압축하지 않았다.* 직전 압축이 손실을 냈고 그것을 잡은 것이
    리뷰어이므로, 손실이 드러난 사이클에 한 번 더 압축하면 미검증 손실이 두 배가 된다. 그 사이클은 손실
    복구 + 위 대조 규칙까지 하고, 다음 사이클에 §6-1 하나만 대조와 함께 하기로 순서를 못박았다.
  - **압축의 실측 이득은 줄 수이지 문자 수가 아니다(2026-09-05, §6-1 압축분).** 63줄 → 46줄
    (-17줄, §6 의 -8.0%)인데 문자는 20,782 → 20,641 (-141자, §6 대비 -1.2%)이고 §6 비중은
    58.8% → 58.6% 로 거의 그대로다. **표는 같은 관측값을 더 적은 줄에 담지만 더 적은 문자에 담지는
    않는다.** 남은 길이의 대부분은 **규칙**이고 규칙은 압축 대상이 아니므로, "읽히는 길이"를 근거 압축만으로
    끌어내리는 데에는 상한이 있다 — 다음 압축은 이 상한을 알고 시작한다. 옛 불릿 ↔ 새 행 대조표는
    `docs/plans/0005-*.md` §과제 3 에 있다.

> **줄이는 근거를 태워 보고 바꿨다.** 심사 권고는 "'표의 칸을 태운다' 한 줄 + 리뷰 절차가 다섯 번을 다
> 잡았다"였는데, `git log -S` 로 확인하니 그 줄은 `685cbc5`(4회차를 고친 커밋)에서야 들어왔고 5회차는
> **그 줄이 태어난 바로 그 커밋에서** 났다. 다섯 번을 다 잡은 것은 그 줄이 아니라 리뷰어다. 그러므로
> 줄이는 근거는 "규칙 하나면 충분하다"가 아니라 **"51%는 읽히지 않는다"** 이고, 줄이는 대상은 규칙이
> 아니라 근거 서사다.
