# 계획 0010 — §후속 21: postgres 축의 흔들림 · **기전을 잡았다**

- 작성: architect
- 날짜: 2026-09-07
- 관련: `docs/adr/0015-heap-placement-is-not-a-test-contract.md`(이 사이클이 쓰는 ADR — 결정의 정본),
  `docs/adr/0014-database-as-a-test-parameter.md` §4-1 `[PG-after-delete-reinsert]` · §5-3,
  `docs/plans/0009-*.md` §후속 1~20 · §Deferred · §M-2-1 · §M-3-c · §M-4 · §M-5 21~28,
  CLAUDE.md §2 소유 · §3-1(테스트를 skip·비활성·격리하지 않는다) · §3-13 · §6-1 ①②③ · §6-2 · §6-3 · §6-4 · §6-5

---

## 0. 이 문서의 실측이 나온 자리 (재현 방법)

**작업 트리** `/home/user/Bim/buildtwin`, **저장소 루트는 `/home/user/Bim`**(프로젝트는 그 하위
`buildtwin/`), 브랜치 `claude/buildtwin-initial-setup-ubulzb`, **HEAD `743bcb9`**.
루트에서 `git status --porcelain` **전문이 빈 출력**이다(모든 탐침 전후로 확인했다 — §작업 분배 말미).

**§6-1 이 말하는 "저장소 루트"는 `/home/user/Bim` 이다.** 이 계획의 모든 전수 목록은 `cd /home/user/Bim`
에서 만들었다(계획 0007 §0 · 계획 0009 §0 의 선례 그대로).

**PostgreSQL 은 이 사이클도 실제로 켜서 쟀다.** 로컬 클러스터, 포트 55432:

```
PostgreSQL 16.13 (Ubuntu 16.13-0ubuntu0.24.04.1) on x86_64-pc-linux-gnu
autovacuum=on  autovacuum_naptime=60  autovacuum_vacuum_threshold=50  autovacuum_vacuum_scale_factor=0.2
```

기준선(전부 이 사이클에서 직접 쟀다):

```
$ cd /home/user/Bim/buildtwin && .venv/bin/pytest -q
823 passed, 4 warnings in 79.88s (0:01:19)
[db-axis] dialect=sqlite tests_on_postgres=0 engines=0 (BUILDTWIN_CI_POSTGRES_URL 없음 — …)

$ BUILDTWIN_CI_POSTGRES_URL=postgresql+psycopg://buildtwin@127.0.0.1:55432/buildtwin_test \
      .venv/bin/pytest -q tests/integration
[db-axis] dialect=postgresql server_version=16.13 tests_on_postgres=181 engines=1 floor=181 …
207 passed, 1 warning in 41.53s

$ make lint
exit 0
```

**계획 0009 마감(§M-1 (나)·§M-1-c)의 값과 전부 일치한다** — 823 / 207 / `tests_on_postgres=181` /
`engines=1` / lint exit 0. **위 실측과 다른 값은 나오지 않았다.** `make test`(unit 492 · invariants 104 ·
regression 8 · integration 207 + vitest 283)는 §작업 분배의 커밋 직전 확인으로 돌린다.

**이 문서의 모든 `파일:줄`·커밋 참조는 HEAD `743bcb9` 트리의 것이다** — HEAD 가 움직여도 갱신하지
않는다(CLAUDE.md §3-13 첫째 갈래). 계획 0009 본문과 ADR 0014 는 **기록물이라 한 글자도 고치지
않는다**; 이 사이클이 그 문서들을 조건부로 만드는 자리는 §1-d 와 ADR 0015 가 적는다.

---

## 목표

1. **§후속 21 의 기전을 잡는다.** 계획 0009 §M-5 21 은 이 항목을 *"다음 발생을 구별할 수 있게
   만든다"* 로 열어 뒀고, 리뷰어의 추정(*"delete+re-insert 된 튜플이 힙 맨 뒤에 앉는 것은 보장이
   아니다"*)은 **추측이라고 명시돼 있었다.** 이 사이클의 첫 산출물은 그 추측을 **실행값으로 가르는
   것**이다(§과제 1).
2. **그 위에서 ADR 0015 를 쓰고, ADR 이 정한 것만 작업으로 내린다.** 결정은 하나다 —
   **테스트는 힙 배치에 기대지 않는다.**
3. **§후속 22**(문구가 관측하지 않은 경위를 지목한다)와 **§후속 28**(강제 셋의 무보호 배선 둘)을
   같은 사이클에 닫는다. 둘 다 소유가 `qa` 이고, 22 는 CLAUDE.md §6-4 2 의 자리다.
4. **§후속 23·24·26·27 을 처분한다**(§처분 표). §후속 25 는 CLAUDE.md 편집이라 **이번이 아니다**
   ("한 사이클 한 절" — §6-5).

---

# 과제 1 — 기전을 잡았다. **추측이 참이고, 두 경로로 태워진다**

## 1-a. 무엇을 물었나 — 단정을 세우고 실행으로 갈랐다

계획 0009 §M-5 21 이 남긴 후보 칸은 하나였다:
`assert [x[0] for x in after] == [second_decision_row, first_decision_row]`
(`tests/integration/test_20_mapping_decision_cancel.py:865`, 실패 문구 *"심기가 먹지 않았다 …"*).

**세운 단정(§6-2 — 재현을 성공 조건으로 삼지 않는다).**

> `_plant_row_at_end_of_scan` 의 `DELETE` + `INSERT` 는 PostgreSQL 에서 **힙 맨 뒤를 보장하지 않는다.**
> 새 튜플은 `PageAddItem` 이 고르는 **첫 재활용 가능한 line pointer**, 또는 FSM 이 주는 **앞 페이지**에
> 앉는다. 그 둘 중 하나가 심기 대상보다 앞이면 스캔 순서는 뒤집히지 않고 :865 가 죽는다.

이 단정은 **재현이 아니라 관측으로** 갈린다 — 축은 `ctid`(튜플의 물리 위치)다. 2.3% 짜리를 N회 돌려
"안 났다"로 닫는 것은 §6-2 1 위반이므로, 이 사이클은 **한 번도 그렇게 재지 않았다.**

## 1-b. 태움 1 — **실제 테스트**에서, 강제 `VACUUM` 아래 5/5 죽는다

탐침은 저장소를 한 글자도 바꾸지 않는다: 외부 pytest 플러그인이 `_plant_row_at_end_of_scan` 을
감싸 전후 `ctid` 를 찍고, `PROBE_VACUUM=1` 일 때만 심기 **직전**에 `VACUUM review_requests` 를 한 번
돌린다(`VACUUM` 은 autovacuum 이 하는 일과 같은 일이다 — §1-c).

| 조건 | 심기 전 대상 `ctid` | 심기 후 대상 `ctid` | 결과 |
|---|---|---|---|
| `VACUUM` 없음 | `(0,7)` | **`(0,10)`** (맨 뒤) | `1 passed` |
| `VACUUM` 있음 | `(0,7)` | **`(0,4)`** (앞으로 갔다) | `1 failed` — `AssertionError: 심기가 먹지 않았다 …` |

심기 전 그 스키마의 `kind='document_mapping'` 요청 행 **7건**은 `(0,1) (0,2) (0,3) (0,5) (0,6) (0,7) (0,9)`
에 있었다(전부 0번 페이지) — **`lp` 4 와 8 이 구멍**이고,
그 구멍은 확정1 → 취소1 → 확정2 가 남긴 죽은 튜플 버전이다. `VACUUM` 이 그 line pointer 를 `LP_UNUSED`
로 되돌리는 순간 다음 `INSERT` 가 **`lp=4` 를 먼저 집는다.** 그러면 심은 행이 비교 대상 `(0,5)·(0,6)·(0,9)`
보다 **앞**에 앉고, 무정렬 조회의 반환 순서가 그대로 뒤집히지 않는다.

**5/5.** 같은 조건으로 5회 반복해 **5회 모두** 같은 자리에서 같은 문구로 죽었다(실행값 —
`run1..run5: 1 failed` 각각에 `심기가 먹지 않았다`). 즉 **이 경로는 흔들림이 아니라 결정론이다** —
흔들리는 것은 `VACUUM` 이 그 사이에 도느냐뿐이다.

*이 표가 함께 답하는 것*: 무정렬 조회의 반환 순서가 **`ctid` 오름차순을 따라간다**는 것도 이 실행이
보인다 — 심은 행이 `(0,4)` 로 앉은 바로 그 실행에서 :865 가 죽었기 때문이다. 별도 `EXPLAIN` 은 재지
않았다(§확인하지 않은 것 3).

## 1-c. 태움 2 — **FSM 이 앞 페이지를 준다**(합성, 여러 페이지)

태움 1 은 한 페이지 안의 line pointer 재활용이다. 여러 페이지 짜리 힙에서는 더 크게 어긋난다.
합성 표(2,000행 · 84페이지)에 앞쪽 400행을 지워 구멍을 낸 뒤 같은 모양의 심기를 두 번 했다:

```
pages: last_page = 83
[plant-no-vacuum]      id=1500 → ctid (83,9)      ← 맨 뒤에 앉는다
VACUUM fsm_probe;
[plant-after-vacuum]   id=1501 → ctid (0,1)       ← 힙의 **맨 앞**에 앉는다
[scan-order 1501 vs 2000]  string_agg(id ORDER BY ctid) = '1501,2000'
```

`VACUUM` 이 FSM 을 갱신하자 `RelationGetBufferForTuple` 이 **0번 페이지**를 돌려줬고, 심은 행이
비교 대상(2000, `(83,*)`)보다 **먼저** 스캔된다. **"맨 뒤로 심는다"가 "맨 앞에 앉는다"로 뒤집힌 것**이
이 표의 값이다.

**그러므로 리뷰어의 추정은 참이고, 기전은 하나가 아니라 둘이다**: ① 같은 페이지의 line pointer 재활용
② FSM 이 주는 앞 페이지. 둘 다 **`VACUUM`(수동 또는 auto)이 방아쇠**다.

## 1-d. **재현하지 못한 것 — 이것을 먼저 적는다**(§6-2)

**CI 에서 실제로 난 그 실패는 이 사이클도 재현하지 못했다.** 재현을 시도한 실행값:

| 조건 | 심기 시점 `n_tup_upd / n_tup_hot_upd / n_dead_tup / autovacuum_count / vacuum_count` | 심긴 자리 | 결과 |
|---|---|---|---|
| 기본 설정, `tests/integration` 전량 1회 | `138 / 6 / 133 / 0 / 0` | `(50,13)` → `(52,3)` | **207 passed** |
| `autovacuum_naptime=1`·`threshold=10` 으로 강제, 전량 3회 | `135/4/60/**2**/0` · `141/8/76/**2**/0` · `137/6/63/**2**/0` | — | **3/3 207 passed** |

**autovacuum 이 실제로 두 번씩 돌았는데도 초록이었다.** 이유는 태움 2 가 말한다: `VACUUM` 이 돌아도
**삽입 대상 페이지에 자리가 남아 있으면** 새 튜플은 그대로 맨 뒤에 앉는다. 뒤집히려면 그 페이지가
가득 차 FSM 이 앞 페이지를 내줘야 하고, 그 조건은 실행마다 다르다. 전량 실행의 `review_requests` 는
**429행 · 53페이지**이고 심기 대상 쌍은 마지막 페이지들 근처에 있었다.

**그래서 이 계획은 "재현했다"고 쓰지 않는다.** 성립하는 문장은 이것까지다 —

> **심기의 착지는 postgres 에서 계약이 아니다. 그 비결정성을 만드는 기전 둘이 실행값으로 태워졌고,
> 그중 하나는 실제 테스트에서 5/5 로 결정론적으로 재현된다. CI 에서 관측된 1회가 그 둘 중
> 어느 것이었는지는 여전히 모른다.**

*이 문장이 §M-2-1 의 형식과 같은 이유*: 그 절이 정정한 것이 정확히 이 실패다 — 단일 표본을 확정으로
쓰는 것. 여기서 표본은 "기전이 존재한다"에 대해서는 5/5 + 합성 1회이고, "CI 의 그 1회가 이것이다"에
대해서는 **0** 이다. 두 문장을 하나로 합치지 않는다.

## 1-e. 파급 — 세 자리가 조건부가 된다

1. **ADR 0014 §4-1 `[PG-after-delete-reinsert] scan = ['R2','R1'] ← 심기가 postgres 에서도 먹는다`**
   는 이제 *"**그 힙 상태에서는** 먹는다"* 까지만 말한다. 계획 0009 §M-5 21 이 미리 적은 그대로다.
   **ADR 0014 는 기록물이라 고치지 않는다**(§3-13) — 정정의 자리는 ADR 0015 다.
2. **단위 짝이 같은 심기를 쓴다.** `tests/unit/progress/test_document_mapping_review_lifecycle.py:282`
   에 **이름까지 같은** `_plant_row_at_end_of_scan` 이 있고 docstring 도 같은 SQLite 사실을 적는다.
   오늘은 sqlite 라 무해하지만 **§후속 16(`tests/unit` 을 postgres 로)이 열리는 순간 같은 결함이
   그 트리로 통째로 옮겨 간다.** 그래서 작업 3 은 **두 파일을 함께** 고친다.
3. **§후속 17(정렬 미지정 42자리)의 입력이 정해졌다.** 그 항목의 첫 질문은 *"42 중 몇이 순서를
   계약으로 갖는가"* 인데, 이 사이클의 답은 그 질문의 **반대편**을 채운다: 순서를 계약으로 갖지 않는
   자리에서 **테스트가 순서를 만들어 내려 하면** 그 테스트가 DB 의 물리 배치에 매인다.
   §후속 17 이 기구를 고를 때 쓸 수 있는 축은 `created_at` 값이지 힙 배치가 아니다.

---

# 과제 2 — ADR 0015 가 정할 것 (요약 — **정본은 ADR 이다**)

| # | 결정 | 근거(실행값) |
|---|---|---|
| 1 | **테스트는 힙 배치(`ctid`·rowid·"맨 뒤")에 기대지 않는다.** 스캔 순서를 어긋나게 해야 하면 **둘의 상대 순서만** 만들고, 그 상대 순서가 만들어졌는지를 **단언한 뒤** 본 계약을 잰다 | §1-b 5/5 · §1-c `(0,1)` |
| 2 | **심기의 실패는 조용히 장식이 되지 않고 그 자리에서 죽는다** — 오늘의 :865 단언이 그 역할을 하고 있고, 그것은 **유지한다.** 바꾸는 것은 심기의 *구현*이지 그 단언의 *존재*가 아니다 | `test_20:865` 인용 |
| 3 | **`autovacuum` 을 테스트 DB 에서 끄지 않는다** | ADR 0015 §Alternatives 2 |
| 4 | **실패했을 때 다음 사람이 즉시 귀속할 수 있어야 한다** — 심기 단언의 메시지가 `ctid`(전·후)와 `autovacuum_count` 를 싣는다. 귀속 수단은 **노드 id + 이 진단**이지 단언 문구가 아니다(§M-5 21 말미: 그 문구는 소스 히트 2건) | 루트 실행값 히트 2건(§전수 목록 A) |

---

# 과제 3 — ⓐⓑⓒ 갈래 · 착수 조건 · **시간 상한**

계획 0009 §M-5 21 은 이 항목을 *"flake 를 없앤다"가 아니라 "다음 발생을 구별할 수 있게 만든다"* 로
열었다. 과제 1 이 한 발 더 갔으므로 갈래를 셋으로 세우고 각각의 착수 조건을 적는다.

### ⓐ 기전을 잡아 고친다 — **착수 조건 충족. 이 사이클의 작업 3 이다**

- **착수 조건**: "심기의 착지가 postgres 에서 비결정적이다"가 **실행값으로** 서 있을 것. → §1-b·§1-c 로 **충족**.
- **내용**: 심기를 힙 배치에 기대지 않는 형태로 바꾼다(구현 선택은 `qa` 의 것 — ADR 0015 §3).
- **성공 조건은 재현이 아니다**(§6-2). 세 칸을 **전부** 만족해야 한다:
  1. **강제 `VACUUM` 을 심기 직전에 넣은 변이**에서 **초록**(오늘 트리는 이 조건에서 **5/5 적색**).
  2. `services/progress/persistence.py:480` 의 `sorted(...)` → `return rows` 변이에서 **적색** —
     **두 축 모두**(sqlite · postgres). 이 칸이 없으면 ⓐ 는 테스트를 장식으로 만드는 변경이다.
  3. `tests/integration` 207 passed · 루트 `pytest -q` 823 passed · `make test` · `make lint` exit 0.
- **상한**: 작업 3 은 **한 커밋**이고, 탐침 실행은 **20회**를 넘기지 않는다. 넘으면 ⓐ 를 접고 ⓑ 만 남긴다.

### ⓑ 못 잡으면 판별 기구만 남긴다 — **ⓐ 를 해도 함께 한다**

- **착수 조건**: ⓐ 의 성공 조건 1 또는 2 를 상한 안에 만족시키지 못했을 때 **ⓑ 만** 남긴다.
  **그러나 ⓑ 의 산출물(진단)은 ⓐ 가 성공해도 들어간다** — ⓐ 는 *이 테스트*의 이 축을 닫을 뿐,
  같은 기전이 다른 자리에서 날 가능성을 닫지 못한다(§후속 17 의 42자리).
- **내용**: 심기 단언 메시지에 심기 **전·후 `ctid`** 와 그 시점의 `autovacuum_count`·`n_dead_tup` 을
  싣는다. 다음 발생에서 §M-5 21 이 세운 세 축(ⓐ 컨테이너 사망 ⓑ 심기 비결정성 ⓒ 그 밖)이
  **출력 한 줄로** 갈린다.
- *한정*: 이 진단은 postgres 축에서만 값을 갖는다. sqlite 에서는 `ctid` 가 없으므로 **없는 값을
  지어내지 않고 비운다**(§6-4 2: 모르는 값은 모른다고 적는다).

### ⓒ 그 사이 CI 를 어떻게 하는가 — **"무엇을 하지 않는가"의 자리**

**CLAUDE.md §3-1 이 테스트를 skip·비활성·격리하는 것을 금지한다. 그 길은 없다.** 그러므로 여기 적는
것은 하지 않을 것들의 전수다 — 적어 두지 않으면 다음 빨간 잡에서 누군가 이 중 하나를 고른다.

| 하지 않는 것 | 왜 |
|---|---|
| `@pytest.mark.skip` · `xfail` · `skipif(postgres)` | §3-1 정면 위반 |
| 마커로 빼고 CI 에서 `-m "not flaky"` | 격리의 다른 이름이다 — 같은 위반 |
| `pytest-rerunfailures` 등 자동 재시도 | **"또 그거겠지"를 기계로 만드는 것**이다. §후속 21 이 겨냥한 습관 자신 |
| 빨간 잡을 손으로 재실행하고 넘어가기 | 위와 같다. 재실행이 필요하면 **재실행 전에 junit·`-rfE` 출력에서 노드 id 를 읽어 기록한다** |
| 바닥값 `min_tests_on_postgres` 를 낮춰 여유를 만들기 | 게이트를 거꾸로 세우는 일이다(계획 0009 §M-2-2) |
| `autovacuum` 을 테스트 DB 에서 끄기 | ADR 0015 §Alternatives 2 — 테스트 DB 를 운영과 다르게 만들면 *"postgres 에서 검증된다"* 가 약해진다 |
| CI 를 postgres 스텝만 `continue-on-error` 로 | required job 의 의미를 지운다 |

**그리고 이 표가 비는 자리**: ⓐ 가 들어가기 전까지 **이 테스트는 여전히 CI 를 빨갛게 만들 수 있다.**
이 사이클은 그것을 **감수한다** — 감수의 근거는 확률(≈2.3%)이 아니라, 위 표의 어떤 칸도
"고치는 것"보다 싸지 않다는 판단이다.

---

## 영향 범위

| 층 | 무엇 | 소유 |
|---|---|---|
| 데이터 모델 | **없음.** `packages/core/models/` 를 읽기만 했다(§전수 목록 C) | — |
| 서비스 | **없음.** `services/progress/persistence.py:480` 은 **변이로만** 태우고 원복했다 | — |
| 화면 | **없음.** `apps/web/` 을 한 글자도 건드리지 않는다 | — |
| 테스트 | `tests/integration/test_20_*.py` · `tests/unit/progress/test_document_mapping_review_lifecycle.py`(심기) · `tests/integration/test_99_db_axis_contract.py`(문구) · 강제 셋 배선의 하위 프로세스 붙들기 | `qa` |
| 문서 | 이 계획 · ADR 0015 | `architect` |
| CI | **없음**(작업 5 가 워크플로를 건드려야 하면 그 판단은 소유자의 것 — 아래 표에 칸을 뒀다) | `qa` |

---

## 작업 분배

**축.** 계획 0006~0009 와 같다("한 소유가 한 커밋으로 끝낼 수 있는 단위" + **낡게 만드는 자리 두
방향**). 축을 바꾸지 않는 이유는 §6-3 9회차다 — 축을 바꾸면 무주공산은 없어지지 않고 자리를 옮긴다.

*역방향 확인 — 이 축이 놓치는 것.* 이 표는 **파일**을 낡게 만드는 것을 본다. 이 사이클이 낡게 만드는
것 중 파일이 아닌 것: **ADR 0014 §4-1 의 `[PG-after-delete-reinsert]` 한 줄**과 **계획 0009 §M-5 21 의
*"기전 미식별"***. 둘 다 `architect` 의 문서이지만 **기록물이라 고치지 않는다** — 그래서 그 칸은
"없음"이 아니라 **"고치지 않기로 한 것"** 으로 마지막 행에 적는다. 지워 두면 다음 사이클이 그 문장을
다시 믿고 컨테이너부터 판다.

| # | 에이전트 | 담당 파일 | 입력 | 출력 | 완료 조건 | 이 작업이 낡게 만드는 남의 자리 → 배정 | 이 작업을 낡게 만들 수 있는 작업 |
|---|---|---|---|---|---|---|---|
| 1 | **architect**(이 커밋) | `docs/plans/0010-*.md` | 계획 0009 §후속 1~20 · §M-4 · §M-5 21~28 · 이 사이클의 실측 | 이 계획 | §과제 1 의 두 표가 전부 실행값이고, §전수 목록 A 의 ③ 이 실제로 태워졌으며, **계획 0009 §후속 2·3·4·6·11~28 이 번호를 유지한 채 전부 옮겨졌다**(닫힌 10 은 취소선) | 없음(문서만) | 작업 2 가 ADR 문안을 바꾸면 §과제 2 요약표가 어긋난다 → 작업 6 이 대조 |
| 2 | **architect**(별도 커밋) | `docs/adr/0015-*.md` | §과제 1 실측 전부 | ADR 0015 | 결정 넷이 각각 근거 + 역방향 확인을 갖는다. §한정어 표의 각 칸이 **실행값 또는 코드 인용**이다. **ADR 0014 §4-1 의 정정이 이 문서에 있다** | 없음 | 없음 |
| 3 | **qa** | `tests/integration/test_20_mapping_decision_cancel.py` · `tests/unit/progress/test_document_mapping_review_lifecycle.py` | ADR 0015 §3, §과제 3 ⓐ·ⓑ | 힙 배치에 기대지 않는 심기 + `ctid`·`autovacuum_count` 진단 | §과제 3 ⓐ 의 세 칸 **전부**. 그리고 **두 파일의 docstring 이 새 사실을 말한다**(오늘 둘 다 *"SQLite 에서 그 행은 새 rowid 를 받아 … 맨 뒤로 간다"*, `test_20:230`·`unit:285`) | `tests/integration/test_20_*.py:29` 표 행이 *"스캔 맨 뒤로 심어야"* 라고 적는다(같은 파일·같은 소유) · `services/progress/persistence.py:433` *"실측(SQLite, …)"* 은 **좁아질 뿐 거짓이 아니다** → §후속 20 그대로 | 작업 5 가 같은 트리의 강제 셋을 만진다 → 순서 제약 |
| 4 | **qa** | `tests/integration/test_99_db_axis_contract.py` **한 줄**(`:162`) | 계획 0009 §M-2-4, CLAUDE.md §6-4 2 | 관측되는 사실만 말하는 문구 | 루트 실행값 `grep -rn "sqlite 모드가 postgres.measured.json 을 건드렸다" .` 의 **소스 히트가 그 줄 하나**였음을 커밋 본문에 적는다. 새 문구는 *"이 실행 전후로 파일이 달라졌다"* 를 말하고 **쓴 주체를 지목하지 않는다** | 없음 | 없음 |
| 5 | **qa** | 강제 셋의 무보호 배선 둘(`tests/integration/conftest.py:62` `check_contract` 호출 · `:65` `pytest_terminal_summary`) + 그것을 붙드는 새 기구 | 계획 0009 §M-5 28 | 하위 프로세스 pytest 로 둘을 붙든다 | ① 배선 ①을 지운 트리에서 **적색** ② 배선 ②를 지운 트리에서 **적색** ③ 기준선 불변(207/823) ④ **새 기구 자신의 무보호 자리를 커밋 본문에 적는다**(§6-3 9회차: 축을 바꾸면 무주공산은 자리를 옮긴다) | `tests/integration/conftest.py:71-76` 주석이 *"이 저장소에 그런 기구가 없다"* 라고 적는다 — **부재 단정이고 이 작업이 그것을 메운다**(CLAUDE.md §6-1: 부재를 적을 때 그것을 메우는 작업이 같은 사이클에 있는지 본다). 같은 파일·같은 소유라 이 작업이 함께 고친다 | 작업 3 이 같은 트리를 만진다 |
| 6 | **architect** | `docs/plans/0010-*.md` §사이클 마감 | 전부 | 마감(작업의 실제 결과 / 계획이 틀린 자리 / 이월) | 루트 clean 트리에서 전량을 다시 재고 계획과 다른 자리를 전부 적는다. **§확인하지 않은 것·§후속 은 본문 목록을 번호로 옮긴다 — 새로 쓰지 않는다** | — | — |
| — | **고치지 않기로 한 것** | `docs/adr/0014-*.md` §4-1 `[PG-after-delete-reinsert]` · `docs/plans/0009-*.md` §M-5 21(*"기전은 잡지 못했다"*) · §M-4 19 | — | — | §3-13 첫째 갈래 — 기록물은 소급 갱신하지 않는다. 정정은 ADR 0015 와 이 계획 §1-e 에 있다 | — | — |

**순서 제약.** 1 → 2 → (3 · 4 병렬 가능) → 5 → 6. 작업 3 과 5 는 **같은 디렉터리**를 만지므로 커밋을
겹치지 않게 한다(3 먼저, 5 는 3 의 트리 위에서). 작업 4 는 다른 파일이라 어디든 들어간다.

**커밋 규칙(CLAUDE.md §2).** `packages/core/models/` 를 **건드리지 않는다**(§전수 목록 C — 읽기만 했다).
작업 1·2·6 은 같은 소유지만 **커밋을 나눈다**. 커밋 전 `git status --porcelain` **전문**을 루트
`/home/user/Bim` 에서 확인하고 **경로를 명시해서** add 한다. `git commit -a` 금지.
**`make test` 통과 뒤 커밋. 푸시하지 않는다.**

**남의 트리를 만지지 않는다.** 이 사이클의 `architect` 는 `docs/` 밖으로 나가지 않는다.
**탐침을 위한 임시 변이는 적용 → 측정 → 원복 → 루트 clean 확인을 한 건씩 했다**(§과제 1 의
`persistence.py:480` 변이 1건). 탐침 플러그인은 **저장소 밖**(스크래치패드)에 두고 `-p` 로만 붙였다 —
`tests/` 에 파일을 만들지 않았다.

**탐침의 부작용 하나를 적는다(§후속 29 의 근거).** `tests/integration` 의 **부분집합**을 postgres 축으로
돌리면 `tests/postgres.measured.json` 이 낮은 `tests_on_postgres` 로 덮여 **트리가 더러워지고**
teardown ERROR 가 난다(실행값: `-k` 로 한 건만 돌린 뒤 `git status --porcelain` → ` M
buildtwin/tests/postgres.measured.json`, 그리고 `AssertionError: postgres 위에서 SQL 을 실행한 테스트가
1건으로 바닥값 181 미만이다`). **계약이 의도한 동작이지만**, 탐침을 돌리는 사람은 매번 원복해야 한다.
이 계획의 모든 탐침 뒤에 `git checkout -- buildtwin/tests/postgres.measured.json` 을 돌렸고
마지막 상태는 clean 이다.

---

## 인터페이스 정의

이 사이클은 **런타임 인터페이스를 바꾸지 않는다**(운영 코드 0줄). 정의되는 것은 **테스트 헬퍼의 계약**
하나이고, 구현은 `qa` 의 것이다.

```python
# tests/integration/test_20_mapping_decision_cancel.py  (qa 소유 — 작업 3)
# tests/unit/progress/test_document_mapping_review_lifecycle.py  (qa 소유 — 같은 작업)
#
# 계약(ADR 0015 §3):
#   "두 요청 행 A(이른 created_at) · B(늦은 created_at) 에 대해, `ORDER BY` 없는 조회가
#    B 를 A 보다 먼저 돌려주는 상태를 만든다. 값(created_at 포함)은 하나도 바꾸지 않는다."
#
#   ① 이름은 **착지를 약속하지 않는다** — "맨 뒤로 심는다"가 아니라 "상대 순서를 어긋나게 한다".
#   ② 만들어졌는지를 **그 자리에서 단언한다**(오늘의 :865 를 유지한다 — ADR 0015 결정 2).
#   ③ 실패 메시지는 심기 전·후 `ctid` 와 `autovacuum_count` 를 싣는다. sqlite 축에서는 비운다.
def _make_scan_order_disagree_with_created_at(...) -> None: ...
```

---

## 전수 목록 (§6-1 ①②③ — 전부 저장소 루트 `/home/user/Bim` 에서 만들었다)

### 목록 A — **"심기의 착지"에 기대는 자리**

**① 생성 기준**: 루트 실행값
`grep -rn "_plant_row_at_end_of_scan\|맨 뒤\|rowid\|스캔 순서\|물리 위치\|삽입 순서" . --include='*.py' --include='*.ts' --include='*.tsx' --include='*.yml'`
— 즉 **경로가 아니라 문자열**로 만들었고(§6-1 규칙), 표기 집합을 여섯으로 넓혔다
(§6-1 *역방향 확인*이 요구하는 것: 값 하나가 아니라 **그 값이 쓰이는 표기 집합**).

| # | 자리 | 무엇을 말하는가 | 판정 |
|---|---|---|---|
| A1 | `tests/integration/test_20_mapping_decision_cancel.py:228-230` | `_plant_row_at_end_of_scan` + *"SQLite 에서 그 행은 새 rowid 를 받아 … 맨 뒤로 간다"* | **대상.** 작업 3 |
| A2 | 같은 파일 `:865` | `assert [x[0] for x in after] == [second, first]` — 흔들리는 그 칸 | **대상.** 작업 3 |
| A3 | 같은 파일 `:29` 표 행 *"스캔 맨 뒤로 심어야 갈린다"* · `:789` · `:809`(*"어긋나게 하는 축은 … 물리 위치다"*) · `:850` 주석 | 심기의 근거를 적는 산문 | **대상.** 작업 3(같은 파일·같은 소유) |
| A4 | `tests/unit/progress/test_document_mapping_review_lifecycle.py:282-285` · `:290` · `:316` · `:319-320` · `:357` · `:369` | **이름까지 같은** 심기와 그 산문. 오늘은 sqlite | **대상.** 작업 3 — §후속 16 이 열리면 같은 결함이 이 트리로 온다(§1-e 2) |
| A5 | `services/progress/persistence.py:444-445` *"새 행은 언제나 맨 뒤에 생기고, 되여는 행도 이 정렬 뒤에는 맨 뒤 행이다"* | **히트지만 대상이 아니다** — 여기서 "맨 뒤"는 **`created_at` 정렬 뒤의 마지막 원소**이지 힙 위치가 아니다(바로 앞 문장이 `.order_by(ReviewRequestRow.created_at.desc())` 를 말한다, `:456`) | 대상 아님. **근거를 적어 둔다** — 다음 사람이 이 줄을 히트로 보고 고치지 않도록 |
| A6 | `tests/unit/ingest/conftest.py:6` · `tests/unit/sync/conftest.py:6` *"SQLAlchemy가 테이블 간 삽입 순서를 보장하지 않는다"* | ORM flush 순서 이야기이고 힙 배치가 아니다 | 대상 아님 |

**② 이 기준이 놓치는 것.** 축을 여섯 표기로 넓혔지만 여전히 **문자 그대로의 문자열**이다.
놓치는 것 셋을 이름 붙여 둔다:
- **말하지 않고 기대는 자리** — `order_by` 없이 다중행을 돌려받아 `[0]`·`[-1]` 을 읽는 코드는 이 목록의
  어느 문자열도 쓰지 않는다. 그 자리의 전수는 **§후속 17 의 42자리**이고 이 사이클은 그것을 열지 않는다.
- **영어 표기** — `heap`·`physical order`·`tuple order`. 루트 실행값
  `grep -rn "heap order\|physical order\|tuple order" . --include='*.py'` → **히트 0**(`.venv` 제외).
- **`ctid` 자체** — 루트 실행값 `grep -rn "ctid" . --include='*.py' | grep -v .venv` → **히트 0**.
  즉 이 저장소는 오늘 힙 위치를 **관측하는 코드가 하나도 없다**. 작업 3 의 ⓑ 가 그 첫 자리가 된다.

*"루트에서 만든다"가 함께 들이는 것 — 이 사이클이 처음 만났다.* 루트 grep 은 **`.git/` 도 훑는다.**
실행값: `grep -rn "심기가 먹지 않았다" .` 의 히트 셋 중 하나가 `.git/COMMIT_EDITMSG:16` 이었다
(작업 트리 파일이 아니라 **직전 커밋의 편집 버퍼**다). 소스 히트는 그것을 뺀 **2건**(A1 · A4)이고
계획 0009 §M-5 21 말미의 값과 같다. **`.git/` 히트를 세면 목록이 부풀고, 빼는 것을 적어 두지 않으면
다음 사람이 다른 수를 얻는다.**

**③ 블라인드 스팟을 태웠다.** 위 첫 번째("말하지 않고 기대는 자리")를 **A2 자신에 대해** 태웠다 —
`test_20:865` 는 *"맨 뒤"* 라는 말을 쓰지 않고 `== [second, first]` 라는 **값**으로 힙 배치에 기댄다.
`ctid` 로 관측하니 그 기댐이 `(0,7)→(0,4)` 에서 정확히 깨졌고, **5/5 죽었다**(§1-b). 문자열 목록만으로는
이 칸이 "심기의 착지에 기대는 자리"인 줄 알 수 없었다.

### 목록 B — 이월 목록의 **열린 항목 전수 재측정** (§6-1 12회차가 요구하는 것)

**① 생성 기준**: 계획 0009 §후속 + §M-5 의 **번호 전부**. 닫힌 것(10)은 취소선으로 남기고,
열린 것은 **HEAD `743bcb9` 에서 다시 쟀다.** ② **놓치는 것**: 이 재측정은 각 항목이 *여전히 열려
있는가*만 본다 — 항목의 **크기**나 **다른 항목과의 결합**은 재지 않는다. ③ 아래 표가 그 실측이다.

| # | 재측정(HEAD `743bcb9`) |
|---|---|
| 2 | `grep -rln "rejected" buildtwin/tests/invariants/` → **exit=1(히트 0)**. 열림 |
| 3 | `apps/web/src/api/client.ts:12` `TODO(round-6+)` **그대로**. 열림 |
| 4 | 저장소로 답할 수 없는 항목(운영 데이터). 열림 |
| 6 | `services/api/scripts/gen_api_doc.py` `ERROR_ENVELOPE_SECTION` 의 열거가 **여전히 다섯**(`InvalidTransitionError`/`RevocationReasonRequiredError`/`TransitionBlockedByReviewError`/`ObjectNotFoundError`/`ReviewRejectionReasonRequiredError`) — 계획 0006 의 예외 둘이 없다. 열림 |
| 11 | `services/api/usecases.py` 에 `record_expert_review` 호출 **7자리**(`:242,354,411,433,451,571` + import). 확정 축 호출 그대로. **변이는 이번에 재지 않았다**(§확인하지 않은 것 6). 열림 |
| 12 | `tests/unit/progress/test_document_mapping_review_lifecycle.py` 머리 *"다음 여섯 항목"*, 실측 `def test_` **8개**. 열림 |
| 13 | `tests/integration/test_20_*.py:29` 표 행이 *"그 전에는 **805 passed**"* 로 트리를 인라인으로 적지 않는다. 열림 |
| 14 | 같은 파일 `:705` *"이 사이클(기준선 807)"* 그대로. 열림 |
| 15 | CLAUDE.md §6-1 12회차 행의 관측이 **여전히 1건**(이 사이클도 §6-1 을 편집하지 않는다). §6-5 문턱 미달 — **이번에도 넓히지 않는다** |
| 16 | `tests/unit` 은 여전히 sqlite. **§1-e 2 가 이 항목에 새 근거를 준다** — 단위 짝의 심기가 같은 결함을 갖고 있다 |
| 17 | 42자리 그대로. **§1-e 3 이 이 항목에 입력을 준다** |
| 18 | `services/api/main.py` 시드 조건 그대로. 열림 |
| 19 | CLAUDE.md §2 트리에 `pyproject.toml`·`Dockerfile`·`docker-compose.yml` **없다**(각 히트 0). 열림 |
| 20 | `services/progress/persistence.py:431` *"실측(SQLite, 이 저장소 통합 테스트 환경)"* 그대로. 열림 |
| 21 | **이 사이클의 대상.** 마감에서 판정한다 |
| 22 | `grep -rn "sqlite 모드가 postgres.measured.json 을 건드렸다" .` → **소스 히트 1건**(`tests/integration/test_99_db_axis_contract.py:162`). **이 사이클의 작업 4** |
| 23 | `server_version` 소스 히트 **8건**(`tests/helpers/postgres_axis.py:104,116,117,147,169` · `test_99:122,126` · `tests/postgres.measured.json:4`) — **실제 서버 값에 대한 단언·비교는 0건**. 계획 0009 §M-3 의 값과 같다. **처분: 이월**(아래 §처분 표) |
| 24 | 로컬 클러스터에 PostGIS **부재**(`pg_available_extensions` 에서 `postgis` 행 없음 — 이 사이클 실행값). 로컬로는 답할 수 없다. **처분: 이월** |
| 25 | CLAUDE.md §6-5 그대로. **이번이 아니다**(한 사이클 한 절) |
| 26 | **처분: 이번 사이클에서 닫는다** — 이 계획의 §확인하지 않은 것이 "재지 않았다"와 "재야 한다(소유·조건)"를 **갈라 적는다** |
| 27 | **처분: 이번 사이클에 적용한다.** §1-d 가 그 형식이다(N 을 함께 적었다: 5/5 · 3/3 · 1회). 규약의 자리는 CLAUDE.md 라 **항목은 열어 둔다** |
| 28 | `tests/integration/conftest.py:62`(`check_contract` 호출) · `:65`(`pytest_terminal_summary`) 둘 다 그대로. **이 사이클의 작업 5** |

### 목록 C — 이름 붙은 블라인드 스팟(`packages/core/models/` 의 주석)

**① 생성 기준**: `grep -rn "순서\|sqlite\|SQLite\|postgres\|PostgreSQL\|스캔\|정렬" buildtwin/packages/core/models/*.py`
— 즉 **이 사이클의 축(DB·순서)에 대해 그 디렉터리의 주석이 하는 모든 단정**.
② **놓치는 것**: 이 여섯 낱말을 쓰지 않고 같은 것을 말하는 주석(예: *"행이 쌓인다"*).

| 자리 | 단정 | 오늘 참인가 |
|---|---|---|
| `orm.py:1` | *"JSON 컬럼으로 bbox/psets/evidence 저장(**SQLite·PostgreSQL 공용**). PostGIS 공간 인덱스는 Deferred(ADR)."* | **참.** 계획 0009 가 postgres 에서 `create_all` 24 테이블을 태웠고 이 사이클의 통합 207 passed 가 같은 스키마 위다. PostGIS 는 여전히 Deferred(§후속 24) |
| `orm.py:236` · `scan.py:1,45` · `document.py:167` · `state.py:205` | 상태기계·좌표·전이에 대한 단정. **DB 축에 대해 아무것도 말하지 않는다** | 이 사이클과 무관 |

**③ 실측 — 이 디렉터리는 힙 배치에 대해 아무 단정도 하지 않는다.**
`grep -rn "ctid\|rowid\|맨 뒤\|스캔 순서" buildtwin/packages/core/models/` → **히트 0**.
그러므로 이 사이클은 `packages/core/models/` 를 **읽기만 하고 한 글자도 고치지 않는다.**
*이 결론이 기대는 것*: 위 ② 가 인정한 표기 변종이 이 디렉터리에 **없다**는 것이고, 그것은
파일 다섯을 눈으로 읽어 확인했다(`orm.py`·`review.py`·`state.py`·`scan.py`·`document.py`의
DB 관련 주석 전부가 위 표 두 행에 들어온다).

---

## 검증 시나리오 (§6-2 — 각 시나리오에 **"결함 있는 코드가 이 기대값을 그대로 만족하는가"** 를 물었다)

**이 사이클에 걸린 특별한 경고**: *"40회 초록"은 결함 있는 코드에서도 참이다* — 44회 중 1회가 이미
그것을 보였다. **재현을 성공 조건으로 삼는 시나리오는 하나도 세우지 않는다.**

| # | 시나리오 | 기대값 | 결함 있는 코드가 이 기대값을 그대로 만족하는가 |
|---|---|---|---|
| V1 | **오늘 트리** + 심기 직전 `VACUUM review_requests` | `test_20:865` 에서 **적색** | **아니다.** 심기가 힙 배치에 기대지 않는 트리에서는 초록이다 — 이 칸이 ⓐ 의 양성 케이스다. 오늘 실행값 **5/5 적색** |
| V2 | **작업 3 뒤 트리** + 같은 강제 `VACUUM` | **초록** | **아니다.** 오늘 트리는 이 조건에서 죽는다(V1). 두 트리가 이 칸에서 갈린다 |
| V3 | 작업 3 뒤 트리 + `persistence.py:480` → `return rows`, **postgres** | **적색** | **아니다.** 이 칸이 없으면 작업 3 은 "테스트를 안 죽게 만든 변경"일 뿐이다. §6-2 4(두 사실을 함께 단언한다): V2·V3 **둘 다**여야 의미가 있다 |
| V4 | 같은 변이, **sqlite** | **적색** | **아니다.** 오늘 sqlite 축에서 그 변이가 잡히는 것은 계획 0008 이 심기를 넣었기 때문이다 — 작업 3 이 그 성질을 지우면 안 된다. **음성 대조군을 한 축에 몰지 않는다**(§6-2 3) |
| V5 | 작업 4 뒤: `postgres.measured.json` 을 **밖에서** 고친 상태로 sqlite 축 실행 | 실패 **문구가 쓴 주체를 지목하지 않는다** | **아니다.** 오늘 문구는 *"sqlite 모드가 … 건드렸다"* 라 밖에서 고쳤을 때도 sqlite 모드를 지목한다(계획 0009 §M-2-4 가 실제로 만났다) |
| V6 | 작업 5 뒤: `check_contract(...)` **호출**만 삭제 | **적색** | **아니다.** 오늘은 postgres 전량 **207 passed**(리뷰어 실측, 계획 0009 §M-5 28) |
| V7 | 작업 5 뒤: `pytest_terminal_summary` 훅 **통째 삭제** | **적색** | **아니다.** 오늘은 두 축 다 **207 passed** 이고 `[db-axis]` 줄만 조용히 사라진다 |
| V8 | 작업 3·5 뒤 기준선 | 루트 `pytest -q` **823** · `make test` **492/104/8/207 + 283** · postgres 통합 **207**(`tests_on_postgres=181`) · `make lint` exit 0 | — (기준선 확인이지 판별 시나리오가 아니다. **이 행을 판별로 쓰지 않는다** — 초록은 증거가 아니다, ADR 0014 §5-3) |

**세우지 않은 시나리오와 그 이유.** *"작업 3 뒤에 postgres 통합을 N회 돌려 초록이면 닫는다"* —
**세우지 않는다.** 2.3% 짜리에 대해 N=40 이 주는 것은 "이 표본에서 안 났다"뿐이고
(계획 0009 §M-5 21 이 이미 40/40·25/25 로 그것을 겪었다), 그것을 성공 조건으로 쓰는 것이
**§6-2 1 위반**이다. 판별은 V1↔V2 의 **강제 조건**이 한다.

---

## 한정어 역방향 확인 표 (§6-3 산출물 — 각 칸은 **실행값 또는 코드 인용**이고 다른 절 참조가 아니다)

| 한정어 | 이 단어를 빼면 무엇이 더 들어오나 | 이 단어 때문에 무엇이 빠지나 | 실행값 / 인용 |
|---|---|---|---|
| **"기전을 잡았다"**(§과제 1 제목) | *CI 의 그 실패를 재현했다*로 읽힌다 | 재현은 못 했다 — 성립하는 것은 *"비결정성을 만드는 기전이 있고 강제하면 결정론적으로 난다"* 까지 | 강제 3회(naptime=1s) **3/3 207 passed** — autovacuum 이 돌아도 안 났다(§1-d) |
| **"5/5 죽는다"**(§1-b) | 이 테스트가 postgres 에서 늘 죽는다고 읽힌다 | **강제 `VACUUM` 을 심기 직전에 넣은 조건**에서만이다 | 같은 명령에서 `PROBE_VACUUM` 없이 → `1 passed`, 대상 `ctid (0,7)→(0,10)` |
| **"맨 뒤에 앉지 않는다"**(§1-a 단정) | *언제나 앞에 앉는다*로 읽힌다 | 자리가 있으면 맨 뒤에 앉는다 — 뒤집히는 것은 **재활용 가능한 앞 자리가 있을 때**뿐 | `[plant-no-vacuum] (83,9)` ↔ `[plant-after-vacuum] (0,1)`(§1-c) |
| **"`VACUUM` 이 방아쇠"**(§1-c) | `VACUUM` 만이 유일한 경로로 읽힌다 | HOT 가지치기(`heap_page_prune_opt`)도 line pointer 를 되돌릴 수 있다 — **이 사이클은 그 경로를 태우지 않았다** | 전량 실행의 `n_tup_hot_upd=6`(전체 `n_tup_upd=138` 중) — HOT 갱신은 적다. 그러나 0 은 아니다 |
| **"단위 짝도 같은 결함"**(§1-e 2) | 오늘 그 테스트가 흔들린다고 읽힌다 | 오늘 `tests/unit` 은 **sqlite** 라 무해하다 | `tests/unit/progress/test_document_mapping_review_lifecycle.py:285` 가 *"SQLite 에서 그 행은 새 rowid 를 받아 … 맨 뒤로 간다"* 라고 스스로 sqlite 로 한정한다 |
| **"운영 코드 0줄"**(§영향 범위) | 서비스가 이 결함과 무관하다고 읽힌다 | 무관한 것은 **이 사이클의 변경**이지 결함이 아니다 — `document_mapping_reviews` 의 무정렬 `select` 는 그대로다 | `services/progress/persistence.py:475-480`: `select(ReviewRequestRow).where(...)` 뒤 `sorted(rows, key=lambda r: r.created_at)` — **`order_by` 가 없다**(§후속 17 의 42자리 중 하나) |
| **"소스 히트 1건"**(§작업 분배 4) | 저장소에 그 문자열이 하나라고 읽힌다 | 문서 히트는 뺐다 | 루트 실행값: 히트 셋 중 `__pycache__/*.pyc` 하나 · `docs/plans/0009-*.md` 하나 · 소스 `tests/integration/test_99_db_axis_contract.py:162` 하나 |
| **"저장소 루트"**(§0) | `buildtwin/` 을 루트로 읽으면 `.github/workflows/` 가 통째로 목록 밖이다 | — (좁히면 실제로 놓쳤다 — 계획 0007 §0 의 선례) | 이 문서의 모든 grep 은 `cd /home/user/Bim` 에서 돌렸다 |
| **"CI 를 빨갛게 만들 수 있다"**(§과제 3 ⓒ) | 반드시 빨개진다고 읽힌다 | 관측된 빈도는 **누적 102회 중 2회**이고 그중 1회만 테스트가 특정됐다 | 계획 0009 §M-4 19 의 표본 인용(qa 10회 2실패 · qa 33회 1 미식별 · 리뷰어 25회 0 · 리뷰어 44회 1) |

*같은 문서의 인접 절과 교차 확인(§6-3).* 이 문서 안에서 같은 값을 다르게 말하는 줄을 찾았고 **한 쌍**이
있었다: §1-b 는 *"5/5 죽는다"* 라 하고 §1-d 는 *"재현하지 못했다"* 라 한다. 정면 충돌처럼 보여서
**실행으로 갈랐다** — 두 문장은 **다른 조건의 실행**을 센다. 앞은 *심기 직전에 `VACUUM` 을 넣은* 실행
5회, 뒤는 *넣지 않고 autovacuum 에 맡긴* 실행 4회(기본 1 + 강제 3)다. 둘 다 참이고, 그 차이가 곧
**"방아쇠는 `VACUUM` 이고 CI 에서 그것을 부르는 것은 autovacuum 이며 그것만으로는 부족하다"** 이다.
그리고 §과제 3 ⓐ 는 *"착수 조건 충족"* 이라 하고 §1-d 는 *"CI 의 1회가 이것인지 모른다"* 라 한다 —
이것도 갈랐다: ⓐ 의 착수 조건은 **"기전이 있다"** 이지 **"그 1회가 이것이다"** 가 아니다. 후자를
착수 조건으로 걸면 §6-2 1 이 금지하는 재현-성공-조건이 된다.

---

## 열린 질문 / 리스크

1. **CI 의 그 1회가 이 기전이 아닐 수 있다.** §1-d 가 그 한계다. 작업 3 이 들어간 뒤에도 같은
   테스트가 다시 죽으면 **기전은 다른 것**이고, 그때 필요한 것이 ⓑ 의 진단이다 — 그래서 ⓑ 는
   ⓐ 가 성공해도 들어간다.
2. **작업 3 의 구현이 또 다른 물리 가정에 기댈 수 있다.** 이 계획이 실측한 후보 하나(둘을 함께 지우고
   `created_at` 역순으로 다시 넣기)는 강제 `VACUUM` 아래에서도 상대 순서를 지켰지만(실행값:
   `VACUUM` 없음 → 늦은 행 `(0,10)` · 이른 행 `(1,1)`; `VACUUM` 있음 → 늦은 행 `(0,4)` · 이른 행 `(1,1)`)
   **그것도 보장이 아니다** — 두 번째 `INSERT` 가 FSM 을 통해 **앞 페이지**로 갈 수 있다(§1-c 가 보인
   바로 그 경로). **그래서 이 계획은 그 후보를 결정으로 내리지 않는다**(ADR 0015 §3: 구현은 소유자의
   판단이고, 요구되는 것은 **§과제 3 ⓐ 의 세 칸**이다).
3. **작업 5 의 새 기구(하위 프로세스 pytest)가 자기 몫의 무보호 배선을 갖고 온다**(§6-3 9회차).
   계획 0009 §M-5 28 이 *"이번에 닫지 않는 것이 옳다"* 고 판정한 근거가 이것이고, 이 사이클은 그것을
   **알고** 연다 — 완료 조건 ④ 가 그 자리를 커밋 본문에 적게 한다.
4. **작업 3 과 5 가 같은 트리를 만진다.** 계획 0009 마감은 병렬 편집으로 절대값이 사이클 안에서
   움직이는 것을 겪었다(§M-0). 순서 제약을 못박은 이유다.
5. **바닥값 여유는 여전히 0 이다**(`min_tests_on_postgres=181`, 측정값 181). 작업 3·5 가 통합
   테스트 수를 바꾸면 `qa` 가 바닥값을 함께 올려야 한다(부등호가 `>=` 라 자동으로 따라오지 않는다).

---

## ADR 필요 여부

**필요하다. 이 사이클이 쓴다** — `docs/adr/0015-heap-placement-is-not-a-test-contract.md`.

근거 셋: ① **ADR 0014 §4-1 의 실측 한 줄을 조건부로 만드는 결정**이라 ADR 밖에 두면 다음 사이클이
옛 줄을 근거로 되돌린다. ② 그 결정이 **§후속 16·17 의 입력**이라 두 항목이 읽을 자리가 필요하다.
③ *"테스트는 힙 배치에 기대지 않는다"* 는 **이 테스트 하나에 대한 수정이 아니라 규칙**이고,
규칙은 계획(한 사이클짜리 기록물)이 아니라 ADR 이 소유한다.

---

## 후속 — 다음 사이클로 넘기는 것

**이월 규칙.** 계획 0009 §후속 **1~20**(본문) + **§M-5 21~28** 을 **번호를 유지한 채** 옮긴다.
닫힌 항목은 지우지 않고 취소선 + 닫은 근거를 남긴다. **열린 항목은 `743bcb9` 에서 전부 재측정했다**
(§전수 목록 B 가 그 표다). 신규는 **29번부터** 잇는다.

*계획 0008 §M-2-2 가 세운 규칙을 이 목록에 건다*: **본문 목록을 번호로 옮긴다, 새로 쓰지 않는다.**
빠진 항목은 닫힌 것과 달리 `git log -S` 로 되찾을 수 없다.

1. ~~**§6-2·§6-4 압축**(architect).~~ → **닫혔다**(계획 0007 §과제 1). 다시 여는 조건은 그 항목에 있다.
2. **`"rejected"` 값 리터럴의 전수 감사**(qa). 재측정: 목록 B 2행.
3. **`apps/web/src/api/client.ts:12` 의 TODO**(qa). 재측정: 목록 B 3행.
4. **검토요청 *승인*의 사유 요건**(ADR 0011 §Deferred 1 / ADR 0012 §Deferred 1). **저장소로는 답할 수 없다**.
5. ~~**취소의 내구 감사를 붙드는 회귀 + V8 docstring**(qa).~~ → **닫혔다(`716d67d`).**
6. **`docs/api.md` 오류 절의 예외 열거와 `internal_error`**(api). 재측정: 목록 B 6행.
7. ~~**`cancelled_review_request_id` 의 갱신 갈래**(progress-engine).~~ → **닫혔다**(`662e91a` + `3ba9226`).
8. ~~**`find_document_mapping_review` 의 `ORDER BY created_at DESC` 가 무보호**(qa).~~ → **닫혔다**
   (`3ba9226`). **한정: 잰 것은 줄 삭제 변이 하나뿐이다.**
9. ~~**`document_mapping_reviews` 의 `sorted(...)`**(qa).~~ → **닫혔다**(`7d44cca`).
   **이 사이클이 더하는 한정**: 그 두 테스트가 postgres 에서 장식이 아닌 것은 **심기가 먹었을 때**뿐이고,
   심기의 착지는 postgres 에서 계약이 아니다(§과제 1). 그래서 §후속 21 이 이 항목의 뒷면이다.
10. ~~**PostgreSQL 경로가 비어 있다**(qa + architect).~~ → **닫혔다**(계획 0009 §M-3-c) —
    *"postgres 에서 아무 테스트도 돌지 않는다"* 하나에 한해서. 다른 축은 16·17·18·21·24 다.
11. **확정 축의 `expert_review_logs` 도 무보호**(qa). 재측정: 목록 B 11행. **변이는 이번에 재지 않았다.**
12. **`tests/unit/progress/test_document_mapping_review_lifecycle.py:5-6` 의 "다음 여섯 항목"**(qa).
    재측정: 실측 `def test_` **8개**. **작업 3 이 같은 파일을 만지므로 함께 고칠 수 있다 —
    그러나 이 사이클의 완료 조건에는 넣지 않는다**(작업 3 의 축은 심기이고, 개수 문장은 다른 축이다).
13. **`tests/integration/test_20_*.py:29` 의 표 행이 트리를 인라인으로 적지 않는다**(qa). 재측정: 그대로.
    **작업 3 이 그 행을 고치므로 같은 커밋에서 트리를 넣을 수 있다.**
14. **같은 파일 `:705` 의 "이 사이클(기준선 807)"**(qa). 재측정: 그대로.
15. **§6-1 12회차 행의 "밀려난 것" 칸을 넓힐지**(architect). 재측정: 관측 **여전히 1건** → 넓히지 않는다.
16. **`tests/unit` 을 postgres 로 돌린다**(qa). **이 사이클이 새 근거를 준다**: 단위 짝의 심기가
    같은 결함을 갖고 있어(§1-e 2), 이 항목을 열기 **전에** 작업 3 이 들어가야 한다.
17. **정렬을 지정하지 않은 42자리의 순서 계약**(architect + progress-engine + api).
    **이 사이클이 입력을 준다**(§1-e 3): 순서를 계약으로 갖지 않는 자리에서 테스트가 순서를 만들려
    하면 그 테스트가 힙 배치에 매인다. 기구의 축은 `created_at` 값이지 물리 위치가 아니다.
18. **데모 시드를 앱 기동에서 떼어낸다**(api + qa, ADR 0014 §Deferred 1).
19. **소유가 §2 에 없는 파일들**(architect). 재측정: 목록 B 19행. CLAUDE.md 편집이라 별도 사이클.
20. **`services/progress/persistence.py:433` 의 *"실측(SQLite, …)"***(progress-engine). 재측정: 그대로.
21. **postgres 축의 흔들림**(qa). **이 사이클의 대상. 마감에서 판정한다.**
    닫는 범위는 **작업 3 의 세 칸**(§과제 3 ⓐ)이고, *"CI 의 그 1회를 재현했다"* 는 **닫지 않는다**(§1-d).
22. **`postgres_axis` 계약의 실패 문구가 경위를 하나로 단정한다**(qa). **이 사이클의 작업 4.**
23. **`postgres.measured.json` 의 `server_version` 을 어떻게 할지**(qa + architect). **이월** —
    처분 근거는 아래 §처분 표.
24. **PostGIS 가 로드된 DB 에서 무엇이 달라지는가**(qa). **이월** — 처분 근거는 아래 §처분 표.
25. **CLAUDE.md §6-5 의 *"잡은 것은 언제나 바깥에서 읽은 리뷰어였다"* 를 좁힌다**(architect).
    **이번이 아니다** — CLAUDE.md 는 한 사이클에 한 절이고, 이 사이클은 **어느 절도 만지지 않는다.**
26. ~~**§확인하지 않은 것 중 *다음 행동이 필요한 항목*에 소유를 배정한다**(architect).~~
    → **닫힌다(이 계획).** 아래 §이 계획이 확인하지 않은 것이 **"재지 않았다"** 와
    **"재야 한다 — 소유·조건"** 을 갈라 적는다. *한정*: 닫힌 것은 **형식**이고, 그 형식이 다음
    사이클에도 유지되는지는 아무 기계도 보지 않는다.
27. **postgres 축에서 "0 실패"·"전부 통과" 같은 부정 단정을 쓸 때는 N 을 함께 적고 N≥10 으로
    잰다**(architect + qa). **이 사이클이 적용했다**(§1-b 5/5 · §1-d 3/3 · 1회를 전부 N 과 함께 적었다).
    *한정*: **N≥10 을 지키지 못한 자리가 있다** — §1-d 의 기본 설정 실행은 **N=1** 이다(전량 실행이
    40초라 10회면 7분이고, 그 값은 "재현 실패"를 말할 뿐 판정에 쓰이지 않는다). **항목은 열어 둔다** —
    규약의 자리는 CLAUDE.md 이고 그 자리는 §후속 25 뒤다.
28. **강제 셋의 배선 둘은 지워도 전량이 초록이다**(qa). **이 사이클의 작업 5.**

**신규(이 사이클이 연다):**

29. **부분집합을 postgres 축으로 돌리면 트리가 더러워진다**(qa). 실행값: `-k` 로 한 건만 돌린 뒤
    `git status --porcelain` → ` M buildtwin/tests/postgres.measured.json`, 그리고 teardown 에서
    `AssertionError: … 1건으로 바닥값 181 미만이다`. **계약이 의도한 동작**(§2-5 2)이지만, 이 축을
    탐침으로 쓰는 사람은 매번 원복해야 하고 **원복을 잊으면 그 파일이 커밋에 섞인다.**
    선택지 셋 — ⓐ 그대로 두고 사용법을 파일 주석에 적는다 ⓑ 부분집합 실행에서는 **쓰지 않는다**
    (`-k`/`--deselect` 가 있으면 기록만 하고 파일을 건드리지 않는다) ⓒ 측정 파일을 `.gitignore` 로
    옮기고 CI 아티팩트로만 남긴다(§후속 23 과 같은 결정이 된다).
30. **테스트 DB 의 `autovacuum` 을 끌 것인가**(qa + architect). ADR 0015 §Alternatives 2 가 **이번엔
    기각**했다(테스트 DB 를 운영과 다르게 만들면 *"postgres 에서 검증된다"* 가 약해진다).
    **§후속 16 이 `tests/unit` 을 postgres 로 옮길 때 다시 물어야 한다** — 그때는 같은 기전이
    수백 건의 테스트에 걸린다.

---

## Deferred — 이 사이클이 보고 고치지 않는 것 (계획 0009 §Deferred 1~6 이월, 재측정하지 않았다)

1. **객체가 검측 루프를 떠난 뒤에도 닫히지 않는 inspection 요청.**
2. **반려된 2D↔3D 매핑이 뷰어 계약에 계속 실린다**(sync-2d3d 소유, 별도 ADR 필요).
3. **확정↔취소 반복의 누적.**
4. **`confirm_mapping_row` 의 `.first()`**(`services/sync/review_queue.py`).
5. **`on_hold` 에 공백만 note 를 보내면 `"   "` 가 그대로 저장된다.**
6. **ADR 0013 §Deferred 1~8** 은 그 ADR 이 소유한다.

**ADR 0014 §Deferred 1~6** 은 **그 ADR 이 소유한다.** 여기 옮겨 적지 않는다. 그중 행동이 필요한 둘은
§후속 18·19 로 번호를 받았다. **ADR 0015 §Deferred** 도 같은 규칙이다.

---

## §처분 표 — 리뷰어가 "판단 항목"으로 넘긴 넷

| # | 처분 | 근거 |
|---|---|---|
| **23** `server_version` | **이월.** 다시 여는 조건을 못박는다: *"`server_version` 을 읽는 자리 중 **단언·비교**가 하나라도 생기는 순간"* | 이 사이클 실측 — 소스 히트 **8건, 단언·비교 0건**(목록 B 23행). 무해함이 유지된다. **그리고 작업 5 가 같은 축 파일(`tests/helpers/postgres_axis.py`·`conftest.py`)을 만진다** — 같은 사이클에 두면 두 변경이 한 커밋에 섞이고 리뷰어가 하나만 보고 판정할 수 없다(§2 커밋 규칙의 정신) |
| **24** PostGIS | **이월.** 다시 여는 조건: *"§후속 16 이 `tests/unit` 을 postgres 로 옮길 때, 또는 스키마에 공간 컬럼이 처음 들어올 때"* | 로컬 클러스터에 PostGIS 가 **부재**해(이 사이클 실행값: `pg_available_extensions` 에 `postgis` 행 없음) 저장소 밖 환경 없이는 답할 수 없다. CI 에서만 재는 것은 이 사이클의 범위를 넘는다 |
| **26** 소유 배정 | **닫는다(이 계획).** §이 계획이 확인하지 않은 것을 두 갈래로 갈라 적었다 | 리뷰어 지적의 내용이 *"목록의 항목은 **재지 않았다**와 **재야 한다**를 구별해야 하고 후자에는 소유가 붙어야 한다"* 이고, 그것은 **형식**이라 문서 하나로 닫힌다 |
| **27** N≥10 | **적용하고 항목은 연다.** 이 사이클의 모든 postgres 축 단정에 N 을 붙였다 | 규약의 자리는 CLAUDE.md 이고 그 자리는 **한 사이클 한 절** 규칙에 걸려 §후속 25 뒤다. 지금 닫으면 다음 사이클이 규약을 읽을 자리가 없다 |

---

## 이 계획이 확인하지 않은 것

**계획 0009 §M-4 1~21 을 번호를 유지한 채 옮기고**(§M-2-2 규칙 — 새로 쓰지 않는다), 이 사이클이
처분한 것은 처분을 적는다. 신규는 **22번부터**.
**§후속 26 이 요구한 형식을 여기서 처음 적용한다** — 각 항목에 **[재지 않았다]** 또는
**[재야 한다 → 소유 · 언제]** 를 붙인다.

1. **[재야 한다 → qa · §후속 24 를 열 때]** CI 의 `postgis/postgis:16-3.4` 에서 아무것도 재지 않았다.
   이 사이클도 잡 로그조차 읽지 않았다.
2. **[재지 않았다]** 정렬 축을 바꾸는 변이는 계획 0008 §M-2-1 이 잰 하나뿐이고, 역순 정렬·다른 키는
   이 사이클도 재지 않았다.
3. **[재야 한다 → qa · 작업 3 에서]** 무정렬 조회의 반환 순서가 `ctid` 오름차순을 따라간다는 것을
   **`EXPLAIN` 으로** 확인하지 않았다. 이 사이클의 근거는 간접적이다(§1-b 말미).
   작업 3 이 힙 배치를 관측하게 되면 그 자리에서 함께 확인할 수 있다.
4. **[재지 않았다]** `persistence.py:455`(§후속 8)의 `.desc()`→`.asc()` 변이.
5. **[재지 않았다]** §Deferred 1~6 을 재측정하지 않았다.
6. **[재야 한다 → qa · §후속 11 을 열 때]** 확정 축 `record_expert_review` 두 줄 삭제 변이를 이
   사이클은 재지 않았다(계획 0009 §후속 11 의 값을 인용만 했다).
7. **[재야 한다 → architect · §후속 25 를 열 때]** §6-5·§6-1·§6-3 의 **근거 표면**을 재지 않았다.
   이 사이클이 잰 것은 절 크기뿐이다: CLAUDE.md **450줄 28,042자**, §6 **236줄 16,948자**(자 60.4% ·
   줄 52.4%), §6-1 5,786 / §6-2 871 / §6-3 5,247 / §6-4 958 / §6-5 3,417자. **§6 가 `bfffcd5` 의
   못박은 분모 15,148자보다 1,800자 위**라 표면 비율이 과대평가된다(§6-5 가 이름 붙인 **불안전한**
   방향). 이 사이클은 CLAUDE.md 를 한 글자도 만지지 않으므로 갱신하지 않는다.
8. **[재지 않았다]** qa 가 보고한 서버 변이 14건을 재현하지 않았다.
9. **[재지 않았다]** `df37433`·`716d67d` 직전 트리를 체크아웃해 803·804 를 재현하지 않았다.
10. **[재지 않았다]** §6-1 12회차 외 다른 행의 관측값을 재현하지 않았다.
11. **[재야 한다 → qa · §후속 24]** = 1 과 같은 자리.
12. **[재지 않았다]** `[PG-after-update-*]` 가 순서를 바꾸지 않은 기전(HOT update 가설).
    *이 사이클이 곁가지로 잰 것*: 전량 실행의 `n_tup_hot_upd=6 / n_tup_upd=138` — **HOT 갱신이
    실제로 일어난다**는 것까지는 값이 있다. 그것이 그 두 줄의 설명인지는 여전히 확인하지 않았다.
13. **[재지 않았다]** `tests/e2e` 를 postgres 로 돌려 보지 않았다. `make e2e` 도 실행하지 않았다.
14. **[재지 않았다]** `[ORD]` 677 은 `tests/integration` 한 트리의 값이다.
15. ~~바닥값이 CI 에서 그 수가 되는지 재지 않았다.~~ → **읽혔다(CI 2회 연속 181).**
    **[재야 한다 → qa · 통합 테스트 수가 바뀔 때]** 표본은 여전히 CI 2회다.
16. ~~`psycopg` 설치를 돌려 보지 않았다.~~ → **돌렸다.**
17. **[재야 한다 → qa+architect · §후속 23 의 재개 조건에서]** 커밋된 `postgres.measured.json` 이
    CI 가 재현하지 않는 환경을 영구히 기술한다.
18. **[재야 한다 → qa · §후속 24]** PostGIS 가 있을 때 무엇이 달라지는가.
19. ~~postgres 축의 흔들림을 마감이 직접 재지 않았다.~~ → **이 계획이 쟀다** — 다만 잰 것은
    **기전**이고 **CI 의 그 1회가 아니다**(§1-d). **[재야 한다 → qa · 다음 발생 시]**
    ⓑ 의 진단이 그때 답한다.
20. ~~`make test` 를 clean 트리에서 다시 돌리지 못했다.~~ → **이 계획은 clean 트리에서 쟀다**
    (루트 `pytest -q` 823 · postgres 통합 207 · `make lint` exit 0).
    **[재야 한다 → architect · 커밋 직전]** `make test` 네 갈래 + vitest 는 커밋 직전에 돌린다.
21. **[재지 않았다]** CI 초록의 표본은 계획 0009 시점의 2회에서 늘지 않았다 — 이 사이클은 CI 를
    돌리지 않았다(푸시하지 않는다).

**신규(이 계획이 더한다):**

22. **[재지 않았다]** **HOT 가지치기(`heap_page_prune_opt`)만으로 line pointer 가 재활용되는지.**
    §1-c 의 방아쇠는 `VACUUM` 이고, 가지치기 단독 경로는 태우지 않았다. 그 경로가 참이면
    `VACUUM` 없이도 같은 실패가 날 수 있고, **CI 의 그 1회가 그것일 수 있다.**
23. **[재야 한다 → qa · 작업 3 에서]** **§리스크 2 의 후보 구현(둘을 함께 지우고 역순으로 재삽입)이
    FSM 앞 페이지 경로에서도 상대 순서를 지키는지 N≥10 으로 재지 않았다.** 이 계획의 표본은
    **각 조건 N=1** 이다(`VACUUM` 없음 1회 · 있음 1회 · 각각 변이 1회). §후속 27 이 요구하는 N 에
    미달이고, 그래서 이 계획은 그 후보를 **결정으로 내리지 않았다.**
24. **[재지 않았다]** **작업 5 의 새 기구가 CI 러너에서 도는지.** 하위 프로세스 pytest 는 로컬에서만
    생각했고, CI 의 잡 시간(통합 두 스텝 33.65s + 37.14s)에 얼마를 더하는지 재지 않았다.
