# 계획 0015 — 로컬 데모 경로에 문이 넷이고, 그 뒤의 정문은 **인증 없는 `POST`** 다

> **이 문서가 여는 것**(§후속 66 의 조항 후보 둘째 — *"커밋 제목·본문은 그 커밋이 무엇을 여는지
> 한 줄로 적는다"*, 그리고 문서 자신에도 같은 것을 건다): 사이클 0015 를 연다. 주제는
> **사용자가 곧 실행할 `make dev` + `make seed` 경로가 실제로 서는가**이고, 그 경로의 끝에
> **§후속 67**(인증 없는 `POST /api/auth/register` 가 `role=admin` 을 만든다)이 있다.
> 이 커밋(A)이 담는 것은 **§0 과 §A** — 리뷰어가 사이클 0014 를 승인하며 남긴 처분 둘의 판정뿐이다.
> **§1 이하와 ADR 0019 는 다음 커밋(B)이 담는다.**

---

## 0. 이 문서의 실측이 나온 자리 (재현 방법)

**작업 트리** `/home/user/Bim/buildtwin`, **저장소 루트 `/home/user/Bim`**, 브랜치
`claude/buildtwin-initial-setup-ubulzb`, **HEAD `4d06867`**(푸시됨).

**이 문서의 모든 `파일:줄`·커밋 참조는 HEAD `4d06867` 트리의 것이다**(CLAUDE.md §3-13 첫째 갈래).
**못박음을 좁히지 않는다 — 전체를 못박고 예외를 이름으로 적는다.** 예외는 둘이다:

- **㉮ 다른 기록물을 인용하는 줄** — 계획 0014 는 자기 §0 이 못박은 트리들, ADR 0018 은 `f4afa42`.
  그 자리에서 트리를 적는다.
- **㉯ CLAUDE.md 를 가리키는 줄** — §3-13 **둘째** 갈래(계속 편집되는 자리)라 줄 번호가 조용히 낡는다.
  **줄 번호로 부르지 않고 그 자리에서 도는 `grep` 으로 적는다.**

**시점을 부르는 말.** 「오늘」·「지금」·「HEAD」는 **이 §0 안에서만** 쓴다(§후속 65 의 조항 후보).
§A 이하는 커밋 해시로 부른다.

**작업 트리의 상태.** 착수 시점 루트 `git status --porcelain` **빈 출력**(남의 `M` 없음). 이 문서를
쓰는 동안 심은 변이 넷은 **전부 심기 직전의 작업 트리 사본으로 원복했고**(§6-2 규칙 5), 각 원복 뒤
`diff` 무차이 + 루트 `git status --porcelain` 빈 출력을 확인했다.

**게이트 — 내가 이 트리에서 돌렸다**(각 **N=1**, §후속 27·34 가 요구하는 「방법과 N」):

| 갈래 | 명령 | 값 |
|---|---|---|
| unit | `make test-unit` | **492 passed** |
| invariants | `make test-invariants` | **106 passed** |
| regression | `make test-regression` | **8 passed** |
| integration(sqlite) | `make test-integration` | **232 passed** |
| web | `npx vitest run` | **283 passed (28 files)** |
| lint | `make lint` | **exit 0** |
| e2e | `make e2e` | **12 passed** (26.89s) |

**DB · 포트 · 도구.** PostgreSQL 클러스터를 **기동하지 않았다**. 포트 55430~55459 를 **하나도 쓰지
않았다** — 반납할 것이 없다. 도구 부재를 관측으로 읽지 않는다(§후속 46): `command -v docker` →
**`/usr/bin/docker`**(있다), 그러나 **데몬은 없다** — `docker info` 가
*"failed to connect to the docker API at unix:///var/run/docker.sock … no such file or directory"*
를 낸다(N=1). **그래서 이 문서의 compose 실측은 전부 `docker compose config`(데몬 불필요)의 것이고,
`docker compose up --build` 는 이 문서도 돌리지 않았다**(§확인하지 않은 것 43 은 열린 채다).

---

## A. 리뷰어가 사이클 0014 를 승인하며 남긴 처분 둘 — **내가 판정한다**

**이 절의 규율.** 리뷰어가 옮겨 준 값은 **하나도 그대로 쓰지 않는다** — 전부 이 트리에서 다시 쟀다.
갈린 칸은 §A-3 의 교차 확인 표에 적는다(§6-3 11회차: **갈리지 않은 칸도 적는다**).

### A-1. 처분 1 — §후속 **72 를 연다.** 문안은 리뷰어의 것을 받고, **이 계획이 그 조항을 실제로 진다**

#### A-1-a. 근거를 내가 다시 쟀다 (실측 셋, 전부 N=1, 트리 `4d06867`)

| # | 변이 | 실행 | 값(내가 잰 것) | 리뷰어/qa 가 적은 값 | 갈리는가 |
|---|---|---|---|---|---|
| ㄱ | 기동 시드를 되살린다(= **사이클 0014 전체의 취소**) + `test_00_seed_boundary.py` 제외 | `pytest tests/integration -q --ignore=tests/integration/test_00_seed_boundary.py` | **226 passed** (38.42s) | 리뷰어 **226 passed** | **갈리지 않는다** |
| ㄴ | 같은 변이, 그 파일 **포함** | `pytest tests/integration -q` | **4 failed, 228 passed** (= 그 파일 6 중 **4 failed / 2 passed**) | 계획 0014 §M-8 4행 **4 failed, 2 passed** | **갈리지 않는다** |
| ㄷ | 통합 픽스처가 시드하지 않는다(= **작업 2 이전 상태**) + 같은 제외 | 같은 명령 | **2 failed, 32 passed, 192 errors** (7.04s) | `b09ae65` 본문 표 4행 **2 failed, 32 passed, 192 errors** | **갈리지 않는다** |

- **기준선**(변이 없음, 같은 제외): **226 passed** (84.64s, N=1). ㄱ 이 그 값과 **한 칸도 다르지 않다**.
- **변이의 적용 확인**은 `git diff` 가 아니라 **심기 직전의 작업 트리 사본과 `diff`** 로 했다
  (§6-2 규칙 5). ㄱ 은 `services/api/main.py` 에 7줄을 **더하는** 변이라 대-HEAD diff 로도 보이지만,
  규칙이 「지금 쓰는 관측」에 걸리므로 사본 축으로 통일했다.
- ㄷ 은 `tests/integration/conftest.py` 의 `seed_all(s)` 두 줄과 그 아래 단언 두 줄을 함께 뗐다 —
  단언만 남기면 그 단언이 죽어 **변이가 자기 자신을 잡는다**(§6-2 1: 결함 있는 코드에서 값이 갈리는
  자리를 남겨야 한다).

**그러므로 리뷰어의 근거는 값이고, 그 값은 내 트리에서 재현된다.** ㄱ 과 ㄷ 의 차이가 §후속 72 의
전부다: **같은 변이가, 작업 2 앞에서는 192개의 에러를 내고 작업 2 뒤에서는 아무 값도 움직이지 않는다.**

#### A-1-b. 리뷰어가 내 처방을 바꾼 것 — **나도 바꾼다. 「작업 순서 규약」은 기각한다**

계획 0014 §M-8 2 에서 나는 *"그 손실을 만든 것은 순서다"* 라고 적었다. **진단은 그대로 둔다**
(위 표가 그것을 값으로 만든다). **처방을 바꾼다.** 근거 셋 — 셋째는 내 실측이다.

1. **그 순서는 CLAUDE.md §3-1 이 강제한 것이다.** 소비자를 먼저 적응시키지 않고 부수 효과를 지우면
   그 커밋이 빨갛고, §3-1 은 빨간 커밋을 금지한다(찾는 명령: `grep -n "커밋 전 .make test. 통과"
   CLAUDE.md` — **파일을 적는다**, §3-13 둘째 갈래). 「작업 3 을 먼저 하라」는 규약은 §3-1 과
   **충돌**하고, 충돌하는 두 규칙은 다음 사이클에서 **재량**으로 읽힌다.
2. **순서를 바꿔도 손실이 옮겨갈 뿐이다.** 작업 3 을 먼저 하면 통합 픽스처가 그 커밋에서 빨개지고,
   그것을 피하려면 같은 커밋이 픽스처도 고쳐야 한다 — 그러면 **소유가 섞인 커밋**(api + qa)이 되어
   §2 의 커밋 경계 규칙이 깨진다. 규약 하나가 다른 규약 둘을 밀어낸다.
3. **저장소에는 순서를 붙들 자리가 없다 — 내가 쟀다.** `tests/e2e/conftest.py` 의 시드 호출을
   `_wait_http` **뒤로** 옮겨 창을 다시 열어도 `make e2e` 는 **12 passed** 다(N=1, 18.07s;
   기준선도 12 passed, 26.89s). 원복 뒤 사본과 무차이. **순서를 지키라는 규약은, 지켜졌는지 아무도
   보지 않는 규약이다.**

**옮겨야 할 규칙은 「가리는 커밋이 그 자리에서 붙든다」이다** — 리뷰어의 문안을 그대로 받는다.

#### A-1-c. 문안과 닫힘의 조건

> **§후속 72 — 관측 가능성을 없애는 커밋이 그 자리에서 그것을 붙든다**(qa + architect).
> *조항*: 부수 효과를 명시 호출로 바꾸거나, 어떤 사실이 값으로 보이던 자리를 없애는 작업은,
> **그 작업의 완료 조건에 「그 커밋이 남길 회귀」를 이름과 값으로 적는다.** 회귀의 소유가 다른
> 에이전트면 **그 회귀가 같은 사이클 안에 있어야** 완료 조건이 충족된다.
> *닫힘의 조건*: 그 조항이 「부수 효과를 명시 호출로 바꾸는 작업」의 완료 조건에 들어가고,
> **한 사이클이 실제로 지킬 때.**
> *한정*: **관측은 1회다**(사이클 0014). 그래서 **지금 CLAUDE.md 절에 넣지 않는다** — §6-5 의 갱신
> 규칙이 *"새로운 종류의 반복이 **두 사이클 이상**에서 관측되면 항목을 추가한다"* 이다
> (찾는 명령: `grep -n "두 사이클 이상에서 관측되면" CLAUDE.md`).

**이 계획이 그 닫힘의 조건을 진다.** §작업 분배의 **작업 6**(부트스트랩을 닫는 api 커밋)이 정확히
그 모양이다 — 그 커밋은 `tests/integration/test_00_seed_boundary.py` 가 **201·`role=admin`** 으로
붙들고 있는 사실을 **없앤다**. 그래서 그 작업의 완료 조건에 **그 커밋이 남길 회귀**를 값으로 적는다
(§작업 6·7 의 완료 조건 상자).

#### A-1-d. 역방향 확인 — 이 조항이 놓치는 것 (§6-1 ②)

- **ⓐ 「관측 가능성을 없앤다」의 판정이 사람 판단이다.** 작업 2 를 쓴 `qa` 는 그것이 관측 가능성을
  없앤다고 생각하지 않았다 — 오히려 **더 좋은 배선**을 만들었다고 커밋 본문이 적고, 그 본문은
  자기 손으로 「가려지는 칸」(2행)을 표에 그려 두기까지 했다(`b09ae65` 본문 14행,
  *"기동 돈다 / 픽스처 지움 → sqlite **226 passed** ← 가려진다(그 변이가 안 죽는 자리)"*,
  **인용 · 발췌**). **즉 그 자리를 본 사람이 있었고, 그것이 사이클 전체의 취소를 가린다는 데까지
  가지 못했다.** 조항은 「없앤다」를 알아보는 것을 요구하는데, **그 알아봄 자체는 붙들지 못한다.**
- **ⓑ 회귀가 「그 사실」을 붙드는지는 여전히 값으로만 갈린다.** 조항은 회귀의 **존재**를 요구하지
  **효력**을 요구하지 않는다 — 효력은 §6-2 1 이 진다(변이를 심어 값이 갈리는지). 그러므로 72 는
  §6-2 를 **대체하지 않고 앞단으로 선다**.
- **ⓒ 관측이 1회다.** 위 한정 그대로. 두 번째 관측이 나오기 전에는 CLAUDE.md 로 올리지 않는다 —
  그리고 **이 계획이 그 두 번째 관측을 만들지 않는다**(이 계획은 조항을 **지키는** 쪽이지 **어기는**
  쪽이 아니다). 그러므로 72 가 닫혀도 CLAUDE.md 항목은 열리지 않는다. **닫힘과 승격은 다른 일이다.**

### A-2. 처분 2 — F1: **새 번호를 만들지 않는다. §후속 62 의 표본 ⓖ 이고, 62 의 조항 후보가 셋이 된다**

#### A-2-a. 리뷰어의 사실 확인을 내가 다시 쟀다 — **참이다. 그리고 리뷰어의 수 하나가 갈린다**

계획 0014 §M-11 54 는 *"남은 미측정은 **창의 길이**와 **그 창에서 register 가 내는 값**이다"* 라고
적는다(**인용 · 발췌**). 그 둘은 **같은 사이클이 이미 쟀고 트리에 커밋돼 있다**:

- `tests/e2e/conftest.py` **`:214-216`**(그 자리에서 도는 참조:
  `grep -n "창을 실제로 쟀다" tests/e2e/conftest.py` → **`214`**) —
  *"창을 실제로 쟀다(방법: 이 배선을 그대로 흉내낸 스크래치 탐침 … **N=3**). `_wait_http` 반환 →
  시드 완료까지 **1.276·1.294·1.307s**, 그 창에서 `POST /api/auth/register`(`Authorization` 없음)는
  **201** 이고 응답 `role` 이 **`admin`**"*(**인용 · 발췌**).
- 그 줄을 들인 커밋: `git log --oneline -S "1.276" -- tests/e2e/conftest.py` → **`5562201`**(N=1).

**갈리는 칸 하나 — 「마감보다 여섯 커밋 앞」이 아니다.** 실측:
`git rev-list --count 5562201..4d06867` → **8**. 즉 `5562201` 에서 마감 커밋까지 **여덟 걸음**이고
**사이에 일곱 커밋**이 있다(`32c109a` `055c5ac` `14a0d00` `e1e1a1a` `3843cc8` `779abd8` `8352814`).
**어느 세는 법으로도 6 이 아니다.** 리뷰어의 판정(마감이 자기 앞 커밋을 못 읽었다)은 그 수에 기대지
않으므로 **결론은 선다** — 그러나 이 수는 §6-3 11회차가 이름 붙인 모양(**한 사이클이 같은 값을 여러
자리에 적는다**)이라 여기 값으로 적는다.

**리뷰어의 진단도 참이다.** 계획 0014 는 §M-2-1 에서 `floor.json` 의 부재 술어를 두고
*"이 수를 움직인 것은 「들어온 테스트」가 아니다"* 라고 진단해 놓고, **여덟 절 뒤 §M-11 54 의 자기
문장에서 같은 모양을 반복했다** — CLAUDE.md §6-1 이 이름 붙인 것 그대로다
(찾는 명령: `grep -n "그것을 메우는 작업이 같은 사이클에 있는지" CLAUDE.md`).

#### A-2-b. 판정 — **73번을 열지 않는다.** 닫힘의 조건이 §후속 62 와 같기 때문이다

리뷰어는 *"닫힘의 조건이 다르다고 당신이 판정하면 73번으로 열어야 한다"* 고 한정했다. **재서 답한다.**

- **62 의 닫힘은 「규약이 서는 것」이다.** 62 는 조항 후보 둘을 지고 **§후속 53 트랙**(전수·인용의
  규약)으로 간다 — 오늘 그 항목에 별도의 닫힘 조건 문장이 없고, 자리가 53 과 같다는 것이 그
  항목의 전부다.
- **F1 의 처방도 「규약」이다**: *마감이 「재지 않았다」·「미측정」을 적을 때, **같은 사이클의 앞
  커밋이 그것을 이미 쟀는지** 본다*(= CLAUDE.md §6-1 의 **부재 단정 확인**을 마감 자신에게 거는 것).
  **새 규칙이 아니다** — 규칙은 이미 CLAUDE.md 에 있고, 없던 것은 **그 규칙이 걸리는 자리**다.
- 그러므로 **닫히는 방식이 같다**: 53 트랙의 규약 문서가 서고, 그 문서가 세 조항을 **각각** 담을 때.
  조항이 하나 늘 뿐 조건이 갈리지 않는다.

**그리고 앞선 판단이 이 방향을 이미 갔다.** §후속 **66** 은 *"자리는 §후속 62 와 같은 트랙이고
62 의 표본 ⓔ 다"* 라고 자기를 62 에 매달면서도 번호를 따로 받았다 — 그 근거는 66 이 **커밋 본문의
내용**(무엇을 여는지 말하지 않는다)이라는 **다른 산출물**을 지기 때문이다. **F1 은 새 산출물을
지지 않는다** — 마감 문서의 한 문장이고, 그 문장을 검사하는 규율은 이미 §6-1 이다. **산출물이 없으면
번호를 만들지 않는다**가 66 과 F1 을 가르는 축이다.

#### A-2-c. 리뷰어가 경고한 반대 방향의 실패를 어떻게 막는가

리뷰어의 경고: 번호를 가르면 §6-1 12회차의 **반대 방향 실패**(한 항목이 두 번호로 갈려 하나만
닫힌다)가 난다. **62 로 접으면 반대 위험이 생긴다 — 조항 셋 중 하나가 조용히 빠진 채 62 가 닫히는
것.** 그 위험은 값으로 막는다:

> **§후속 62 의 닫힘 규약(이 계획이 더한다).** 62 를 닫는 문서는 **세 조항을 각각 이름으로
> 적는다** — ⓐ 「뒤집은 쪽이 문서에 적는다」 ⓑ 「근거 값은 커밋 본문을 정본으로 삼지 않는다」
> ⓒ **「마감의 부재 단정에 §6-1 을 건다 — 「재지 않았다」를 적기 전에 같은 사이클의 앞 커밋이
> 그것을 이미 쟀는지 본다」**. **하나라도 이름이 빠지면 62 는 닫히지 않는다.**

#### A-2-d. §확인하지 않은 것 **54** 의 상태 — 소급 수정하지 않고 **갈음**한다

계획 0014 는 **삭제 0** 으로 닫혔다. 그 문서를 고치지 않는다(§3-13 첫째 갈래 — 기록물). 여기 적는다:

| 54 가 「미측정」이라 적은 것 | 실제 | 어디 |
|---|---|---|
| 창의 **길이** | **쟀다** — `1.276·1.294·1.307s`, **N=3** | `tests/e2e/conftest.py:215`(`5562201`) |
| 그 창에서 **register 가 내는 값** | **쟀다** — **201**, 응답 `role`=**`admin`** | 같은 자리 `:216-217` |
| 창이 **무보호**인가 | **쟀다**(리뷰어), **내가 재현했다** — 순서를 뒤집어도 `make e2e` **12 passed**(N=1) | §A-1-b 3 |

**남는 미측정 — 여기가 54 의 오늘 자리다.** ① 위 값은 **e2e 배선 자신이 아니라 그것을 흉내낸
스크래치 탐침**의 것이고(그 주석이 그렇게 적는다), **DB 도 postgres 로 바꿔** 기동이 시드하지 않는
조건을 만든 값이다 — **실제 e2e 갈래(sqlite, 하위 프로세스 uvicorn)에서의 창은 재지 않았다.**
② 그 탐침은 `5562201` 시점의 배선을 흉내냈고, 그 뒤 **일곱 커밋**이 지났다. **[재야 한다 → qa ·
§후속 67 을 닫을 때 — 그때 이 창은 「길이」가 아니라 「무해한가」로 물어진다]**

#### A-2-e. 역방향 확인 — 이 판정이 놓치는 것 (§6-1 ②)

- **ⓐ 「산출물이 없으면 번호를 만들지 않는다」는 새 기준이고, 그 기준의 한계는 재지 않았다.**
  이 축으로 기존 항목들을 다시 나누면 어떤 번호가 접히는지 **세지 않았다** — 기준은 결과가 아니라
  기준에서 판단하지만(§6-1), **이 기준을 소급 적용하지 않는다는 것도 판단이다**. 적어 둔다.
- **ⓑ 62 의 조항이 셋이 되면 62 는 더 무겁고 더 늦게 닫힌다.** 접는 값은 「하나만 닫히는 실패」를
  피하는 것이고, 치르는 값은 **닫힘이 늦어지는 것**이다. 리뷰어의 경고는 앞을 막고 §A-2-c 는
  뒤를 막는다 — **둘 다 완전히 막지는 못한다.**
- **ⓒ 이 판정은 마감을 쓴 사람(나)이 자기 문장을 두고 내린 것이다.** 「자기 검증에는 재귀가 있고
  「방금 이 규칙을 썼다」는 그 재귀를 멈춰 주지 않는다」(CLAUDE.md §6-3, 찾는 명령:
  `grep -n "그 재귀를 멈춰 주지 않는다" CLAUDE.md`). **바깥에서 칸을 읽는 리뷰어가 여전히 필요하다.**

### A-3. 이 절의 교차 확인 표 (§6-3 11회차 — **갈리지 않은 칸도 적는다**)

| 값 | 리뷰어가 적은 것 | 내가 이 트리에서 잰 것 | 갈리는가 |
|---|---|---|---|
| 사이클 취소 변이 · 새 파일 제외 | 226 passed | **226 passed** | 아니오 |
| 작업 2 이전 반사실 | 2 failed / 32 passed / 192 errors | **2 failed / 32 passed / 192 errors** | 아니오 |
| `b09ae65` 본문 4행 | 위와 같은 셋 | 본문 16행에 같은 셋(`git log -1 --format=%B b09ae65`) | 아니오 |
| 창의 값이 실린 자리 | `tests/e2e/conftest.py:214-221`(`5562201`) | `:214` 부터, 커밋 **`5562201`** | 아니오(줄 범위는 안 셌다) |
| `5562201` ↔ 마감의 거리 | **여섯 커밋 앞** | **여덟 걸음 / 사이 일곱 커밋** | **갈린다** |
| e2e 시드 창의 무보호 | 순서를 뒤집어도 12 passed | **12 passed**(N=1) | 아니오 |
| §6 크기(CLAUDE.md 미접촉) | 21,128자 · 276줄 · 65.57% | **21,128자 · 276줄 · 65.5701%** | 아니오 |
| §2 에 소유가 없는 파일 | 셋(`.env.example`·`README.md`·`docker-compose.yml`) | §2 절 안 히트: `.env.example` **0** · `docker-compose.yml` **0** · `README.md` **1**(그 한 줄은 `config/README.md`) | 아니오 |

### A-4. 이 절이 쓴 자원 · 원복 · 게이트

- **심은 변이 넷**(전부 원복, 각 사본 `diff` 무차이): `services/api/main.py`(ㄱ·ㄴ) ·
  `tests/integration/conftest.py`(ㄷ) · `tests/e2e/conftest.py`(창) · `services/api/usecases.py`
  (§후속 11 재측정 — §B 가 받는다).
- **포트 0개 · postgres 0회 · docker 데몬 0회.** `docker compose config` 는 **저장소 밖 사본**
  (스크래치패드)에서 돌렸고 저장소 파일을 편집하지 않았다.
- **게이트**는 §0 의 표가 정본이고, 이 절을 쓰는 동안 트리는 `4d06867` 과 **동일**하다
  (`git status --porcelain` 빈 출력).

---

## 1. 분석 — 사용자가 곧 칠 명령 둘이 무엇을 지나는가

### 1-a. 경로를 명령 단위로 편다

사용자 결정은 **「데모는 로컬에서 먼저 돌린다」** 이고, 그 경로는 저장소가 이미 이름으로 적고 있다 —
`README.md:33`(*"전체 스택(PostGIS·Redis·MinIO·워커)은 `make dev` (docker compose)."*, **인용 · 전체**)
와 `Makefile:19`(`seed:` 타깃). 명령 둘이 지나는 걸음은 다섯이다:

1. `make dev` → `docker compose up --build`(`Makefile:17`) → compose 가 **파일을 해석**한다.
2. 컨테이너 여섯이 뜨고 `api` 가 `postgresql+psycopg://…@db:5432/buildtwin` 로 붙는다(`docker-compose.yml:35`).
3. `make seed` → `python -m services.api.seed`(`Makefile:22`) → **호스트에서** `settings.database_url` 이
   가리키는 DB 에 만든다.
4. 브라우저가 `127.0.0.1:5173` 을 열고 vite 가 `/api` 를 프록시한다(`apps/web/vite.config.ts:11-14`).
5. 로그인 → `services/api/auth/security.py:59` 가 `settings.resolve_jwt_secret()` 으로 토큰을 만든다.

### 1-b. 전수 — 「이 경로를 결정하는 자리」 (§6-1 ①②③)

**① 생성 기준.** **저장소 루트에서** 만들었다(경로로 먼저 좁히지 않는다):

```
git grep -nIE "docker compose|docker-compose|env_file|JWT_SECRET|DATABASE_URL|localhost:8000" -- . ':!docs'
```

→ **42줄 / 13파일**(N=1, `4d06867`). 파일별: `.env.example` 2 · `CLAUDE.md` 1 · `Makefile` 4 ·
`README.md` 3 · `apps/web/README.md` 1 · `apps/web/vite.config.ts` 1 · `docker-compose.yml` 5 ·
`packages/core/db.py` 2 · `packages/core/settings.py` 5 · `pyproject.toml` 1 · `tests/e2e/conftest.py` 4 ·
`tests/integration/conftest.py` 8 · `tests/integration/test_00_seed_boundary.py` 5.
**소유는 목록을 만든 뒤에 나눴다**(§작업 분배).

**② 이 기준이 놓치는 것.** 넷을 이름으로 적는다.

- **ⓐ 표기 변종·활용형** — `Dockerfile` 자신은 이 집합의 어느 낱말도 쓰지 않는다(히트 0). `make dev`
  가 **빌드하는 이미지**의 내용은 이 목록 밖이다.
- **ⓑ 보간**(§6-1 표기 부류 ③) — URL 을 조각으로 조립하는 자리는 `DATABASE_URL` 문자열을 갖지 않는다.
- **ⓒ 말하지 않고 기대는 자리** — 포트 `5432`·`8000`·`5173` 을 **숫자로만** 쓰는 줄.
- **ⓓ 데몬이 없어 보이지 않는 자리** — 이미지가 실제로 무엇을 담는지. **§0 이 그 한계를 적었다.**

**③ 블라인드 스팟 하나를 태웠다 — ⓒ.** 루트 `git grep -nIE ":5432|:8000|:5173" -- . ':!docs'` 를
돌려 **각 히트를 읽었다**(수가 아니라 히트를 — §6-1 역방향 확인). **14줄**이 나오고 그중 **위 42줄
목록 밖의 자리가 넷**이다: `docker-compose.yml:17`·`:40`·`:58`(publish 하는 `ports:` 세 줄 —
문 1·4 가 어느 주소에 서는지를 정하는 자리) · **`packages/core/settings.py:32`**
`cors_allow_origins: list[str] = ["http://localhost:5173", "http://127.0.0.1:5173"]`(**인용 · 전체**).
**마지막 줄이 이 태움의 값이다** — 브라우저의 오리진을 정하는 자리인데 첫 기준의 여섯 낱말 어디에도
걸리지 않는다. 읽어 보니 **오늘은 해가 없다**(compose 가 web 을 `127.0.0.1:5173` 으로 publish 하므로
두 값 중 하나에 맞는다). **결과가 무해했다는 사실이 기준을 정당화하지 않는다**(§6-1) — 기준이
놓쳤다는 것이 값이고, 그래서 이 넷을 목록에 **더한다: 42 → 46줄 / 13파일**.
그리고 이 태움은 내가 §1-b ② 에서 예상한 자리(`vite.config.ts` 의 `port: 5173`)를 **내지 않았다** —
그 줄은 `port: 5173` 이라 `:5173` 패턴에 걸리지 않는다. **예상한 블라인드 스팟과 태워서 나온
블라인드 스팟이 달랐다.**

### 1-c. 문 넷 — 그리고 그 뒤의 정문 (실측 표. **각 칸은 실행값이거나 코드 인용이고 다른 절 참조가 아니다**)

| # | 무엇이 막는가 | 값(**내가** 이 트리에서 · N=1) 또는 코드 인용 | 오늘 이것을 보는 게이트 |
|---|---|---|---|
| **문 1** | `.env` 가 없으면 compose 가 **파일 해석 단계에서** 죽는다 | 저장소 밖 사본에서 `docker compose config` → **rc=1**, `env file …/.env not found: stat …: no such file or directory`. 빈 `.env` 를 두면 **rc=0** | **없다** |
| **문 2** | `.env` 가 있어도 `JWT_SECRET` 이 비면 compose 의 **postgres URL 에서 로그인이 죽는다** | `Settings(database_url="postgresql+psycopg://buildtwin:buildtwin@db:5432/buildtwin", jwt_secret=None).resolve_jwt_secret()` → **`RuntimeError: JWT_SECRET 환경변수가 필요합니다 (.env)`**. 같은 호출을 sqlite URL 로 하면 **43자 난수**를 낸다 | **없다** — 테스트는 sqlite 갈래거나 `JWT_SECRET` 을 준다(`tests/integration/conftest.py:6`, **인용 · 발췌**) |
| **문 3** | `make seed` 는 **호스트의** `settings.database_url` 에 만든다. compose 의 DB 는 `db:5432` 이고 **호스트는 그 이름을 풀지 못한다** | 코드 인용: `docker-compose.yml:35`·`:47` `DATABASE_URL: postgresql+psycopg://…@db:5432/buildtwin` ↔ `Makefile:21` *"이 셸의 `DATABASE_URL` 이 비어 있으면 … 기본값(로컬 sqlite ./buildtwin.db)이다"*(**인용 · 전체**) ↔ `services/api/seed.py` 가 `settings.database_url` 을 쓴다 | **없다** |
| **문 4** | web 컨테이너 안의 vite `/api` 프록시가 **자기 자신**을 가리킨다 | 코드 인용: `apps/web/vite.config.ts:13` `target: "http://localhost:8000",`(**인용 · 전체**) ↔ `docker-compose.yml` `web.command`(`node:22-alpine` 컨테이너 안에서 vite 를 돌린다) | **없다** |
| **정문** | 문 셋을 지나 계정이 0개면 **인증 없는 `POST /api/auth/register` 하나가 `role=admin` 을 만든다** | 리뷰어 실측(`uvicorn --host 0.0.0.0`, 빈 sqlite, 55437 → **`192.0.2.2` 에서 201 · `role=admin`**). in-process 축은 `tests/integration/test_00_seed_boundary.py` 가 붙든다(`32c109a`) | **값은 붙들려 있다. 문은 닫혀 있지 않다** |

**문 1·2 의 순서가 이 계획의 가장 중요한 실측이다.** 문 1 은 **컨테이너가 뜨기 전에** 큰 소리로 죽고,
문 2 는 **스택이 다 뜬 뒤 로그인에서** 조용히 죽는다. 이 저장소의 지배적 실패 모드가 후자다.

### 1-d. 곱 표 — (문 × 그것을 볼 수 있는 자리) (§6-1: 관계를 세는 목록은 **곱**으로)

축 둘: 문 다섯 × 오늘의 관측 자리 넷(`make test` · `make lint` · `make e2e` · CI). **20칸.**

| | `make test` | `make lint` | `make e2e` | CI |
|---|---|---|---|---|
| 문 1(`.env`) | 못 본다 | 못 본다 | 못 본다 | 못 본다 |
| 문 2(JWT) | 못 본다(sqlite 갈래) | 못 본다 | 못 본다(sqlite) | 못 본다 |
| 문 3(시드 DB) | 못 본다(픽스처가 자기 DB 를 쥔다) | 못 본다 | 못 본다(하위 프로세스가 자기 URL 을 준다) | 못 본다 |
| 문 4(프록시) | 못 본다(vitest 는 jsdom) | 못 본다(`tsc` 는 값을 모른다) | 못 본다(Playwright 는 `vite preview` 로 서빙) | 못 본다 |
| 정문(register) | **본다**(`test_00_seed_boundary.py` — **201·admin 을 계약으로**) | 못 본다 | 못 본다 | **본다** |

**20칸 중 18칸이 「못 본다」이고, 2칸은 「보되 열린 값을 계약으로 붙든다」.** 곱을 만든 이유가 이것이다 —
「문이 있는가」로만 물었으면 정문의 두 칸이 **덮여 있다**는 사실이 목록에 못 들어온다.

### 1-e. 한정어 역방향 확인 (§6-3 — 새로 쓰는 조건절도 예외가 아니다)

| 한정어·조건 | 빼면 무엇이 더 들어오는가 | 넣으면 무엇이 빠지는가 | 실행으로 가른 값 |
|---|---|---|---|
| 문 1 의 처방을 **`env_file: [{path: .env, required: false}]`** 로 좁히면 | `.env` 를 만들라는 요구가 사라져 진입이 쉬워진다 | **문 2 가 남는다 — 그리고 실패 시점이 「기동 전」에서 「로그인」으로 밀린다** | 사본 실측 N=1: `required: false` + `.env` 없음 → `docker compose config` **rc=0**. 그 상태의 `JWT_SECRET` 은 여전히 비어 있고 §1-c 문 2 의 `RuntimeError` 가 그대로다. **그러므로 이 처방 단독은 기각한다** |
| 문 1 의 처방을 **「`.env` 를 만들게 한다」** 로 두면 | 문 2 가 함께 닫힌다(`JWT_SECRET` 이 그 파일에 생긴다) | 사용자가 한 걸음을 더 밟는다 | 사본 실측 N=1: **빈** `.env` 만으로도 `docker compose config` **rc=0** — 즉 파일의 **존재**가 문 1 을 열고, 그 **내용**이 문 2 를 연다. **두 문은 같은 파일에 있다** |
| 「데모 경로」를 **compose 갈래로** 좁히면 | `make api`+`make web`(호스트 sqlite) 갈래가 목록에 들어온다 | 그 갈래에는 문 1·2·3·4 가 **하나도 없다**(sqlite 라 JWT 는 난수, 프록시는 진짜 `localhost:8000`, 시드는 같은 DB) | `Settings(sqlite).resolve_jwt_secret()` → 43자 난수(N=1). **그래서 좁힌다** — 그리고 좁힌 결과 **정문은 두 갈래 모두에 남는다**(`8352814` 가 sqlite 갈래를 그 자리로 수렴시켰다) |
| 정문을 **「닫는다」** 로 적으면 | 빈 DB 에 첫 계정을 만드는 경로가 **명령 하나**로 줄어든다 | **대체가 없으면 스택이 죽는다** — 계획 0014 §B-3 1 이 기각한 근거 그대로 | **그 근거가 이 사이클에서 바뀌었다**: 대체 경로(`python -m services.api.seed`)를 `6ab9f6a` 가 만들었고 `779abd8` 가 rc 계약을 붙였다. **그래서 이번엔 닫을 수 있다 — 단, 문 3 을 먼저 닫아야 한다**(작업 순서가 아니라 **의존**이다) |

*옛 조건이 잡던 것(§6-3 이 요구하는 뒷면).* 계획 0014 §B-3 의 「지금 닫지 않는다」가 잡던 것은
**「대체 없이 닫으면 아무도 로그인할 수 없다」** 이고, 이 계획은 그것을 버리지 않는다 — 작업 6 의
완료 조건에 **양성**(명령 뒤 로그인 200)을 함께 둔다.

### 1-f. 이 사이클의 범위

**안**: 문 1·2·3·4 와 정문. **밖**: `docker compose up --build` 전량 실행(§확인하지 않은 것 43 —
**데몬이 없다**, §0), PostGIS 위에서의 측정(§후속 24), 순서 축(§후속 17), CLAUDE.md 편집.

---

## 2. 판정 — 대기열 **2·3·6·11·23·24** 중 무엇을 이 사이클로 삼는가

**결론: 여섯 중 어느 것도 삼지 않는다. 이 사이클은 §1 의 문 넷 + 정문(§후속 67)이다.**
그리고 **여섯을 「재측정하지 않았다」로 넘기지 않는다** — 아래 표가 각 항목을 이 트리에서 다시 잰
값이다(§6-1 12회차에 대한 이 계획의 답: 목록을 옮기는 것과 상태를 아는 것은 다른 일이다).

| # | 문안(계획 0014 §후속에서 **인용 · 발췌**) | **내가 `4d06867` 에서 다시 잰 값**(N=1) | 이 사이클로 삼지 않는 근거 |
|---|---|---|---|
| **2** | *"`"rejected"` 값 리터럴의 전수 감사"*(qa) | 루트 `git grep -nI '"rejected"' -- .` **176줄**; `docs` 제외 **109줄 / 27파일**. 그 값을 붙드는 불변식은 **없다**(`tests/invariants/test_identity_drift_cause_contract.py` 는 `cause` 축이다) | 데모 경로 밖. 소유 `qa` 이고 이 사이클의 qa 예산은 작업 2·7 이 쓴다. **109줄을 읽는 일**이라 곁다리로 붙이면 §6-1 이 경고한 「좁힌 기준」이 그대로 나온다 |
| **3** | *"`apps/web/src/api/client.ts:12` 의 TODO"*(qa) | 그 TODO 가 그대로다(`grep -n "TODO(round-6" apps/web/src/api/client.ts` → **`12`**) | 이 목록은 **오류 문구**의 자리다 — 문 넷이 열려 있으면 사용자는 그 문구에 **닿지 못한다**. 순서상 뒤다 |
| **6** | *"`docs/api.md` 오류 절의 예외 열거와 `internal_error`"*(api) | 생성기 열거에 두 예외가 **없다**(`grep -c "MappingDecisionNotCancellableError\|MappingDecisionCancelReasonRequiredError" services/api/scripts/gen_api_doc.py` → **0**, ERE 가 아니라 BRE 라 `\|` 가 교대다 — §M-13 이 이름 붙인 방언 함정의 반대편). `internal_error` 는 소스에 **`services/api/errors.py:65` 한 자리**뿐이고 `docs/api.md` 에는 **0** | **§후속 64 ③ 과 같은 파일의 일**이다(`gen_api_doc.py`). 둘을 한 사이클에 묶어야 생성기를 두 번 만지지 않는다 — 그 사이클은 **생성기 사이클**이어야 하고 이 사이클은 아니다 |
| **11** | *"확정 축의 `expert_review_logs` 도 무보호"*(qa) | **다시 쟀다**: `services/api/usecases.py:354-355` 의 `record_expert_review(...)` 두 줄을 지우고 `pytest tests/unit tests/invariants tests/regression tests/integration -q` → **838 passed**(기준선과 **동일**). 원복 뒤 사본 `diff` 무차이 | **여전히 무보호이고 값이 다섯 사이클 만에 갱신됐다**(계획 0009 의 807 → 838, 수만 자랐다). 그러나 이것은 **로그** 경로이지 판정·인증 경로가 아니고, 데모가 서지 않으면 그 로그를 만들 사용자가 없다 |
| **23** | *"`postgres.measured.json` 의 `server_version`"*(qa + architect). 재개 조건: **단언·비교가 하나라도 생기는 순간** | 소스 히트 **14줄 / 5파일**(계획 0011 의 8줄에서 늘었다). 그중 `test_99:177`·`:266`·`:561` 셋이 `server_version` 을 단언·비교하지만 **셋 다 합성 입력**이다 — 기록기에 `"16.13"` 을 손으로 먹이고 그 **포맷**을 본다. **잰 값에 대한 단언·비교는 0건** | **재개 조건이 성립하지 않는다.** 이월 |
| **24** | *"PostGIS 가 로드된 DB 에서 무엇이 달라지는가"*(qa) | **잴 방법이 이 환경에 없다** — `docker info` 가 데몬 부재를 낸다(§0). 도구 부재를 음성 관측으로 읽지 않는다(§후속 46) | 이월. **다만 자리가 바뀐다**: compose 의 `db` 가 `postgis/postgis:16-3.4`(`docker-compose.yml`)라 **문 1 이 열리는 순간 24 는 처음으로 측정 가능해진다.** 24 의 닫힘은 이제 §확인하지 않은 것 **43** 과 한 자리다 |

**이 사이클로 §1 을 삼는 근거 다섯.**

1. **대기열 여섯은 전부 「이미 서 있는 것의 품질」이고, §1 은 「서는가」다.** 문 1 의 값이 `rc=1` 이다.
2. **사용자 결정이 그 경로를 곧 실행한다.** 대기열의 어느 항목도 그 경로 위에 없다(위 표의 셋째 열).
3. **§후속 67 은 열린 채이고 ADR 이 필요하다**, 그리고 리뷰어가 그 구멍이 **실제 네트워크 소켓**에서
   열린다는 것을 쟀다(in-process 가 아니다). 계획 0014 §B-3 이 「지금 닫지 않는다」의 첫째 근거로 든
   *"대체 경로가 없다"* 는 **같은 사이클이 만든 명령**으로 사라졌다.
4. **§후속 72 의 닫힘 조건을 지려면 「관측 가능성을 없애는 작업」이 있어야 한다.** 대기열 여섯에는
   그 모양이 하나도 없고, **부트스트랩을 닫는 일이 정확히 그 모양**이다(작업 6·7).
5. **대기열은 순서이지 잠금이 아니다.** 순서를 정한 것은 계획 0013 §후속 머리말이고, 그 뒤에 들어온
   것이 **사용자 결정**과 **리뷰어의 소켓 실측** 둘이다. 순서를 바꾸는 근거는 그 둘이고, 여기 적는다.

*역방향 확인 — 이 판정이 놓치는 것.* ⓐ **대기열을 한 번 밀면 다음에도 밀 수 있다** — 2·3·6·11 은
계획 0006~0008 에서 열려 **여덟 사이클째** 열려 있다. 그 자체가 값이고, 이 계획은 그것을 없애지
못한다(할 수 있는 것은 위 표처럼 **상태를 갱신해 넘기는 것**뿐이다). ⓑ **11 은 「급하지 않다」가
아니라 「이 경로가 아니다」로 밀렸다** — 확정 축의 감사 로그가 사라져도 아무것도 빨개지지 않는다는
사실은 오늘도 참이다(838). ⓒ 이 판정은 **사용자 결정을 근거로 삼는다** — 그 결정이 바뀌면 근거 2 가
사라지고, 근거 3·4 만 남는다(그 둘로도 선다고 나는 판단하지만, **그 판단은 여기 적어야 검증된다**).

---

## 작업 분배

**소유는 CLAUDE.md §2 가 정본이다.** 아래 「소유」 열이 그것이고, 「배정」은 이 사이클의 것이지
소유가 아니다(§2: *"계획의 작업 분배 행은 그 사이클의 배정이지 소유가 아니다"*, **인용 · 발췌**).
**소유 없는 파일 셋**(`.env.example` · 루트 `README.md` · `docker-compose.yml`)은 §후속 19 가 지고,
이 계획은 **배정만** 한다.

| # | 작업 | 소유 | 만지는 트리 | 의존 |
|---|---|---|---|---|
| 1 | `make env` — `.env` 가 없으면 만든다(문 1·2) | `qa` | `Makefile` | — |
| 2 | 「`make dev` 가 설 수 있는가」를 붙드는 회귀 | `qa` | `tests/` | 1 |
| 3 | 시드가 **compose 의 DB** 에 닿는 자리를 연다(문 3) | `qa` | `Makefile` | 1 |
| 4 | vite `/api` 프록시 대상이 환경에서 온다(문 4) | `frontend` | `apps/web/src` 밖 `apps/web/vite.config.ts` | — |
| 5 | compose·`.env.example`·README 가 그 경로를 말한다 | **배정 `architect`**(소유 없음) | `docker-compose.yml` · `.env.example` · `README.md` | 1·4 |
| 6 | **정문을 닫는다** — 빈 DB 의 인증 없는 register 가 403 | `api` | `services/api/auth/` · `services/api/README.md` | **3** · ADR 0019 |
| 7 | 6 이 없애는 관측 가능성을 **그 자리에서 붙드는 회귀** | `qa` | `tests/` | 6 |
| 8 | 마감 | `architect` | `docs/` | 전부 |

**의존 6←3 은 순서 규약이 아니라 의존이다.** 정문을 닫으면 빈 DB 에 계정을 만드는 경로는 **명령
하나**뿐인데, 그 명령이 compose 의 DB 에 닿지 못하면(문 3) 스택이 **로그인 불가**가 된다.
§1-e 넷째 행의 「옛 조건이 잡던 것」이 그 자리다.

### 완료 조건

**형식(§후속 55).** 각 인용에 **ⓐ**(그 자리에서 도는 참조)와 **ⓑ**(인용/재서술 · 전체/발췌)를 붙인다.

---

**작업 1 (qa · `Makefile`) — `make env`**

1. `.env` 가 없는 트리에서 `make env` 를 돌린 뒤 `docker compose config` 가 **rc=0** 이다.
   **오늘의 값을 커밋 본문에 함께 적는다**: 같은 명령이 `.env` 없이 **rc=1** 이고
   `env file …/.env not found: stat …: no such file or directory` 를 낸다(architect 실측, N=1,
   저장소 밖 사본).
   - **ⓐ** `docker compose config; echo $?` — **데몬은 필요 없다**(§0: `docker info` 는 데몬 부재를
     낸다. 그 상태에서 `config` 는 돈다).
2. 만들어진 `.env` 의 `JWT_SECRET` 이 **비어 있지 않다**. 값은 **난수**이고 저장소의 어떤 파일에도
   상수로 있지 않다(CLAUDE.md §3-4 — ⓐ `grep -n "외부 API 키·시크릿은" CLAUDE.md`, **ⓑ 발췌**).
3. **멱등이다** — `.env` 가 이미 있으면 **한 바이트도 바꾸지 않는다**. 양성 검사: 값을 바꾼 `.env` 로
   두 번 돌려 내용 해시가 같다.
4. **§후속 72 — 이 커밋이 남길 회귀.** 작업 2 가 붙일 회귀의 **이름과 기대값**을 이 커밋 본문에
   적는다(`docker compose config` rc=0 을 보는 테스트, skip 조건 포함). 이 작업은 **실패를 없애는**
   쪽이지만, 그 실패를 **아무 게이트도 보지 않았다**는 것이 이 작업의 근거이므로 조항을 그대로 건다.

**작업 2 (qa · `tests/`) — 그 회귀**

1. `docker compose config` 의 rc 를 보는 테스트가 `make test` 안에서 돈다.
2. **작업 1 의 수정을 되돌린 변이에서 빨개진다.** 몇 개가 어떤 문구로 빨개지는지 **값으로** 적는다
   (§6-2 1: 결함 있는 코드가 같은 값을 내면 그 시나리오는 장식이다).
3. **도구 부재를 음성 관측으로 읽지 않는다**(§후속 46). `docker` CLI 가 없으면 **skip 으로 보고**하고,
   그 skip 이 전량을 조용히 초록으로 만들지 않는다 — skip 수를 실행 요약에서 볼 수 있어야 한다.
4. **데몬을 요구하지 않는다.** 이 테스트가 `docker compose up` 을 부르면 CI 와 이 환경 둘 다에서
   못 돈다(§0).

**작업 3 (qa · `Makefile`) — 시드가 어느 DB 에 닿는지**

1. compose 갈래에서 데모를 시드하는 **명령이 하나 있다.** 그 명령이 쓰는 `DATABASE_URL` 이 컨테이너의
   것임을 **코드 인용으로** 보인다(`docker-compose.yml` 의 `api.environment.DATABASE_URL`).
2. **호스트 `make seed` 의 오늘 동작은 바뀌지 않는다** — `settings.database_url` 이 가리키는 DB 에
   만든다(ⓐ `grep -n "어떤 DB 인지 가리지 않는다" services/api/seed.py`, **ⓑ 발췌**).
3. **`make dev` 의 안내 두 줄이 compose 갈래에서 거짓이다 — 이 사이클이 고친다**(§6-4 1).
   그 두 줄은 로컬 sqlite(`buildtwin.db`)와 `make db-reset` 을 말하는데, compose 의 DB 는
   `pgdata` 볼륨의 postgres 이고 `make db-reset` 은 그것을 지우지 못한다.
   - **ⓐ** `sed -n '13,17p' Makefile` — **ⓑ 인용 · 발췌**.
4. **데몬 없이 검증되는 부분과 아닌 부분을 갈라 적는다.** 문자열·타깃 존재는 여기서 검증되고,
   `docker compose exec` 의 실제 동작은 **재지 않는다**(§확인하지 않은 것 43 과 같은 한계).

**작업 4 (frontend · `apps/web/vite.config.ts`) — 프록시 대상**

1. 기본값이 **오늘과 같다**: `http://localhost:8000`(ⓐ `grep -n "target:" apps/web/vite.config.ts`,
   **ⓑ 인용 · 전체**). 호스트 `make web` 갈래의 값이 바뀌지 않는다.
2. 환경변수로 덮을 수 있다. 이름은 `frontend` 가 정하고, **그 이름을 작업 5 가 compose 에 그대로 쓴다**.
3. `npx vitest run` **283 passed (28 files)** 불변 · `make lint` exit **0**.
4. *역방향*: 이 변경은 **문 4 를 닫지 않는다** — 값을 주는 것은 작업 5 다. 둘이 함께여야 의미가
   있으므로(§6-2 4) 두 커밋이 **같은 사이클 안에** 있어야 완료다.

**작업 5 (배정 `architect`) — compose·`.env.example`·README**

1. `docker compose config` **rc=0**(N=1, 데몬 없이).
2. `web` 서비스가 작업 4 의 환경변수에 `http://api:8000` 을 준다.
3. `.env.example` 이 **데모가 실제로 요구하는 키**를 말한다 — 오늘 그 파일은 키 이름 열 개를 나열만
   하고 *"어느 것이 없으면 무엇이 죽는지"* 를 말하지 않는다(ⓐ `cat .env.example`, **ⓑ 인용 · 전체**).
   **값은 적지 않는다**(§3-4).
4. 루트 `README.md` 가 데모 경로를 **명령 다섯 걸음**으로 적는다(§1-a). 오늘 그 자리는 한 줄이다
   (ⓐ `grep -n "전체 스택" README.md` → **`33`**, **ⓑ 인용 · 전체**).
5. **소유 없는 파일 셋을 만진다** — 커밋 본문이 그 사실과 §후속 19 를 이름으로 적는다.

**작업 6 (api) — 정문을 닫는다 (ADR 0019 의 구현)**

1. 빈 DB 에서 `Authorization` 없는 `POST /api/auth/register` 가 **403** 이고 code 가
   **`forbidden_role`** 이다. **오늘의 값은 201 · `role=admin`** 이다(계획 0014 §B-2, architect 실측).
2. **양성 — 명령이 유일한 경로임을 함께 단언한다**(§6-2 4): 같은 빈 DB 에 `python -m services.api.seed`
   → **rc=0** → 로그인 **200**. **두 사실을 한 자리에 적는다** — 닫힘만 단언하면 「스택이 죽었다」와
   구별되지 않는다.
3. `users` 가 비어 있음을 **register 가 바꾸지 못한다**: 403 뒤에도 `users` **0행**.
4. **`services/api/README.md` 와 `auth/router.py` 의 부트스트랩 문구가 거짓이 된다 — 이 커밋이
   고친다**(§6-4 1). 오늘 그 둘은 참이다(ⓐ `grep -n "첫 사용자는" services/api/README.md` ·
   `grep -n "users_count" services/api/auth/router.py`, **ⓑ 발췌**).
5. **§후속 72 — 이 작업이 관측 가능성을 없앤다. 그 커밋이 남길 회귀를 여기 이름과 값으로 적는다.**
   없어지는 관측: `tests/integration/test_00_seed_boundary.py` 가 붙들고 있는 **201 · `role=admin`**
   (ⓐ `grep -n "role.*admin" tests/integration/test_00_seed_boundary.py`, **ⓑ 발췌**). 남길 회귀:
   **작업 7 의 ①②③** — 그 회귀가 같은 사이클 안에 없으면 **이 작업은 완료가 아니다.**
   커밋 본문은 ⓐ 무엇이 201 → 403 으로 뒤집히는지 ⓑ 그것이 §후속 67 의 닫힘인지 ⓒ 어느 테스트가
   그 새 값을 지는지를 **이름으로** 적는다.

**작업 7 (qa · `tests/`) — 6 이 없애는 것을 그 자리에서 붙든다**

1. 빈 DB · 인증 없음 → **403 `forbidden_role`** (상태 코드와 code 를 붙든다. **문장을 베끼지
   않는다** — §6-4 3).
2. 명령 뒤 로그인 **200**, 그리고 그 계정의 role 이 시드가 정한 값이다.
3. **명령이 유일한 경로**: 빈 DB 에서 register 로는 어떤 계정도 생기지 않는다(`users` 0행 유지).
4. **뒤집는 변이에서 값이 갈린다**: 부트스트랩 갈래를 되살리는 변이에서 **몇 개가 빨개지는지**
   값으로 적는다. **오늘 그 반대 변이는 아무것도 빨갛게 하지 않았다**(계획 0014 §M-8 1·3행,
   **226 passed** — architect 가 §A-1-a 에서 재현했다).
5. **e2e 시드 창의 처분 — 순서 규약을 새로 만들지 않는다.** 정문이 닫히면 그 창에서 register 가
   403 이므로 창이 **무해**해진다. 값으로 적는다: 시드 호출을 `_wait_http` 뒤로 옮긴 트리에서
   ⓐ `make e2e` 의 값 ⓑ **그 창의 register 가 403 인 것**. 오늘의 값은 **12 passed**(창이 열려 있고
   아무것도 그것을 보지 않는다 — architect 재현, N=1).
   **원복한다** — 이 항목이 요구하는 것은 배선 변경이 아니라 **값**이다.

**작업 8 (architect) — 마감**

1. 게이트를 마감 시점 트리에서 다시 돌리고 값을 적는다.
2. **§6 자수·줄수 재측정**(§후속 38) — 이 사이클은 CLAUDE.md 를 만지지 않으므로
   **21,128자 · 276줄 · 65.57%** 가 마지막 커밋까지 그대로여야 한다.
3. **교차 확인 표**(§6-3 11회차) — 이 사이클이 두 번 이상 적은 값을 모으고 **갈리지 않은 칸도 적는다**.
   그 목록에 **이 사이클이 실행으로 만들어 낸 값**을 반드시 넣는다(§6-1 13회차).
4. **§후속 72 의 닫힘을 판정한다**: 조항이 작업 6 의 완료 조건에 들어갔고 **한 사이클이 실제로
   지켰는가.** 지켰으면 닫고, **CLAUDE.md 로 올리지는 않는다**(관측 1회 — §A-1-c 의 한정).
5. **§후속 67 의 닫힘을 판정한다**: 대체 경로가 ADR 로 정해졌고, 빈 DB 의 인증 없는 register 가
   403 이며, 그 값을 붙드는 회귀가 있는가.
6. **마감 자신에게 §6-1 부재 단정 확인을 건다**(§A-2 가 62 에 더한 셋째 조항 ⓒ):
   「재지 않았다」를 적기 전에 **같은 사이클의 앞 커밋이 그것을 이미 쟀는지** 본다.

---

## 게이트

**모든 커밋 전**: `make test`(unit·invariants·regression·integration + vitest) · `make lint` ·
`make e2e`. 기준선은 §0 의 표다(각 N=1). **푸시하지 않는다.**

- 작업 2 가 테스트 수를 늘린다 — **바닥값 `tests/postgres.floor.json` 187 은 이 사이클이 올리지
  않는다**(계획 0014 §M-2 의 판정을 뒤집을 근거가 이 사이클에 없다). 새 테스트가 postgres 축을
  지나는지는 **qa 가 값으로 적는다**(§후속 69 가 그 정의를 진다).
- 작업 6·7 이 `tests/integration/test_00_seed_boundary.py` 의 값을 바꾼다 — **통합 232 가 유지되는지**
  (수가 아니라 **어느 것이** 바뀌는지)를 커밋 본문에 적는다.

## 리스크

1. **정문을 닫았는데 문 3 이 안 닫히면 데모가 로그인 불가가 된다.** 그래서 6 은 3 에 의존한다.
   *관측*: 작업 6 의 완료 조건 2(양성)가 그 자리다.
2. **`make env` 가 사용자의 기존 `.env` 를 덮어쓰면 그 사람의 개발 환경이 죽는다.**
   완료 조건 3(멱등)이 그 자리이고, **양성 검사**(값을 바꾼 `.env` 로 두 번)를 요구한다.
3. **데몬이 없어 이 사이클의 compose 단정은 전부 `config` 축이다.** `up --build` 는 아무도 돌리지
   않는다 — §확인하지 않은 것 43 은 이 사이클 뒤에도 열린 채다. **닫혔다고 적지 않는다.**
4. **작업 4·5 가 갈라져 있어 한쪽만 들어가면 문 4 는 그대로다**(§6-2 4). 두 커밋이 같은 사이클
   안에 있어야 완료다.
5. **`.env` 는 커밋되지 않는다** — 그래서 이 사이클의 어떤 회귀도 「사용자의 `.env` 가 옳은가」를
   보지 못한다. 볼 수 있는 것은 **`.env` 가 없을 때 compose 가 서는가**뿐이다.

## ADR 필요 여부 — **연다. `docs/adr/0019-the-first-account-comes-from-a-command.md`**

**근거.** 작업 6 은 **인증 모델을 바꾼다** — 오늘 저장소가 문서로 약속하는 경로
(`services/api/README.md` · `auth/router.py` docstring)를 없앤다. CLAUDE.md 는 *"이 문서와 충돌하는
결정은 반드시 `docs/adr/`에 ADR로 남겨야 유효하다"* 고 적는다(ⓐ `grep -n "ADR로 남겨야 유효하다"
CLAUDE.md`, **ⓑ 인용 · 발췌**). **§후속 67 자신이 *"ADR 필요"* 를 항목 안에 적고 있다.**
그 ADR 이 이 커밋(B)에 함께 들어간다.

---

## 후속 — 다음 사이클로 넘기는 것

> **다음 사이클 순서(이 계획이 다시 정한다 — 근거는 §2).**
> **0015 = 데모 경로의 문 넷 + §후속 67**(이 계획), 그 뒤 **2 · 3 · 6 · 11 · 23 · 24**(순서 불변 —
> 이 계획은 그 여섯을 **밀었을 뿐 지우지 않았고**, §2 의 표가 각 항목의 오늘 값을 싣는다).
> **CLAUDE.md 대기열**: **§6-5**(25 + 27·34·38 + §후속 46[보강] + 63 + 69·71) → **§2**(19).
> **§후속 72 는 이 대기열에 넣지 않는다** — 관측 1회이므로(§A-1-c 의 한정).
> **이 사이클은 CLAUDE.md 를 만지지 않는다**(§1-f).

**이월 규칙.** 계획 0014 §후속 **1~67**(본문) + **68**(§C-6) + **69·70·71**(§M-10) 을 **번호를
유지한 채** 옮긴다. 닫힌 항목은 지우지 않고 취소선 + 닫은 근거 + **닫힘의 한정**을 남긴다.
신규는 **72번부터** 잇는다. **본문 목록을 번호로 옮긴다, 새로 쓰지 않는다** — 아래는 계획 0014 의
자구 그대로이고, **이 계획이 상태를 바꾸는 항목은 목록 뒤에 따로 적는다**(다시 쓰면 §6-1 12회차다).

1. ~~**§6-2·§6-4 압축**(architect).~~ → **닫혔다**(계획 0007 §과제 1).
2. **`"rejected"` 값 리터럴의 전수 감사**(qa). **재측정하지 않았다.**
3. **`apps/web/src/api/client.ts:12` 의 TODO**(qa). **재측정하지 않았다.**
4. **검토요청 *승인*의 사유 요건**(ADR 0011 §Deferred 1 / ADR 0012 §Deferred 1). **저장소로는 답할 수 없다.**
5. ~~**취소의 내구 감사를 붙드는 회귀 + V8 docstring**(qa).~~ → **닫혔다**(`716d67d`).
6. **`docs/api.md` 오류 절의 예외 열거와 `internal_error`**(api). **재측정하지 않았다.**
   *이 계획이 자리를 하나 붙인다*: **§후속 64 의 `docs/api.md` 두 행이 같은 파일의 일**이고, 둘 다
   고칠 자리가 산출물이 아니라 **생성기**다.
7. ~~**`cancelled_review_request_id` 의 갱신 갈래**(progress-engine).~~ → **닫혔다**(`662e91a` + `3ba9226`).
8. ~~**`find_document_mapping_review` 의 `ORDER BY created_at DESC` 가 무보호**(qa).~~ → **닫혔다**(`3ba9226`).
   **한정: 잰 것은 줄 삭제 변이 하나뿐이고, 그 정렬은 타이를 가르지 못한다**(§후속 41).
9. ~~**`document_mapping_reviews` 의 `sorted(...)`**(qa).~~ → **닫혔다**(`7d44cca`).
   **한정: 그 배역이 운영 엔진 위에서 도는 것은 계획 0013 작업 4·5 가 만들었다.**
10. ~~**PostgreSQL 경로가 비어 있다**(qa + architect).~~ → **닫혔다**(계획 0009 §M-3-c) — 그 한 축에 한해서.
11. **확정 축의 `expert_review_logs` 도 무보호**(qa). **재측정하지 않았다.**
12. ~~**단위 파일 머리의 "다음 여섯 항목"**(qa).~~ → **닫혔다**(`e6fa92f`).
13. ~~**`test_20:29` 의 표 행이 트리를 인라인으로 적지 않는다**(qa).~~ → **닫혔다**(`f6ad00e`).
14. ~~**`test_20:770` 의 *"이 사이클(기준선 807)"***(qa).~~ → **닫혔다**(`68b5795`).
15. ~~**§6-1 12회차 행의 *"밀려난 것"* 칸을 넓힐지**(architect).~~ → **닫혔다**(`f101001`).
    **한정: 규칙 안이라는 판정이지 그 행이 더 안 커진다는 뜻이 아니다**(§후속 38).
16. ~~**`tests/unit` 을 postgres 로 돌린다**(qa).~~ → **닫혔다**(계획 0013 §M-3: 배선 `7ff7b88`·`cf52781`,
    CI 초록 `87d743b`). **한정 셋**: ① 표본은 **CI 1회**다 ② `tests/e2e` 는 그대로 sqlite(§후속 57)
    ③ **CI 의 엔진은 PostGIS 가 로드된 16.4** 이고 로컬 값은 전부 PostGIS 없는 16.13 이다(§후속 24).
17. **정렬을 지정하지 않은 35자리의 순서 계약**(architect + progress-engine + api + sync-2d3d + qa).
    **열린 채다 — 닫힌 것은 ⓐ 다섯뿐이고 30 이 열려 있다**(ⓑ 24 = §후속 42 · ⓒ 6 = §후속 40).
    **다음 이월 목록이 이 항목을 통째로 취소선으로 옮기면 §6-1 12회차의 재발이다.**
    *이 계획이 더하는 것*: **없다.** 이 사이클은 순서 축을 건드리지 않는다 — 그리고 **이 사이클의
    통합 트리가 초록이라는 사실은 ⓑ 24 의 재검증이 아니다**(그 소비자 다수가 HTTP 경계 너머다).
18. **데모 시드를 앱 기동에서 떼어낸다**(api + qa, ADR 0014 §Deferred 1). **이 계획의 대상이다.**
    *닫힘의 조건*: 기동이 시드하지 않고(작업 3), 세 소비자(로컬 개발 · 통합 · e2e)가 각자 명시적
    경로로 시드하며(작업 1·2·4), `settings.seed_dev_data` 가 저장소에서 사라질 때(작업 5).
19. **소유가 §2 에 없는 파일들**(architect). **§2 트랙 — CLAUDE.md 대기열의 마지막.**
    *이 계획이 실례를 둘 더한다*: **`.env.example`** 과 **`docker-compose.yml`** 이 이 사이클의
    작업 대상인데 §2 에 소유가 없다(ADR 0014 §Deferred 5 가 후자를 이미 이름 붙였다).
    이 계획은 그 둘을 **배정**만 하고 소유를 정하지 않는다(§2: *"계획의 작업 분배 행은 그 사이클의
    배정이지 소유가 아니다"*).
20. ~~**`services/progress/persistence.py:433` 의 *"실측(SQLite, …)"***(progress-engine).~~ → **닫혔다**(`ac6b30b`).
21. ~~**postgres 축의 흔들림**(qa).~~ → **닫혔다**(계획 0010 §M-8, 작업 3 의 세 칸에 한해서).
22. ~~**`postgres_axis` 계약의 실패 문구가 경위를 하나로 단정한다**(qa).~~ → **닫혔다**(계획 0010 §M-2-2).
23. **`postgres.measured.json` 의 `server_version`**(qa + architect). **이월.** 재개 조건 그대로.
    **이 계획은 클러스터를 기동하지 않았으므로 재측정하지 않았다.**
24. **PostGIS 가 로드된 DB 에서 무엇이 달라지는가**(qa). **이월, 그리고 문안이 계획 0013 §M-3 에서
    바뀌었다**(옮겨 적는다): *"CI 는 처음부터 PostGIS 로드된 16.4 위에서 돌았고, 로컬 측정은 전부
    그렇지 않다 — 그 차이를 아무도 재지 않았다."* 로드되는 DB 는 `template_postgis` 와
    `buildtwin_test` 둘이고 각각 `CREATE EXTENSION` 넷이며 `/docker-entrypoint-initdb.d/10_postgis.sh`
    가 **최초 init 때만** 돈다. **확장 이름은 잡 로그에 없으므로 적지 않는다**(§6-4 2).
25. **CLAUDE.md §6-5 의 *"잡은 것은 언제나 바깥에서 읽은 리뷰어였다"* 를 좁힌다**(architect).
    **대기열의 첫째** — 이 사이클이 아니다(§1-f). **반례는 3건**(계획 0012 §M-5 가 센 값).
26. ~~**§확인하지 않은 것에 소유를 배정한다**(architect).~~ → **닫혔다**(계획 0010). 이 계획도 그 형식을 지킨다.
27. **postgres 축의 부정 단정에는 N 을 함께 적는다**(architect + qa). 자리는 **§6-5**.
    **그때까지 계획마다 손으로 적는다** — 이 계획: §0 기준선 표의 N=1 넷.
28. ~~**강제 셋의 배선 둘은 지워도 전량이 초록이다**(qa).~~ → **닫혔다**(계획 0010 §M-8).
29. ~~**부분집합을 postgres 축으로 돌리면 트리가 더러워진다**(qa).~~ → **닫혔다**(`98ce2d6`).
30. ~~**테스트 DB 의 `autovacuum` 을 끌 것인가**(qa + architect).~~ → **닫혔다**(ADR 0017 §8-2·§8-3).
    **한정 둘**: ① 값은 **테스트마다 새 스키마**의 것이고 세션 스키마는 재지 않았다(§후속 59)
    ② **재개 조건의 문안이 좁혀졌다** — *"`pg_catalog` 를 뺀 관계에서 넘거나 사용자 테이블에서
    autovacuum 이 실제로 돌면"* 이다. **문턱을 넘으면 다시 연다.**
31. **재배치의 상대 순서가 FSM 앞 페이지 경로에서도 지켜지는지**(qa). **열린 채다** — 값 10/10 은
    **그 경로가 날 수 없는 조건**의 것이다. **§후속 59 와 한 항목의 앞뒤로 묶여 있다.**
32. ~~**CLAUDE.md §6-3 의 *인접 절 교차 확인*을 "같은 사이클의 문서들"로 넓힌다**(architect).~~ → **닫혔다**(`ceb18e9`).
33. ~~**§6-1 *역방향 확인*의 표기 집합에 "보간"을 더한다**(architect).~~ → **닫혔다**(`f101001`).
34. **수치 조건에는 값을 만든 방법과 N 을 함께 적는다**(architect + qa). 자리는 §6-5.
    *이 계획이 실례를 하나 더한다(아홉 번째)*: 계획 0013 의 *"실행 중 2"* 가 **표본 간격에 걸린
    스냅샷**이었고 참된 봉우리는 **127** 이었다. **값 옆에 「무엇을 몇 초 간격으로 몇 번」이 없으면
    그 값은 최댓값이 아니라 목격담이다.**
35. ~~**ADR 0015 §2-1 표 1행과 `test_20` docstring 이 한 인덱스 이름을 계약처럼 적는다**~~ → **닫혔다**(`3dddf58` + `a6f64a6`).
36. ~~**ADR 0015 §0 의 못박음을 좁힌 것**~~ → **닫혔다**(`2fb0c1a` + `a6f64a6`). **한정: 센 것은 ADR 0015 하나뿐이다.**
37. ~~**`test_99` 를 postgres 축에서 단독으로 돌리면 `test_axis_mode_matches_the_environment` 가 빨갛다**(qa).~~
    → **닫혔다**(`37e04ce`). **한정: 닫힌 것은 `1 failed` 하나이고 `1 error` 는 계약으로 남는다.**
38. **§6-1 의 「근거 추가」가 압축보다 빠르게 커진다 — 갱신 규칙에 흡수 한도를 둔다**(architect).
    자리는 **§6-5**. *이 사이클의 값*: CLAUDE.md 를 만지지 않으므로 §6 은 이 사이클에 자라지 않는다
    (`15a063f` 의 **21,128자 · 276줄** 그대로 — 내가 `ab42761` 트리에서 다시 쟀다, N=1).
    **이 문장은 이 사이클의 마지막 커밋까지 참이어야 참이다**(§후속 62 가 이름 붙인 모양) — 마감이
    다시 잰다.
39. **탐침의 필터에도 §6-1 을 건다**(architect + qa). 자리는 CLAUDE.md §6-1, 순서는 §6-5 커밋.
40. **ⓒ 여섯 자리 — 스키마가 유일성을 강제하지 않는데 코드가 유일하다고 믿는다**
    (api + sync-2d3d + bim-ingest + progress-engine + architect). `services/api/auth/router.py:23` 이 가장 급하다.
    *이 계획이 그 자리를 지나간다*: 그 파일의 `:16` 이 `from .seed import users_count` 이고
    작업 3 이 시드의 소비자를 바꾸지만 **`:23` 의 `.first()` 는 건드리지 않는다** — 40 은 열린 채다.
41. **정렬이 있는데 타이가 남는 자리**(architect + qa). 알려진 자리 하나:
    `services/progress/persistence.py:458`. **전수는 세지 않았다.**
42. **ⓑ 판정을 기계가 붙들 수 있는가**(architect + qa). 계획 0004 §후속 1 의 `make lint` 감사와 같은 자리.
43. **`ORDER BY` 를 붙인 다섯 자리에서 플래너가 무엇을 고르는가**(qa). **열린 채다.**
    자리는 **행 수 축**(ADR 0016 §Deferred 8)이다 — 계획 0013 이 *"§후속 16 이 힙을 키울 때"* 라는
    전제를 반증하고 다시 못박았다.
44. **`apps/web/src/pages/ViewerPage.test.tsx` 를 연다 — A1·A5 의 화면 축**(frontend + qa 공동 편집).
    붙일 계약 **A1 하나 + A5 셋**. **§후속 49 가 그 앞에 필요하다.**
45. **도면 목록의 표시 이름(`FileRow.filename`)과 정렬 키(`drawing_id`)가 다른 축이다**(api + frontend).
46. **환경 탐침의 「도구 부재」를 「음성 관측」으로 읽지 않는다**(architect + qa). 자리는 CLAUDE.md,
    §6-2 행의 소유를 넓히는 것과 함께 — **§6-5 커밋**.
    *이 계획이 실례를 하나 더한다*: **양성 `bind` 검사도 경합을 없애지 못한다** — `bind` 가 성공했다는
    것은 **그 순간** 그 포트가 비어 있었다는 뜻이고, 검사와 사용 사이에 남이 잡을 수 있다.
    처방은 「검사한다」가 아니라 **「쓰는 쪽이 실패를 값으로 보고한다」** 이다.
    *이 계획 자신의 값*: 포트를 **하나도 쓰지 않았다**(§0) — 그래서 이 사이클은 그 경합을 만들지 않는다.
47. **A3 삭제 변이 postgres 10세션 중 1회 `test_20::test_cancel_names_the_decision_by_created_at_…`
    이 함께 빨개진 것의 귀속**(qa). **재지 않았다.**
48. **`ORDER BY` 「줄 삭제」를 붙드는 축이 A3·sqlite 하나뿐이다**(architect + qa). 값은 `test_22`
    docstring 이 정본이다. **이 계획은 재현하지 않았다**(변이를 하나도 심지 않았다).
49. **「다중행」 한정어를 클라이언트 소비자에도 건다**(architect + sync-2d3d + frontend).
50. **§6-1·§6-3·§6-5 의 근거 표면**(= 규칙을 뺀 자수)(architect). **여전히 재지 않았다 — 세 칸 전부.**
    **§후속 38 과 같은 커밋**(§6-5 사이클). *이 계획이 정정한다*: 계획 0013 `:934` 는 §6-2·§6-4 의
    재측정이 *"그중 두 절을 갚는다"* 라 적었는데 **그 둘은 50 의 목록에 없다**(§A-1). 50 은
    **한 칸도 갚아지지 않았다.** 그 재측정이 실제로 낳은 것은 **신규 63** 이다.
51. **세션 안 `EXPLAIN` 계측을 저장소에 둔다**(qa 소유 · architect 가 ADR 문안). 완료 조건 셋
    (ⓐ 옵트인 ⓑ 양성 검사 ⓒ 계측이지 계약이 아님)은 그 사이클이 진다. **§후속 56 이 같은 커밋의 일이다.**
52. **무엇이 세션마다 무정렬 계획을 뒤집는가**(qa). **열린 채다.**
53. **전수 목록의 「자리」 단위와 「자기 커밋을 세는가」를 규약으로 못박는다**(architect).
    **그때까지 계획마다 손으로 적는다** — 이 계획이 §A-2 와 §1-b 에서 각각 답했다.
    *이 계획이 자리를 하나 붙인다*: **§후속 64** 가 같은 트랙이고 다른 물음이다(§A-4 의 구분 표).
54. ~~**바닥값이 산문으로 복창되는 자리를 기계가 잡는가**(qa).~~ → **닫힌 채다**(`5392179`).
    **한정 셋**: ① 그 기계가 보는 것은 **비교식**이라 *"바닥값은 187 이다"* 같은 표기는 못 잡는다
    ② **바닥값 자신이 옳은지는 여전히 아무 테스트도 보지 않는다** ③ 새 트리 키의 복창은 그 기계가
    **거부**한다(계획 0013 §M-3 이 태웠다).
    **이 이월 목록이 54 를 「열림」으로 옮기면 §6-1 12회차의 재발이다 — 옮기지 않았다.**
55. **정본을 다른 자리에 실을 때 「인용인가 재서술인가」를 표시한다**(architect). **열린 채다.**
    *이 계획이 적용했다*: §A 의 인용 넷과 완료 조건 상자 전부에 ⓐ(그 자리에서 도는 참조)와
    ⓑ(인용/재서술 · 전체/발췌)를 붙였다. *열린 채 남는 칸*: **정본이 나중에 바뀌어 인용이 낡는 것.**
56. **`[ORD]`(정렬 없는 다중행 실행)를 `tests/unit` 트리에서 센다**(qa). **재측정하지 않았다.**
57. **`tests/e2e` 의 축**(qa + architect). 16 이 닫혔으므로 sqlite 전용 트리는 e2e 하나만 남았다.
    *이 계획이 값을 하나 더한다*: **그 트리가 sqlite 인 것과 이 사이클의 주제가 얽힌다** —
    e2e 의 `api_server` 는 `DATABASE_URL=sqlite:///…` 로 **하위 프로세스 uvicorn** 을 띄우고, 그
    기동의 부수 효과로 데모 계정이 생긴다(§1-b). **57 을 닫을 때 이 의존이 이미 끊겨 있어야 한다.**
58. **미추적 산출물을 저장소 사실로 세지 않는다**(architect). *조항 후보*: 전수 명령은 **제외
    디렉터리를 명시**하고, 미추적 산출물의 **수**는 문서에 적지 않는다. 자리는 §후속 53 과 같은 트랙.
    *이 계획이 이행했다*: §A-2 와 §1-b 의 전수는 `git grep`·`git ls-files` 로 만들었다(추적 파일만).
59. **격리 단위를 세션으로 골랐을 때의 카탈로그·`n_dead_tup` 값**(qa). **재지 않았다** — 이 계획은
    클러스터를 기동하지 않았다. §후속 30 의 재개 조건과 31 의 닫힘 조건이 이 값에 걸려 있다.
60. **키 없는 트리가 같은 세션에 있을 때 축이 그것을 말하지 않는다**(qa 소유 · 문안 architect).
    **열린 채다.** 완료 조건의 모양은 **부재를 침묵으로 삼키지 않는 것**이다(§후속 46).
61. ~~**`.github/workflows/buildtwin-ci.yml:82` 의 주석이 실행값과 어긋난다**(qa).~~ → **닫혔다**(`972ffd2`).
    **한정: 닫힌 것은 그 한 줄이고, 그 파일의 다른 주석은 아무도 훑지 않았다.**
    *이 계획이 같은 부류를 하나 더 연다*: 같은 파일 `:192` 의 *"seed_dev_data 는 꺼진 채로"* 가
    **작업 5 뒤에 거짓이 된다**(그 이름이 저장소에서 사라진다) — **이 사이클이 고친다**(작업 4, §6-4 1).
    **이 이월 목록이 61 을 「열림」으로 옮기면 §6-1 12회차의 재발이다 — 옮기지 않았다.**
62. **마감(작업 8) 뒤에 오는 같은 사이클의 커밋이 §후속 목록을 낡게 만든다**(architect).
    *조항 후보 둘*: **「뒤집은 쪽이 문서에 적는다」** + **「근거 값은 커밋 본문을 정본으로 삼지
    않는다」**. **표본은 셋**(ⓐ `15a063f` architect · ⓑ `972ffd2` qa · ⓒ 라운드 3 반려 셋).
    *이 계획이 표본을 하나 더 얻었다(ⓓ)*: 이 사이클이 열리는 시점에 **소유가 다른 두 커밋 본문이
    둘 다 못 고쳐진 채 남아 있다** — `fa9f42d`(architect, 「열세 커밋」·「열한 커밋」)와
    `972ffd2`(qa, R-B 가 옮겨 적은 수). **푸시된 커밋 본문은 정정이 닿지 않는 자리**라는 조항이
    이 사이클에서 값으로 확인된다.
    *이 계획의 이행*: 이 문서는 **근거 값을 커밋 본문에서 읽지 않았다** — §A 의 넷 전부 저장소
    파일과 `git log -S`·`git show --stat` 의 출력에서 다시 쟀다.

**(계획 0014 가 연 신규 — 번호를 유지한 채 옮긴다)**

63. **CLAUDE.md §6-5 의 *"§6-2·§6-4 는 압축하지 않는다 — 아래 바닥값에 걸렸다"* 가 거짓이다**
    (architect). 걸린 근거였던 표면 **678자 = 4.48%**(`bfffcd5`)를 `15a063f` 가 **1,090자 =
    7.20%(못박은 분모 15,148) · 5.16%(그 커밋의 §6 21,128)** 로 올렸다 — **어느 분모로도 5% 위**다
    (§A-1 이 다시 쟀다, N=1). **자리는 §6-5 커밋**(대기열: 25 + 27·34·38 + 46 + **63**).
    *닫힘의 모양*: 그 문장을 **정정**하는 것이지 압축하는 것이 아니다 — §6-5 는 압축을
    *"한 절씩, 리뷰어가 읽고 통과시킨 뒤"* 로 못박았고 **「문턱을 넘겼다」는 「압축하라」가 아니다.**
    *한정*: 이 거짓은 **과대 차단** 방향이라 급하지 않다. 급하지 않은 것과 적히지 않아도 되는 것은
    다르다. *그리고 이 항목은 §후속 50 과 다른 항목이다* — 50 은 **재지 않은 셋**(§6-1·§6-3·§6-5)을
    지고, 63 은 **이미 잰 둘이 낡았다**를 진다.
64. **문서가 담은 값이 렌더에서 사라진다 — GFM 표의 칸 수와 중첩 볼드**
    (architect + api). **전수는 표의 행 단위로 다섯이다**(§A-2 의 표 넷 + 중첩 볼드 하나).
    ① `docs/plans/0013-…:516`(4↔6, 실행값 칸이 버려진다) ② `docs/glossary.md:120`(3↔16,
    `Evidence.method` 규약값 대부분이 버려진다) ③ `docs/api.md:39`·`:40`(6↔7, **응답 열**이 버려지고
    `null` 이 그 자리에 렌더된다) ④ `docs/plans/0011-…:1047`(5↔4, 열이 밀린다)
    ⑤ `docs/plans/0013-…:907`(중첩 볼드로 강조가 뒤집힌다).
    **③ 은 산출물이 아니라 생성기의 결함이다** — `services/api/scripts/gen_api_doc.py:99` 가 요약
    칸만 이스케이프하고 `:59` 의 `_ref_name` 이 `anyOf` 를 날 파이프로 잇는다. 손으로 고치면
    다음 `make docs` 가 다시 만든다. **규약의 자리는 §후속 53 트랙이고, 53·58 과 묻는 것이
    다르다**(§A-4). *닫힘의 조건*: 다섯 자리가 렌더에서 값을 되찾고 **생성기가 표 행 전체를**
    이스케이프할 때. *한정*: 이 전수는 **`.md` 의 GFM 표**만 센다 — docstring·주석 안의 표와
    다른 렌더러의 방언은 재지 않았다.
65. **움직이는 라벨은 표에서 지운다고 없어지지 않는다 — 산문에도 두지 않는다**(architect).
    계획 0013 `:978` 이 표에서 「HEAD」를 지우는 그 문단에서 *"오늘 HEAD 는 `fa9f42d` 다"* 라고
    적었고, 그 문장을 담은 커밋은 `ab42761` 이다(`git log -S` 로 확인 — §A-3). **값은 안전했다**
    (같은 괄호가 두 커밋의 CLAUDE.md 미접촉을 적고 §M-9-10 이 트리를 못박는다). *조항 후보*:
    기록물에서 시점을 부를 때는 **커밋 해시**로 부르고, 「오늘」·「지금」·「HEAD」는 **§0 의 못박음
    안에서만** 쓴다. *이 계획이 첫 이행이다*(§0 ㉰ — A 커밋의 해시를 §1 이하가 적지 않는다).
    *역방향 확인 — 이 조항이 놓치는 것*: 해시로 불러도 **그 해시가 무엇인지 모르는 독자**에게는
    「오늘」보다 나쁘다. 그래서 조항은 라벨을 금지하는 것이 아니라 **못박은 자리 밖에서** 금지한다.
66. **아직 없는 절의 값을 앞질러 적는다 — 그리고 그것을 담은 커밋 본문이 무엇을 여는지
    말하지 않는다**(architect). `f4afa42` 는 이 계획 파일을 **새로 만든**(`A`) 497줄 커밋인데,
    그 본문 36줄에 `0014` **0** · `계획 0014` **0** · `후속 18` **0** · `시드` **0** 이다(§1-g, N=1).
    같은 커밋이 §1 을 담지 않은 채 §확인하지 않은 것 37·43·46 에 **§1-b·§1-d 의 값**을 앞질러 적었고,
    §1 이 다시 재니 §1-d 는 갈리지 않았으나 §1-b 의 두 수(**80줄 · 35행**)는 **갈렸다** — 실측은
    71줄 / 표 15행 · 자리 28칸이고, 내가 시도한 어떤 기준도 80 이나 35 를 내지 않았다(§1-b 교차 확인 표).
    *조항 후보 둘*: **「계획의 절을 가리키는 값은 그 절이 실행으로 만든 뒤에 적는다」** +
    **「커밋 제목·본문은 그 커밋이 무엇을 여는지 한 줄로 적는다」**. **자리는 §후속 62 와 같은 트랙이고
    62 의 표본 ⓔ 다** — 62 는 「마감 뒤 커밋이 목록을 낡게 만든다」이고 66 은 「**마감 앞** 커밋이 아직
    없는 절을 낡게 만든다」로, 시간 축의 반대편이다.
    *한정*: 푸시된 `f4afa42` 의 본문은 **고칠 수 없다**(62 의 ⓓ 가 이미 값으로 확인한 사실).
    처분은 정정이 아니라 **기록**이고, 이 사이클이 할 수 있는 것은 **이번 커밋 메시지가 무엇을 여는지
    정확히 적는 것**뿐이다.

67. **register 부트스트랩 자체의 처분 — 인증 없는 요청 하나가 `role=admin` 을 만든다**
    (architect + api, **ADR 필요**). **이 사이클이 만든 것이 아니고 이 사이클이 닫지 않는다**(§B-3 의
    기각 근거 셋). 실측(§B-2 표 1, HEAD `6ab9f6a`, PostgreSQL 16.13, 변이 없음): 빈 postgres 에서
    `POST /api/auth/register` 를 `Authorization` 없이 치면 **201** 이고 응답 `role` 이 **`admin`**
    이다(요청은 `"client"` 를 보냈다 — `router.py:40` 이 덮는다). 그 계정으로 로그인 **200**.
    그리고 그 상태는 **문서화된 설계**다(`services/api/README.md:27-28` · `router.py:33`).
    **작업 3 이 sqlite 갈래를 그 자리로 수렴시킨다**(§B-2 표 2: **403 → 201**) — 새 구멍이 아니라
    이미 열려 있던 자리의 확대다. *묻는 것*: 첫 admin 을 만드는 경로를 열린 엔드포인트가 아니라
    **무엇**이 져야 하는가(일회용 토큰 · 환경변수 · CLI 전용 생성 — 작업 1 이 만든 명령이 이미 그
    모양의 자리다). *닫힘의 조건*: 대체 경로가 ADR 로 정해지고, 빈 DB 의 `POST /api/auth/register`
    (인증 없음)가 **403** 이 되며, 그 값을 붙드는 회귀가 있을 때. *한정 둘*: ① **급하지 않다는 뜻이
    아니다** — 오늘 이미 열려 있고, 그 갈래(`make dev`)가 유일하게 루프백 밖으로 publish 되는
    갈래다(§B-2 노출 축 표). ② 이 항목은 **인증 경로**이지 판정 경로가 아니다 — §3-8 의 `actor == cm`
    게이트와 같은 자리가 아니라는 것이 §B-3 이 「지금 닫는다」로 가지 않은 이유다.


68. **`pyproject.toml:7` 의 `passlib[bcrypt]` 를 핀한다**(architect 문안 + 소유는 §후속 19 가 정한 뒤).
    실측(N=1, `779abd8`): 설치본 `passlib 1.7.4` + `bcrypt 5.0.0` 은 passlib 의 백엔드 자가진단에서
    깨지고, 그래서 **성공하는 명령도 stderr 에 7줄**을 낸다(앞 6줄은 passlib 이 스스로 찍는
    `(trapped)` traceback, 7번째는 이 저장소 자신의 폴백 경고 —
    `services/api/auth/security.py` 의 `_build_context`). `services/api/auth/security.py:3-4` 가
    그 조합을 이미 이름으로 적고 있다(**인용 · 발췌**). *묻는 것*: 핀을 `bcrypt<5` 로 둘지,
    `passlib` 을 떠날지(`bcrypt` 직접 사용·`argon2`), 아니면 폴백을 정식 결정으로 승격할지 —
    **셋 다 해시 스킴을 바꾸므로 기존 해시의 마이그레이션이 딸린다.** *닫힘의 조건*: 그 셋 중 하나가
    ADR 로 정해지고, `python -m services.api.seed` 의 성공 실행 stderr 가 **0줄**이 될 때.

69. **통합 트리의 축 계약이 「세션당 엔진 하나」라, 빈 DB 를 스스로 만드는 테스트는 축을 따라 돌아도
    바닥값에 세어지지 않는다**(qa 가 요청, 소유 qa + **architect 가 ADR 문안**). 오늘의 계약은
    `postgres_axis.check_contract(..., engines_expected=1)` 이고, 그래서
    `tests/integration/test_00_seed_boundary.py` 의 여섯은 **자기 엔진을 기록기에 등록하지 않는다** —
    등록하면 그 계약이 깨진다. 실측 근거 둘: ① **구조** — 기록기에 세어지는 유일한 경로가 `client`
    세션 픽스처의 `observe_engine` + `before_cursor_execute` 리스너이고 그 여섯은 `client` 를 받지
    않는다(§M-2-2, 코드 인용) ② **값** — 그 파일이 들어온 뒤에도 `tests_on_postgres` 가 **188** 이다
    (qa 의 N=1). *묻는 것*: 축 계약을 **엔진 집합**으로 다시 쓰면 무엇이 갈리는가 — 세션이 여러 엔진을
    보는 것을 **결함**으로 볼 것인가 **정상**으로 볼 것인가, 그리고 바닥값은 그 집합의 **합**을 셀
    것인가 **주 엔진**만 셀 것인가. *닫힘의 조건*: 그 ADR 이 서고, **바닥값이 두 엔진을 함께 셀 때.**
    *한정 둘*: ① 이것은 **커버리지 물음이 아니다** — 바닥값은 원래 커버리지가 아니라 「조용히 줄어들지
    않음」의 감시다(`floor.json` `_comment` 한계 (1)). ② **`_measured_at` 의 부재 단정도 함께 다시
    써야 한다** — 그 술어가 「들어온 테스트」라 **이미 있던 테스트가 새로 엔진을 지나는 경로**에 칸이
    없다(§M-2-1 이 187 → 188 에서 그것을 값으로 봤다). *이 항목이 §후속 23·15 와 다른 이유*: 23 은
    `server_version`, 15 는 CI 표본 수, 69 는 **무엇을 세는가의 정의**다.
70. **ADR 0018 §2-4 자리가 §9 를 가리키지 않는다 — 살아 있는 트리 11줄이 대체된 문안의 주소를
    인용한다**(architect). 실측(§M-5): §2-4 본문에 `§9` 히트 **0**, 살아 있는 트리의 `"0018 §2-4"`
    **11줄 / 7파일**, `"0018 §9"` **4줄 / 3파일**. 그중 `tests/e2e/conftest.py:107`(②)과
    `tests/integration/test_99_db_axis_contract.py:397`(①)이 §9-2 가 대체한 두 항을 **번호로** 부른다.
    *문안은 거짓을 퍼뜨리지 않는다* — 그 둘은 빈 DB 에서 돌아 두 문안 모두에서 참이다. **갈리는 것은
    주소뿐이다.** *닫힘의 조건*: §2-4 자리에서 §9 로 가는 줄이 생기고, **같은 커밋이 §9 머리의
    *"위 §0~§8 을 한 글자도 고치지 않는다"* 를 시점으로 좁힐 때** — 그 문장을 그대로 두고 §2-4 를
    고치면 새 거짓이 하나 태어난다(§6-1 이 이름 붙인 **시제 표현**). *한정*: 급하지 않다(소비자는
    자기 자리에서 옳은 문안을 재서술하고 있다). 급하지 않은 것과 적히지 않아도 되는 것은 다르다.
71. **전수의 기준은 그 사이클이 대상의 이름을 바꾸면 낡는다**(architect). 실측(§M-4 축 B): §1-b 가
    못박은 표기 집합으로 `32c109a` 를 세면 **27줄 / 7파일**이 밖에 있고, **그 밖에 ⓐ 기전이 전부**
    들어 있다(`seed_all(s)` 두 자리 · `python -m services.api.seed` 두 자리). 전수를 만든 절(§1-b)과
    그 이름을 만든 작업(1·3)이 **같은 문서** 안에 있다. *이것이 §6-1 *역방향 확인*의 표기 부류
    넷(표기 변종 · 활용형·동의어 · 보간 · 줄바꿈)과 다른 부류인 이유*: 넷은 **같은 트리 안에서 못 잡는
    표기**이고 이것은 **시간 축**이다 — 목록은 옳게 만들어졌고 그 뒤에 대상이 개명됐다.
    *닫힘의 조건*: **두 번째 관측**이 나오면 CLAUDE.md §6-1 의 표기 부류에 ⑤ 로 더한다(§6-5 의 갱신
    규칙: *"두 사이클 이상에서 관측되면 항목을 추가한다"* — 한 번으로는 넣지 않는다). *그때까지의
    처방*: 전수를 만든 계획이 **자기 작업이 그 표기 집합을 바꾸는지** 완료 조건에서 한 번 본다.
    *이 항목이 §후속 53·58 과 다른 이유*: 53 은 **자리의 단위**, 58 은 **미추적 산출물**, 71 은
    **기준의 수명**이다.

**신규(이 계획이 연다):**

72. **관측 가능성을 없애는 커밋이 그 자리에서 그것을 붙든다**(qa + architect).
    *조항*: 부수 효과를 명시 호출로 바꾸거나, 어떤 사실이 값으로 보이던 자리를 없애는 작업은,
    **그 작업의 완료 조건에 「그 커밋이 남길 회귀」를 이름과 값으로 적는다.** 회귀의 소유가 다른
    에이전트면 **그 회귀가 같은 사이클 안에 있어야** 완료 조건이 충족된다.
    *근거는 값이다*(§A-1-a, architect 재현 N=1, `4d06867`): 사이클 0014 전체를 되돌리는 변이가
    `32c109a` 없이 **226 passed**(기준선과 동일)이고, 작업 2 **이전**이었다면 같은 자리에서
    **2 failed / 32 passed / 192 errors** 였다(`b09ae65` 본문 표 4행과 갈리지 않는다).
    *처방이 「작업 순서 규약」이 아닌 이유 셋*: ① 그 순서는 CLAUDE.md §3-1 이 강제한 것이라 규약이
    §3-1 과 충돌하고 다음 사이클에서 재량으로 읽힌다 ② 순서를 바꾸면 소유가 섞인 커밋이 되어 §2 를
    밀어낸다 ③ **저장소에 순서를 붙들 자리가 없다** — e2e 시드 호출을 `_wait_http` 뒤로 옮겨 창을
    다시 열어도 `make e2e` **12 passed**(architect 재현, N=1).
    *닫힘의 조건*: 그 조항이 「부수 효과를 명시 호출로 바꾸는 작업」의 완료 조건에 들어가고
    **한 사이클이 실제로 지킬 때**. **이 계획의 작업 6·7 이 그것을 진다.**
    *한정*: **관측 1회다.** 그래서 CLAUDE.md 절에 넣지 않는다(§6-5 갱신 규칙 —
    ⓐ `grep -n "두 사이클 이상에서 관측되면" CLAUDE.md`, **ⓑ 인용 · 발췌**). **닫힘과 승격은
    다른 일이다** — 72 가 닫혀도 CLAUDE.md 항목은 열리지 않는다.

**이 계획이 상태를 바꾸는 항목(위 목록의 자구를 고치지 않고 여기 적는다):**

- **2 · 3 · 6 · 11 · 23 · 24 — 열린 채. 그러나 「재측정하지 않았다」가 아니다.** §2 의 표가 여섯을
  **전부** 이 트리에서 다시 잰 값이다. 특히 **11 은 다섯 사이클 만의 재측정**이고 값이 갱신됐다
  (계획 0009 의 **807 passed** → 이 트리에서 **838 passed**, 무보호는 그대로). **23 은 재개 조건이
  성립하지 않는다**(잰 값에 대한 단언·비교 **0건**; 소스 히트는 8 → **14줄**로 늘었다).
  **24 는 자리가 바뀐다** — compose 의 `db` 가 `postgis/postgis:16-3.4` 라, **§확인하지 않은 것 43
  이 닫히는 순간 24 가 처음 측정 가능해진다.** 두 항목의 닫힘이 이제 한 자리에 있다.
- **17 — 열린 채**(35 중 30). 이 사이클은 순서 축을 건드리지 않는다. **취소선으로 옮기지 않았다.**
- **19 — 열린 채, 그리고 무게가 는다.** 이 계획의 **작업 5** 가 소유 없는 파일 **셋을 다시** 만진다
  (`.env.example` · 루트 `README.md` · `docker-compose.yml`). 사이클 0014 가 이미 그 셋을 만졌고
  (`3843cc8` · `e1e1a1a`), **두 사이클 연속**이 된다. §2 절 안 히트 재측정(N=1):
  `.env.example` **0** · `docker-compose.yml` **0** · `README.md` **1**(그 한 줄은 `config/README.md`).
  *닫힘의 조건·순서 불변*(§6-5 대기열의 마지막).
- **46 — 열린 채, 실례가 하나 는다.** 이 계획의 **작업 2** 가 그 조항을 이행한다: `docker` CLI 부재를
  **음성 관측으로 읽지 않고 skip 으로 보고**한다. 그리고 **§0 이 데몬 부재를 값으로 적었다**(도구는
  있고 데몬이 없다 — 부재의 두 층을 갈라 적는 것이 이 항목의 모양이다).
- **54 · 61 — 닫힌 채다. 이 계획도 「열림」으로 옮기지 않았다**(§6-1 12회차).
- **62 — 열린 채. 표본 ⓖ 와 조항 후보 ⓒ 가 붙는다**(§A-2). ⓖ: 계획 0014 §M-11 54 가 「미측정」이라
  적은 둘을 **같은 사이클의 여덟 걸음 앞 커밋 `5562201`** 이 이미 쟀다. ⓒ:
  **「마감의 부재 단정에 §6-1 을 건다 — 「재지 않았다」를 적기 전에 같은 사이클의 앞 커밋이 그것을
  이미 쟀는지 본다」.** **62 를 닫는 문서는 세 조항을 각각 이름으로 적는다 — 하나라도 빠지면 62 는
  닫히지 않는다**(§A-2-c).
- **65 — 열린 채, 이 계획이 두 번째로 이행한다**: 「오늘」·「지금」·「HEAD」를 §0 밖에서 쓰지 않았다.
- **66 — 열린 채, 이 계획이 조항 후보 둘째를 이행한다**: 커밋 A(`50ddaa5`)의 본문이 **무엇을 여는지**
  첫 문단에 적었고, 이 문서도 머리말에 같은 것을 적는다.
- **67 — 열린 채이고, 이 계획이 닫으러 간다.** 닫힘의 조건 불변(대체 경로가 ADR 로 정해지고, 빈 DB 의
  인증 없는 register 가 **403** 이 되며, 그 값을 붙드는 회귀가 있을 때). *이 계획이 더하는 것*:
  ① **ADR 0019** 가 대체 경로를 정한다 ② 작업 6·7 이 나머지 둘을 진다 ③ **한정 ①이 갱신된다** —
  그 항목은 `make dev` 가 *"유일하게 루프백 밖으로 publish 되는 갈래"* 라 적었는데 `e1e1a1a` 뒤에는
  compose 도 루프백이다. **그럼에도 리뷰어가 `--host 0.0.0.0` 로 띄운 실행에서 비-루프백
  `192.0.2.2` 의 인증 없는 register 가 201·`role=admin` 임을 쟀다** — 즉 **구멍은 compose 의
  publish 설정이 아니라 앱 자신에 있다.** 그 실측이 이 항목을 이 사이클로 끌어올린 근거다.
- **68 — 열린 채.** 이 계획은 `pyproject.toml` 을 만지지 않는다. *더하는 것 없음.*
- **69 · 70 · 71 — 열린 채.** 이 계획은 축·ADR 0018 문안·시드 표기 집합을 만지지 않는다.
  다만 **69 가 작업 2 에 걸린다**: 새 회귀가 postgres 축을 지나는지를 qa 가 값으로 적어야 하고,
  그 정의를 69 가 진다.

---

## Deferred — 이 계획이 보고 고치지 않는 것

계획 0014 §Deferred 를 **재측정하지 않고** 그대로 이월한다. 이 계획이 더하는 것 하나:

- **`packages/core/settings.py` 의 `resolve_jwt_secret` 이 「운영」을 `database_url` 이 sqlite 가
  아닌 것으로 정의한다**(ⓐ `grep -n "def resolve_jwt_secret" packages/core/settings.py`,
  **ⓑ 재서술**). 그 정의 때문에 **compose 개발 스택이 「운영」으로 분류되고**, 그것이 §1-c 문 2 의
  기전이다. **이 계획은 그 정의를 바꾸지 않는다** — 바꾸면 「개발 편의를 위해 시크릿 요구를 낮춘다」가
  되어 §3-4 와 부딪친다. 처방은 정의를 낮추는 것이 아니라 **`.env` 를 만들어 주는 것**(작업 1)이다.
  *묻지 않은 것*: 그 분기 자체가 옳은 축인가(sqlite 여부가 아니라 명시적 환경 플래그여야 하는가).

---

## 이 계획이 확인하지 않은 것

**계획 0014 §확인하지 않은 것(본문 1~55 + 56·57[§C-7] + 58·59[§M-11])을 번호를 유지한 채 옮기고**,
이 계획이 처분한 것만 적는다. 형식은 **[재지 않았다]** / **[재야 한다 → 소유 · 언제]**.

1. **[재야 한다 → qa · §후속 24 를 열 때]** CI 의 `postgis/postgis:16-3.4` 에서 아무것도 재지 않았다.
   **이 계획도 CI 를 돌리지 않았다**(푸시하지 않는다).
2. **[재지 않았다]** 정렬 축을 바꾸는 변이. **이 계획은 코드 변이를 하나도 심지 않았다.**
3. **[재지 않았다]** `[ORD]` 677 ↔ 679 ↔ 542 의 귀속. → **§후속 56.**
4. **[재지 않았다]** 전량 둘을 동시에 돌렸을 때의 파일 경합.
5. **[재야 한다 → qa · §후속 43]** 정렬을 붙인 다섯 자리의 `EXPLAIN`.
6. **[재지 않았다]** 이 목록이 쓰인 뒤 **같은 사이클의 커밋이 닫는 항목**. **이 계획도 막는 기계를
   갖지 않는다** — 마감 절이 다시 센다(§후속 62).
7. **[재야 한다 → architect · §후속 36 의 뒷면]** 다른 ADR·계획의 못박음이 같은 모양인지.
8. **[재야 한다 → architect · §후속 25·50·63 을 열 때]** §6-5·§6-1·§6-3 의 **근거 표면 세 칸**.
   **이 사이클은 CLAUDE.md 를 안 만지므로 그 값이 움직이지 않는다** — 그리고 그 단정 자신이
   §후속 38·62 가 이름 붙인 모양이므로 **마감이 다시 잰다.**
9. **[재지 않았다]** `qa` 가 보고한 서버 변이 14건.
10. **[재지 않았다]** §6-1 12회차 외 다른 행의 관측값 재현.
11. **[재야 한다 → qa · ⓑ 24 의 소비자가 HTTP 를 넘어갈 때]** **ⓑ 24 자리의 소비자를 「전부」
    훑었는가.** 이 사이클의 통합 초록은 그 확인이 아니다.
12. **[재지 않았다]** `[PG-after-update-*]` 가 순서를 바꾸지 않은 기전(HOT update 가설).
13. **[재지 않았다]** `tests/e2e` 를 postgres 로. **`make e2e` 도 이 사이클에 실행하지 않는다.**
    → **§후속 57.** *이 계획의 한정*: 그런데 **e2e 는 이 사이클의 소비자 중 하나다**(§1-b) —
    그 트리를 돌리지 않고 배선을 바꾸는 것의 위험은 §리스크에 적었다.
14. **[재지 않았다]** `[ORD]` 를 `tests/unit` 에서 세지 않았다 → **§후속 56.**
15. **[재야 한다 → qa · 통합 테스트 수가 바뀔 때]** 바닥값의 CI 표본은 여전히 2회다.
    *이 계획이 값을 하나 더한다*: **통합 트리가 226 으로 커졌다**(§0) — 바닥값 187 과 이 수의
    관계는 이 계획이 재지 않았다.
16. ~~A4 의 동률을 픽스처로 만들 수 있는가~~ → **닫혔다**(계획 0012 §M-6 16).
17. **[재야 한다 → qa+architect · §후속 23]** 커밋된 `postgres.measured.json` 이 CI 가 재현하지 않는
    환경을 기술한다.
18. **[재야 한다 → qa · §후속 24]** PostGIS 가 있을 때 무엇이 달라지는가.
19. **[재야 한다 → qa · 다음 발생 시]** CI 의 그 1회.
20. **[재야 한다 → architect · 커밋 직전]** `make test` 네 갈래 + vitest + `make lint`.
21. **[재지 않았다]** CI 초록의 표본은 늘지 않았다.
22. **[재지 않았다]** HOT 가지치기 단독 경로.
23. **[재지 않았다]** 화면이 서버 순서를 잃는 방향(§후속 44 가 그 축을 연다).
24. **[재지 않았다]** 새 회귀·새 축이 **CI 러너에서** 도는 비용.
25. **[재지 않았다]** 남의 미커밋 편집 내용. **이 사이클 착수 시 루트 `git status --porcelain` 은
    빈 출력이었다 — 남의 `M` 은 없다.**
26. ~~축이 포트를 공유 자원으로 쓴다~~ → **닫혔다**(계획 0011 §1-c). **이 계획은 포트를 쓰지 않았다.**
27. **[재지 않았다]** `test_99` 자식 기구와 게이트의 상호작용.
28. ~~인덱스 선택~~ → **질문이 틀렸다**(계획 0011 §M-2-1). 남는 세 칸은 ADR 0015 §Deferred 6.
29. **[재지 않았다]** OID 타이브레이크 가설의 기전.
30. **[재지 않았다]** 세 측정의 비율 차이가 유의한가.
31. **[재지 않았다]** 게이트 제거 변이가 남긴 측정 파일 더러움의 뒷이야기.
32. **[재야 한다 → architect · §후속 40 을 열 때]** **ⓐ 의 정의가 놓치는 소비자 모양**
    (`groupby`·`zip`·`enumerate`·파이썬 `dict` 의 삽입 순서).
33. **[재지 않았다]** `ss` 가 언제부터 없었는가. **이 계획은 `ss` 를 부르지도 않았다**(포트 미사용).
34. **[재지 않았다]** 네 관측자 값의 비율 차이 검정.
35. **[재지 않았다]** 역방향 변이 열 칸과 A5·sqlite 0/10. 값은 `test_22` docstring 이 정본이다.
36. **[잴 방법이 없다]** IPv6 로만 물린 리스너(`/proc/net/tcp6` 부재).
37. **[재야 한다 → qa · 작업 4 의 배선에서]** §1-b 35행 표의 자리별 ⓐ/ⓑ/ⓒ 재검증.
38. **[재지 않았다]** 어떤 변이 아래에서도 `make test` 를 돌리지 않았다. **이 계획도 그렇다.**
39. **[관측하지 않았다]** 변이 측정의 공유 트리 오염. **이 계획은 변이를 심지 않았다.**
40. **[재지 않았다]** 탐침의 리다이렉트가 「모든」 엔진을 잡았는가.
41. **[재지 않았다]** 탐침이 만든 504 스키마의 정리 비용.
42. **[재지 않았다]** `tests/invariants`·`tests/regression` 이 축 위에서 무엇을 하는가.

**(계획 0014 가 더한 신규 — 번호를 유지한 채 옮긴다)**

43. **[재지 않았다]** **`docker compose up` 을 돌리지 않았다.** `make dev` 에 대한 이 계획의 단정은
    **커밋된 `docker-compose.yml` 이 기술하는 환경**을 `Settings` 에 먹여 조건식을 평가한 것이고
    (§1-d, N=1), 컨테이너를 띄운 값이 아니다. **[재야 한다 → qa · §후속 19 를 열 때]**
44. **[재지 않았다]** **§후속 64 의 손해를 GFM 이 아닌 렌더러에서** 확인하지 않았다. 코드 스팬 안의
    파이프를 보호하는 렌더러가 있다면 네 자리의 손해가 달라진다.
45. **[재지 않았다]** **`.env` 가 있는 개발자 환경.** `.env` 는 커밋되지 않으므로(`.env.example` 만
    있다) `SEED_DEV_DATA` 가 켜진 로컬이 실제로 있는지 **저장소로는 답할 수 없다.**
46. **[재지 않았다]** 이 계획의 전수(§1-b)가 센 80줄 중 **작업이 실제로 만지는 줄이 몇인가.**
    센 것은 「시드를 말하는 줄」이지 「고쳐야 하는 줄」이 아니다 — 그 둘의 차는 작업이 낸다.
47. **[재지 않았다]** **말하지 않고 기대는 자리.** §1-b 의 표기 집합은 시드를 **이름으로 부르는**
    줄만 센다 — 데모 계정 넷이 있다고 **가정만 하는** 코드(`ROLES` 네 값을 도는 루프 따위)는 그 밖이다.
    **[재야 한다 → qa · 작업 7 의 회귀에서]**
48. **[재지 않았다]** **같은 프로세스가 앱을 두 번 띄울 때.** §1-c 의 곱은 축을 「프로세스」로 잡아
    그 칸을 접었다. `tests/integration/conftest.py:11-13` 이 그 상호작용을 이미 이름 붙이고 있다.
49. **[재지 않았다]** **`seed_dev_project` 만 사라지는 갈래.** §1-e 의 변이는 `seed_dev_users` 하나를
    지웠다 — 멤버십만 없으면 로그인은 **200** 이고 프로젝트 목록만 빈다(ADR 0006).
    **[재야 한다 → qa · 작업 7 완료 조건 4]**
50. **[잴 방법이 없다 — 저장소 밖]** **PR 제목·본문.** §1-g 는 **커밋 본문의 문자열**을 셌다.
    「이 커밋이 무엇을 여는가」를 저장소 밖(PR 제목·브랜치 이름)이 말하고 있는지는 여기서 답할 수 없다.

51. **[재지 않았다]** **§1-b 의 「자리 = (파일 × 갈래)」 28칸을 다시 세지 않았다.** 그 수는 **갈래가
    셋**이라는 전제 위에 있는데 §B-1 이 넷째 갈래(**모듈 결합** — `router.py:16`)를 하나 만들었다.
    **[재야 한다 → architect · 작업 8 의 마감]**
52. **[재지 않았다]** **실제 바인드 주소.** §B-2 의 노출 축 표는 `docker-compose.yml:32` ·
    `Makefile:24` · `tests/e2e/conftest.py:168` 의 **인용**이지 띄워서 잰 값이 아니다.
    `docker compose up` 을 돌리지 않았다(§43 과 같은 한계). **[재야 한다 → qa · §후속 19 를 열 때]**
53. **[재지 않았다]** **부트스트랩 표기 집합이 놓치는 셋.** §B-4 의 목록은
    `부트스트랩|bootstrap|users_count|첫 사용자|first user|forbidden_role` 로 만들었다. 밖에 있는 것:
    ① `forbidden_role` 을 쓰지 않고 403 을 말하는 자리 ② **영어 산문**(`first admin`·`initial setup`)
    ③ **보간**(부트스트랩 문구를 f-문자열로 조립하는 자리 — 그 문자열이 소스에 없다, §6-1 부류 ③).
54. **[재지 않았다]** **작업 4 가 만드는 시드 창.** e2e `api_server` 는 uvicorn 을 띄우고
    `_wait_http("/api/health")` 로 기다린 **뒤** 시드 명령을 부르게 된다 — 그 사이 구간에는
    **부트스트랩이 열린 리스너**가 실제 포트에 살아 있다(루프백이다 — `tests/e2e/conftest.py:168`).
    창의 길이도, 그 창에서 register 가 무엇을 내는지도 재지 않았다. **[재야 한다 → qa · 작업 4]**
55. **[재야 한다 → architect · qa 가 작업 2 를 커밋한 뒤]** **깨끗한 트리에서의 게이트.**
    §B-5 의 실행은 qa 의 미커밋 `M` 둘을 담은 트리를 쟀고, 그 트리의 integration 은
    **1 failed, 225 passed** 였다(빨간 한 건이 qa 가 지금 고치는 파일이다). **`6ab9f6a` + 이 문서**
    만 담은 트리에서 네 갈래를 잰 값은 이 사이클에 **없다.**

56. **[재지 않았다]** **부트스트랩과 노출을 한 실행으로 잇지 않았다.** §B-2 표 1 은 빈 postgres 에서
    인증 없는 `POST /api/auth/register` 가 **201·`role=admin`** 임을 쟀고(그것은 in-process 였다),
    C-2 표 5행은 그 포트가 **0.0.0.0 에 붙었음**을 쟀다(그것은 리스너가 탐침이었다). **둘을 한
    실행으로 이은 값 — 비-루프백 주소에서 실제 api 에 register 를 쳐서 201 을 받는 것 — 은 이
    사이클에 없다.** `e1e1a1a` 뒤에는 그 실행이 **거부**를 내야 하고, 그것도 재지 않았다.
    **[재야 한다 → qa · §후속 19 또는 67 을 열 때]**
57. **[재지 않았다]** **`e1e1a1a` 뒤 `make dev` 의 개발자 경험.** 브라우저가 `localhost:5173` 으로
    붙는 한 손해가 없다는 것이 이 절의 판단인데, 그 스택을 띄워 확인하지 않았다. 그리고 그 확인은
    **다른 결함 하나를 만나게 된다**: `apps/web/vite.config.ts` 의 `/api` 프록시 대상이
    `http://localhost:8000` 이라 **web 컨테이너 안에서는 자기 자신을 가리킨다**(compose 의 `web` 은
    `node:22-alpine` 컨테이너 안에서 vite 를 돌린다 — `docker-compose.yml` 의 `web.command`).
    **이 계획은 그것을 재지 않았고 이 사이클이 고치지 않는다** — 소유는 `frontend`(§2) 이고,
    `e1e1a1a` 와 무관하게 오늘도 그 상태다(코드 인용으로만 답한다). **[재야 한다 → frontend + qa ·
    §후속 19 를 열 때]**

58. **[재지 않았다] 계획 0014 의 마감은 postgres 축을 재현하지 않았다.** 클러스터를 띄우지 않았고
    포트 55430~55459 를 쓰지 않았다. `232 passed` · `tests_on_postgres=188 engines=1 floor=187` ·
    `measured` md5 불변 · **작업 7 표의 셋째 열 여섯 칸**은 전부 **qa 의 N=1** 이다. 내가 구조로
    확인한 것은 §M-2-2 하나뿐이다. **[재야 한다 → qa · CI 가 이 트리를 처음 돌릴 때 — §후속 69 · 15]**
59. **[재지 않았다] `32c109a` 에서의 「자리 = 파일 × 갈래」.** §M-4 축 B 가 그 이유를 값으로 적었다 —
    §1-b 의 표기 집합이 낡아(27줄이 밖) 그 집합으로 센 수는 **28 보다 나쁘다.** 새 집합으로 세는 것은
    **새 전수**이고 그것을 쓰는 물음이 지어야 한다. **[재야 한다 → 다음에 시드 축의 전수가 필요한
    사이클 · §후속 71]**


**이 계획이 상태를 바꾸는 항목:**

- **43 — 열린 채, 그리고 이유가 갱신된다.** 이 환경에는 **docker 데몬이 없다**(§0, N=1). 도구
  부재가 아니라 **데몬 부재**다 — 둘을 갈라 적는다(§후속 46). `docker compose config` 는 데몬 없이
  돌므로 이 계획의 compose 단정은 전부 그 축이다. **[재야 한다 → qa · 데몬이 있는 자리에서]**
- **53 — 열린 채.** 부트스트랩 표기 집합이 놓치는 셋(`forbidden_role` 없이 403 · 영어 산문 · 보간)은
  **이 계획도 재지 않았다.** 작업 6 이 그 표기를 바꾸므로 §후속 71(기준의 수명)이 여기 걸린다.
- **54 — 열린 채이고 자리가 좁혀진다**(§A-2-d). 창의 **길이**(`1.276·1.294·1.307s`, N=3)와 그 창의
  **register 값**(201·`role=admin`)은 `5562201` 이 이미 쟀다 — 계획 0014 §M-11 이 그것을 「미측정」이라
  적은 것이 이 계획이 정정하는 거짓이다(**갈음이지 소급 수정이 아니다**). **남는 미측정**: 그 값은
  **실제 e2e 배선(sqlite, 하위 프로세스)이 아니라 그것을 흉내낸 postgres 탐침**의 것이고, 그 뒤로
  일곱 커밋이 지났다. **[재야 한다 → qa · 작업 7 — 그때 이 창은 「길이」가 아니라 「무해한가」로
  물어진다]**
- **56 — 열린 채이고 앞 절반이 리뷰어에게서 채워졌다.** *"비-루프백 주소에서 실제 api 에 register 를
  쳐서 201 을 받는 것"* 을 **리뷰어가 쟀다**(`uvicorn --host 0.0.0.0`, 빈 sqlite, 포트 55437 →
  `192.0.2.2` 에서 **201 · `role=admin`**). **나는 재현하지 않았다.** 남은 뒤 절반은
  **`e1e1a1a` 뒤 compose 의 api 컨테이너로 이은 거부**이고, 그것은 데몬을 요구한다(43 과 한 자리).
  **[재야 한다 → qa · 데몬이 있는 자리에서]**
- **57 — 열린 채, 그리고 이 계획의 작업 4·5 가 그 절반을 진다.** 그 항목이 코드 인용으로 이름 붙인
  vite 프록시 결함(`apps/web/vite.config.ts:13`)이 §1-c 의 **문 4** 다. 나머지 절반(`make dev` 를
  실제로 띄운 개발자 경험)은 43 과 함께 열린 채다.

**신규(이 계획이 더한다):**

60. **[재지 않았다] `docker compose up --build` 는 물론 `docker compose exec` 도 돌리지 않았다.**
    이 계획의 compose 단정은 전부 `config` 축이고, **작업 3 이 만들 시드 명령의 실제 동작은
    이 계획이 검증하지 않는다.** **[재야 한다 → qa · 데몬이 있는 자리에서 · §확인하지 않은 것 43]**
61. **[재지 않았다] `.env` 가 있는 개발자 환경에서 `make env` 가 무엇을 하는가.** 완료 조건 3(멱등)이
    그것을 요구하지만 **이 계획은 그 파일을 만들지 않았다** — 저장소에 `.env` 가 없고(`.gitignore:1`),
    만들면 이 세션 밖의 사람 환경을 바꾼다. **[재야 한다 → qa · 작업 1]**
62. **[재지 않았다] 이미지 안의 것.** §1-b ②ⓓ 그대로 — `Dockerfile` 이 무엇을 담는지, `make dev` 가
    빌드하는 이미지에서 `python -m services.api.seed` 가 도는지 재지 않았다.
63. **[재지 않았다] 문 4 의 손해를 브라우저에서 확인하지 않았다.** *"web 컨테이너 안에서 프록시가
    자기 자신을 가리킨다"* 는 **코드 인용 둘의 결합**이고(vite `target` + compose `web.command`),
    실제 요청이 어떤 오류를 내는지(연결 거부인지 타임아웃인지 5173 자신의 응답인지)는 재지 않았다.
    **[재야 한다 → frontend + qa · 작업 4 뒤]**
64. **[재지 않았다] `resolve_jwt_secret` 이 실제 요청 경로에서 언제 불리는가.** §1-c 문 2 는
    `Settings` 를 직접 만들어 부른 값이고(N=1), **앱을 띄워 로그인을 쳐서 500 을 받은 값이 아니다.**
    호출부는 코드 인용으로만 답했다(`services/api/auth/security.py:59`·`:64`).
    **[재야 한다 → qa · 작업 1 뒤]**
65. **[재지 않았다] 대기열 여섯의 「밀린 사이클 수」를 세지 않았다.** §2 의 역방향 확인 ⓐ 가
    *"여덟 사이클째"* 라 적은 것은 항목이 열린 계획 번호(0006~0008)에서 뺀 값이고, **각 항목이
    실제로 몇 번의 이월 목록을 지나왔는지는 세지 않았다.**

---

## 다음에 부를 에이전트

**`qa` 를 먼저 부른다 — 작업 1·2·3 을 한 묶음으로.** 받을 것: 이 계획 §1-c 의 **문 1·2·3**(각 칸이
실행값이거나 코드 인용이고 다른 절 참조가 아니다), §1-d 의 **20칸 곱 표**(그중 자기 트리가 오늘 보는
칸은 2칸뿐이라는 값), §1-e 의 **`required: false` 기각 근거**(그 처방 단독은 실패 시점을 「기동 전」에서
「로그인」으로 민다 — 사본 실측 N=1), 그리고 **완료 조건 상자 셋**(작업 1·2·3). 특히 작업 1 의 조건
4 와 작업 2 의 조건 2·3 — **§후속 72 의 조항과 §후속 46 의 조항이 각각 거기 걸려 있다.** `qa` 는
`Makefile` 과 `tests/` 를 소유하고 그 밖은 만지지 않는다. 데몬이 없다는 것(§0)과 그래서 검증이
`docker compose config` 축에 한정된다는 것을 **한계로 적어서** 넘긴다 — *"놓칠 수 있다"* 고 적는 것은
커버리지가 아니므로(§6-1), **못 잰 것은 §확인하지 않은 것 60~64 의 번호로** 넘긴다. `qa` 다음은
`frontend`(작업 4) → `architect`(작업 5) → `api`(작업 6, **ADR 0019 를 읽고**) → `qa`(작업 7) →
`architect`(작업 8 마감) → `reviewer` 다.
