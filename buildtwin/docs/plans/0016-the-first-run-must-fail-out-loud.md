# 계획 0016 — 첫 실행자는 **사용자**다: 그 경로의 실패는 **소리내어 죽는가**

> **이 문서가 여는 것**(계획 0015 §후속 66 의 조항 후보 둘째 — *"커밋 제목·본문은 그 커밋이 무엇을
> 여는지 한 줄로 적는다"*, 문서 자신에도 같은 것을 건다): 사이클 0016 을 연다. 주제는 **사용자가
> 처음 치는 `make env` → `make dev` → `make seed-compose` → 브라우저 경로에서, 실패가
> 「관측 가능한 형태」로 나오는가** 하나이고, 이 계획이 닫으려는 것은 그 경로에서 **오늘 조용한 두
> 자리**다(§1-c ㉠ 빌드 · ㉡ 프록시·화면).
> **이 커밋이 담는 것은 이 계획 문서 하나뿐이다** — 작업 1~6 의 산출물은 각 소유의 커밋이 담는다.

---

## 0. 이 문서의 실측이 나온 자리 (재현 방법)

**작업 트리** `/home/user/Bim/buildtwin`, **저장소 루트 `/home/user/Bim`**, 브랜치
`claude/buildtwin-initial-setup-ubulzb`, **트리 `a03f13d`**(루트 `git status --porcelain` 빈 출력 —
이 문서를 여는 시점과 커밋 직전 둘 다).

**이 문서의 모든 `파일:줄`·커밋 참조는 트리 `a03f13d` 의 것이다**(CLAUDE.md §3-13 첫째 갈래).
**못박음을 좁히지 않는다 — 전체를 못박고 예외를 이름으로 적는다.** 예외 둘:

- **㉮ 다른 기록물을 인용하는 줄** — 계획 0015 는 그 문서의 §0·§M-12-0·§M-13-0 이 못박은 트리들
  (`4d06867` · `6759ec8` · `a03f13d`). 그 자리에서 트리를 적는다.
- **㉯ CLAUDE.md 를 가리키는 줄** — §3-13 **둘째** 갈래(계속 편집되는 자리)라 줄 번호가 조용히 낡는다.
  **줄 번호로 부르지 않고 그 자리에서 도는 `grep` 으로 적는다.**

**명령의 못박음**(계획 0015 §M-13-2 가 §후속 65 에 더한 **확장 항 ㉡** — 이 문서가 그 조항의 **둘째**
준수 사례다). 이 문서에 적는 모든 `git` 명령은 **트리 인자를 담고 `--all` 을 쓰지 않는다**:
`git grep <패턴> a03f13d -- .` · `git log -S <문자열> a03f13d` · `git rev-list --count <A>..<B>`.
**트리 인자를 받지 못하는 명령**(`make` · `docker` · `dockerd` · `ps` · `pytest` · `pip` · `getent`)은
**전부 이 §0 이 못박은 `a03f13d` 의 작업 트리에서 돌린 것**이고, 그 사실을 여기 한 번 적어 각 자리에서
되풀이하지 않는다.

**시점을 부르는 말**(65 의 원래 항 ㉠). 「오늘」·「지금」·「HEAD」는 **이 §0 안에서만** 쓴다.
§1 이하는 커밋 해시로 부른다.

**방법 · N · 표본 간격**(§후속 34 · 27). 아래 값은 **2026-09-08 한 세션 안**(대략 14:00~14:35 UTC)에서
잰 것이고, 각 표에 **N 을 칸으로** 적는다. **「없다」·「죽었다」에도 N 을 적는다.**

**게이트**(계획 0015 §M-13-1 의 표를 **인용 · 발췌** 한다 — 같은 트리이고 이 문서는 다시 돌리지 않았다):
unit **492** · invariants **112** · regression **8** · integration **232** · vitest **289**(29 files) ·
`make lint` **rc 0** · `make e2e` **12 passed** · 고아 `vite preview` 전 **0** → 후 **0**.

**자원**: 포트 **0개** · postgres 클러스터 **0회** · 심은 변이 **0** · 저장소 루트에 `.env` 를 만들지
않았다(`ls -a /home/user/Bim/buildtwin | grep -c '^\.env$'` → **0**, N=1) · **docker 데몬 1회**
(계획 0015 §M-13-5 에서 띄웠고 껐다 — 잔여 `dockerd` **0** · `containerd` **0**).

---

## 1. 분석 — 사용자의 첫 실행이 지나는 문과, **각 문이 죽는 소리**

### 1-a. 경로를 명령 단위로 편다

사용자의 마지막 지시는 *"데모는 로컬에서 먼저 돌리자"* 였고, 사이클 0014·0015 가 그 경로의 문 넷을
열었다. 남은 사실 하나가 이 사이클의 전제다 — **`docker compose up --build` 를 끝까지 돌린 사람이
아무도 없다**(계획 0015 §확인하지 않은 것 43·60·62, 그리고 §M-13-5 가 그 이유를 다시 이름 붙였다).
**사용자가 첫 실행자다.**

```
① make env           → .env(JWT_SECRET) 를 만든다
② make dev           → docker compose up --build
                        ├ 문 2  데몬에 붙는다
                        ├ 문 3  이미지를 받고 **빌드**한다(api·worker 의 pip, web 의 npm)
                        └ 문 4  여섯 서비스가 뜬다(준비 **순서**는 계약이 없다)
③ make seed-compose  → docker compose exec -T api python -m services.api.seed   (다른 셸)
④ 브라우저 :5173     → 문 6  vite `/api` 프록시 → api
⑤ 로그인             → 문 7  계정이 있는가
```

문 1(`.env`)·문 5(시드가 compose 의 DB 에 닿는가)는 사이클 0015 가 열었다. **이 계획이 묻는 것은
「열렸는가」가 아니라 「닫혀 있을 때 그것이 보이는가」** 다.

### 1-b. 전수 — 「이 경로의 실패를 말하는 자리」 (§6-1 ①②③)

**① 생성 기준.** 저장소 루트에서, 이 경로를 **이름으로 부르는** 줄을 센다 — 표기 집합은
`make dev` · `make env` · `make seed-compose` · `docker compose` 넷이고, **경로로 먼저 좁히지 않으며
자기 커밋도 센다**(§53). 값(각 N=1, 트리 `a03f13d`):

- `git grep -nI "make dev" a03f13d -- .` → **75줄 / 16파일**
- `git grep -cI "docker compose" a03f13d -- .` → **13파일**(`.env.example` 2 · `CLAUDE.md` 1 ·
  `Makefile` 4 · `README.md` 2 · `apps/web/vite.proxy-target.ts` 1 · `docker-compose.yml` 1 ·
  ADR 0018 2 · ADR 0019 2 · 계획 0014 14 · 계획 0015 27 · `services/api/seed.py` 1 ·
  `tests/integration/test_00_seed_boundary.py` 1 · `tests/invariants/test_demo_stack_can_stand.py` 14)

**② 이 기준이 놓치는 것**(§6-1 ②).
- **ⓐ 경로 위에 있으면서 경로의 이름을 부르지 않는 자리.** 사용자가 **실제로 보는 문자열**을 만드는
  코드가 여기 있다.
- **ⓑ 화면.** 사용자가 보는 것은 소스의 문자열이 아니라 **렌더 결과**이고, 렌더는 이 grep 밖이다.
- **ⓒ 보간**(f-문자열·템플릿 리터럴)과 **ⓓ 줄바꿈** — §6-1 역방향 확인의 ③④ 그대로.
- **ⓔ 저장소 밖의 소리**: docker·vite·pip 가 내는 메시지는 이 저장소가 쓰지 않았지만 **사용자가
  가장 먼저 보는 문자열**이다.

**③ 블라인드 스팟을 한 건 태웠다 — ⓐ 를 태우니 이 사이클의 표적이 나왔다.**
`apps/web/src/api/client.ts` 에는 위 네 표기가 **하나도 없다**
(`git grep -c "make dev\|docker compose\|make seed" a03f13d -- buildtwin/apps/web/src/api/client.ts`
→ **rc 1 = 히트 0**, N=1 — `git grep` 기본 **BRE** 라 `\|` 가 교대다). 그런데 그 파일의
**`:164`** 가 `` return `HTTP ${status}`; `` 이고, **api 가 없을 때 사용자가 화면에서 보는 문자열이
그것**이다(§1-c ㉡). **이름으로 만든 목록은 이 자리를 영원히 담지 못한다.**

### 1-c. 문별 실측 (**각 칸은 실행값이거나 코드 인용이고, 다른 절 참조가 아니다**)

| 문 | 닫혀 있을 때 무엇이 일어나는가 | rc | 사용자가 보는 것 | 붙드는 게이트 | 판정 |
|---|---|---|---|---|---|
| **1** `.env` 없음 | `docker compose config` 가 **파일 해석 단계**에서 죽는다(N=1) | **1** | stderr **1줄**: `env file /home/user/Bim/buildtwin/.env not found: stat …: no such file or directory` | `tests/invariants/test_demo_stack_can_stand.py::test_compose_config_fails_without_env_and_stands_after_make_env` | **소리낸다.** 그리고 `Makefile` 의 `dev:` 첫 `@echo` 가 *"먼저 'make env'"* 를 이름으로 말한다 |
| **2** 데몬 없음 | `docker compose up --build` 가 **첫 이미지에서** 죽는다(scratchpad 사본, `.env` 를 그 사본에 두고 잰 값, N=1) | **1** | stderr **1줄**: `unable to get image 'minio/minio:latest': failed to connect to the docker API at unix:///var/run/docker.sock; … daemon is running` | **없다** | **소리는 docker 가 낸다. 저장소의 안내는 0** — `git grep -nIi "daemon\|docker\.sock" a03f13d -- buildtwin/Makefile buildtwin/README.md buildtwin/.env.example buildtwin/docker-compose.yml` → **rc 1 = 히트 0**(N=1) |
| **3** 빌드 | **여기가 조용하다 ㉠.** `Dockerfile:5` 가 `RUN pip install --no-cache-dir -e . psycopg[binary] `**`||`**` pip install --no-cache-dir fastapi … "psycopg[binary]"` 다(코드 인용) — **앞 절이 실패해도 `\|\|` 가 그것을 삼키고 뒤 절이 이미지를 완성한다** | **0**(빌드는 성공한다) | **아무것도**. 빌드 로그에 `||` 가 탄 흔적은 남지만 rc 는 0 이고 스택은 뜬다 | **없다** | **조용히 죽는다.** 두 목록이 **같지 않다**: `pyproject.toml:5-12` 의 의존성 **23** ↔ 폴백 목록 **22**, **`pyproject` 에만 있는 것 = `openpyxl` 하나, 폴백에만 있는 것 = 없음**(방법: `git show a03f13d:buildtwin/pyproject.toml` 과 `…:buildtwin/Dockerfile` 을 파이썬으로 읽어 `||` 뒤 절의 인자에서 `pip`·`install`·`--no-cache-dir` 를 뺀 집합과 버전 한정어를 벗긴 이름으로 대조, N=1). 폴백은 **`-e .` 도 버린다** — 프로젝트 자신을 설치하지 않는다. `openpyxl` 은 **운영 경로**가 모듈 최상단에서 import 한다(`services/progress/importers/document_register.py:32` `import openpyxl`) — 그 이미지는 **뜨고**, 대장 업로드에서 `ImportError` 로 죽는다 |
| **4** 준비 순서 | api 는 기동에서 **소리내어 죽는다**: `DATABASE_URL=postgresql+psycopg://…@db:5432/…` 로 `services.api.main.create_app()` 의 lifespan 을 인-프로세스로 열면 `psycopg.OperationalError: failed to resolve host 'db'`(포트 0개, N=1) | — | 컨테이너 로그 | **없다** | **소리내어 죽는다. 그러나 그 뒤가 조용하다** — `git grep -c "healthcheck" a03f13d -- buildtwin/docker-compose.yml` → **rc 1 = 히트 0**, `restart:` 도 **히트 0**, `depends_on` 은 `:45`·`:56`·`:75` **셋 다 짧은 형식**(각 N=1) → **api 가 죽어도 web 은 뜬다** |
| **6** 프록시·화면 | **여기가 조용하다 ㉡.** `apps/web/vite.config.ts:19-22` 의 proxy 는 `target` 과 `changeOrigin` **두 키뿐**이고 **에러 핸들러가 없다**(코드 인용). 대상이 죽어 있으면 vite 기본 동작이 **500 · 본문 빈 채**다 | — | 그 응답이 화면까지 가면: `apps/web/src/api/client.ts` 의 `parseBody` 가 빈 본문을 **`null`** 로 만들고, `errorMessage(500, null)` 이 마지막 줄 **`:164`** 의 `` `HTTP ${status}` `` 를 돌려준다(코드 인용) → 사용자가 보는 문자열은 **`HTTP 500`** | `apps/web/vite.proxy-target.test.ts`(6 tests) 는 **프록시 대상의 이름·값**을 붙들지 프록시가 **실패할 때**를 붙들지 않는다 | **문의 이름도 원인도 다음 명령도 없다** |
| **7** 계정 없음 | 로그인 401. `services/api/main.py` 의 `init_database` docstring 이 그 상태를 이름으로 적는다(*"빈 DB 로 이 앱을 띄우면 계정이 하나도 없고, 아무도 로그인할 수 없다"*) | — | **재지 않았다**(§확인하지 않은 것 3) | — | 소스 주석은 **화면이 아니다** |

**500 · 본문 빈 채 는 이 계획이 잰 값이 아니다.** 그 값은 계획 0015 §확인하지 않은 것 63 이 인계로
받은 것이고 **저장소에 재현 커밋이 없다**(못박은 확인: `git log --oneline -S "ECONNREFUSED" a03f13d`
→ **`5bc8d5c` 하나** = 그 값을 적은 커밋 자신, N=1). **그러므로 이 계획은 그것을 근거로 삼지 않는다** —
㉡ 이 서는 근거는 **코드 두 줄**(에러 핸들러가 없다 · 폴백이 `HTTP ${status}` 다)이고, **본문이 비어
있을 때 무엇이 보이는가**는 그 두 줄만으로 결정된다.

### 1-d. 곱 표 — (문 × 그 죽음을 볼 수 있는 자리) (§6-1: 관계를 세는 목록은 **곱**으로)

축은 **관측자 넷**이다: **rc**(명령의 종료 코드) · **stderr/로그** · **게이트**(저장소가 붙드는 자리) ·
**화면**(사용자가 브라우저에서 보는 것). ✔ = 그 자리에서 보인다, ✘ = 안 보인다, — = 해당 없음.

| 문 | rc | stderr/로그 | 게이트 | 화면 |
|---|---|---|---|---|
| 1 `.env` | ✔ 1 | ✔ 경로를 이름으로 | ✔ invariants 1건 | — (스택이 안 뜬다) |
| 2 데몬 | ✔ 1 | ✔ 소켓을 이름으로(docker 가 낸다) | ✘ | — |
| **3 빌드** | **✘ 0** | **✘**(성공으로 끝난다) | **✘** | **✘**(런타임에 `ImportError` 로 뒤늦게) |
| 4 준비 순서 | — (컨테이너 안) | ✔ api 로그 | ✘ | ✘ (web 은 뜬다) |
| **6 프록시** | — | ✘ (vite 는 기본 동작) | ✘ | **△ `HTTP 500`** — 있으나 **아무것도 이름하지 않는다** |
| 7 계정 | — | ✔ 401 | ✘ | 재지 않았다 |

**곱이 만든 칸이 이 계획의 범위를 정한다.** ✘ 가 **네 축 모두**인 행은 **3** 하나이고, **화면 축이
△ 인 행은 6** 하나다. **그 둘이 작업 1·2 다.** 4 는 ✘ 가 둘이지만 **로그 축에서 소리가 난다** —
그래서 이 사이클은 4 를 **닫지 않고** §후속으로 넘긴다(근거는 §1-f).

### 1-e. 한정어 역방향 확인 (§6-3 — **새로 쓰는 조건절도 예외가 아니다**)

이 계획의 조건절은 **「첫 실행 경로의 실패가 소리내어 죽는가」** 다. 한정어마다 한 줄씩 답한다.

| 한정어 | 빼면 무엇이 더 들어오나 | 있어서 무엇이 빠지나 | 이 계획의 처분 |
|---|---|---|---|
| **첫 실행** | 재실행·업그레이드 경로(`docker compose down -v` 뒤, 모델이 바뀐 뒤의 볼륨)가 들어온다 | **두 번째 실행부터의 실패** — `make dev` 의 선행 `@echo` 안내가 **정확히 그 상황**을 말하는 자리를 갖는다(`docker compose down -v` · `create_all` 이 컬럼을 추가하지 않는다 · `make db-reset`) — **개수는 세지 않는다**(§6-1 9회차: 열거는 길이가 곧 개수다) | 범위 밖으로 **명시**한다(§1-f). 첫 실행자가 사용자라는 것이 이 사이클의 근거이므로 |
| **소리내어** | 「사용자가 원인을 알 수 있는가」까지 들어온다 | **rc 만 0 이 아니면 통과** — 문 6 의 `HTTP 500` 이 그 예다(rc 축이 없다) | 관측자를 **넷으로 못박는다**(§1-d). 「소리」는 **네 축 중 최소 하나에서 보이고, 그 문자열이 문의 이름과 다음 명령을 담는 것**으로 정의한다 |
| **죽는가** | 「느려진다」·「반만 된다」가 들어온다 | **성공으로 끝나는 실패** — 문 3 이 정확히 그것이다(rc 0) | **한정어를 뺀다.** 조건절을 「실패가」로 쓴다 — **rc 0 으로 끝나는 실패가 이 사이클의 표적**이기 때문이다 |
| **경로** | 저장소 전체의 침묵이 들어온다 | 그 경로 밖의 같은 모양(`\|\|`·빈 `except`·기본값 폴백) | 범위는 경로로 두되, **전수는 그 모양으로 한 번 돌린다**(작업 5 의 §확인하지 않은 것) |

**옛 조건이 잡던 것도 묻는다**(§6-3). 계획 0015 의 조건절은 **「문이 열려 있는가」** 였고 그것이
잡던 것은 **`rc=1` 로 죽는 자리**였다. 이 계획의 조건절로 바꾸면 그 자리들은 **전부 「통과」로 넘어간다**
(문 1·2 가 위 표에서 ✔ 다) — **잃는 것은 없다**: 문 1 은 게이트가 붙들고 있고, 문 2 는 저장소가 만든
소리가 아니다. **잃지 않는다는 이 판단 자체가 §1-c 표의 「붙드는 게이트」 칸이고, 그 칸이 비면
(문 2) 작업 3 이 그것을 채운다.**

### 1-f. 이 사이클의 범위

**닫는다**: ㉠ 문 3(빌드가 실패를 삼킨다) · ㉡ 문 6(사용자가 보는 문자열이 아무것도 이름하지 않는다) ·
그리고 문 2 의 **저장소 안내 0**.
**닫지 않는다**: 문 4(준비 순서 — healthcheck·`depends_on: condition`) · 문 7(로그인 화면의 문구) ·
재실행 경로. 근거 셋: ⓐ 문 4 는 **로그 축에서 이미 소리가 난다**(§1-d) — 조용한 두 자리보다 뒤다
ⓑ healthcheck 를 두는 것은 **관측 가능성이 아니라 기동 순서**를 바꾸는 일이라 축이 다르다(§6-3 이
경고하는 「축을 넓히면 무주공산이 자리를 옮긴다」) ⓒ 이 환경에서는 `docker compose config` 축까지만
잴 수 있어(§0) 그 변경의 **실행값**을 이 사이클이 낼 수 없다. **셋 다 §후속으로 번호를 열어 넘긴다.**

---

## 2. 판정 — 큐 여섯 · CLAUDE.md 대기열 · 경쟁 후보 중 **무엇을 이 사이클로 삼는가**

**결론: 경쟁 후보(사용자의 첫 실행 경로)를 삼는다.** 큐 여섯도, CLAUDE.md 대기열도 삼지 않는다.
**그리고 여섯을 「재측정하지 않았다」로 넘기지 않는다** — 아래가 `a03f13d` 에서 다시 잰 값이다
(§6-1 12회차에 대한 이 계획의 답: 목록을 옮기는 것과 상태를 아는 것은 다른 일이다).

| # | 문안(계획 0015 §후속에서 **인용 · 발췌**) | **`a03f13d` 에서 다시 잰 값**(각 N=1) | 삼지 않는 근거 |
|---|---|---|---|
| **2** | *"`"rejected"` 값 리터럴의 전수 감사"*(qa) | `git grep -nI '"rejected"' a03f13d -- .` → **186줄**, 그중 `/docs/` 밖 **117줄**. 계획 0015 가 `4d06867` 에서 잰 176 / 109 에서 **늘었다**(두 사이클이 문서를 더 썼다 + 코드가 늘었다) | 이 경로 밖. **117줄을 읽는 일**이고, 이 사이클의 qa 예산은 작업 3·4 가 쓴다 |
| **3** | *"`apps/web/src/api/client.ts:12` 의 TODO"*(qa) | `git grep -n "TODO(round-6" a03f13d -- buildtwin/apps/web/src/api/client.ts` → **`:12`** 그대로 | **자리가 겹친다** — 작업 2 가 같은 파일의 `:156-164` 를 만진다. 그러나 3 이 묻는 것은 **코드 카탈로그의 단일 소스**이고 작업 2 는 **본문이 빈 응답의 문구**다. 겹치는 것은 파일이지 축이 아니다 → **작업 2 의 완료 조건에 「3 을 닫지 않는다」를 명시**한다 |
| **6** | *"`docs/api.md` 오류 절의 예외 열거와 `internal_error`"*(api) | `git grep -c "MappingDecisionNotCancellableError\|MappingDecisionCancelReasonRequiredError" a03f13d -- buildtwin/services/api/scripts/gen_api_doc.py` → **rc 1 = 히트 0**(BRE 라 `\|` 가 교대다) | **§후속 64 ③ 과 같은 파일의 일**(생성기). 그 사이클은 **생성기 사이클**이어야 하고 이 사이클은 아니다 |
| **11** | *"확정 축의 `expert_review_logs` 도 무보호"*(qa) | **변이를 심지 않았다.** 다만 계획 0015 가 적은 기준선 **838** 은 낡았다 — `a03f13d` 의 파이썬 넷 합은 **844**(492+112+8+232, §0) | **로그** 경로이고 판정·인증 경로가 아니다. 그리고 데모가 서지 않으면 그 로그를 만들 사용자가 없다 |
| **23** | *"`postgres.measured.json` 의 `server_version`"*(qa + architect). 재개 조건: **단언·비교가 하나라도 생기는 순간** | `git grep -nI "server_version" a03f13d -- .` 의 `/docs/` 밖 **15줄**(계획 0015 의 14 에서 하나 늘었다). **잰 값에 대한 단언·비교는 여전히 0건** | **재개 조건이 성립하지 않는다.** 이월 |
| **24** | *"PostGIS 가 로드된 DB 에서 무엇이 달라지는가"*(qa) | **자리가 또 바뀐다.** 계획 0015 §M-13-5 가 값으로 보였다 — 이 환경에서 **데몬은 뜨고**(Server 29.3.1) **이미지 pull 은 된다**(rc 0). 막는 것은 **컨테이너 네트워킹**(`ip_forward=0`)이다 | 이월. **그러나 「못 잰다」의 이유가 바뀌었다** — `--network host` 로 db 하나만 띄우는 갈래는 아무도 재지 않았다(계획 0015 §확인하지 않은 것 69). **24 의 닫힘은 이제 43 이 아니라 69 와 한 자리다** |

**CLAUDE.md 대기열도 삼지 않는다.** §6-5 압축 문턱은 넘겼다(근거 표면 **1,090자 = 7.20%** — 계획 0015
§A-1 이 잰 값, **architect 의 N=1**, 이 계획은 규칙/근거의 분할을 다시 하지 않았다). **그러나
「문턱을 넘겼다」는 「압축하라」가 아니다**(§후속 63 의 *닫힘의 모양*). 그리고 그 커밋이 져야 할 항목은
**여덟**로 늘었다(25 + 27·34·38 + 46 + 63 + **65** + 69·71 — 계획 0015 §M-13-4 가 65 를 더했다).
**여덟 항목을 한 커밋에 담는 일은 그 자체로 한 사이클**이고, 이 사이클의 architect 예산은 작업 5·6 이 쓴다.

**이 경로를 이 사이클로 삼는 근거 넷 — 전부 값이다.**

1. **사용자가 첫 실행자다.** `docker compose up --build` 를 끝까지 돌린 사람이 아무도 없다
   (계획 0015 §확인하지 않은 것 43·60·62, 세 사이클 연속 열림).
2. **이 프로젝트의 지배적 실패 양식이 「조용히 죽는 것」이고**(CLAUDE.md §6 머리말), **그 경로에
   그 모양이 실재한다** — 문 3 의 `||` 는 **rc 0 으로 끝나는 실패**이고 그 결과가
   **`openpyxl` 이 빠진 이미지**다(§1-c, 두 목록 대조 N=1). 이것은 「있을 수 있다」가 아니라
   **오늘 코드에 적혀 있는 갈래**다.
3. **여기서 잴 수 없는 것을 재려 들지 않는다 — 그리고 잴 수 있는 것이 생각보다 많다.** 문 3 과 문 6 은
   **데몬 없이 닫히고 게이트도 데몬 없이 선다**: 문 3 은 `Dockerfile` 과 `pyproject.toml` 을 **읽는**
   불변식, 문 6 은 **vitest**(프록시 설정과 `errorMessage` 는 둘 다 순수 함수 경계다). **이 사이클의
   어떤 완료 조건도 데몬을 요구하지 않는다.**
4. **큐 여섯은 전부 「이미 서 있는 것의 품질」이고 이 경로 위에 없다**(위 표 셋째 열) — 계획 0015 §2 의
   판정과 같은 값이고, 이 계획이 다시 쟀다.

*역방향 확인 — 이 판정이 놓치는 것(§6-1 ②)*.
- **ⓐ 큐를 두 사이클 연속 민다.** 2·3·6·11 은 계획 0006~0008 에서 열려 **아홉 사이클째**다. 계획 0015
  §2 의 역방향 확인 ⓐ 가 *"대기열을 한 번 밀면 다음에도 밀 수 있다"* 라고 적었고 **이 계획이 그 문장의
  첫 실례다.** 이 계획이 할 수 있는 것은 위 표처럼 **상태를 갱신해 넘기는 것**뿐이고, 그 사실을 여기 적는다.
- **ⓑ 「조용한 두 자리」라는 목록의 기준은 §1-d 의 곱이다** — 관측자 축을 **넷**으로 골랐고, 그 넷은
  내가 골랐다. **다섯째 축**(예: CI · 다른 개발자의 재현)에서만 조용한 자리는 이 곱에 칸이 없다.
- **ⓒ 문 3 의 손해를 실행으로 재지 못했다.** `||` 의 앞 절이 실제로 실패하는지는 **빌드를 돌려야**
  알고 이 환경은 그것을 못 한다. 이 계획이 아는 것은 ⓘ `||` 가 있다 ⓙ 두 목록이 `openpyxl` 하나만큼
  다르다 ⓚ 그 이름을 **운영 코드가 최상단에서 import 한다** 셋이고, **「앞 절이 자주 실패한다」는
  주장이 아니다.** 처방이 서는 근거는 **빈도가 아니라 침묵**이다.
- **ⓓ 이 판정은 사용자 결정을 근거로 삼는다**(근거 1). 그 결정이 바뀌면 근거 1 이 사라지고 2·3 만 남는다 —
  **그 둘로도 선다고 나는 판단하지만, 그 판단은 여기 적어야 검증된다.**

---

## 3. 이월 규약 판정 — **자구 그대로 옮기지 않고 「상태 표」로 옮긴다**

**근거는 값이다**(계획 0015 §M-13-4, 트리 `a03f13d`, N=1): 계획 0015 는 §6-1 12회차의 처방을 지켰다 —
*"본문 목록을 번호로 옮긴다, 새로 쓰지 않는다 … 상태를 바꾸는 항목은 목록 뒤에 따로 적는다"*.
**그런데 그 문서의 §후속 본문 목록은 18·67·72 를 「열림」으로 적고, 같은 문서의 §M-8 은 셋을 「닫혔다」로
판정한다.** 규율을 지켰는데도 **한 문서 안에서 세 항목이 갈렸다.** 자구 보존은 **두 자리를 겹쳐 읽는
독자**에게만 참이고, 목록만 읽으면 세 항목이 거짓이다.

**그러므로 이 계획의 §후속은 이렇게 옮긴다.**
- **정본은 못박은 트리의 계획 0015 다**: `git show a03f13d:buildtwin/docs/plans/0015-the-demo-path-opens-the-front-door.md`
  의 §후속 + §M-8 + §M-12 + §M-13-4. **이 계획은 그 산문을 다시 쓰지 않는다.**
- **이 계획이 싣는 것은 「번호 · 한 줄 문안 · 소유 · 상태」 표**이고, **상태 칸은 세 자리를 겹쳐 읽은
  결과 하나**다(그래서 위의 갈림이 이 계획에서는 구조적으로 생기지 않는다).
- **이 계획이 지는 항목**(작업의 완료 조건에 이름으로 들어가는 것)만 **문안을 그 자리에 적는다.**

*역방향 확인 — 이 규약이 놓치는 것.* ⓐ **한 줄 문안은 발췌다** — 원래 항목의 *한정*·*닫힘의 조건*을
싣지 못한다. 그래서 항목을 **닫으려면** 정본을 열어 그 조건을 읽어야 하고, **이 표만 보고 닫지 않는다.**
ⓑ **상태 칸은 내가 겹쳐 읽은 판정이다** — 세 자리가 갈리면 내 판정이 하나의 답을 내지만, 그 판정이
틀릴 수 있고 그때는 **표가 갈림을 감춘다**(자구 보존은 최소한 갈림을 **보이게** 두었다). 그래서
**갈렸던 세 항목(18·67·72)은 상태 칸에 「갈렸다 → 닫힘으로 판정」이라고 적는다.**
ⓒ 표본 **1**(계획 0015 하나). 이 규약이 다음 사이클에서도 나은지는 재지 않았다.

---

## 작업 분배

**소유는 CLAUDE.md §2 가 정본이다.** 아래 「소유」 열이 그것이고, 「배정」은 이 사이클의 것이지
소유가 아니다(§2: *"계획의 작업 분배 행은 그 사이클의 배정이지 소유가 아니다"*, **인용 · 발췌**).
**소유 없는 파일 둘**(`Dockerfile` · `pyproject.toml`)은 §후속 19 가 지고, 이 계획은 **배정만** 한다.
확인(각 N=1): `git grep -n "Dockerfile" a03f13d -- buildtwin/CLAUDE.md` → **rc 1 = 히트 0**;
`git grep -n "pyproject" a03f13d -- buildtwin/CLAUDE.md` → **rc 1 = 히트 0**.

| # | 작업 | 소유 | 만지는 트리 | 의존 |
|---|---|---|---|---|
| 1 | **㉠ 빌드가 실패를 삼키지 않는다** — `Dockerfile:5` 의 `\|\|` 폴백을 없앤다 | **배정 `architect`**(소유 없음, §후속 19) | `Dockerfile` | 5 |
| 2 | **㉡ 프록시가 죽을 때 사용자가 무엇을 보는가** — vite `/api` 프록시의 실패 응답과 빈 본문 폴백 문구 | `frontend` | `apps/web/vite.config.ts` · `apps/web/src/api/client.ts` | 5 |
| 3 | **문 2 를 저장소가 이름으로 말한다** — `make dev` 의 선행 안내에 데몬 한 줄 | `qa` | `Makefile` | 5 |
| 4 | **1·2·3 을 붙드는 게이트** — `Dockerfile`↔`pyproject.toml` 대조 불변식 + 프록시·문구의 vitest | `qa` | `tests/invariants/` · `apps/web/src/**/*.test.ts(x)`(§2 의 **공동 편집** 자리; 초판은 `apps/web/**/*.test.ts` 라 적었다 — **마감 §M-5 가 고쳤다**) | 1·2·3 |
| 5 | **ADR 0020** — 첫 실행 경로의 실패 계약 | `architect` | `docs/adr/` | — |
| 6 | **마감** | `architect` | `docs/` | 전부 |

**의존 1·2·3 ← 5 는 순서 규약이 아니라 의존이다.** 셋이 만드는 문자열이 **무엇을 담아야 하는가**를
ADR 0020 이 정하고, 세 소유가 각자 정하면 §6-3 11회차(같은 값이 여러 문서에 다르게 적힌다)가 그대로 난다.

### 완료 조건

**형식(§후속 55).** 각 인용에 **ⓐ**(그 자리에서 도는 참조)와 **ⓑ**(인용/재서술 · 전체/발췌)를 붙인다.
**그리고 각 상자는 「이 작업이 거짓으로 만드는 *남의* 자리」 행을 이름으로 갖는다**
(계획 0015 §후속 **72·74·76** 의 공통 처방 — **이 계획이 그 처방의 첫 이행이다**).

**작업 1 (배정 architect · `Dockerfile`)**
1. `Dockerfile` 의 `RUN pip install …` 이 **`||` 를 갖지 않는다.** 확인: `grep -c '||' Dockerfile` → **0**.
2. 의존성 목록을 **손으로 두 번 적지 않는다** — 설치는 `pyproject.toml` 하나를 정본으로 삼는다
   (§3-2·§6-1 9회차: 열거는 길이가 곧 개수다).
3. **거짓으로 만드는 남의 자리**: **없다고 판단했다.** 근거(N=1):
   `git grep -nI "Dockerfile" a03f13d -- . ':!buildtwin/docs/plans' ':!buildtwin/docs/adr'` 의 히트를
   전부 읽고, `||` 나 폴백 목록을 **단정하는** 줄이 있으면 이 상자에 이름으로 옮긴다. **히트를 읽고
   0 이면 「0 을 읽었다」를 커밋 본문에 적는다** — 안 적으면 안 읽은 것과 구별되지 않는다.

**작업 2 (frontend · `apps/web/`)**
1. vite `/api` 프록시가 **대상에 닿지 못할 때** 응답이 **본문을 갖는다.** 그 본문이 담는 셋:
   ⓘ **프록시 대상 주소**(`resolveApiProxyTarget()` 의 값) ⓙ **원인**(하위 오류 코드/메시지)
   ⓚ **다음에 칠 명령**. 문구는 ADR 0020 의 것을 쓴다(**ⓑ 인용 · 전체**).
2. `apps/web/src/api/client.ts:164` 의 `` `HTTP ${status}` `` 폴백이 **본문이 비어 있다는 사실**을
   말한다 — 그 문자열은 **그 상황에서 참일 수 없는 말을 담지 않는다**(§6-4 3).
3. **`vite.proxy-target.ts` 의 이름 계약을 건드리지 않는다** — 그 파일의 `API_PROXY_TARGET_ENV` 는
   compose 와의 계약이고 invariants 가 붙든다(**ⓐ** `grep -n "API_PROXY_TARGET_ENV = " apps/web/vite.proxy-target.ts`).
4. **§후속 3 을 닫지 않는다.** 같은 파일을 만지지만 축이 다르다(§2 표).
5. **거짓으로 만드는 남의 자리**: ⓧ `tests/e2e/conftest.py` 의 preview 프록시 설정이 같은 축을
   가정하는지(소유 `qa`) ⓨ `apps/web/vite.proxy-target.ts` 머리 주석의 표(소유 `frontend` — 자기 소유)
   ⓩ `docker-compose.yml` 의 `web:` 머리 주석(소유 **없음**, §후속 19). **셋을 각각 읽고 값으로
   판정해 커밋 본문에 적는다** — 거짓이 되면 ⓧ 는 **같은 사이클 안에서 qa 가 고쳐야 완료**다(§후속 76).

**작업 3 (qa · `Makefile`)**
1. `make dev` 의 선행 `@echo` 가 **데몬을 이름으로** 말한다: 무엇이 없으면 무엇이 나오는지 +
   그 stderr 의 실제 문구 조각. 확인: `make dev` 를 데몬 없이 돌려 **rc 1 + 그 안내가 stdout 에** 있음.
2. **`docker compose` 를 실제로 부르는 레시피를 바꾸지 않는다**(문 1 의 게이트가 그 rc 를 붙든다).
3. **거짓으로 만드는 남의 자리**: `README.md`(소유 **없음**) 의 개발 명령 절이 데몬을 말하지 않는
   채로 남는지 — 읽고 값으로 판정해 커밋 본문에 적는다.

**작업 4 (qa · `tests/`, `apps/web/src/**/*.test.ts(x)` 공동 — 초판은 `apps/web/**/*.test.ts` 라 적었다, 마감 §M-5)**
1. **`Dockerfile` ↔ `pyproject.toml` 대조 불변식**: `Dockerfile` 이 의존성 이름을 **열거하지 않는다**를
   붙든다(작업 1 의 2). **개수를 세지 않는다**(§6-1 9회차) — 세는 대신 **`||` 의 부재**와
   **`pyproject.toml` 밖에 의존성 열거가 없음**을 단언한다.
2. **양성 케이스가 결함 코드에서 값이 갈리는지 태운다**(§6-2 1): `Dockerfile` 에 `||` 를 되살리는
   변이에서 그 불변식이 **빨개진다**를 값으로 보인다(변이는 한 자리씩 + `git diff`; **같은 커밋이 넣은
   줄을 지우는 변이는 `git diff` 가 침묵하므로 워크트리 사본 `diff` 로 확인한다** — §6-2 규칙 5).
3. **프록시·문구 축**: 작업 2 의 셋(대상 주소 · 원인 · 다음 명령)을 vitest 로 붙들되 **문장을 통째로
   베끼지 않는다** — *"그 상황에서 참일 수 없는 말이 없다"* 를 단언한다(§6-4 3).
4. **테스트를 skip·비활성·격리하지 않는다.** 데몬을 요구하는 단언을 두지 않는다(§0).
5. **거짓으로 만드는 남의 자리**: 새 불변식이 `tests/invariants/test_demo_stack_can_stand.py` 머리
   주석의 단정(그 파일이 무엇을 붙드는지)을 낡게 만드는지 — 같은 소유이므로 같은 커밋에서 고친다.

**작업 5 (architect · `docs/adr/0020-*.md`)**
1. 계약을 **세 항**으로 못박는다: 첫 실행 경로의 각 문은 ⓘ **rc 0 으로 끝나지 않거나**, 끝난다면
   ⓙ 그 실패가 **네 관측자 중 최소 하나**(§1-d)에서 보이고 ⓚ 그 문자열이 **문의 이름**과
   **다음에 칠 명령**을 담는다. **실패를 삼키는 폴백**(`||` · 빈 `except` · 조용한 기본값)을 두지 않는다.
2. §8 표(**「이 ADR 이 거짓으로 만드는 문구」**)의 **각 행이 위 완료 조건 상자에 이름으로 있다**
   — 계획 0015 §후속 **74** 가 정확히 그 자리에서 났다. **마감(작업 6)이 그 대응을 표로 태운다.**
3. **Deferred 로 미루는 문구 정정을 두지 않는다**(§6-4 1).

**작업 6 (architect · `docs/`)**
1. 게이트 재측정(아래 §게이트), **방법 · N · 표본 간격**과 함께.
2. **교차 확인 표** — 이 사이클이 **두 번 이상 적은 값** + **ADR §8 ↔ 완료 조건 상자의 배정 대조**
   (§후속 74 의 조항 후보 둘째). **갈리지 않은 칸도 적는다.**
3. §후속 · §확인하지 않은 것 갱신은 **§3 의 규약대로 상태 표**로.
4. **CLAUDE.md 를 만지지 않는다** — 승격은 §6-5 대기열의 순서 안에서 한다(§2).

---

## 게이트

- **커밋 전 `make test` 통과**(§3-1). 문서만 고쳤어도 **직접 돌리고 전후 `git status` 를 본다.**
- `make lint` **rc 0**, `make e2e` **12 passed**(고아 `vite preview` 전 0 → 후 0 · `.e2e-preview.*` 잔여 0).
- **바닥값(`tests/metrics.json` · `postgres.floor.json`)을 이 사이클이 올리지 않는다.**
- **통합 232 · vitest 289 가 유지되는지를 수가 아니라 「어느 것이 바뀌는지」로 커밋 본문에 적는다.**
- **포트를 잡았으면 반납한다.** `ss` 없음 · `/proc/net/tcp6` 없음. **저장소 루트에 `.env` 를 만들지 않는다.**
- **어떤 완료 조건도 docker 데몬을 요구하지 않는다**(§2 근거 3).
- **소유 경계가 원자적 변경을 가르면**(§후속 73) 커밋 경계는 소유로, 원자성은 PR 로, **빨간 이유는
  어느 테스트가 왜 빨간지를 이름과 값으로** 커밋 본문에.

## 리스크

1. **작업 1 이 빌드를 실제로 깨뜨릴 수 있다.** `||` 의 앞 절이 어떤 환경에서 실패하는지 이 사이클은
   재지 못한다(§2 역방향 ⓒ). **그것이 이 작업의 목적이다** — 깨지면 **소리내어** 깨지고, 그때 고칠
   자리는 `pyproject.toml` 이지 폴백 목록이 아니다. 다만 **사용자가 첫 실행자**이므로 이 위험은
   사용자에게 간다 → 작업 5 의 ADR 이 그 판단을 근거와 함께 적고, 작업 3 의 안내가 그 소리를 받는다.
2. **작업 2 가 e2e 의 preview 프록시 축을 건드릴 수 있다**(계획 0015 §M-12-5 ③: 그 픽스처는 환경을
   일부러 덮는다). `make e2e` **12 passed** 가 그 게이트다.
3. **§후속 3 과 자리가 겹친다**(§2 표) — 작업 2 의 완료 조건 4 가 그것을 막는다.
4. **이 계획이 큐를 두 사이클 연속 민다**(§2 역방향 ⓐ). 다음 계획이 또 밀면 **그 자체를 §후속 항목으로
   연다** — 미루는 것과 안 하기로 하는 것은 다르다.

## ADR 필요 여부 — **연다. `docs/adr/0020-a-first-run-failure-must-name-the-door.md`**

근거: 세 소유(architect 배정 · frontend · qa)가 **같은 문자열 계약**을 각자 만든다. 계약이 한 자리에
없으면 §6-3 11회차(같은 값이 여러 문서에 다르게 적힌다)가 그대로 난다. **그리고 「실패를 삼키는 폴백을
두지 않는다」는 CLAUDE.md §3 의 어느 항목도 오늘 말하지 않는다** — 확인(N=1):
`git grep -n "폴백\|삼키" a03f13d -- buildtwin/CLAUDE.md` 의 히트를 읽고 이 축을 말하는 줄이 있으면
ADR 대신 그 줄을 가리킨다. **승격(CLAUDE.md 편집)은 이 사이클이 하지 않는다** — §6-5 대기열의 순서 안이다.

## 후속 — 이월 (§3 의 규약: **상태 표**로 옮긴다)

**정본**: `git show a03f13d:buildtwin/docs/plans/0015-the-demo-path-opens-the-front-door.md` 의
§후속 + §M-8 + §M-12 + §M-13-4. **계수**(그 문서 §M-13-4, **인용**): **76 항목 = 닫힘 29 · 열림 47.**

**이 계획이 지는 항목**(완료 조건 상자에 이름으로 들어간 것) — 문안을 여기 적는다:
- **19** *"소유가 §2 에 없는 파일들"*(architect). **이 계획이 실례를 둘 더한다**: `Dockerfile` ·
  `pyproject.toml`(둘 다 §2 히트 0, 위 §작업 분배). 이 계획은 **배정만** 한다.
- **72 · 74 · 76** — 세 항목의 처방이 하나다(계획 0015 §M-12-2 ④, **인용 · 발췌**):
  *"계획의 완료 조건 상자에 **이 작업이 남기는/거짓으로 만드는 남의 자리** 행을 이름으로 넣는다"*.
  **이 계획의 완료 조건 상자 넷이 그 행을 갖는다 — 그 처방의 첫 이행이다.** 셋은 **닫히지 않는다**
  (72 는 이미 닫혔고, 74·76 은 **마감이 그 대응을 표로 태울 때** 닫힌다).
- **65 · 46** — 이 계획의 §0 과 측정 방법이 그 두 항목의 조항을 진다(확장 항 ㉡ · 처방 ㉢).
- **63 · 50 · 38 · 25 · 27 · 34 · 69 · 71** — **§6-5 커밋**의 여덟. 이 사이클은 만지지 않는다(§2).
- **3** — 작업 2 와 파일이 겹치고 축이 다르다. **닫지 않는다**(§2 표).

**신규(이 계획이 연다):**

77. **compose 에 준비 상태 계약이 없다 — `healthcheck` 0 · `restart:` 0 · `depends_on` 셋 다 짧은 형식**
    (배정은 §후속 19 가 정한 뒤 · 문안 architect). 실측(트리 `a03f13d`, 각 N=1): 위 셋과,
    api 는 기동에서 **소리내어 죽는다**(`psycopg.OperationalError`, 인-프로세스). **그래서 api 가
    죽어도 web 은 뜬다.** *묻는 것*: db 에 `healthcheck`(`pg_isready`)를, api·worker 에
    `condition: service_healthy` 를 둘 것인가. `python:3.11-slim` 에는 `curl`·`wget` 이 없으므로
    api 의 healthcheck 는 `python -c "urllib.request.urlopen(...)"` 모양이 된다(**이 계획은 그것을
    돌려 보지 않았다**). *닫힘의 조건*: 그 계약이 `docker-compose.yml` 에 있고 `docker compose config`
    축의 불변식이 그것을 붙들 때. *이 계획이 닫지 않는 근거*: §1-f 의 셋.
78. **로그인 화면이 「계정이 없다」를 말하는가**(frontend + api). `services/api/main.py` 의
    `init_database` docstring 이 그 상태를 이름으로 적지만 **그것은 소스 주석이고 화면이 아니다**.
    사용자가 보는 401 문구가 `make seed-compose` 를 이름으로 말하는지 **재지 않았다**.
    *닫힘의 조건*: 그 화면 문구가 ADR 0020 의 세 항(문의 이름 · 다음 명령)을 만족할 때.
79. **「실패를 삼키는 모양」의 전수를 이 경로 밖으로 돌리지 않았다**(architect + qa). §1-e 가 적은
    한정어 「경로」의 값이다 — `||` · 빈 `except` · 조용한 기본값 폴백을 **저장소 루트에서** 세는 일.
    *한정*: 표기 집합이 셋뿐이고 **보간·줄바꿈**은 §6-1 역방향 확인 ③④ 그대로 밖이다.
    *닫힘의 조건*: 그 전수가 기준·놓치는 것·한 건 실측과 함께 어느 계획에 적힐 때.
80. **이월을 「상태 표」로 바꾼 규약의 표본이 1 이다**(architect). §3 의 판정이다. *닫힘의 조건*:
    두 사이클이 그 형식으로 이월하고, **그 표가 갈림을 감췄는지**를 마감이 값으로 볼 때.

## 이 계획이 확인하지 않은 것

**정본**: 계획 0015 의 §확인하지 않은 것 + §M-9 + §M-12-9 + §M-13-5(신규 69·70·71). **번호를 잇는다.**

72. **[재지 않았다] `||` 의 앞 절이 실제로 실패하는가.** 빌드를 돌려야 알고 이 환경은 못 한다(§0).
    이 계획이 아는 것은 셋뿐이다(§2 역방향 ⓒ). **[재야 한다 → 사용자 또는 CI · 데몬과 컨테이너
    네트워킹이 있는 자리]**
73. **[재지 않았다] vite 가 프록시 실패에서 실제로 무엇을 돌려주는가.** 「500 · 본문 빈 채」는
    계획 0015 §확인하지 않은 것 63 이 받은 인계이고 **저장소에 재현 커밋이 없다**
    (`git log --oneline -S "ECONNREFUSED" a03f13d` → `5bc8d5c` 하나, N=1). 이 계획의 ㉡ 은 그 값이
    아니라 **코드 두 줄**에 기댄다(§1-c). **[재야 한다 → frontend + qa · 작업 2 를 만들 때, 그리고
    그때 값을 저장소에 남긴다]**
74. **[재지 않았다] 문 7 의 화면.** 로그인 401 이 사용자에게 무엇으로 보이는지 렌더를 확인하지 않았다
    (§후속 78). **[재야 한다 → frontend]**
75. **[재지 않았다] 재실행 경로.** `docker compose down -v` 뒤·모델이 바뀐 뒤의 실패는 §1-e 대로
    범위 밖이다. `make dev` 의 선행 `@echo` 안내가 그 상황을 말하는데 **그것이 맞는지 재지 않았다.**
    **[재야 한다 → qa · 데몬이 있는 자리에서]**
76. **[재지 않았다] `--network host` 갈래.** 계획 0015 §확인하지 않은 것 69 그대로 — 그 갈래가 서면
    §후속 **23·24** 가 처음으로 측정 가능해질 수 있다. **[재야 한다 → qa]**
77. **[재지 않았다] 관측자 축이 넷인 것의 근거.** §1-d 의 곱은 rc · stderr/로그 · 게이트 · 화면
    넷으로 만들었고 **그 넷은 내가 골랐다**(§2 역방향 ⓑ). 다섯째 축에서만 조용한 자리는 세지 않았다.
    **[재야 한다 → architect · §후속 79 를 열 때]**
78. **[재지 않았다] 근거 표면 1,090자 = 7.20%.** §2 가 쓴 그 값은 계획 0015 §A-1 의 것(**architect 의
    N=1**)이고 **이 계획은 규칙/근거의 분할을 다시 하지 않았다.** **[재야 한다 → architect · §6-5 커밋]**

## 다음에 부를 에이전트

```
@architect  작업 5 — ADR 0020 을 쓴다(문자열 계약 셋 + §8 표, 각 행이 완료 조건 상자에 있는지 대조).
@architect  작업 1 — Dockerfile 의 `||` 를 없앤다(배정, 소유 없음 — §후속 19).
@frontend   작업 2 — vite `/api` 프록시의 실패 응답과 client.ts 의 빈 본문 폴백 문구. ADR 0020 의 문구를 인용한다.
@qa         작업 3 — make dev 의 선행 안내에 데몬 한 줄.
@qa         작업 4 — 1·2·3 을 붙드는 게이트(Dockerfile↔pyproject 불변식 + 프록시·문구 vitest). 변이로 값이 갈리는 것을 보인다.
@architect  작업 6 — 마감(게이트 · 교차 확인 표 · ADR §8 ↔ 완료 조건 배정 대조 · §후속/§확인하지 않은 것 상태 표).
@reviewer   전체 리뷰 — 5체크 + 계획·ADR 이 §6-1·§6-3 을 지켰는지 + 새 사용자 문구가 §6-4 를 지켰는지.
```

---

## M. 마감 (작업 6, architect) — 게이트 재측정 · 교차 확인 표 둘 · 인계값 이관 · 상태 표

### M-0. 이 절의 트리 · 자원 · 이 절이 지키는 것

**작업 트리** `/home/user/Bim/buildtwin`, **저장소 루트 `/home/user/Bim`**, 브랜치
`claude/buildtwin-initial-setup-ubulzb`. 이 절을 열 때의 트리는 **`ddf6bb9`**(작업 1~5 가 끝난 자리,
루트 `git status --porcelain` **빈 출력**, N=1). 커밋 직전의 `git status --porcelain` 은 이 문서와
`docs/adr/0020-a-first-run-failure-must-name-the-door.md` **둘뿐**이다(§M-5 가 그 둘을 고친다).

**CLAUDE.md 를 만지지 않는다**(완료 조건 4). 값(각 N=1): `git log -1 --format='%h' ddf6bb9 --
buildtwin/CLAUDE.md` → **`15a063f`**(이 사이클 밖의 커밋) · `git log --oneline 757bc8f~1..ddf6bb9 --
buildtwin/CLAUDE.md` → **빈 출력**. 승격은 §6-5 대기열의 순서 안이다(§2).

**명령의 못박음**(§후속 **65** 확장 항 ㉡). 이 절의 모든 `git` 명령은 **트리 인자를 명령 안에 담고
`--all` 을 쓰지 않는다**: `git grep <패턴> ddf6bb9 -- <경로>` · `git log --oneline <A>..<B> -- <경로>` ·
`git log -1 --format='%h' ddf6bb9 -- <경로>`. **트리 인자를 받지 못하는 명령**(`make` · `docker` ·
`ps` · `pytest` · `npx` · `grep` · `find` · `ls`)은 전부 이 M-0 이 못박은 **`ddf6bb9` 의 작업 트리**에서
돌린 것이고, 그 사실을 여기 한 번 적어 각 자리에서 되풀이하지 않는다.

**시점을 부르는 말**(65 의 원래 항 ㉠). 「오늘」·「지금」·「HEAD」는 **이 M-0 안에서만** 쓴다.

**방법 · N · 표본 간격**(§후속 **34** · 27). 이 절의 값은 **2026-09-08 한 세션 안**(16:17~16:36 UTC,
`date -u` 로 앞뒤를 찍었다)에 잰 것이고, **따로 적지 않으면 N=1**, 서로 몇 분 간격이다.
**`make test` 만 N=2** — 이 절을 쓰기 전(깨끗한 트리)과 쓴 뒤(이 문서와 ADR 이 뜬 트리) 각 한 번이고
**두 값이 같다**(§M-1). **「없다」·「죽었다」에도 N 을 적는다.**

**자원.** 포트 **0개** · postgres 클러스터 **0회** · **심은 변이 0** · **docker 데몬 0회**
(`docker info` → **rc 1**, stderr *"failed to connect to the docker API at unix:///var/run/docker.sock;
… no such file or directory"*, N=1; `/proc/sys/net/ipv4/ip_forward` → **0**, N=1) · 저장소 루트에도
`buildtwin/` 에도 **`.env` 를 만들지 않았다**(`ls -a <각 디렉터리> | grep -c '^\.env$'` → **0**,
각 N=1, 이 절의 앞과 뒤 둘 다).
**이 절이 돌린 명령 중 컨테이너를 만들 수 있었던 것은 `make dev` 하나**이고(M-2 9행), 그 실행은
데몬이 없어 **rc 2 로 죽었다** — 컨테이너 0 · 이미지 0 · 그 실행 뒤에도 `.env` **0**(각 N=1).

**소급 수정하지 않는다**(§3-13 첫째 갈래). 예외는 **§M-5 의 세 자리**(내가 만든 문서의 **잘못된 인용**과
좁은 조건절)이고, 세 자리 모두 **고친 자리에 초판의 문안을 적어** 갈음이 보이게 두었다 — 그것이
§6-4 1(*"사실과 다른 문구는 그것을 만든 사이클이 고친다"*)의 요구이고, 자구 보존이 그 요구를 이기지
않는다.

### M-1. 게이트 — **내가 다시 돌린 값** (완료 조건 1)

**이 절은 측정을 둘 싣는다.** 아래 표는 **`ddf6bb9`** 의 것(초판)이고, 심사 0016 의 R1·R2 가 닫힌 뒤
**`a70c495`** 에서 다시 잰 값은 **§M-1-2** 다. 초판을 지우지 않는 이유는 그것이 그때의 트리를 못박은
값이기 때문이다(CLAUDE.md §3-13 첫째 갈래).

방법: `make test`(16:17:56 → 16:19:44 UTC, **깨끗한 트리**) · `make lint`(16:20:14 → 16:20:22) ·
`make e2e`(16:20:35 → 16:21:04) · **`make test` 다시**(16:34:02 → 16:35:59, **이 절과 §M-5 의 편집이
뜬 트리** — §3-1 이 요구하는 「커밋 전」이 그 실행이다). **`make test` 는 N=2 이고 아래 값은 두 실행이
같다**; 나머지는 N=1. 전후 루트 `git status --porcelain`: 첫 실행은 **둘 다 빈 출력**, 둘째 실행은
**둘 다 이 문서와 ADR 두 줄**(추적 밖 변화 0).

| 갈래 | **이 마감의 재측정**(`ddf6bb9`) | `ddf6bb9` 커밋 본문 | 계획 §0 인계값(`a03f13d`) | 갈리는가 |
|---|---|---|---|---|
| `pytest tests/unit -q` | **492 passed** | 492 | 492 | 아니오 |
| `pytest tests/invariants -q` | **115 passed** | 115(112 → +3) | 112 | 아니오 — **늘어난 셋의 이름이 있다**(아래) |
| `pytest tests/regression -q` | **8 passed** | 8 | 8 | 아니오 |
| `pytest tests/integration -q` | **232 passed** | 232 | 232 | 아니오 |
| `npx vitest run` | **303 passed (31 files)** | 303(289 → +14, 29 → 31 files) | 289(29 files) | 아니오 — **늘어난 둘의 이름이 있다** |
| `make test` | **rc 0** | rc 0 | — | 아니오 |
| `make lint` | **rc 0**(ruff *All checks passed!* · mypy *no issues found in 110 source files* · eslint · `tsc --noEmit`) | rc 0 | rc 0 | 아니오 |
| `make e2e` | **12 passed · rc 0** | 12 passed | 12 passed | 아니오 |
| 고아 `vite preview` | **전 0 → 후 0** | 전 0 → 후 0 | 전 0 → 후 0 | 아니오 |
| `.e2e-preview.*` 잔여 | **0**(전·후) | 0 | — | 아니오 |

**「어느 것이 바뀌는지」를 수가 아니라 이름과 값으로**(계획 §게이트). 늘어난 것은 **작업 4 가 더한
파일 셋뿐**이고, 그것을 **따로 실행해** 값으로 확인했다(각 N=1):
`pytest tests/invariants/test_the_image_installs_from_one_source.py -q` → **3 passed**;
`npx vitest run src/api/apiProxyFailure.test.ts src/api/previewProxyParity.test.ts` →
**14 passed (2 files)**. 그래서 112+3 · 289+14 · 29+2 이고, **기존 단언은 하나도 바뀌지 않았으며
`tests/integration`·`tests/unit` 은 파일이 늘지 않았다.**

**고아 세는 법**(§후속 **46**): `ps -eo args --no-headers | grep -c "^node .*vite preview"` 를
**별도 셸 호출**로 `make e2e` 앞뒤 각 한 번. 대괄호 없는 `grep` 이나 `pgrep -af "vite preview"` 는
**자기 명령줄을 히트로 낸다**(이 사이클의 실례 둘은 §M-7 ⓗ).

**바닥값을 올리지 않았다.** `git log --oneline 757bc8f~1..ddf6bb9 -- buildtwin/tests/metrics.json
buildtwin/tests/postgres.floor.json` → **빈 출력**(N=1), 그리고 이 마감도 그 둘을 만지지 않는다
(커밋 직전 `git status --porcelain -- <그 둘>` 빈 출력). `min_tests_on_postgres` 의 키는
`tests/integration`·`tests/unit` 둘인데 이 사이클은 **그 두 트리에 테스트를 더하지 않았다**(위 문단).

### M-1-2. 게이트 **재측정** — 심사 0016 R1·R2 가 닫힌 뒤 (`a70c495`)

**방법 · N · 표본 간격**(§후속 **34**). 트리 **`a70c495`**, 작업 트리 `/home/user/Bim/buildtwin`,
저장소 루트 `/home/user/Bim`. 값은 **2026-09-08 한 세션 안**(17:39~17:57 UTC, `date -u` 로 앞뒤를 찍었다)에
잰 것이고 **따로 적지 않으면 N=1**, 서로 몇 분 간격이다. **`make test` 만 N=2** — 이 절을 쓰기 전
(깨끗한 트리)과 쓴 뒤(이 문서와 ADR 이 뜬 트리) 각 한 번이고 **아래 값은 두 실행이 같다**(§3-1 이
요구하는 「커밋 전」이 둘째 실행이다). 전후 루트 `git status --porcelain`: 첫 실행은 **둘 다 빈 출력**,
둘째 실행은 **둘 다 이 문서와 ADR 두 줄**(추적 밖 변화 0).
**자원**: `make e2e` 가 잡은 포트는 그 명령이 반납했다(아래 고아·잔여 값) · postgres 클러스터 **0회** ·
**docker 데몬 0회** · 저장소 루트에도 `buildtwin/` 에도 **`.env` 를 만들지 않았다**
(`ls -a <각 디렉터리> | grep -c '^\.env$'` → **0**, 각 N=1). **심은 변이는 하나**(아래 축 3 의 X5)이고
심기 직전의 작업 트리 사본과 `diff` 로 적용을 확인한 뒤 **그 사본으로 원복**했다(§6-2 규칙 5) —
원복 뒤 루트 `git status --porcelain` **빈 출력**.

| 갈래 | **재측정**(`a70c495`) | §M-1 초판(`ddf6bb9`) | 갈리는가 |
|---|---|---|---|
| `pytest tests/unit -q` | **492 passed** | 492 | 아니오 |
| `pytest tests/invariants -q` | **118 passed** | 115 | **예 — +3**(축 1) |
| `pytest tests/regression -q` | **8 passed** | 8 | 아니오 |
| `pytest tests/integration -q` | **232 passed** | 232 | 아니오 |
| `npx vitest run` | **310 passed (31 files)** | 303 (31 files) | **예 — +7, 파일 수는 그대로**(축 2) |
| `make test` | **rc 0** | rc 0 | 아니오 |
| `make lint` | **rc 0**(ruff *All checks passed!* · mypy *no issues found in 110 source files* · eslint · `tsc --noEmit`) | rc 0 | 아니오 |
| `make e2e` | **12 passed · rc 0** | 12 passed · rc 0 | **아니오 — 그리고 이 「아니오」가 축 3 이다** |
| 고아 `vite preview` | **전 0 → 후 0** | 전 0 → 후 0 | 아니오 |
| `.e2e-preview.*` 잔여 | **0**(전·후) | 0 | 아니오 |

**바닥값을 올리지 않았다.** `git log --oneline ddf6bb9..a70c495 -- buildtwin/tests/metrics.json
buildtwin/tests/postgres.floor.json` → **빈 출력**(N=1), 그리고 이 갱신도 그 둘을 만지지 않는다.

#### 「어느 것이 바뀌는지」 — **수가 아니라 이름과 값으로.** 축마다 나눈다

**축 1 — invariants +3: 새 파일 하나.** `git show --name-only --format="" a70c495` 가 내는 이름 중
`tests/` 쪽이 **`tests/invariants/test_the_first_run_names_its_doors.py`** 이고(N=1), 그것을 **따로 실행한
값**은 `pytest tests/invariants/test_the_first_run_names_its_doors.py -q` → **3 passed**(N=1).
115 + 3 = 118. **기존 불변식 파일은 한 줄도 바뀌지 않았다** — 같은 `--name-only` 출력에 다른 `tests/`
경로가 없다.

**축 2 — vitest +7: 기존 파일 하나에 단언이 늘었다.** 같은 출력의 다른 이름이
**`apps/web/src/api/apiProxyFailure.test.ts`** 이고, 따로 실행하면
`npx vitest run src/api/apiProxyFailure.test.ts` → **18 passed (1 file)**(N=1). 초판에서 그 파일은
**11** 이었다(§M-3 4행이 적는 `previewProxyParity.test.ts` **3** 과 §M-1 초판의 두 새 파일 합계
**14 passed** 의 차 — **옮겨 적은 값이고 내가 다시 재지 않았다**). 그래서 303 + 7 = 310 이고
**파일 수가 31 그대로인 것이 「새 파일이 아니라 단언이 늘었다」의 값**이다.

**축 3 — `make e2e` 12 는 「바뀌지 않은 것」이 아니라 「보이지 않는 것」이다 (심사 0016 R3).**

§M-1 초판은 늘어난 것을 이름으로 적고 *"기존 단언은 하나도 바뀌지 않았다"* 로 끝냈다. **단언 집합에
대해서는 그 말이 참이다.** 그러나 이 사이클은 **`make e2e` 가 실제로 지나는 프록시 옵션을 바꿨다** —
`65ac781` 이 `tests/e2e/conftest.py` 의 `PREVIEW_CONFIG` 에서 `preview.proxy["/api"]` 를 **키 단위로 다시
선언하던 것**을 base 의 옵션 객체를 **펼쳐 물려받는 모양**으로 바꿨다(코드 인용, 그 커밋의 diff):

```ts
-  preview: { proxy: { "/api": { target: resolveApiProxyTarget(), changeOrigin: true } } },
+  preview: { proxy: { "/api": { ...apiProxy, target: resolveApiProxyTarget() } } },
```

**그 축은 수로도 이름으로도 보이지 않는다.** 지나는 것은 **같은 12** 이고(수집된 이름의 파일은
`tests/e2e/test_core_flow.py` 와 `tests/e2e/test_web_smoke.py`, 이 사이클의 뒤 두 커밋은 그 트리를
만지지 않았다 — `git show --name-only --format="" a70c495 f3eeeeb -- buildtwin/tests/e2e` → **빈 출력**,
N=1), 바뀐 것은 **그 12 가 지나는 배선**이다.

**변이로 값이 갈리는지 태웠다**(§6-2 1 — X5, N=1). 옛 배선을 되심고(위 diff 의 `-` 줄로 되돌린다) 재고,
심기 직전의 작업 트리 사본과 `diff` 로 적용을 확인한 뒤 그 사본으로 원복했다:

| 트리 | `make e2e` | `npx vitest run` |
|---|---|---|
| 음성 대조군(`a70c495`) | **12 passed · rc 0** | **310 passed (31 files)** |
| **X5** — `PREVIEW_CONFIG` 의 `/api` 를 옛 두 키 모양으로 되돌린다 | **12 passed · rc 0** — **갈리지 않는다** | **2 failed, 308 passed (1 failed \| 30 passed files)** — `previewProxyParity.test.ts` |

**그래서 이 칸의 답은 이렇다.** e2e 축에서 바뀐 것은 **단언도 이름도 수도 아니고 경로**이며,
**`make e2e` 는 그 경로가 옛것이어도 12 passed 를 낸다** — 그 축을 가르는 것은 이 사이클이 만든
**vitest 의 `previewProxyParity.test.ts`**(작업 4, `ddf6bb9`)뿐이다. 그 배선이 실제로 무엇을 바꿨는지의
값은 **preview 갈래의 응답**이고(옛 배선 **500 · 본문 0바이트** → 새 배선 **502 · 본문 404바이트**),
그 값은 **`65ac781` 커밋 본문의 것을 옮겨 적었다 — 내가 다시 재지 않았다**(§M-7 ⓑ 가 그 출처를 진다).

*역방향 확인 — 이 칸이 여전히 놓치는 것.* ⓐ X5 는 `65ac781` 의 **배선 한 줄만** 되돌린 것이지 그 트리를
되돌린 것이 아니다 — 같은 커밋의 `Makefile` 변경은 그대로 두고 잰 값이다. ⓑ 「12 가 같은 12 다」는
**수집된 이름**으로 답한 것이고(`pytest tests/e2e --collect-only -q`), **각 테스트가 무엇을 지나는지**는
여전히 재지 않았다. ⓒ **실제 응답의 status·바이트를 붙드는 게이트는 지금도 없다** —
§확인하지 않은 것 **81** 이 그 자리이고 이 갱신도 닫지 않는다.

### M-2. 교차 확인 표 ① — 이 사이클이 **두 번 이상 적은 값** (완료 조건 2, §6-3 11회차)

**축은 「같은 문서」가 아니라 「같은 사이클」이다.** ADR 0020 §5-2 의 표를 이어받아 **작업 1·2·3·4 의
커밋까지 넣고 다시 태운다.** **갈리지 않은 칸도 적는다** — 갈린 것만 적으면 이 표가 「무엇을
확인했는가」를 싣지 못한다. **표에 넣을 값의 목록도 §6-1 ①②③ 을 받는다**(§6-1 13회차): 기준은
*"계획이 미리 이름 붙인 값"* 이 **아니라** *"이 사이클의 어느 자리든 두 번 이상 적힌 값 — **실행이
만들어 낸 값 자신**을 포함해서"* 이고, 그래서 아래 4·5·6·13·16·17행은 **계획에 칸이 없던 값**이다.

| # | 값 | 이 사이클이 그것을 적은 자리들 | 이 마감이 잰/읽은 값 | 갈리는가 |
|---|---|---|---|---|
| 1 | `grep -c '\|\|' Dockerfile` | 계획 작업 1 조건 1 = **0** · ADR §3 1 = **0** · `c82cf47` 본문 = **0** · 불변식 파일의 변이 표 음성 대조군 칸 = **0** | **0**(rc 1 — grep 은 0건에서 rc 1 을 낸다), N=1 | **아니오** |
| 2 | 그 자리를 붙드는 **축**(토큰이냐 모양이냐) | 계획 작업 4-1 = **「`\|\|` 의 부재」(토큰)** · ADR §5 셋째 행 = **「토큰이 아니라 모양」** · 저장소 불변식 = **모양** · `ddf6bb9` 본문 = 모양 | 두 기준이 **다른 답을 내는 트리가 있다**: 불변식 파일의 M-A 행(주석 한 줄에 그 토큰을 되살린다) = **3 passed** ↔ 같은 트리의 `grep -c` = **1**(저장소 파일의 표, 내가 읽었다) | **예 — 축이 갈린다.** 저장소 게이트는 ADR 축(모양)에 섰고, 계획의 문안은 **기록물이라 갱신하지 않는다**. 두 축이 갈리는 자리는 「주석」 하나이고 그 대가가 `Dockerfile` 주석에 남아 있다(`grep -n "셸 OR" Dockerfile`) |
| 3 | `pyproject` 의존성 ↔ 폴백 목록(**23 ↔ 22**, 차집합 = `openpyxl` 하나, 폴백은 `-e .` 도 버린다) | 계획 §1-c 문 3 행 · ADR §5-2 1~3행 · `c82cf47` 본문(*"그 목록에는 `-e .` 자신도 `openpyxl` 도 없었다"*) | **재계수할 대상이 없다** — `c82cf47` 이 그 목록을 지웠다. 값은 `a03f13d`·`757bc8f` 트리의 것이고 세 자리가 같다 | **아니오** |
| 4 | dev 갈래 프록시 실패의 실측(**502 · 404바이트** · `ECONNREFUSED connect ECONNREFUSED …`) | `6ac7a19` 본문 표 2행(frontend 가 쟀다) · `65ac781` 본문 표 1행(**qa 가 같은 트리에서 다시 쟀다**) | 두 자리가 같다. **이 마감은 서버를 띄우지 않았다**(자원 0) — 옮기는 값이므로 출처를 §M-7 에 표시한다 | **아니오** |
| 5 | preview(옛 `PREVIEW_CONFIG`) 갈래 **500 · 0바이트** | `6ac7a19` 본문 표 4행 · `65ac781` 본문 표 2행 | 두 자리가 같다(§M-7 이 옮긴다) | **아니오** |
| 6 | `c82cf47` 트리 dev 갈래 **500 · 0바이트** ↔ §확인하지 않은 것 **73** 의 인계값 *"500 · 본문 빈 채"* | 계획 §1-c 아래 문단(*"이 계획이 잰 값이 아니다"*) · `6ac7a19` 본문 표 1행(**처음 잰 값**) | 인계값과 실측이 **같다** — 세 사이클을 인계로만 돌던 값이 이 사이클에서 처음 재현됐다 | **아니오** |
| 7 | `client.ts` 마지막 폴백이 말해야 하는 것 | 계획 작업 2-2 = **「본문이 비어 있다는 사실」** · ADR §2-2 (나)·§5-2 = **「응답에 읽을 수 있는 오류 메시지가 없다」** · `6ac7a19` 코드 · `ddf6bb9` 의 W6 변이 | 저장소 코드는 ADR 문안이고(내가 읽었다), 계획 문안으로 되돌리는 W6 변이는 **1 failed, 302 passed**(저장소 파일의 표) | **예 — 갈린다.** ADR 이 **코드로** 갈랐다(`errorMessage` 는 본문이 비지 않아도 그 줄에 닿는다). 계획은 기록물이라 갱신하지 않는다 |
| 8 | **ⓘⓙⓚ** 라는 글자의 뜻 | 계획 = 작업 5 상자에서 **계약의 항**, 작업 2 상자에서 **문자열의 슬롯**(한 문서 안 두 뜻) · ADR §2-1 끝 = 「항 1·2·3」과 「ⓘⓙⓚ」로 **갈라 부른다** | `git grep -nI "ⓘ" ddf6bb9 -- buildtwin/apps/web buildtwin/tests buildtwin/Makefile` 의 히트를 **읽었다** — `vite.config.ts` 머리 주석과 `apiProxyFailure.test.ts` 의 것들이고 **전부 슬롯 뜻**(N=1) | **예 — 갈린다. 그러나 갈림은 계획 안에서만 살아 있고** ADR·코드·테스트는 슬롯 하나로 수렴했다 |
| 9 | 데몬 없이 `make dev` 의 **rc** | 계획 작업 3 조건 1 = **rc 1** · `65ac781` 본문 = **rc 2**(그리고 `docker compose up --build` 자신은 rc 1) | **rc 2**(N=1, 내가 다시 쟀다). stderr 두 줄이 기전을 싣는다: `env file /home/user/Bim/buildtwin/.env not found: …` + **`make: *** [Makefile:40: dev] Error 1`** — **make 가 compose 의 1 을 「Error 1」로 보고하고 자신은 2 로 죽는다.** 안내 넉 줄은 전부 **stdout**(그 실행의 stdout 은 열 줄) | **예 — 갈린다.** 계획의 값이 틀렸고 **실측이 정본**이다 |
| 10 | 문 2 의 stderr **조각** | ADR §2-2 (다) = 공통 조각 *"failed to connect to the docker API at unix:///var/run/docker.sock"*, 앞머리 `unable to get image '…'` 는 **뺀다** · 계획 §1-c 문 2 = `unable to get image 'minio/minio:latest': …` · `65ac781` 본문 = `unable to get image 'door2-api': …` · 저장소 `Makefile` = 공통 조각만 | 내 `docker info` 도 **같은 문장**을 냈다(N=1). **앞머리는 프로젝트 이름에 따라 갈린다**(minio ↔ door2-api) — ADR 의 「조각에서 뺀다」 판단이 값 위에 선다 | **아니오**(조각). **앞머리는 갈리고, 갈릴 것을 미리 뺐다** |
| 11 | 문 2 안내의 **조건절** | ADR §2-2 (다) 형태 = 「{문의 이름} 이(가) **없으면**」 · 저장소 `Makefile` = 「**닿지 못하면**(꺼져 있거나 소켓에 권한이 없으면)」 · `65ac781` 본문 = 그 확장의 §6-3 역방향 확인 | 저장소 문안을 읽었다(`grep -n "닿지 못하면" Makefile`, N=1). **넓힌 쪽이 옳다** — 좁은 조건절은 **소켓 권한 거부**를 밖으로 낸다 | **예 — 갈린다.** ADR 이 *"문안은 `qa` 가 쓴다"* 로 열어 둔 자리라 계약 위반은 아니고, **이 마감이 ADR (다)에 정정 각주를 달았다**(§M-5) |
| 12 | CLAUDE.md §2 의 **공동 편집 자리 표기** | CLAUDE.md §2(**정본**) = `apps/web/src/**/*.test.ts(x)` · 계획 §작업 분배·작업 4 상자 = `apps/web/**/*.test.ts` · ADR §8 9행 = 같은 잘못된 인용 + 「`frontend`·`qa` **공동**」 | `ls apps/web \| grep -c "\.test\.ts"` → **1**(`vite.proxy-target.test.ts`) · `find apps/web/src -name "*.test.ts*" \| wc -l` → **30**(각 N=1). **루트의 그 파일은 §2 문자로 공동 자리가 아니다** | **예 — 갈린다.** §M-5 가 **내 문서 둘**을 고쳤다(§6-4 1) |
| 13 | **심은 변이의 수** | `ddf6bb9` 본문 산문 = **13** · 저장소 세 파일의 변이 표 = **열거**(M-A~M-E · W1~W6 · P1~P3; `W1` 은 두 표에 나오지만 한 번 심었다) | `grep -hoE "\| (M-[A-E]\|W[0-9]\|P[0-9]) " <세 파일> \| sort -u \| wc -l` → **14**(N=1). `git grep -nI "심은 변이" ddf6bb9 -- .` 의 히트를 읽었다 — **저장소에는 그 수가 없다**(ADR 0020 §0 의 「0」과 계획 0014·0015 의 것들뿐) | **예 — 갈린다.** 정본은 저장소의 **열거**이고 갈린 것은 **커밋 본문의 산문**이다. §6-1 9회차(*"열거는 길이가 곧 개수다"*)가 **커밋 본문에서** 났다 → §후속 **84** |
| 14 | 게이트 수(112 → 115 · 289 → 303) | 계획 §0 인계값 · `ddf6bb9` 본문 · **이 마감의 재측정** | §M-1 표. 늘어난 셋을 **따로 실행해** 값으로 봤다(3 passed · 14 passed) | **아니오** |
| 15 | 고아 `vite preview` 전 0 → 후 0 | `c82cf47`·`6ac7a19`·`65ac781`·`ddf6bb9` 네 본문 · **이 마감의 재측정** | **0 → 0**(별도 호출 둘, 각 N=1) | **아니오** |
| 16 | **M-B 의 예측 ↔ 실측** | `ddf6bb9` 본문 = 예측 2 failed ↔ 실측 **3 failed** · 저장소 불변식 파일의 표 = 같은 값 + 이유(*"그 줄은 `.` 말고 `psycopg[binary]` 도 설치한다"*) | 두 자리가 같다(내가 읽었다) | **아니오** — 갈린 것은 **예측과 실측**이고, **저장소가 그 갈림을 싣는다** |
| 17 | **P2 의 불충분** | `ddf6bb9` 본문 · `previewProxyParity.test.ts` 머리 주석 = 예측 1 failed ↔ 실측 **303 passed 그대로**, 이유는 `Object.is`(원시값 사본을 구별하지 못한다) | 두 자리가 같다(내가 읽었다). 그 칸은 그 파일의 §보지 못하는 것에 **값으로** 적혀 있다 | **아니오** |
| 18 | 데몬·네트워킹 상태 | ADR §0 · `c82cf47` 본문 · `6ac7a19` 본문 · **이 마감** | `docker info` **rc 1** + 같은 stderr · `ip_forward` **0**(각 N=1) | **아니오** |
| **19** | **ⓘ(`대상:` 슬롯)의 정의** — 「`resolveApiProxyTarget()` 의 값」이냐 「프록시가 실제로 받은 `options.target`」이냐 | ADR §2-2 (가) 치환자 표 `{target}` 행 · ADR §2-3 능력 표 ㉡ 행 · 계획 §완료 조건 작업 2 조건 1 · `f3eeeeb` 뒤의 `vite.config.ts` 코드 · `apiProxyFailure.test.ts` 의 새 단언 | 전수(트리를 명령 안에, 각 N=1): `git grep -nI "resolveApiProxyTarget" a70c495 -- .` 와 `git grep -nI "대상 주소" a70c495 -- .` 의 히트를 **읽었다** — ⓘ 를 **정의하는** 자리는 위에 적은 것들뿐이고(§M-12 가 ㉮㉯㉰㉱ 로 갈라 판정한다) 나머지 히트는 식별자 사용이거나 `603ed5d` 트리의 코드 인용이다. 저장소 코드는 `options.target` 이고(`vite.config.ts` 의 `configure` 안, 코드 인용), 같은 파일에서 `resolveApiProxyTarget` 의 히트는 `:4`·`:12` 둘로 **핸들러 밖**이다 | **예 — 갈린다.** 심사 0016 R1 이 잡았다. **자리마다 처분이 다르다** → §M-12 가 갈라서 판정하고, ADR §2-2 (가)에 **마감 정정**이 실렸다 |

**갈린 칸은 2 · 7 · 8 · 9 · 11 · 12 · 13 · 19 이고, 그중 이 마감이 문서를 고쳐 닫은 것은 11 · 12 · 19 다**
(§M-5). 2 · 7 · 8 · 9 는 **기록물(계획)과 실행 결과의 갈림**이라 계획을 갱신하지 않고 이 표가 정본을
가리킨다(§3-13 첫째 갈래). 13 은 **저장소가 아니라 커밋 본문**이 갈린 것이라 고칠 문서가 없다 —
§후속 **84** 로 연다. **19 는 심사 뒤에 더한 행이다** — 이 표의 기준(*"이 사이클의 어느 자리든 두 번 이상 적힌 값"*)이 그것을 담는데 초판이 못 봤고, 그 이유는 §M-12 가 값으로 적는다.

### M-3. 교차 확인 표 ② — **ADR 0020 §8 표 ↔ 완료 조건 상자의 「배정」 대조** (§후속 74 조항 후보 둘째)

74 의 조항 후보 둘째는 *"마감의 교차 확인 표는 값뿐 아니라 **배정**도 대조하고, 대조는 ADR ↔ 완료 조건
상자 사이에서 한다"* 이다(**ⓑ 인용 · 발췌**). **각 칸은 실행값이거나 코드·문서 인용이고 다른 절
참조가 아니다.**

| §8 # | 자리 | 계획의 상자(배정) | 그 상자를 실행한 커밋 | 그 커밋이 낸 값 / 이 마감이 읽은 것 | 대조 |
|---|---|---|---|---|---|
| 1 | `Dockerfile:5` | 작업 1 조건 1·2 | `c82cf47` | `grep -c '\|\|' Dockerfile` → **0**(내 재측정) · 불변식 **3 passed** | **맞다** |
| 2 | `apps/web/vite.config.ts` 의 `/api` proxy | 작업 2 조건 1 | `6ac7a19` | 502 · 404바이트(출처 표시, §M-7) · W1~W5 변이가 전부 빨개진다(저장소 표) | **맞다** |
| 3 | `apps/web/src/api/client.ts:164` | 작업 2 조건 2 — **단 그 상자의 문안이 거짓** | `6ac7a19` | 저장소 문자열은 ADR §2-2 (나)의 것 · W6 **1 failed** | **맞다, 문안은 갈렸다** → M-2 7행이 태웠다 |
| 4 | `tests/e2e/conftest.py` 의 `PREVIEW_CONFIG` | 작업 2 조건 5 **ⓧ**(소유 `qa`) | `65ac781`(**같은 사이클, 소유자의 커밋**) | preview 갈래가 **500·0바이트 → 502·404바이트** · `previewProxyParity.test.ts` **3 passed** | **맞다. §후속 76 의 첫 시험이고 통과했다** |
| 5 | `docker-compose.yml` 의 `web:` 머리 주석 | 작업 2 조건 5 **ⓩ**(소유 **없음**, §후속 19) | 없다 — `6ac7a19` 가 **읽고 판정만** 했다(*"거짓이 아니라 낡는다"*) | 내가 그 주석을 읽었다: 단정하는 것은 프록시의 **이름·값·빈 값** 셋이고 셋 다 참이다 | **맞다(고치지 않는 것이 배정이었다)** → §M-6 이 함께 판정 |
| 6 | `apps/web/vite.proxy-target.ts:31-36` | 작업 2 조건 5 **ⓨ**(소유 `frontend` — 자기 소유) | `6ac7a19` 가 읽고 **참 그대로**로 판정 | 그 파일은 이 사이클에서 **한 줄도 바뀌지 않았다**(`git log --oneline 757bc8f~1..ddf6bb9 -- buildtwin/apps/web/vite.proxy-target.ts` → 빈 출력, N=1) | **맞다** |
| 7 | `tests/invariants/test_demo_stack_can_stand.py` 머리 주석의 「이미지 안의 것」 | 작업 4 조건 5 | `ddf6bb9`(**같은 소유, 같은 커밋**) | 그 절이 새 불변식이 무엇을 보는지와 **둘 다 못 보는 것**을 갈라 적는다(내가 읽었다) | **맞다** |
| 8 | `README.md:45` 「걸음을 건너뛰면」 | 작업 3 조건 3(소유 **없음**) | `65ac781` 이 **읽고 판정만** 했다(*"데몬을 말하지 않은 채 남는다"*) | `git grep -nIi "daemon\|docker\.sock" ddf6bb9 -- buildtwin/README.md` → **rc 1 = 히트 0**(N=1) | **맞다(판정이 배정이었다)** → **§M-6 이 처분을 정한다** |
| 9 | `apps/web/vite.proxy-target.test.ts:50-52` | **없다** — 조건 5 는 ⓧⓨⓩ 셋만 이름한다 | 없다. `6ac7a19`·`ddf6bb9` 두 본문이 각각 **읽고 「거짓 아님」으로 판정**했다 | **§M-4 가 이 칸을 태운다** | **어긋난다 — 이 사이클이 74 의 조항 후보 첫째를 못 지켰다** |

**대조의 결론.** ADR §8 의 행 중 **여덟은 상자에 이름으로 있었고 그 배정대로 실행됐다.** 어긋난 것은
**9행 하나**이고, ADR 이 그 사실을 **행 안에 적어** 마감으로 넘겼다 — **그래서 이 표가 그것을 잡을 수
있었다.** 74 의 조항 후보 첫째(*"ADR 이 「거짓이 된다」로 지목한 행은 **전부** 계획의 완료 조건 상자에
이름으로 들어간다"*)는 **이 사이클에서 지켜지지 않았다**: 상자를 쓴 것도(계획) ADR 을 쓴 것도 같은
`architect` 인데, ⓧⓨⓩ 목록의 생성 기준이 **「작업 2 가 만지는 파일의 이웃」** 이라 **테스트 파일 축에
칸이 없었다.** 그러므로 **74 는 닫지 않는다**(§M-8).

### M-4. §8 **9행** — 마감이 지는 칸. **거짓이 아니다. 낡는다.**

자리(트리 `ddf6bb9` 에서 내가 다시 열었다, 줄은 그대로 **`:50-52`**): *"**브라우저에서의 손해는 여전히
재지 않았다**(계획 0015 §확인하지 않은 것 63) — 이 환경에 docker 데몬이 없어(`docker info` 가 소켓
부재를 낸다) 컨테이너 안에서 프록시가 무엇을 내는지(연결 거부인지 5173 자신의 응답인지)는 이 커밋도
답하지 못한다."* (**ⓑ 인용 · 전체**)

**세 단정을 따로 판정한다**(각 N=1).

| 단정 | 판정 | 값 |
|---|---|---|
| *"브라우저에서의 손해는 여전히 재지 않았다"* | **참** | 이 사이클의 탐침은 전부 `curl` 이고 **렌더를 잰 사람이 없다**(§확인하지 않은 것 **74** 는 열린 채다) |
| *"이 환경에 docker 데몬이 없어"* | **참** | `docker info` **rc 1** · `ip_forward` **0**(이 마감의 재측정) |
| *"컨테이너 안에서 프록시가 무엇을 내는지 … 이 커밋도 답하지 못한다"* | **참** | 「이 커밋」은 그 주석을 쓴 커밋이고, **컨테이너 축은 이 사이클의 누구도 재지 않았다** |

**그래서 고치지 않는다.** §후속 **76** 의 방아쇠는 **「남의 소유 문구를 거짓으로 만든다」** 인데
거짓이 되지 않았다. **낡는 것은 그 문장이 싣는 「미지」의 크기**다: 호스트 축에서는 dev·preview 두
갈래의 답이 값으로 정해졌고(§M-7 ⓑ), 저장소가 그 문자열 축을 vitest 로 붙든다(새 파일 둘 **14 passed**).
남은 미지는 **컨테이너 축과 렌더 축 둘**이다. 그 자리는 **소유가 `frontend`**(§M-5 가 ADR 의 소유
인용을 고쳤다)이므로 **이 마감이 만지지 않고** §후속 **81** 로 연다.

### M-5. **ⓕ — 내가 만든 문서의 잘못된 인용을 이 사이클 안에서 고쳤다** (§6-4 1)

**무엇이 틀렸나.** CLAUDE.md §2 의 공동 편집 자리는 `apps/web/src/**/*.test.ts(x)` 와
`apps/web/src/test/` 인데(그 자리에서 도는 참조: `grep -n "공동 편집 자리다" CLAUDE.md`), **내가 쓴 두
문서가 그것을 `apps/web/**/*.test.ts` 로 인용했다** — `src/` 와 `(x)` 가 빠진다. 그 인용대로면 루트의
`apps/web/vite.proxy-target.test.ts` 가 **공동 자리가 되지만**, §2 문자로는 아니다(그 파일은 `src/`
밖이고, §2 의 트리 표는 `apps/web/`(뷰어 제외)를 `frontend` 에 둔다).

**고친 자리 셋**(각각 고친 자리에 초판 문안을 적었다 — 갈음을 보이게 둔다):

| 자리 | 초판 | 이 마감 |
|---|---|---|
| 계획 §작업 분배 표 작업 4 행 | `apps/web/**/*.test.ts`(§2 의 공동 편집 자리) | `apps/web/src/**/*.test.ts(x)` + 초판 표시 |
| 계획 §완료 조건 「작업 4」 상자 머리 | `(qa · tests/, apps/web/**/*.test.ts 공동)` | `apps/web/src/**/*.test.ts(x)` + 초판 표시 |
| ADR 0020 §8 **9행** 소유 칸 | *"소유는 `frontend`·`qa` 공동(CLAUDE.md §2 의 `apps/web/**/*.test.ts` 줄)"* | *"소유는 **`frontend`**"* + 근거(§2 트리 표 · 그 파일은 `src/` 밖) + 초판 표시 |

**그리고 넷째 자리 하나 — 조건절**(M-2 11행): ADR §2-2 (다)의 형태가 「{문의 이름} 이(가) **없으면**」
이라 **소켓 권한 거부가 그 조건절 밖으로 샌다.** `qa` 가 그 역방향 확인을 하고 저장소 문안을
「**닿지 못하면**(꺼져 있거나 소켓에 권한이 없으면)」으로 넓혀 실었으므로, **ADR (다)에 마감 정정
각주**를 달아 **조건절의 정본이 저장소 문안임**을 적고 한정(권한 거부의 실제 문구는 이 사이클의
누구도 재지 못했다)을 함께 적었다.

**이 인용이 실제로 손해를 냈는가 — 두 축으로 갈린다.**
- **파일이 놓인 자리 축: 손해 0.** `qa` 는 §2 를 정본으로 삼아 새 vitest 파일 둘을 `src/api/` 에 뒀다
  (`find apps/web/src -name "*.test.ts*"` 에 둘이 있다, N=1) — 부수 효과로 `tsconfig.json` 도 건드리지
  않았다. **실행자가 계획의 인용이 아니라 §2 를 읽었기 때문에** 어긋남이 값으로 나타나지 않았다.
- **배정 축: 손해가 있었다.** §8 **9행**은 그 잘못된 인용 위에서 소유를 「공동」이라 적었다 — 그 행에
  고칠 것이 있었다면 **누가 고치는가가 틀렸을 것**이다. 74 의 조항이 「ADR §8 의 행 ↔ 상자」 대조만
  요구하고 **그 행이 적는 소유**는 대조하지 않는 것이 여기서 보인다 → §후속 **82**.

**고치지 않은 자리 하나 — 사실이 아니라 렌더의 결함이다.** §1-c 표의 문 3 행은 코드 인용 안의
셸 OR 를 이스케이프하지 않아 **그 행만 표의 칸 수가 다르다.**

*값*(심사 0016 N1 뒤 **내가 다시 쟀다**, N=1). 방법: §1-c 의 `|` 로 시작하는 줄마다
`줄.count("|")` 와 `줄.count("\\|")` 를 파이썬으로 세고 그 차를 **유효 파이프**로 읽는다.

| §1-c 의 줄 | 파이프 총 | 이스케이프 | 유효 |
|---|---|---|---|
| 머리·구분·문 1·문 4·문 6·문 7 행 | 7 | 0 | **7** |
| **문 2 행**(`:104`) | 8 | **1** | **7** |
| **문 3 행**(`:105`) | **15** | 2 | **13** |

**그 행만 유효 파이프가 다르다** — 다른 행이 전부 7 인 표에서 13 이므로 렌더가 그 행에 칸을 더 만든다.

*판정: 단정은 참이고 갈린 것은 렌더다.* 그 행이 말하는 것(`Dockerfile:5` 의 모양 · 두 목록의 차집합 ·
`openpyxl` 이 운영 경로에서 import 된다)은 **오늘도 참**이고, 갈린 것은 그 문자열이 마크다운 표에서
**어떻게 그려지는가**다(§3-13 첫째 갈래의 첫 문장 — 묻는 것은 *"이 단정의 시점이 문서에 보이는가"* 다).

*고치지 않는 근거는 **소급 수정 금지 하나**다.* `docs/plans/` 는 첫머리(§0)에 HEAD 와 재현 방법을
못박은 **기록물**이고, HEAD 가 움직였다고 뒤늦게 갱신하지 않는다(CLAUDE.md §3-13 첫째 갈래 —
그 자리에서 도는 참조: `grep -n "뒤늦게 갱신하지 않는다" CLAUDE.md`). §M-2 의 2 · 7 · 8 · 9 행을
같은 근거로 세워 두고 이 자리만 다르게 처분하면 그 표가 무너진다.

*초판이 적었던 둘째 근거는 **철회한다** — 같은 표가 그것을 반박한다.* 초판은
*"이스케이프를 넣으면 그 자리의 코드 인용 문자열 자체가 바뀐다"* 라고 적었는데, **바로 위 줄**인
문 2 행(`:104`)이 코드 인용(`git grep -nIi "daemon\|docker\.sock" …`) 안에서 **이스케이프하고
그대로 렌더된다**(위 표의 둘째 줄: 총 8 · 이스케이프 1 · 유효 7). 즉 「이스케이프하면 인용이 바뀐다」는
**이 문서가 자기 이웃 줄에서 이미 반대로 하고 있다** — 그 근거를 이 마감이 새로 쓴 표들과 나란히
두면 더 약해지므로(그 표들도 이스케이프한다) 세우지 않는다. **근거는 소급 수정 금지 하나로 선다.**

*역방향 확인 — 이 판정이 무는 대가.* 소급 수정 금지는 **렌더까지 얼린다**: 그 표를 화면에서 읽는
사람은 문 3 행의 칸을 다른 행과 맞추지 못한다. 그 손해는 **0 이 아니다.** 이 저장소는 그 교환에서
기록물 쪽에 섰고(선례: 2026-09-04 `e2d306a` — 낡은 행 번호를 지우지 않고 「작성 시점 트리」로 표시했다),
**그래서 이 마감이 새로 쓴 표들은 그 자리에서 이스케이프한다** — 얼리는 것은 이미 적힌 값이지
앞으로 쓸 표가 아니다.

### M-6. **ⓓ 판정 — `README.md` 는 이 사이클이 고치지 않는다. §후속으로 남긴다.**

**물음**(오케스트레이터가 이 마감에 넘긴 것): §후속 **76** 의 조항(*"한 사이클이 **남의 소유 문구를
거짓으로 만들면**, 그 소유자의 커밋이 같은 사이클 안에 있어야 그 작업이 완료다"*, **ⓑ 인용 · 전체**)이
**「불완전」에도 걸리는가.**

**답: 걸리지 않는다. 방아쇠 **둘 다** 당겨지지 않고, 근거는 76 자신의 역방향 확인과 값이다.**

| 방아쇠 | 이 자리에서 | 값 (각 N=1) |
|---|---|---|
| **「남의 소유」** | **성립하지 않는다 — 그 파일은 소유가 없다.** 76 의 역방향 확인 ⑥ 이 그것을 이름으로 조항 **밖**에 둔다: *"**소유가 없는 자리는 이 조항 밖이다** — 그것은 §후속 19 이고 … 19 는 「고칠 사람이 없다」, 76 은 「고칠 사람이 있는데 칸이 없다」이고 처방이 다르다"*(**ⓑ 인용 · 발췌**) | `git grep -nI "README" ddf6bb9 -- buildtwin/CLAUDE.md` → 히트 하나이고 그것은 **`config/README.md`** 다(루트 `README.md` 가 아니다) |
| **「거짓으로 만든다」** | **성립하지 않는다 — 이 사이클이 그 문단을 거짓으로 만들지 않았다.** 그 문단은 **이 사이클 이전에도** 데몬을 말하지 않았다 | `git grep -nIi "daemon\|docker\.sock" a03f13d -- buildtwin/README.md` → **rc 1 = 히트 0**; 같은 명령 `ddf6bb9` → **rc 1 = 히트 0** |

**그리고 그 문단은 「전수」를 주장하지 않는다.** `README.md:45` 를 통째로 읽었다 — *"걸음을 건너뛰면:
**1** 이 없으면 … **3** 을 호스트의 `make seed` 로 대신하면 …"* 이고 **개수도 「전부」도 적지 않는다.**
그래서 ADR §8 8행의 판정(*"거짓이 아니라 불완전"*)이 그대로 참이고, **§6-4 1 은 「사실과 다른 문구」에
걸리는데 불완전은 사실과 다르지 않다.**

**이 사이클이 그 자리에 실제로 무엇을 했는가**: 문 2 의 「저장소 안내 0」은 **닫혔다** — `make dev` 가
실패 **앞에서** 데몬을 이름으로 말한다(저장소 `Makefile`, `65ac781`). ADR §2-1 항 3 이 요구하는 것은
*"저장소가 **그 앞 또는 옆에** 자기 문자열을 둔다"* 이고 **그 요구는 실행 자리에서 채워졌다** — 같은
말을 모든 문서가 되풀이하라는 요구가 아니다.

**그러므로 처분은 §후속이다.** 소유가 없는 자리 둘(`README.md` 의 그 문단 · `docker-compose.yml` 의
`web:` 머리 주석 — §8 5행, 둘 다 「불완전·낡음」)을 **§후속 83** 으로 함께 열고, 그 안에 **「76 이
「불완전」에도 걸리는가」라는 조항 물음 자체**를 적는다. 그 물음을 이 마감이 **혼자** 답해 조항을 넓히지
않는 근거: 76 은 아직 CLAUDE.md 밖이고(승격은 §6-5 대기열의 순서 안, 72·74·76 을 한 조항으로),
**관측 없이 조항을 넓히면 그것이 §6-3 이 경고하는 「축을 넓히면 무주공산이 자리를 옮긴다」** 다.

*역방향 확인 — 이 판정이 놓치는 것.* ⓐ **사용자는 `README.md` 를 먼저 읽는다.** 문 2 의 안내가
`Makefile` 에만 있으면, **명령을 치기 전에** 그 문을 아는 경로는 없다 — 이 판정은 「실행 자리에서
보인다」로 서 있고 **「치기 전에 보인다」 축은 열린 채**다(§후속 83 이 그 축을 싣는다). ⓑ 표본은
**이 자리 하나**다 — 「불완전」이 76 밖이라는 이 판정을 다음 사이클이 다른 자리에서 다시 물어야 한다.

### M-7. **인계값 이관** — 커밋 본문에만 있던 값을 이 문서로 옮긴다 (§후속 **62**)

§후속 62 의 조항: **근거 값은 커밋 본문을 정본으로 삼지 않는다.** 아래는 이 사이클의 커밋 본문에만
있던 값이고, **이 표가 그것을 이 문서로 옮긴다 — 각 행에 출처 커밋을 적는다.** *"내가 다시 쟀는가"*
칸이 **아니오**인 값은 **옮겨 적은 값**이고, 그 사실이 그 값의 무게다.

| 표시 | 값 | 출처(커밋 본문) | 내가 다시 쟀는가 | 옮긴 자리 |
|---|---|---|---|---|
| **ⓐ** | **빌드를 돌리지 못했다** — `docker info` **rc 1** · `ip_forward` **0**. 그래서 작업 1 이 보인 것은 「토큰이 없다」와 「이름을 두 번 적지 않는다」뿐이고 **`docker compose up --build` 통과는 이 사이클의 누구도 재지 않았다**(계획 §리스크 1 이 그대로 산다) | `c82cf47` | **데몬 상태는 다시 쟀다**(rc 1 · 0). **빌드는 나도 못 돌렸다** | §확인하지 않은 것 **72**(열림) + 신규 **79** |
| **ⓐ′** | `pyproject.toml` 하나만 있는 디렉터리에서 `pip install -e . --no-deps --no-build-isolation` 이 **rc 0** 이다(scratchpad, `--system-site-packages` venv, python 3.11.15 · setuptools 68.1.2 · pip 24.0). **이것은 이미지가 아니고 의존성 해결을 재지 않았다** | `c82cf47` | **아니오** | 신규 **79** 의 한정 |
| **ⓑ** | 프록시 탐침 넷: dev **502 · 404바이트**(닫힌 포트) · dev **502 · 445바이트**(없는 호스트, `ENOTFOUND`) · preview(옛 설정) **500 · 0바이트** · `c82cf47` 트리 dev **500 · 0바이트**. 방법은 호스트 vite + `curl`(포트 반납, 데몬 0회) | `6ac7a19`(넷) · `65ac781`(그중 셋을 **qa 가 다시 쟀다** + 새 설정 **502 · 404바이트**) | **아니오** — 이 마감은 서버를 띄우지 않았다 | §확인하지 않은 것 **73**(**닫는다** — 값이 이 문서에 남았다) + 신규 **81**(그 값을 붙드는 게이트는 없다) |
| **ⓒ①** | 데몬 없이 `make dev` → **rc 2**(계획은 rc 1 이라 적었다). `docker compose` 자신은 **rc 1** 이고 **make 가 그것을 2 로 감싼다** | `65ac781` | **예** — 재현했다(M-2 9행). stderr 의 `make: *** [Makefile:40: dev] Error 1` 이 기전을 싣는다 | M-2 9행(정본) |
| **ⓒ②** | ADR §2-2 (다)의 「없으면」을 「**닿지 못하면**(꺼져 있거나 소켓에 권한이 없으면)」으로 넓혔다 — 좁게 두면 **소켓 권한 거부**가 조건절 밖으로 샌다. **권한 거부의 실제 문구는 재지 못했다** | `65ac781` | 저장소 문안은 읽었다. **권한 거부 문구는 나도 못 쟀다**(데몬이 없다) | M-2 11행 + **ADR (다)의 마감 정정 각주**(§M-5) + §확인하지 않은 것 신규 **80** |
| **ⓒ③** | 문 2 stderr 의 앞머리 `unable to get image '…'` 는 **프로젝트 이름에 따라 갈린다**(`minio/minio:latest` ↔ `door2-api`) — ADR 이 그것을 조각에서 뺀 판단이 값으로 확인됐다 | `65ac781`(사본 실행) · 계획 §1-c(minio) | **아니오**(공통 조각만 다시 쟀다) | M-2 10행 |
| **ⓓ** | `README.md` 판정 — `git grep -nIi "daemon\|docker\.sock" 6ac7a19 -- buildtwin/README.md` **히트 0**, §빠른 시작 ② 걸음 2 는 `make dev` 뿐, `:45` 문단은 걸음 1·3 만 말한다 | `65ac781` | **예** — `a03f13d`·`ddf6bb9` 두 트리에서 다시 쟀다(각 히트 0) | **§M-6**(판정) + §후속 신규 **83** |
| **ⓔ** | 작업 4 의 토큰/모양 판정과 변이값(M-A~M-E · W1~W6 · P1~P3) | `ddf6bb9` | **저장소 사실이다** — 세 파일의 머리 주석 표가 그 값을 싣는다(내가 읽었다). **커밋 본문에만 있는 것은 「심은 변이 13」이라는 산문의 수 하나**이고 그것이 갈렸다 | M-2 2·13·16·17행 |
| **ⓖ** | ADR §8 **9행**을 `frontend`·`qa` 두 담당이 읽고 **거짓 아님**으로 판정했다 | `6ac7a19` · `ddf6bb9` | **예** — 내가 그 파일을 열어 세 단정을 각각 판정했다 | **§M-4** |
| **ⓗ** | 자기-계수 실례 둘: ① `pgrep -af "vite preview"` 와 대괄호 없는 `grep` 이 **자기 명령줄을 히트로 냈다**(전 1 → 후 1 이 전부 측정 도구 자신) ② 그래서 작업 3·4 는 `ps -eo args --no-headers \| grep -c "^node .*vite preview"` 를 **별도 호출**로 썼다 | `c82cf47`(①) · `65ac781`·`ddf6bb9`(②) | **예** — 이 마감도 별도 호출로 0 → 0 을 얻었다 | §후속 **46**(실례가 는다, §M-8) |

### M-8. §후속 — 최종 상태 표 (§3 의 규약: **상태 표**로 옮긴다)

**정본**은 계획 0015 의 §후속 + §M-8 + §M-12 + §M-13-4 이고(트리 `a03f13d`), 이 계획 §후속 이
그것을 상태 표로 받았다. **아래는 이 사이클이 상태를 바꾸는 항목만** 적는다 — 자구는 고치지 않는다.
**한 줄 문안은 발췌이므로 항목을 닫으려면 정본을 연다**(§3 역방향 확인 ⓐ).

| # | 한 줄 문안(발췌) | 소유 | 이 사이클 뒤 상태 |
|---|---|---|---|
| 3 | `client.ts:12` 의 TODO — 코드 카탈로그의 단일 소스 | qa | **열림. 닫지 않았다**(계획대로) — `grep -n "TODO(round-6" apps/web/src/api/client.ts` → **`:12`** 그대로(N=1) |
| 19 | 소유가 §2 에 없는 파일들 | architect | **열림. 실례가 넷으로 는다** — `Dockerfile` · `pyproject.toml`(이 계획이 더했다) · `README.md` · `docker-compose.yml` 의 `web:` 머리 주석(§M-6) |
| 46 | 환경 탐침의 「도구 부재」·자기-계수 | architect + qa | **열림. 실례 둘 더**(§M-7 ⓗ). 세는 법의 정본은 **별도 호출 + 대괄호**이고 이 마감이 그것으로 0 → 0 을 얻었다 |
| 62 | 근거 값의 정본은 커밋 본문이 아니다 | architect | **열림. 이 마감이 그 조항을 두 번 이행했다** — §M-7 이 이 사이클의 커밋 본문 값을 출처와 함께 옮겼고, 심사 뒤 갱신이 **저장소 밖 인계값**(R1·R2 의 실측)을 근거로 쓸 때마다 **다시 쟀거나 출처를 표시했다**(§M-11 의 「내가 다시 쟀는가」 칸) |
| 65 | 기록물의 `git` 명령은 트리 인자를 **명령 안에**, `--all` 을 뺀다 | architect | **열림. 준수 사례가 는다**(계획 0016 · ADR 0020 · 이 마감). **예외 하나가 그 자리에 기록됐다**(출처: `c82cf47` 본문): 그 커밋은 **자기 커밋 해시가 아직 없어** 한 자리에서 트리 인자를 해시로 적지 못했고 **그 이유를 그 자리에 적었다** |
| 72 | 관측 가능성을 없애는 커밋이 그 자리에서 그것을 붙든다 | qa + architect | **닫힌 채**(계획 0015). 이 사이클이 다시 태우지 않았다 |
| **74** | ADR §8 의 각 행이 완료 조건 상자에 이름으로 · 마감이 **배정**을 표로 대조 | architect | **열림 — 닫지 않는다.** §M-3 이 대조 표를 태웠으나 **조항 후보 첫째가 이 사이클에서 깨졌다**(9행이 상자 밖). **표본이 2 로 늘고**, 둘째 표본에서 **어긋난 행이 하나** 났다. **심사 0016 뒤에도 그대로다** — ADR §8 9행이 상자 밖이었다는 **사실이 바뀌지 않았기 때문**이고(§M-11), R1·R2 는 그 행이 아니라 다른 자리에서 났다 |
| **76** | 남의 소유 문구를 거짓으로 만들면 그 소유자의 커밋이 같은 사이클 안에 | architect + 각 소유 | **열림(승격 전). 첫 시험이 났고 통과했다** — ⓧ(`frontend` 가 `qa` 소유 `conftest.py` 의 주석을 거짓으로 만들었다 → `65ac781` 이 같은 사이클에서 고쳤다, §M-3 4행). **조항 밖으로 판정한 자리도 값으로 하나 났다**(§M-6) |
| 77 | compose 에 준비 상태 계약이 없다 | 배정 미정 | **열림**(이 사이클이 닫지 않기로 한 것, §1-f) |
| 78 | 로그인 화면이 「계정이 없다」를 말하는가 | frontend + api | **열림** |
| 79 | 「실패를 삼키는 모양」의 전수를 경로 밖으로 | architect + qa | **열림. 실례가 하나 났고 표기 집합이 넓어진다** — 아래 79-1 |
| 80 | 이월을 「상태 표」로 바꾼 규약의 표본이 1 | architect | **열림. 표본이 2 가 된다**(이 표가 둘째다). **그 표가 갈림을 감췄는가 → 감추지 않았다**: 이 사이클에서 놓친 것은 표가 아니라 **ADR 이 상자 밖에 남긴 행**이었고(§M-3 9행), 그것은 표의 형식이 아니라 **상자의 생성 기준**의 문제다 |

**79-1. 79 에 실례를 더한다 — 새 번호를 열지 않는다.** 심사 0016 R1 이 드러낸 자리는
**「오늘 손해가 0 인데 참인 근거가 배선이 아니라 우연인 자리」** 다: `f3eeeeb` 전의 핸들러는 `대상:` 에
**모듈 상수**를 실었고, 옵션을 펼쳐 대상만 덮는 호출자가 있는데도 그 문장이 참이었던 이유는 **두
호출자가 같은 환경을 읽어 우연히 같은 값을 냈기 때문**이다(오늘 저장소에서 그 둘은 여전히 같다 —
ADR 0020 §2-2 (가) 마감 정정의 *한정*). **그리고 게이트가 두 구현을 가르지 못하면 그 우연은 관측되지
않는다**: 옛 게이트에서 옛 구현을 되살리면 **옛 303 은 전부 초록**이었다(출처: `a70c495` 커밋 본문의
X1 표 — **저장소 밖 인계값이고 이 갱신은 다시 재지 않았다**; 그 값은 지금 저장소 안에도 있다 —
`apps/web/src/api/apiProxyFailure.test.ts` 머리의 변이 표 X1 행, 내가 읽었다).

*새 번호를 열지 않는 근거.* ⓐ **규칙은 이미 CLAUDE.md 에 있다** — §6-2 1(*"이 단언의 기대값을,
결함 있는 코드가 그대로 만족하는가?"*, 그 자리에서 도는 참조:
`grep -n "결함 있는 코드가 그대로" CLAUDE.md`). ⓑ **자리는 이미 79 다** — 79 의 표기 집합은
`||` · 빈 `except` · **조용한 기본값 폴백** 이고, R1 의 결함은 그 마지막 것이다(없는 대상을 모듈 상수로
대신한다 = ADR 0020 §2-1 항 1 의 *"실패한 값을 조용한 기본값으로 대신하는 자리"*). ⓒ **처분도 79 의
것이다** — 남은 일은 그 모양을 **저장소 루트에서 세는 전수**이고, 그것이 79 의 닫힘 조건이다.
CLAUDE.md §6-1 이 같은 자리에서 같은 판단을 적는다(*"새 항목을 만들지 않는다: 규칙은 §6-4 1 이,
자리는 §6-1 이 이미 갖고 있다"*).

*그러나 79 의 표기 집합에 **넷째 모양**을 더한다*(§6-1 ② — 기준이 놓치는 것을 목록 옆에 적는다):
**「두 자리가 오늘 같은 값을 내서 참인 단정」**. 이것은 폴백이 **아닌** 자리에서도 난다 —
`PREVIEW_CONFIG` 는 폴백을 두지 않고 `resolveApiProxyTarget()` 을 **다시 부를** 뿐인데, dev 갈래의
대상과 그 값이 같은 것은 **배선이 아니라 같은 환경을 읽는다는 사실**이다. *한정*: 이 넷째 모양은
`||`·`except` 처럼 **문자열로 셀 수 있는 토큰이 없다** — 세는 법을 79 가 정해야 하고 이 갱신은
정하지 않았다. *관측*: **1회**(R1).

**신규(이 마감이 연다):**

81. **`apps/web/vite.proxy-target.test.ts:50-52` 의 「미지」가 낡는다**(소유 **`frontend`**). 그 문장의
    세 단정은 **전부 참**이지만(§M-4), 그것이 싣는 미지는 이제 **컨테이너 축과 렌더 축 둘**로 줄었다 —
    호스트 축의 답은 값으로 정해졌고(dev·preview 둘 다 **502 · 404바이트**, §M-7 ⓑ) 저장소가 문자열
    축을 붙든다. *묻는 것*: 그 절을 **오늘 무엇이 미지인지**로 좁혀 다시 쓸 것인가. *닫힘의 조건*:
    `frontend` 가 그 절을 현재형 사실로 바꾸고 **개수를 세지 않으며**, 그 자리가 기대는 **부재**를
    적을 때. *한정*: 주석이라 **어떤 게이트도 보지 않는다**(§후속 75 와 같은 종류, 같은 파일 옆자리).

82. **ADR §8 표의 행이 적는 「소유」는 74 의 대조 밖이다**(architect). 74 의 조항 후보는 **행 ↔ 상자**의
    대조를 요구하는데, **그 행이 적는 소유가 틀리면 상자에 넣어도 배정이 틀린다.** 실례(§M-5): 9행이
    CLAUDE.md §2 를 잘못 인용해 소유를 「`frontend`·`qa` 공동」이라 적었고, §2 문자로는 **`frontend`**
    단독이다. *조항 후보*: **「ADR·계획이 소유를 적을 때 §2 의 문자열을 **그대로** 인용하고, 마감의
    배정 대조는 그 인용 자체를 §2 와 맞댄다」**. *닫힘의 조건*: 그 대조가 한 사이클의 마감에서 값으로
    돌고, 그 사이클이 인용 오류를 **0 으로** 보일 때. *한정*: **관측 1회**.

83. **소유 없는 두 자리가 「불완전·낡음」으로 남는다 — 그리고 76 이 「불완전」에 걸리는가는 열린 물음이다**
    (architect + §후속 19). 자리 둘: `README.md:45` 「걸음을 건너뛰면」 문단(데몬을 말하지 않는다,
    히트 0 — `a03f13d`·`ddf6bb9` 두 트리) · `docker-compose.yml` 의 `web:` 머리 주석(대상에 닿지 못할
    때를 말하지 않는다). *묻는 것 둘*: ⓐ 그 둘을 **누가** 고치는가(19 의 축) ⓑ 76 의 방아쇠를
    **「거짓으로 만든다」에서 「불완전하게 남긴다」로 넓힐 것인가**(§M-6 이 이 사이클에서는 **넓히지
    않기로** 판정했다 — 근거는 76 자신의 역방향 확인 ⑥ 과 「축을 넓히면 무주공산이 자리를 옮긴다」).
    *이 항목이 싣는 축 하나 더*: **사용자는 명령을 치기 전에 `README.md` 를 읽는다** — 문 2 의 안내가
    실행 자리(`Makefile`)에만 있으면 「치기 전에 보인다」 축은 비어 있다(§M-6 역방향 확인 ⓐ).
    *닫힘의 조건*: 19 가 그 둘의 소유를 정하고, ⓑ 가 **관측 2회 이상**으로 판정될 때.

84. **산문의 개수 금지는 커밋 본문에도 걸린다**(architect + 각 소유). 실측(§M-2 13행): `ddf6bb9` 본문이
    *"심은 변이 13"* 이라 적었는데 **저장소 세 파일의 열거는 14** 다(`W1` 은 두 표에 나오지만 한 번
    심었다). **저장소는 수를 적지 않아 거짓이 아니고**, 갈린 것은 **커밋 본문의 산문**이다 — 그리고
    그 수는 **다음 사이클로 인계될 때 그대로 옮겨 다닌다**(이 마감이 받은 인계 문안이 같은 수를 실었다).
    §6-1 9회차가 적는 문장은 **열거는 길이가 곧 개수다** 이고(**ⓑ 인용 · 발췌**; 그 자리에서 도는
    참조: `grep -n "열거는 길이가 곧 개수다" CLAUDE.md`), 그 처방은 **문서에는 걸려 있고 커밋 본문에는
    걸려 있지 않다.** *조항 후보*: **「커밋 본문도 산문이다 — 열거를 싣는 자리에 그
    길이를 적지 않는다」**. *닫힘의 조건*: 그 조항이 CLAUDE.md §6-1 로 승격되거나(대기열 순서 안),
    두 사이클이 그것을 지켜 인계에서 수가 사라질 때. *한정*: **관측 1회**.

### M-9. §확인하지 않은 것 — 최종 상태 표

**번호를 잇는다**(정본은 계획 0015 의 §확인하지 않은 것 + 이 계획 §확인하지 않은 것 72~78).

| # | 한 줄 문안(발췌) | 이 사이클 뒤 상태 |
|---|---|---|
| 72 | `\|\|` 의 앞 절이 실제로 실패하는가 | **열린 채.** 이 사이클도 빌드를 돌리지 못했다(§M-7 ⓐ) — 데몬 없음(rc 1) · `ip_forward` 0. **[재야 한다 → 사용자 또는 CI]** |
| **73** | vite 가 프록시 실패에서 실제로 무엇을 돌려주는가 | **닫는다.** 값이 났고(§M-7 ⓑ) **이 문서에 남았다** — 그 항목의 닫힘 조건이 *"그때 값을 저장소에 남긴다"* 였다. **한정 둘**: ⓐ 재현 방법은 **호스트 vite + `curl`** 이고 컨테이너 축이 아니다 ⓑ 그 **status·바이트를 붙드는 게이트는 없다** → 신규 **81** |
| 74 | 문 7 의 화면(로그인 401 의 렌더) | **열린 채.** 이 사이클의 탐침은 전부 `curl` 이다 |
| 75 | 재실행 경로(`docker compose down -v` 뒤 · 모델이 바뀐 뒤) | **열린 채.** `make dev` 의 선행 안내가 그 상황을 말하는데 **그것이 맞는지는 여전히 재지 않았다** — 이 마감이 잰 것은 그 안내가 **stdout 에 있다**와 **rc 2** 뿐이다 |
| 76 | `--network host` 갈래 | **열린 채** |
| 77 | 관측자 축이 넷인 것의 근거 | **열린 채.** 다섯째 축(CI · 다른 개발자의 재현)에서만 조용한 자리는 이 사이클도 세지 않았다 |
| 78 | 근거 표면 1,090자 = 7.20% | **열린 채.** **CLAUDE.md 를 만지지 않았다**(§M-0) — 분할을 다시 재지 않았다 |

**신규(이 마감이 연다):**

79. **[재지 않았다] `c82cf47` 뒤의 `Dockerfile` 로 `docker compose up --build` 가 서는가.** 72 는
    *"`||` 의 앞 절이 실패하는가"* 를 묻고, 이것은 **폴백을 지운 뒤의 빌드가 통과하는가**를 묻는다 —
    **다른 물음이고 아무도 재지 않았다**(§M-7 ⓐ). 이 사이클이 아는 것은 ⓘ 토큰이 없다 ⓙ 이름을 두 번
    적지 않는다 ⓚ `pyproject.toml` 하나만 있는 디렉터리에서 `-e . --no-deps` 가 rc 0 이다(ⓐ′ —
    **이미지가 아니고 의존성 해결을 재지 않았다**) 셋뿐이다. **계획 §리스크 1 이 그대로 산다: 깨지면
    소리내어 깨지고 그 위험은 사용자에게 간다.** **[재야 한다 → 사용자 또는 CI · 데몬과 컨테이너
    네트워킹이 있는 자리]**
80. **[재지 않았다] docker 소켓 **권한 거부**의 실제 stderr 문구.** `Makefile` 안내의 조건절은 그
    갈래를 담지만(「소켓에 권한이 없으면」) **조각을 단정하지 않고** *"같은 조각이 나오면 그 문이다"* 로
    쓴다 — 그 문구를 이 환경에서 잴 수 없기 때문이다(데몬이 없다). **[재야 한다 → qa · 데몬이 있는
    자리에서]**
81. **[재지 않았다 · 저장소가 붙들지 않는다] 프록시 실패의 status·본문 바이트를 붙드는 게이트가 없다.**
    저장소가 붙드는 것은 **순수 함수의 문자열 축**(`apiProxyFailureBody`)과 **설정 객체의 대조**
    (`PREVIEW_CONFIG` ↔ `server.proxy`)이고, **실제 응답의 502·404바이트는 어떤 게이트도 보지 않는다.**
    `make e2e` 는 preview 갈래를 지나지만 (가)를 **태우지 않는다**(ADR 0020 §7-5 가 그 자리를 열어
    뒀다). **[재야 한다 → qa · e2e 가 그 응답을 태우게 만들 때]**

### M-10. 이 마감이 **반증한 것 · 재지 않은 것** (명시한다)

**반증한 것 넷**(전부 값이다):
- 계획 작업 3 조건 1 의 **rc 1** → 실측 **rc 2**(M-2 9행, 내가 재현).
- 계획 작업 2-2 의 문안 **「본문이 비어 있다」** → 코드가 거짓으로 만든다(M-2 7행).
- 내 두 문서의 **CLAUDE.md §2 인용** → §M-5 가 고쳤다.
- `ddf6bb9` 본문의 **「심은 변이 13」** → 저장소의 열거는 **14**(M-2 13행).

**재지 않은 것**(누가 재야 하는지와 함께 — *"놓칠 수 있다"* 는 커버리지가 아니다):
- **빌드**(§확인하지 않은 것 72·79) · **렌더**(74) · **컨테이너 안의 프록시**(M-4) ·
  **권한 거부 문구**(80) · **실제 응답을 붙드는 게이트**(81).
- **프록시 탐침 넷을 내가 다시 재지 않았다**(§M-7 ⓑ) — 옮겨 적은 값이고 출처를 표에 적었다.
- **이 마감은 변이를 하나도 심지 않았다**(자원 0). 작업 4 의 변이 표를 **재현하지 않았고**, 값의 정본은
  저장소 세 파일의 머리 주석이다(§후속 62 의 축에서는 그것이 옳은 자리다).

### M-11. 심사 0016 **처리 표** — 무엇이 닫혔고 무엇이 남았는가 (갱신, 2026-09-08)

**이 절의 트리 · 자원.** 트리 **`a70c495`**(작업 트리 `/home/user/Bim/buildtwin`, 저장소 루트
`/home/user/Bim`), 값은 **2026-09-08 17:39~17:47 UTC 한 세션 안**이고 따로 적지 않으면 **N=1**.
자원은 §M-1-2 가 값으로 적는다(심은 변이 **X5 하나**, 사본 `diff` 로 적용 확인 · 사본으로 원복 ·
루트 `git status --porcelain` 빈 출력 · 데몬 0회 · `.env` 0). **명령은 트리 인자를 명령 안에 담고
`--all` 을 쓰지 않는다**(§후속 **65** 확장 항 ㉡). **저장소 밖 인계값은 근거로 쓸 때 다시 재거나
출처를 표시했다**(§후속 **62**) — 아래 「내가 다시 쟀는가」 칸이 그것이다.

| 지적 | 요구한 것 | 이 갱신이 한 것 | 값 | 내가 다시 쟀는가 | 소유 | 상태 |
|---|---|---|---|---|---|---|
| **R1**(블로킹) | 실패 본문의 `대상:` 이 **프록시가 받은 대상**을 말하고, 게이트가 **두 구현을 가른다** | **이 갱신 밖에서 닫혔다** — `f3eeeeb`(frontend) · `a70c495`(qa) | 게이트 재측정 §M-1-2(`make test` rc 0 · invariants **118** · vitest **310**). 변이 표 X1~X4 는 **저장소 안에** 있다(`apps/web/src/api/apiProxyFailure.test.ts` 머리, 내가 읽었다) | **게이트는 예. 커밋 본문의 변이 실행값은 아니오**(저장소 문서가 같은 값을 싣는다) | frontend · qa | **닫힘** |
| **R2**(기록 요구) | `make dev` 의 **선행 안내**를 게이트가 읽는다 | **이 갱신 밖에서 닫혔다** — `a70c495` 의 `tests/invariants/test_the_first_run_names_its_doors.py` | `pytest tests/invariants/test_the_first_run_names_its_doors.py -q` → **3 passed**; invariants 115 → **118**(§M-1-2 축 1) | **예** | qa | **닫힘** |
| **R3**(기록 요구) | §M-1 의 「어느 것이 바뀌는지」가 빠뜨린 **e2e 축**을 채운다 | **§M-1-2 축 3 이 그 칸이다** — 바뀐 것이 단언이 아니라 **12 가 지나는 배선**임을 이름과 값으로 | X5(옛 배선 되심기): `make e2e` **12 passed · rc 0** ↔ 같은 트리 `npx vitest run` **2 failed, 308 passed**. 음성 대조군은 **12 passed** ↔ **310 passed** | **예**(변이도 내가 심고 원복했다). preview 응답의 **500·0 → 502·404** 는 **아니오** — `65ac781` 본문의 값이고 §M-7 ⓑ 가 출처를 진다 | architect | **닫힘** |
| **N1**(비블로킹) | §M-5 의 렌더 결함 **판정은 맞지만 근거가 약하다** — 정리한다 | **§M-5 의 근거를 다시 세웠다** — 둘째 근거를 **철회**하고 「소급 수정 금지」 하나로 세웠다 | §1-c 의 유효 파이프: 정상 행 **7** · **문 2 행 7**(총 8 · 이스케이프 1 — 같은 표가 코드 인용 안에서 이스케이프한다) · **문 3 행 13**(총 15 · 이스케이프 2) | **예** | architect | **닫힘** |
| **frontend 가 넘긴 것** | **ⓘ 의 정의가 낡았다** — §3-13 첫째 갈래인가 §6-4 1 인가 | **자리마다 갈라 판정했다** → **§M-12**. ㉮(ADR §2-2 (가))만 고쳤다 | 전수는 §M-2 **19행**(`git grep -nI "resolveApiProxyTarget" a70c495 -- .` · `git grep -nI "대상 주소" a70c495 -- .` 의 히트를 읽었다) — ⓘ 를 **정의하는** 자리는 §M-12 가 ㉮㉯㉰㉱ 로 연다 | **예** | architect | **닫힘** |
| **qa 가 넘긴 것**(인계값 정정) | `git grep -nI "docker API at unix" f3eeeeb -- buildtwin/tests buildtwin/apps` 는 **히트 1**(0 이 아니다) — 마감이 그 값을 인용했으면 고친다 | **인용하지 않았음을 확인했다 — 고칠 자리가 없다** | 그 명령을 다시 돌렸다 → **히트 1**, 그것은 `tests/invariants/test_demo_stack_can_stand.py:44` 의 **머리 주석 산문**이다. 그리고 `grep -rn "docker API at unix" docs/` 의 히트를 **읽었다** — 이 마감이 **그 grep 의 히트 수를 인용하는 자리는 없다**(히트는 문 2 stderr **조각**을 인용하는 자리들이다: §1-c 문 2 행 · §M-0 · §M-2 10행) | **예** | architect | **고칠 것 없음** |

**닫지 않은 것 · 그대로 둔 것.**

- **§후속 74 를 닫지 않는다.** ADR §8 **9행이 완료 조건 상자 밖이었다**는 사실은 이 심사의 어느
  지적도 바꾸지 않는다 — 그 행이 밖이었던 것은 ⓧⓨⓩ 목록의 **생성 기준**(*「작업 2 가 만지는 파일의
  이웃」*)의 일이고(§M-3), 이 갱신은 그 기준을 다시 만들지 않았다.
- **§후속 76 의 첫 시험 통과도 그대로다.** 그 시험의 값은 §M-3 4행(ⓧ → `65ac781`)이고 이 갱신이
  다시 태우지 않았다.
- **§후속 82·83·84 · §확인하지 않은 것 79·80·81** — 이 갱신이 상태를 바꾸지 않는다.
- **CLAUDE.md 를 만지지 않았다**(각 N=1): `git log --oneline 757bc8f~1..a70c495 -- buildtwin/CLAUDE.md`
  → **빈 출력**, `git log -1 --format='%h' a70c495 -- buildtwin/CLAUDE.md` → **`15a063f`**(이 사이클
  밖의 커밋). 승격은 §6-5 대기열의 순서 안이다(§2).
- **바닥값 무접촉**(§M-1-2 의 값).

**이 갱신이 반증한 것.**

- §M-1 초판의 *"기존 단언은 하나도 바뀌지 않았다"* — **단언 집합에 대해서는 참이지만 「어느 것이
  바뀌는지」의 답으로는 부족하다.** e2e 축에서 바뀐 것은 단언이 아니라 **경로**이고, 그 축은 수로도
  이름으로도 보이지 않는다(§M-1-2 축 3, X5 값).
- §M-5 초판의 둘째 근거 *"이스케이프를 넣으면 그 자리의 코드 인용 문자열 자체가 바뀐다"* — **같은
  표의 이웃 줄(문 2 행)이 코드 인용 안에서 이스케이프하고 그대로 렌더된다**(§M-5 의 값).

**이 갱신이 재지 않은 것**(누가 재야 하는지와 함께).

- **R1·R2 의 커밋 본문 값** — 프록시 응답의 status·바이트, 변이 표의 실행값. 옮겨 적은 자리마다
  **출처를 표시**했다(§M-1-2 축 3 · §M-8 79-1). **[재야 한다 → 그 값을 게이트로 만들 때 · qa,
  §확인하지 않은 것 81]**
- **빌드 · 렌더 · 컨테이너 안의 프록시 · 권한 거부 문구** — §M-10 그대로다.
- **`make e2e` 의 각 테스트가 무엇을 지나는지** — 12 가 「같은 12」인 것은 **수집된 이름**으로만
  답했다(§M-1-2 축 3 역방향 ⓑ).

### M-12. **ⓘ 의 정의가 낡은 자리** — 자리마다 갈라서 판정한다 (frontend 가 넘긴 것)

**물음.** `f3eeeeb` 뒤 ⓘ(`대상:` 슬롯)의 정본은 **`options.target`**(프록시가 실제로 받은 대상)인데,
ADR 0020 과 계획 0016 은 아직 ⓘ 를 **`resolveApiProxyTarget()` 의 값**으로 정의한다.
**§3-13 첫째 갈래**(기록물은 소급 갱신하지 않는다)인가, **§6-4 1**(*"사실과 다른 문구는 그것을 만든
사이클이 고친다"*)인가.

**경계는 「ADR ↔ 계획」이 아니다 — 한 문서 안에 두 종류가 산다.** 가르는 물음은 하나다:
**그 문장을 다음 사람이 「읽고 그대로 만드는가」(처방·계약)인가, 「그때 그 트리를 잰 값」인가.**
처방이면 고치지 않을 때 **다음 구현이 같은 거짓을 갖고 태어나고**(§6-4 의 표제 그 자체), 잰 값이면
고칠 때 **그 문서가 보고하는 실측과 인용 트리가 어긋난다**(§3-13 첫째 갈래).
**각 자리의 종류는 그 절 자신의 머리말이 답한다** — 아래 넷째 칸이 그 인용이다.

| # | 자리 | 그 자리가 쓰는 것 | 종류 — **근거는 그 절 자신의 머리말** | 오늘 참인가 | 판정 · 처분 |
|---|---|---|---|---|---|
| **㉮** | ADR 0020 §2-2 **(가)** 치환자 표 `{target}` 행 | *"`resolveApiProxyTarget()` 이 돌려준 값 **그대로**"*(**ⓑ 인용 · 전체**) | **처방(계약)** — §2-2 머리말: *"결정 2 — 문구 원본(작업 2 가 **ⓑ 인용 · 전체**로 가져다 쓴다)"* | **처방으로서 거짓이다.** 그대로 만들면 핸들러가 모듈 상수를 닫아 싣고, 옵션을 펼쳐 대상만 덮는 호출자에서 **본문의 두 줄이 서로를 반박한다**(R1 이 잡은 그 결함) | **§6-4 1 이 걸린다 → 고쳤다.** 그 행을 `options.target` 으로 바꾸고 **마감 정정 + 초판 문안 표시**를 달았다(§M-5 가 (다)에 단 것과 같은 형식) |
| **㉯** | ADR 0020 §2-3 능력 표 **㉡ 프록시** 행의 ⓘ 칸 | *"**안다** — `resolveApiProxyTarget()`"* | **그때 잰 값** — §2-3 머리말: *"근거는 값이다 — 각 자리가 슬롯을 채울 수 있는지를 **코드로 판정했다**(각 N=1)"* | **참이다.** 판정(*"안다"*)은 지금도 참이다 — 그 함수는 `apps/web/vite.config.ts` 에서 여전히 모듈 상수 `apiProxyTarget` 을 정하고(`:12` `const apiProxyTarget = resolveApiProxyTarget();`), 그 상수가 `server.proxy["/api"].target` 이다(코드 인용, N=1). **핸들러가 그것을 읽지 않을 뿐이다** | **§3-13 첫째 갈래 → 고치지 않는다.** 거짓이 아니고, 그 표는 계약이 아니라 그 트리를 읽은 **판정**이다 |
| **㉰** | ADR 0020 §1-2 ⓑ 의 코드 인용(`target: resolveApiProxyTarget(),`) | `603ed5d` 의 `vite.config.ts` proxy 블록 | **기록물 · 코드 인용** — §1-2 제목: *"오늘의 기전 — 코드 인용(전부 `603ed5d` 에서 다시 읽었다, 각 N=1)"* | **참이다.** `git show 603ed5d:buildtwin/apps/web/vite.config.ts` 의 `:16-23` 과 맞댔다(N=1) — 코드 줄은 글자 그대로 같고, **인용이 싣지 않은 것은 그 블록 안의 주석**(`:18-19`)이다. 그 인용이 지는 단정(*"키가 `target`·`changeOrigin` 둘뿐이고 에러 핸들러가 없다"*)은 그 두 줄과 무관하게 참이다 | **§3-13 첫째 갈래 → 고치지 않는다** |
| **㉱** | 계획 0016 §완료 조건 **작업 2 조건 1** | *"ⓘ **프록시 대상 주소**(`resolveApiProxyTarget()` 의 값)"* | **그 사이클의 배정** — 이미 실행됐고(`6ac7a19` → `f3eeeeb`) 그 결과는 마감이 진다 | **처방으로서 거짓이었다** — ㉮ 와 같은 문장이다 | **§3-13 첫째 갈래 → 고치지 않고 §M-2 19행이 정본을 가리킨다.** 근거는 **이 마감의 선례**: 같은 상자의 조건 2(*「본문이 비어 있다」*)도 코드가 거짓으로 만들었고 §M-2 **7행**이 *"계획은 기록물이라 갱신하지 않는다"* 로 처분했다 — 한 상자의 두 조건을 다르게 처분할 근거가 없다 |

**그러면 §M-5 는 왜 계획을 고쳤나 — 축이 하나 더 있다.** §M-5 가 계획 두 자리를 고친 것은 그것이
**살아 있는 정본(CLAUDE.md §2)을 잘못 인용**했기 때문이고, 그 인용은 **쓰인 그 순간에 이미 거짓**이었다
(§2 는 그때도 `apps/web/src/**/*.test.ts(x)` 였다). ㉱ 는 다르다 — 쓰일 때 어떤 정본도 잘못 인용하지
않았고 **같은 사이클의 뒤 커밋이 낡게 만들었다.** 그래서 두 축을 함께 쓴다:

| 그 문장이 … | 처분 | 이 사이클의 자리 |
|---|---|---|
| **쓰일 때 이미 살아 있는 정본을 잘못 인용했다** | **고친다**(§6-4 1) — 문서의 종류를 가리지 않는다 | §M-5 의 셋(CLAUDE.md §2 오인용) |
| 쓰일 때 참이었고 뒤 실행이 낡게 만들었다 **+ 처방·계약이다** | **고친다**(§6-4 1) — 다음 구현자가 읽고 만든다 | **㉮** |
| 쓰일 때 참이었고 뒤 실행이 낡게 만들었다 **+ 그때 잰 값이다** | **고치지 않는다**(§3-13 첫째 갈래) — 마감의 표가 정본을 가리킨다 | **㉯ · ㉰ · ㉱**, 그리고 §M-2 의 2 · 7 · 8 · 9행 |

*역방향 확인 — 이 경계가 놓치는 것.*
ⓐ **「처방인가 잰 값인가」를 그 절 자신의 머리말로 판정했다** — **머리말이 없는 절에서는 이 축이
답을 내지 못한다.** 이번에 갈린 절들(§1-2 · §2-2 · §2-3)이 모두 머리말을 가졌던 것은 **결과이지 기준의
정당화가 아니다**(§6-1).
ⓑ **한 문장이 둘 다일 수 있다.** ㉮ 는 처방이면서 「그때 그 함수가 그 값을 준다」는 잰 값이기도 하다 —
이 판정은 **처방 쪽을 먼저 본다**(§6-4 의 표제가 그 순서를 정한다). 반대로 읽으면 ㉮ 가 ㉯ 와 같은
칸으로 떨어지고 **R1 이 고친 거짓이 계약에 남는다.**
ⓒ **오늘 손해는 0 이다.** 저장소의 두 호출자는 같은 값을 준다(dev 갈래는 모듈 상수, preview 갈래는
`PREVIEW_CONFIG` 가 **같은 함수를 다시 부른** 값) — 그래서 이 판정은 **값의 갈림이 아니라 처방의
거짓** 위에 서 있고, 그 자리가 §후속 **79-1** 이 여는 넷째 모양이다.
ⓓ **표본은 이 사이클 하나**다. 다음 사이클이 다른 자리에서 이 경계를 다시 물어야 한다.
