# 계획 0018 — 잡은 아직 **삼킬 수 있다**: rc 를 성공으로 바꾸는 자리와 그것을 붙드는 게이트

> **주제 한 줄.** 사이클 0017 이 「이미지를 짓는 rc 를 읽는 주체」를 CI 에 세웠다. **그런데 그 주체
> 자신이, 셸 안에서 rc 를 0 으로 만드는 편집에 대해 침묵한다**(`set +e` + 빌드 + `exit 0` → **4 passed**).
> 이 사이클은 **관측자를 하나 더 세우기 전에** 그 축을 붙든다.

---

## 0. 이 문서의 실측이 나온 자리 (재현 방법)

**HEAD `580d3f6`**(계획 0017 §N 을 담은 커밋의 **부모**가 아니라, 그 §N 을 여는 시점의 HEAD 다 —
이 계획과 §N 은 **한 커밋**으로 들어가므로 아래 값들은 전부 **그 커밋의 부모 트리** `580d3f6` 에서 잰
것이다). **작업 트리** `/home/user/Bim/buildtwin`, **저장소 루트 `/home/user/Bim`**.
이 문서를 여는 시점의 루트 `git status --porcelain` 은 **빈 출력**.

**명령의 못박음**(§후속 **65** ㉡ · §후속 **96**). 이 문서의 모든 `git` 명령은 **트리 인자를 명령 안에
담고 `--all` 을 쓰지 않는다**. **그리고 전수는 전부 저장소 루트에서 돌렸다** — 96 의 함정이 CWD 이기
때문이다(계획 0017 §M-6 의 곱 표). 트리 인자를 받지 못하는 명령(`make` · `pytest` · `grep` · `wc` ·
`ls` · `pgrep` · `python3`)은 전부 위 작업 트리에서 돌린 것이다.

**방법 · N · 표본 간격**(§후속 **34** · 27). 아래 값은 **2026-09-08 한 세션 안**(20:36~20:49 UTC,
`date -u` 로 앞뒤를 찍었다)에 잰 것이고, **따로 적지 않으면 N=1**, 서로 몇 분 간격이다.
**「없다」·「히트 0」·「죽었다」에도 N 을 적는다.**

**저장소 밖 값의 출처**(§후속 **62**). 이 문서가 싣는 저장소 밖 값은 **계획 0017 §N-2 의 이관표**에서
온다(원출처: 리뷰어가 읽은 `d7ad6f7` 잡 로그 본문). **커밋 본문도 그 표도 근거의 정본이 아니다** —
이 계획이 그 값에 기대는 자리는 §2 의 **비용 칸 하나**이고, 거기에 **「내가 재지 않았다」를 적는다**.

**자원**: **docker 데몬 0회**(`docker info` **rc 1** · `/proc/sys/net/ipv4/ip_forward` **0**, 각 N=1) ·
postgres 클러스터 0회 · 심은 변이 **0**(이 문서는 값을 읽기만 했다) · **저장소 두 자리에 `.env` 를
만들지 않았다**(`ls -a <각 디렉터리> | grep -c '^\.env$'` → **0 · 0**, 각 N=1).

---

## 1. 분석 — **rc 를 성공으로 바꾸는 자리**와 **그것을 붙드는 게이트**

### 1-a. 사이클 0017 이 남긴 자리

0017 은 「이미지를 짓는 자리가 저장소에 하나이고 그것은 사용자의 손이다」를 거짓으로 만들었다.
그 산출물은 잡 하나(`image`)와 불변식 둘이고, 넷째 실행까지 초록이다(계획 0017 §N-5, **저장소 밖**).

**그런데 같은 사이클이 그 관측자의 구멍을 값으로 냈다.** `test_ci_builds_the_image.py` 머리의 변이
표 **C7**: 짓는 스텝의 `run` 을 `set +e` + 빌드 + `exit 0` 으로 바꾸면 **4 passed**(= 무변화).
qa 가 심었고 **리뷰어가 재현했다**(계획 0017 §N-6 ㄱ).

**그 구멍의 이름은 「워크플로 `run:` 축」이고, 오늘 그 축에는 값만 있고 게이트가 없다.**
그것을 지는 것은 **§후속 79**(「실패를 삼키는 모양」의 전수)이고, **79 의 걸음 1 목록은 아직 0회**다.

### 1-b. 전수 — **「rc 를 성공으로 바꾸는 표기」** (§6-1 ①②③ · §53)

**① 생성 기준(이 목록의 한계가 곧 이 기준이다).** 「도구나 셸이 **실패를 성공으로 보고하게 만드는
표기**」의 **표기 집합**을 먼저 적고, 그 집합을 **저장소 루트에서 경로를 좁히지 않고** 훑는다.
**자기 커밋도 센다**(§53) — 아래 값은 이 계획 문서가 들어가기 **전**의 트리이고, 이 문서가 표기를
산문에 쓰므로 **다음 사이클이 같은 전수를 돌리면 이 문서가 히트로 나온다**.

**표기 집합**(§6-3 11회차 — 낱말이 아니라 **표기**를 적는다):
`|| true` · `|| :` · 셸 OR 일반(`||`) · `set +e` · `; exit 0` · `exit 0` · `2>/dev/null` ·
`continue-on-error` · `if:`(잡·스텝을 끄는) · Makefile 레시피의 `-` 접두 ·
테스트 선택(`--ignore` · `--deselect` · `-p no:` · `pytest.mark.skip*` · `xfail` · `--exit-zero`).

**② 실측**(전부 `git grep -nIF -- '<표기>' 580d3f6 -- .` 을 **저장소 루트**에서, 각 N=1):

| 표기 | 저장소 전체 | `/docs/` 밖 | `/docs/` 밖 히트가 **어디**인가 |
|---|---|---|---|
| `set +e` | **5** | **3** | 전부 **불변식 파일 머리의 산문·변이 표**(`test_ci_builds_the_image.py` 둘 · `test_the_image_installs_from_one_source.py` 하나) — **실행되는 자리는 0** |
| `\|\| true` | **1** | **1** | 같음(`test_the_image_installs_from_one_source.py:41`) — **실행되는 자리는 0** |
| `\|\| :` | **0** | **0** | — |
| `continue-on-error` | **12** | **8** | 워크플로 **2**(`: false` 하나 · 주석 하나) · 불변식 파일 **6**(상수·변이 표·산문) |
| `exit 0` | **50** | **2** | 둘 다 산문 |
| `2>/dev/null` | **5** | **0**(워크플로 안 **0**) | — |
| `if: always()` | **2** | **1** | `.github/workflows/buildtwin-ci.yml:130`(아티팩트 업로드) |
| Makefile `-` 접두 레시피 | — | **0** | `grep -c '^\t-' buildtwin/Makefile` → **0** |
| `pytest.mark.skip*` | **3** | **2** | 둘 다 **도구 부재** 조건이고 그 자리에 사유가 적혀 있다 |
| `xfail` | **2** | **1** | `.claude/agents/qa.md:80` — **금지 문구 자신** |
| `--ignore` / `--deselect` / `-p no:` | 5 / 23 / 2 | 2 / 0 / 0 | 전부 주석·docstring·기록물 |

**③ 워크플로 안만 다시 훑은 값**(`git grep -nIF -- '<표기>' 580d3f6 -- .github/workflows`, 각 N=1):
`|| true` **0** · `|| :` **0** · `set +e` **0** · `exit 0` **0** · `2>/dev/null` **0** · `;` **0** ·
`continue-on-error` **2**(`: false` 하나 + 주석 하나) · `if:` **5**(주석 하나 + `if: ${{ !cancelled() }}`
셋 + `if: always()` 하나).
`run: ` 줄: `buildtwin-ci.yml` **27** · `deploy-pages.yml` **2**, 그중 다중행(`run: |`) **1**.

**그러므로 오늘 이 저장소의 워크플로에는 rc 를 성공으로 바꾸는 표기가 실행되는 자리로 하나도 없다.**
**이 사이클이 만드는 것은 「고치는 게이트」가 아니라 「그 상태가 조용히 바뀌지 못하게 하는 게이트」다.**
그 필요의 근거는 이 저장소가 이미 값으로 갖고 있다 — `test_the_first_run_names_its_doors.py` 머리의
표(안내를 지워도 `123 passed`)와 `test_ci_builds_the_image.py` 머리의 「이 파일이 서기 전」 표
(`image` 잡을 통째로 지워도 `126 passed`).

**④ 블라인드 스팟 — 이 기준이 밀어내는 것**(§6-1 ②, 이름으로 적고 **태운다**):
- **ⓐ 표기가 아니라 기전으로만 삼키는 자리.** 실패해도 rc 0 을 내는 **래퍼 스크립트**(`scripts/` 밑),
  파이썬의 `except Exception: pass`(저장소 전체 히트 **39**, N=1 — 판정 경로인지 로깅인지 이 계획은
  가르지 않았다), 워크플로 `paths:` 축소(잡을 아예 안 부른다). **이 사이클은 표기 축만 닫는다.**
- **ⓑ `if:` 는 삼키지 않는다.** 스텝의 `if:` 가 거짓이면 그 스텝은 **건너뛰어지고**, 도는 스텝의 rc 는
  여전히 잡의 rc 다. 오늘의 넷(`!cancelled()` 셋 · `always()` 하나)은 **정당한 자리**이고 게이트가
  그것을 잡으면 거짓 양성이다. **다만 「짓는 스텝을 끄는 `if:`」는 다른 실패다** — 그것은 삼킴이 아니라
  **관측자의 소멸**이고, 이미 `test_ci_builds_the_image.py` 의 ⓚ 가 `image` 잡에 대해 진다(변이 C6).
- **ⓒ 두 번째 워크플로를 오늘 아무 게이트도 `run:` 축으로 읽지 않는다.** `deploy-pages.yml` 은
  존재하고(`run:` 둘), `test_ci_builds_the_image.py` 가 읽는 경로는 `buildtwin-ci.yml` **하나**다
  (그 파일 `:102` 의 `WORKFLOW`). **선례는 있다** — `test_postgres_floor_recitation.py` 의
  `SCANNED_ROOTS` 는 `.github/workflows` **디렉터리 전체**를 훑는다(그 파일 `:136`). 이 사이클의
  게이트는 그 선례를 따른다.
- **ⓓ 「rc 를 성공으로 바꾼다」와 「테스트를 줄인다」는 다른 축이다.** 후자(`--ignore`·skip)는 위 표에서
  **실행되는 자리 0** 이고 CLAUDE.md §3-1 이 이미 금지 문구로 진다. **이 사이클은 전자만 닫는다.**

### 1-c. 곱 표 — (삼키는 층 × 그것을 붙드는 게이트) (§6-1: 관계를 세는 목록은 **곱**으로)

**각 칸은 실행값이거나 코드 인용이다 — 다른 절 참조로 갈음하지 않는다**(§6-3).

| 삼키는 층 | 오늘 저장소에 **실행되는 자리**로 있는가 | 붙드는 게이트 | 그 게이트의 한정 |
|---|---|---|---|
| ① `Dockerfile` 의 셸 OR | **0**(`c82cf47` 이 지웠다) | `test_the_image_installs_from_one_source.py` | **셸 OR 하나만** 본다(그 파일 `:41` 이 스스로 적는다) |
| ② 워크플로 **잡·스텝의 열쇠**(`continue-on-error: true`) | **0**(`: false` 하나) | `test_ci_builds_the_image.py` ⓚ | **`image` 잡에만** 걸린다(그 파일 `:105` 의 `IMAGE_JOB`) |
| ③ 워크플로 **잡 배선**(`if: false` · `needs`) | **0**(오늘의 `if:` 넷은 정당) | 같은 파일 ⓚ | **`image` 잡·짓는 스텝에만** |
| ④ **워크플로 `run:` 안의 rc 조작**(`set +e` · `\|\| true` · `; exit 0`) | **0** | **없다** | ← **C7 = 4 passed. 이 사이클의 표적** |
| ⑤ **Makefile 레시피의 `-` 접두** | **0** | **없다** | ← 같은 축의 두 번째 자리(qa 소유 트리) |
| ⑥ 두 번째 워크플로(`deploy-pages.yml`)의 ②③④ | **0** | **없다**(`run:` 축으로 읽는 게이트가 0) | ← §1-b ⓒ |
| ⑦ 표기가 아닌 기전(래퍼 · `except: pass` · `paths:`) | (재지 않았다) | **없다** | ← §1-b ⓐ. **이 사이클이 닫지 않는다** |

**④⑤⑥ 이 이 사이클의 범위이고, ⑦ 은 이름으로 남긴다.**

### 1-d. 한정어 역방향 확인 (§6-3 — **새로 쓰는 조건절도 예외가 아니다**)

이 계획이 작업 1 에 주는 조건절은 이것이다:
> **「`.github/workflows/` 의 *모든* 워크플로의 *모든* 잡의 *모든* 스텝」의 `run:` 스크립트에
> rc 를 성공으로 바꾸는 표기가 **없다**, 그리고 어느 잡·스텝에도 `continue-on-error: true` 가 **없다**.」**

| 한정어 | 빼면 무엇이 더 들어오는가 | 두면 무엇이 빠지는가 | 실행으로 태운 값 |
|---|---|---|---|
| **모든 워크플로** | (없다 — 오늘 둘뿐) | 없다 | `ls .github/workflows` → **둘**(N=1). 옛 조건(`buildtwin-ci.yml` 하나)은 `deploy-pages.yml` 의 `run:` **둘**을 놓친다 |
| **모든 잡** | `image` 밖 잡 넷의 스텝 전부 | 없다 | 옛 조건(ⓚ = `image` 잡)에서 **다른 잡의 `run:` 에 `set +e` 를 넣어도 죽지 않는다** — 이것이 **작업 1 의 변이 표가 태워야 할 칸**이다 |
| **`run:` 스크립트** | — | **`uses:` 액션이 삼키는 경우**(액션 안의 rc 는 이 축 밖) | 워크플로의 `uses:` 는 이 계획이 세지 않았다 → §확인하지 않은 것 |
| **`continue-on-error: true`**(`: false` 아님) | `: false` 하나가 거짓 양성이 된다 | — | 오늘 값 **1**(`buildtwin-ci.yml:153`) — **음성 대조군으로 반드시 산다** |
| **`if:` 를 넣지 않는다** | 오늘의 정당한 넷이 전부 거짓 양성 | **짓는 스텝을 끄는 `if:`** | 그 칸은 **ⓚ 가 이미 진다**(변이 C6 = 1 failed, 3 passed) — 옛 조건이 잡던 것을 잃지 않는다 |
| **Makefile 레시피의 `-` 접두** | (없다 — 오늘 0) | **`$(MAKE) -k`·서브셸 안의 `\|\|`** | 오늘 값 0(N=1). 이 계획은 `-` 접두 **하나만** 조건으로 준다 → 나머지는 §확인하지 않은 것 |

***옛 조건이 잡던 것을 잃는가 — 아니다.*** 새 게이트는 ⓚ 를 **대체하지 않고 넓힌다**. ⓚ 는 `image`
잡에 대해 **존재·짓는 명령·열쇠 부재** 셋을 함께 보고, 새 게이트는 **모든 잡에 대해 rc 조작 하나**만
본다. **두 축이 겹치는 칸은 `image` 잡의 `continue-on-error` 하나**이고, 그 칸은 **둘 다 죽는 것이
정답**이다(§6-2 4: 두 사실이 함께여야 의미가 있으면 함께 단언한다). **겹침을 무주공산으로 만들지
않기 위해** 작업 2 가 두 파일 머리에 경계를 적는다(§6-3 8·9회차: 축을 바꾸면 무주공산은 없어지지 않고
자리를 옮긴다).

### 1-e. 이 사이클의 범위

**닫는다**: §1-c 의 ④⑤⑥.
**닫지 않는다**: ⑦(표기 아닌 기전) · `uses:` 액션 안 · **컨테이너가 뜨는가**(§2 가 값으로 판정한다) ·
compose 의 준비 상태 계약(§후속 77) · 큐 여섯 · CLAUDE.md 대기열.

---

## 2. 판정 — 큐 여섯 · CLAUDE.md 대기열 · §2(19) · **경쟁 후보** 중 무엇을 이 사이클로 삼는가

**결론: §1-c 의 ④⑤⑥ 을 삼는다.** 경쟁 후보(**컨테이너가 실제로 뜨는가**)는 **이월하고, 재개 조건을
값으로 못박는다.** 큐 여섯도, CLAUDE.md 대기열도, §2(19) 도 삼지 않는다.

### 2-a. 큐 여섯 — **다시 잰 값**(§6-1 12회차: 목록을 옮기는 것과 상태를 아는 것은 다른 일이다)

전부 `580d3f6` 에서, **저장소 루트에서**, 각 N=1.

| # | 문안(계획 0017 §2 에서 **인용 · 발췌**) | **`580d3f6` 에서 다시 잰 값** | 계획 0017 이 잰 값과의 차 | 삼지 않는 근거 |
|---|---|---|---|---|
| **2** | *"`"rejected"` 값 리터럴의 전수 감사"*(qa) | `git grep -nI '"rejected"' 580d3f6 -- .` → **188줄**, `/docs/` 밖 **117줄** | 187 → **188**(**문서 쪽만** 하나 늘었다 — 0017 §M 이 그 낱말을 썼다). 코드 쪽 **117 무변** | 이 경로 밖이고, 이 사이클의 qa 예산은 작업 1·2 가 쓴다 |
| **3** | *"`apps/web/src/api/client.ts:12` 의 TODO"*(qa) | `git grep -n "TODO(round-6" 580d3f6 -- buildtwin/apps/web/src/api/client.ts` → **`:12`** 그대로 | 무변 | 이 경로 밖이고 **자리도 겹치지 않는다** |
| **6** | *"`docs/api.md` 오류 절의 예외 열거와 `internal_error`"*(api) | 같은 명령 → **rc 1 = 히트 0** | 무변 | **생성기 사이클**의 일이다 |
| **11** | *"확정 축의 `expert_review_logs` 도 무보호"*(qa) | `git grep -nI "expert_review_logs" 580d3f6 -- .` → **41줄** | 40 → **41**(문서 쪽) | **로그** 경로이고 판정·인증 경로가 아니다 |
| **23** | *"`postgres.measured.json` 의 `server_version`"*(qa + architect). 재개 조건: **단언·비교가 하나라도 생기는 순간** | `git grep -nI "server_version" 580d3f6 -- .` → **66줄**, `/docs/` 밖 **15줄**. **단언·비교는 여전히 0건** | 65 → **66**(문서 쪽), 코드 쪽 **15 무변** | **재개 조건이 성립하지 않는다.** 이월 |
| **24** | *"PostGIS 가 로드된 DB 에서 무엇이 달라지는가"*(qa) | `git grep -nIi "postgis" 580d3f6 -- .` 의 `/docs/` 밖 **13줄**. 데몬 **rc 1** · `ip_forward` **0** | 무변 | 이월. 닫힘은 여전히 §확인하지 않은 것 76 과 한 자리다 |

***이 표가 낸 값 하나.*** **여섯 중 넷이 「문서 쪽만 하나 늘었다」로 움직였고 코드 쪽은 전부 무변이다.**
즉 **이 큐를 미루는 동안 그 축들은 코드에서 자라지 않았다** — 미루는 비용이 「목록이 낡는다」가 아니라
**「같은 크기로 계속 있다」**라는 것이 이번에 값으로 났다. **그 사실이 미루는 것을 정당화하지는 않는다**:
§후속 **86** 이 그 사실을 지고, **이 사이클로 네 사이클째**다.

### 2-b. CLAUDE.md 대기열 — 삼지 않는다 (값은 갱신한다)

대기열은 **§6-5(압축) → §2(19 무소유)** 순서다.

- **§6-5 압축**: `580d3f6` 에서 **CLAUDE.md 무접촉**이 확인된다(`git log --oneline f9c6265^..580d3f6 --
  buildtwin/CLAUDE.md` **빈 출력**, N=1). **내가 다시 잰 값**: §6 = **21,128자 · 277줄**, 파일 전체
  32,222자 · 491줄 대비 **65.6%**(N=1, `python3`). 문턱을 넘긴 근거 표면 값(**1,090자** = 못박은 분모
  15,148 대비 7.20%)은 **계획 0017 §2 에서 이월한 것이고 내가 다시 재지 않았다**(§후속 62).
  **그리고 착수를 막는 것은 문턱이 아니다** — §후속 **50** 의 세 칸이 `**규칙.**` 줄로 갈리지 않는다
  (§6-1 은 그 줄 **뒤**가, §6-3 은 그 줄 **앞**이 섞이고, §6-5 에는 그 줄이 **없다**). **그러므로
  §6-5 사이클의 첫 일은 압축이 아니라 「무엇이 규칙인가」를 한 줄씩 가르는 일이고, 그것만으로 한
  사이클이다.** 이 사이클의 architect 예산은 작업 3·4 가 쓴다.
- **§2(19) 무소유 — 그리고 0017 의 선례가 축을 바꿨는가.** 전수를 다시 만들었다
  (`git ls-tree --name-only 580d3f6 -- buildtwin/` 의 각 이름을 §2 트리 블록과 대조, N=1):
  **소유가 적히지 않는 자리에 `.dockerignore` 가 더해졌다.** **판정: 축은 바뀌지 않았다** — §2 정본이
  *"계획의 작업 분배 행은 **그 사이클의 배정**이지 소유가 아니다"*(**인용 · 발췌**)라고 스스로 적고,
  0017 은 그 조항대로 배정만 했다.
  ***그러나 값 하나가 새로 났다.*** **소유가 없는 그 파일을 이제 상시로 도는 게이트가 읽는다** —
  `tests/invariants/test_the_build_context_excludes_the_secret.py`(qa 소유)가 `.dockerignore` 를 읽는다.
  즉 **「고칠 수 있는 사람은 계획이 배정할 때만 있는데, 그것을 붙드는 게이트는 매 실행 돈다」**는 모양이
  이 저장소에 처음 생겼다. **19 는 그 값을 지고 열린 채다**(§후속 표).

### 2-c. **경쟁 후보 심사 — 「컨테이너가 실제로 뜨는가」**(§확인하지 않은 것 **90** · §후속 **88**)

**후보의 값은 진짜다.** 사용자가 칠 것은 `make dev`(= `docker compose up --build`)이지 `build` 가
아니고(`buildtwin/Makefile:40`, N=1), **그 축에서 사용자는 여전히 첫 실행자다**(계획 0017 §N-5:
네 실행 모두 `build` 에서 끝난다). CI 러너에는 데몬도 네트워크도 있으니 **여기서는 못 재고 CI 에서는
잴 수 있는 자리**다. **그런데 비용과 값을 견주면 이 사이클에서는 진다.** 근거 넷 — **전부 값이다.**

| # | 근거 | 값(방법 · N) |
|---|---|---|
| **비용 1** | **compose 에 준비 상태 계약이 하나도 없다.** `up -d` 의 rc 는 「컨테이너가 만들어졌다」이지 「떴다」가 아니다 → **그 잡은 준비 판정을 자기 `run:` 안에 새로 써야 한다** | `grep -c healthcheck buildtwin/docker-compose.yml` → **0**; `depends_on` 셋(`api`·`worker`·`web`)이 전부 **리스트 형식**이고 `condition:` 이 없다(N=1) |
| **비용 2** | **그 `run:` 축이 오늘 붙들리지 않는다.** 즉 새 관측자의 **판정 로직**이 저장소의 어느 게이트도 읽지 않는 자리에 놓인다 — **0017 이 세운 관측자와 같은 상태가 둘이 된다** | C7 = **4 passed**(qa 가 심었고 리뷰어가 재현, 계획 0017 §N-6 ㄱ) |
| **비용 3** | **러너 시간.** 짓는 것만 콜드로 61.2~84초이고, 거기에 이미지 풀 넷(`postgis/postgis:16-3.4` · `redis:7-alpine` · `minio/minio:latest` · `node:22-alpine`)과 **web 컨테이너의 `npm install`**(compose 의 `command`)이 더해진다 | 61.2s·79s·84s 는 **저장소 밖**(계획 0017 §N-2 이관표) — **내가 재지 않았다**. 이미지 넷과 `npm install` 은 `docker-compose.yml` 이 적는다(N=1) |
| **비용 4** | **`up` 이 검사하는 것은 이미지 레이어의 앱 코드가 아니다.** `api`·`worker` 는 `volumes: [".:/app"]` 로 호스트 트리를 덮어쓴다 → 뜨는 것이 확인해 주는 것은 **compose 배선**이고 **레이어 안은 §확인하지 않은 것 97 이 그대로 진다** | `docker-compose.yml` 의 `api`·`worker` `volumes`(N=1) |

***그리고 값 쪽에서도 한 겹이 이미 서 있다.*** 그 배선의 **선언**은 계획 0015 가 이미 붙든다 —
`tests/invariants/test_demo_stack_can_stand.py` 가 문 1~4 를 **데몬 없이** 읽는다(그 파일 머리).
**그러므로 후보 A 가 새로 더하는 것은 「선언이 참인데 런타임이 죽는」 경우뿐이다.**

**판정: 이월한다. 재개 조건을 값으로 못박는다(둘 다 성립할 때).**
- **ⓐ §후속 79 의 게이트가 선다**(= 이 사이클의 작업 1). 그러면 그 잡의 `run:` 이 붙들린다.
- **ⓑ §후속 77 이 닫힌다** — compose 에 `healthcheck` + `depends_on: {condition: service_healthy}` 가
  서면 **준비 판정이 compose 파일 안으로 들어오고**, `docker compose up --wait` 한 줄이 그 계약을
  읽는다. 그때 **데몬 없는 불변식이 그 계약을 읽을 수 있고**(0015 가 문 1~4 에 대해 이미 하는 것),
  CI 잡의 `run:` 은 **판정을 담지 않는 한 줄**이 된다.
- **그 둘 전에는 A 는 「붙들리지 않는 셸 판정」을 CI 에 하나 더 세우는 일이다.**

***역방향 확인 — 이 판정이 놓치는 것(§6-1 ②).***
- **ⓐ 미루는 동안 사용자는 여전히 첫 실행자다**(§후속 88 의 축). 이 계획이 그것을 없애지 않는다 —
  **순서를 정할 뿐이다.** 그 사실은 §후속 표의 88 행이 진다.
- **ⓑ 「비용 3」의 세 값은 내가 재지 않았다**(저장소 밖). 그 값이 틀렸다면 비용 3 의 무게가 줄고
  **비용 1·2·4 만 남는다** — 그 셋으로도 선다고 나는 판단하지만, 그 판단은 여기 적어야 검증된다.
- **ⓒ 「ⓑ 가 A 를 싸게 만든다」는 예측이다.** `--wait` 가 러너의 compose 판에서 어떻게 죽는지 아무도
  재지 않았다 → §확인하지 않은 것으로 연다.
- **ⓓ 이 판정은 「관측자를 붙드는 것이 관측자를 늘리는 것보다 먼저다」에 선다.** 그 순서가 항상 옳은
  것은 아니다 — **붙들리지 않는 관측자도 없는 것보다는 낫다**는 반론이 성립하고, 0017 자신이 그
  반론의 실례다(ⓚ 가 C7 을 못 보지만 C1~C6 은 잡는다). 이 계획이 A 를 **기각이 아니라 이월**로
  두는 이유가 그것이다.

### 2-d. 이 경로를 이 사이클로 삼는 근거 — **전부 값이다**

1. **저장소의 가장 새 관측자에 재현된 구멍이 있다**(C7 = 4 passed, 심은 사람 둘).
2. **곱 표에서 게이트가 「없다」인 칸이 셋이고**(④⑤⑥), **셋 다 qa 소유 트리**다 — 계약을 나눠 쓰는
   소유가 없다(§1-c).
3. **이 환경에서 값이 난다.** 데몬도 네트워크도 필요 없다 — 게이트가 읽는 것은 YAML 과 `Makefile`
   이다(0017 작업 2 와 같은 모양).
4. **다음 사이클의 후보 A 가 이 게이트 위에 선다**(§2-c 재개 조건 ⓐ). 순서를 바꾸면 붙들리지 않는
   관측자가 둘이 된다.

---

## 3. 이월 규약 판정 — **계획 0016·0017 과 같다: 「상태 표」로 옮긴다**

**근거는 값이다**: 그 규약의 표본이 다섯이 됐고(계획 0015 → 0016 → 0017 §후속 → 0017 §M-8 →
0017 §N-10), **그 표가 갈림을 감췄는가**의 답은 **감추지 않았다**이다 — 0017 에서 갈린 값(§N-3 ⓑ)은
상태 표가 아니라 **예측과 실측** 사이에서 났다.

**그러므로 이 계획의 §후속도 이렇게 옮긴다.**
- **정본**: `git show 12a2d34:buildtwin/docs/plans/0016-the-first-run-must-fail-out-loud.md` 의 §후속 +
  §M-8 + §M-13-5, 그리고 `git show 580d3f6:buildtwin/docs/plans/0017-nobody-builds-the-image.md` 의
  §후속 · §3-6 · §M-8 · **§M-9**, 그리고 계획 0017 §N-10 · §N-11. **이 계획은 그 산문을 다시 쓰지 않는다.**
- **이 계획이 싣는 것은 「번호 · 한 줄 문안 · 소유 · 상태」 표**이고, 상태 칸은 **겹쳐 읽은 판정 하나**다.
- **이 계획이 지는 항목**(작업의 완료 조건에 이름으로 들어가는 것)만 문안을 그 자리에 적는다.

*역방향 확인 — 이 규약이 놓치는 것.* ⓐ **한 줄 문안은 발췌다** — 닫으려면 정본을 연다.
ⓑ **상태 칸은 내가 겹쳐 읽은 판정이다** — 갈리면 표가 갈림을 감춘다. ⓒ 표본 **6**(위 다섯 + 이 계획).
§후속 80 이 그 축이다.

---

## 작업 분배

**소유는 CLAUDE.md §2 가 정본이다.** 아래 「소유」 열이 그것이고, 「배정」은 이 사이클의 것이지
소유가 아니다(§2, **인용 · 발췌**). 확인(각 N=1, 트리 `580d3f6`): `.github/workflows/` · `tests/` ·
`Makefile` 의 소유는 **`qa`** 다 — CLAUDE.md §4 의 에이전트 표가 그렇게 적는다(그 자리에서 도는 참조:
`grep -n "github/workflows" CLAUDE.md`). **`docker-compose.yml` 은 소유가 없다**(§후속 19) —
이 계획은 그것을 **읽기만** 하고 만지지 않는다.

| # | 작업 | 소유 | 만지는 트리 | 의존 |
|---|---|---|---|---|
| 1 | **rc 를 성공으로 바꾸는 모양을 qa 소유 트리 전부에서 붙든다** — 워크플로 **전부**의 **모든 잡·스텝**의 `run:` + `continue-on-error` + `Makefile` 레시피의 `-` 접두 | `qa` | `tests/invariants/` | — |
| 2 | **두 게이트의 경계를 적는다** — `test_ci_builds_the_image.py` 머리 「보지 못하는 것」의 **셋째 항목**(다른 모양의 삼킴)이 이제 **누가 지는지**, 그리고 새 게이트가 **`image` 잡에 대해 무엇을 겹쳐 보는지** | `qa` | `tests/invariants/test_ci_builds_the_image.py` · `test_the_image_installs_from_one_source.py` | 1 |
| 3 | **범위 밖으로 민 축을 이름으로 남긴다** — §후속·§확인하지 않은 것 신규 문안 + **후보 A 의 재개 조건** | `architect` | `docs/plans/` | 1·2 |
| 4 | **마감** — 게이트 재측정 · 교차 확인 표 · 상태 표 | `architect` | `docs/` | 전부 |

**작업 1·2 를 한 소유가 진다** → **계약을 나눠 쓰는 소유가 없다** → **ADR 을 열지 않는다**(아래).
**작업 2 를 작업 1 과 떼는 이유는 소유가 아니라 순서다** — 작업 1 이 서기 전에는 그 경계 문장을
**참으로 쓸 수 없다**(§6-4 1: 사실과 다른 문구는 그것을 만든 사이클이 고친다).

### 완료 조건

> **각 상자의 마지막 행은 「이 작업이 거짓으로 만드는 남의 자리」다**(§후속 72·74·76 공통 처방 —
> 0016 이 첫 이행, 0017 이 둘째·셋째, 이 계획이 넷째). **그 행이 비면 그 작업은 끝나지 않았다.**

#### 작업 1 (qa · `tests/invariants/`)

1. **새 파일 하나**가 서고, 그것이 보는 것은 **넷**이다 — ⓟ `.github/workflows/` **아래 모든 워크플로**
   의 **모든 잡의 모든 스텝**의 `run:` 스크립트에 **rc 를 성공으로 바꾸는 표기가 없다**
   (`set +e` · `|| true` · `|| :` · 셸 OR 로 이은 무조건 성공 · `; exit 0` 꼬리) /
   ⓠ 어느 잡·스텝에도 **`continue-on-error: true`** 가 없다(**`: false` 는 산다**) /
   ⓡ `buildtwin/Makefile` 의 어느 레시피도 **`-` 접두**를 갖지 않는다 /
   ⓢ **탐침 자신**(§6-2 5) — 읽는 경로·파싱이 틀리면 **초록으로 지나가지 않는다.**
   워크플로 목록은 **디렉터리를 훑어 만든다**(하드코딩한 파일 이름 하나가 아니다 — 선례:
   `test_postgres_floor_recitation.py` 의 `SCANNED_ROOTS`).
2. **`run:` 을 문자열 부분일치로 읽지 않는다.** `set +e` 가 **주석 안**이나 **다른 낱말의 일부**로
   있는 경우와 **셸이 실행하는 자리**를 가른다(0017 작업 2 가 `shlex` 로 첫 낱말을 편 것과 같은 층).
   **거짓 양성의 값을 표로 남긴다** — 오늘의 `if: ${{ !cancelled() }}` 셋 · `if: always()` 하나 ·
   `continue-on-error: false` 하나가 **전부 살아야 한다**(음성 대조군).
3. **변이 표**(§6-2 1 — 각 N=1, **심기 직전의 작업 트리 사본과 `diff` 로 적용 확인** 후 그 사본으로
   원복하고 **저장소 루트에서** `git status --porcelain` 확인). **최소한 이 칸들을 태운다**:
   음성 대조군 / **C7 재현**(`image` 짓는 스텝을 `set +e`+`exit 0` 으로 — **이제 죽어야 한다**) /
   **`image` 가 아닌 잡**의 `run:` 에 같은 것을 심는다(§1-d 가 이름으로 요구한 칸) /
   `deploy-pages.yml` 의 `run:` 에 심는다(§1-b ⓒ) / 어느 스텝에 `continue-on-error: true` /
   `Makefile` 레시피에 `-` 접두 / **`continue-on-error: false` 를 그대로 둔 채**(거짓 양성 음성 대조군) /
   **탐침 변이**(읽는 디렉터리를 없는 이름으로 → **초록이면 안 된다**).
   ***주의**: 이 파일이 처음 넣는 줄을 지우는 변이는 대-HEAD `git diff` 가 침묵할 수 있다*
   (§6-2 규칙 5 · 계획 0017 §M-6 · §N-7) — **사본 `diff` 로 확인하고, `git diff` 를 쓰면 저장소
   루트에서 돌린다.**
4. **「이 파일이 서기 전」 표**를 함께 남긴다 — 같은 변이 둘을 **이 파일 없이** 심고
   `pytest tests/invariants tests/regression -q` 값을 적는다(0017 작업 2 와 같은 형식).
5. **개수를 세지 않는다**(§6-1 9회차). *"잡이 다섯이다"* 같은 문장을 쓰지 않고, **무엇이 없는가**만
   단언한다.
6. **`make test` rc 0** 이고 **invariants 가 늘어난다**(그 수는 이 작업이 낸다). 나머지 넷
   (unit **492** · regression **8** · integration **232** · vitest **310**)은 **이름으로 불변**이다.
7. **`tests/postgres.floor.json` · `tests/metrics.json` 을 만지지 않는다** — 이 파일은 DB 엔진을
   만들지 않는다(그 사실을 파일 머리에 적는다).
8. **이 작업이 거짓으로 만드는 남의 자리**:
   - `tests/invariants/test_ci_builds_the_image.py` 머리 **「보지 못하는 것」 셋째 항목**
     (*"`set +e` … 는 전부 이 단언을 지나간다"*) — **이제 다른 파일이 진다.** 소유는 같은 `qa` 이고
     **작업 2 가 고친다**(이 커밋이 아니다 — 순서가 그렇다).
   - `tests/invariants/test_the_image_installs_from_one_source.py:41` 의 같은 모양의 문장 — **작업 2.**
   - `.github/workflows/buildtwin-ci.yml:270` 의 주석(*"삼키는 열쇠를 두지 않는다"*) — **낡지 않는다**
     (그 단정은 여전히 참이고, 이제 게이트가 그것을 붙들 뿐이다). **만지지 않는다.**
   - CLAUDE.md — **만지지 않는다.**

#### 작업 2 (qa · `tests/invariants/` 의 기존 두 파일)

1. `test_ci_builds_the_image.py` 머리의 **「보지 못하는 것」 셋째 항목**을 **작업 1 이 만든 사실로
   고친다** — 무엇이 이제 붙들리고 무엇이 여전히 안 붙들리는지(§1-b ⓐ 의 ⑦: 래퍼 스크립트 ·
   `except: pass` · `paths:` 축소)를 **자리마다** 적는다. **변이 표의 C7 행은 지우지 않는다** —
   **그 값이 그 시점의 참**이고(§3-13 기록물), 대신 **「지금은 어느 파일이 그것을 죽이는가」를 옆에
   적는다.**
2. `test_the_image_installs_from_one_source.py:41` 의 같은 모양의 문장도 같은 처분.
3. **두 게이트가 겹치는 칸을 이름으로 적는다** — `image` 잡의 `continue-on-error` 는 **둘 다 죽는
   것이 정답**이다(§1-d). **겹침을 「중복」이라 부르며 한쪽을 지우지 않는다**(§6-2 4).
4. **`make test` rc 0** 이고 **다섯 수가 작업 1 뒤의 값에서 움직이지 않는다**(문서 문자열만 만진다면
   그래야 하고, 움직이면 그 이유를 커밋 본문에 적는다).
5. **이 작업이 거짓으로 만드는 남의 자리**: **없다** — 이 작업은 **자기 소유 트리의 두 파일 머리
   문자열만** 만진다. *역방향 확인*: 그 문자열을 **인용하는 기록물**(계획 0017 §3-2-5 · §N-6 ㄱ)은
   **기록물이라 고치지 않는다**(§3-13 첫째 갈래) — 그 사실을 이 작업의 커밋 본문에 적는다.

#### 작업 3 (architect · `docs/plans/`)

1. 이 계획이 **범위 밖으로 민 축**(§1-e)을 §후속·§확인하지 않은 것의 **신규 항목 문안**으로 적는다 —
   **번호는 하나의 공간에서 뽑는다**(계획 0017 §3-6 이 정한 규약, §후속 92).
2. **후보 A 의 재개 조건을 그 자리에 적는다**(§2-c ⓐⓑ) — **조건이 성립했는지를 다음 계획이 값으로
   재는 형태**로. *"준비되면"* 같은 문장을 쓰지 않는다.
3. 작업 1·2 가 낸 값(변이 표 · 거짓 양성 표 · 「서기 전」 표)을 **읽고**, 계획의 §1-c·§1-d 와
   **갈리는 칸이 있으면 실행으로 가른다**(§6-3 11회차).
4. **이 작업이 거짓으로 만드는 남의 자리**: 이 계획 §1-b·§1-c·§1-d 의 값 — **고치지 않는다**
   (기록물, §3-13). 갈린 칸은 **작업 4 의 교차 확인 표**가 싣는다.

#### 작업 4 (architect · `docs/`)

1. **게이트를 내가 다시 돌린다** — `make test`·`make lint`·`make e2e`, **방법 · N · 표본 간격**과 함께.
   고아 탐침은 **§후속 46 의 처방대로 별도 호출**로 돌린다(대괄호만으로는 부족하다 —
   계획 0017 §N-6 ㄴ).
2. **교차 확인 표**(§6-3 11회차) — 이 사이클이 **두 번 이상 적은 값**을 모으고 **갈리지 않은 칸도
   적는다.** 목록에는 **이 사이클이 실행으로 만들어 낸 값**(변이 표의 수 자신)도 넣는다(§6-1 13회차).
3. **§후속 · §확인하지 않은 것 최종 상태 표**(§3 규약).
4. **이 작업이 거짓으로 만드는 남의 자리**: §M/§N 형식대로 **자리마다 판정**하고, **자기 자신에게도
   그 행을 건다.**

---

## 게이트

**커밋 전 `make test` 통과**(CLAUDE.md §3-1). 각 작업이 **자기 커밋 직전에 직접 돌리고** 전후로
**저장소 루트에서** `git status --porcelain` 을 확인한다.

| 게이트 | 이 계획을 여는 시점의 값(`580d3f6`, 내가 쟀다) | 이 사이클 뒤 기대 |
|---|---|---|
| `make test` | **rc 0** · unit **492** · invariants **126** · regression **8** · integration **232** · vitest **310**(31 files), **N=2**(20:38:57→20:40:44 · 20:40:44→20:42:35) | **rc 0**. **invariants 만 는다**(수는 작업 1 이 낸다). 나머지 넷은 **이름으로 불변** |
| `make lint` | **rc 0**(N=1, 20:42:42→20:42:50) | **rc 0** |
| `make e2e` | **12 passed · rc 0**(N=1, 20:42:54→20:43:24) | **12 passed** |
| 고아 | `pgrep -fc '[u]vicorn'` **0** · `pgrep -fc '[v]ite'` **0** · `.e2e-preview.*` **0**(각 N=1, **별도 호출**) | 각 **0** |
| 바닥값 | `min_tests_on_postgres` 통합 **187** · 단위 **252**(N=1) | **무변** |
| CLAUDE.md | **무접촉**(N=1) | **무접촉** |
| `.env` | 루트 · `buildtwin/` 각 **0**(N=1) | 각 **0**, 사이클 앞뒤로 |
| 데몬 | `docker info` **rc 1** · `ip_forward` **0**(각 N=1) | **띄우지 않는다** |

---

## 리스크

1. **거짓 양성이 이 게이트의 주된 실패 모드다.** 오늘 워크플로에 삼키는 표기가 **실행되는 자리로
   0** 이므로, 이 게이트가 처음 빨개진다면 그것은 **회귀가 아니라 파싱 오류**일 가능성이 크다.
   그래서 완료 조건 1-2 가 **거짓 양성 음성 대조군을 표로** 요구한다.
2. **축이 겹쳐 무주공산이 자리를 옮길 수 있다**(§6-3 9회차). ⓚ 와 새 게이트가 `image` 잡의 한 칸을
   함께 본다 — **작업 2 가 그 경계를 적지 않으면**, 다음 편집이 「저쪽이 본다」고 믿고 양쪽에서 지운다.
3. **게이트가 산문을 잡을 수 있다.** 이 저장소는 변이 표와 파일 머리 산문에 그 표기들을 **글자
   그대로** 쓴다(§1-b 의 `/docs/` 밖 히트가 전부 그것이다). 파싱이 YAML 구조를 지나지 않고
   문자열을 훑으면 **자기 자신과 이웃 불변식이 먼저 빨개진다.**
4. **이 사이클도 후보 A 를 미룬다** — 사용자가 `make dev` 의 런타임을 처음 받는 사실은 그대로다
   (§2-c 역방향 ⓐ). 그 사실은 §후속 88 이 진다.
5. **큐 여섯이 네 사이클째다**(§2-a). §후속 86 이 진다.

---

## ADR 필요 여부 — **열지 않는다**

**근거**: ① 데이터 모델·상태기계·API 계약을 건드리지 않는다. ② **계약을 나눠 쓰는 소유가 없다** —
작업 1·2 가 전부 `qa` 트리다(0016 은 세 소유가 같은 문자열 계약을 나눠 써서 ADR 이 필요했다).
③ 이 사이클이 세우는 것은 **새 규칙이 아니라 이미 있는 규칙의 게이트**다 — 「실패를 삼키지 않는다」는
ADR 0020 §2-1 항 1 과 CLAUDE.md §3-1 이 이미 적는다.
***역방향 확인 — 이 판정이 놓치는 것***: 만약 작업 1 이 **오늘의 어떤 자리를 거짓 양성으로 잡아
그 자리를 고쳐야 한다면**, 그것은 워크플로의 **행동 변경**이고 이 판정 밖이다 — 그때는 작업 3 이
멈추고 ADR 을 연다.

---

## 후속 — 이월 (§3 의 규약: **상태 표**로 옮긴다)

**정본은 §3 이 못박은 자리들이다.** 아래는 **이 사이클이 상태를 만지는 항목**이고 자구는 고치지 않는다.

| # | 한 줄 문안(발췌) | 소유 | 이 계획을 여는 시점의 상태 |
|---|---|---|---|
| 19 | 소유가 §2 에 없는 파일들 | architect | **열림. 값이 하나 났다** — 소유 없는 `.dockerignore` 를 **상시로 도는 게이트가 읽는다**(§2-b) |
| 34 · 27 | 값에는 **방법 · N · 표본 간격** | architect + qa | **열림. 이 계획이 그 형식을 진다**(§0) |
| 46 | 환경 탐침의 자기-계수 | architect + qa | **열림. 처방이 「별도 호출」로 정정됐다**(계획 0017 §N-6 ㄴ) — 이 계획의 게이트 표가 그대로 진다 |
| 62 | 근거 값의 정본은 커밋 본문이 아니다 | architect | **열림. 이 계획이 이관값에 「내가 재지 않았다」를 적었다**(§0 · §2-c 비용 3) |
| 65 · 96 | `git` 명령의 트리 인자 · CWD | architect + qa | **열림. 이 계획의 모든 전수가 저장소 루트에서 돌았다**(§0) |
| 72 · 74 · 76 | 완료 조건에 **「거짓으로 만드는 남의 자리」** 행 | architect + 각 소유 | **열림. 이 계획이 넷째 이행이고, 네 상자 전부에 그 행이 있다** |
| 77 | compose 에 준비 상태 계약이 없다 | 배정 미정 | **열림. 값이 붙었다** — `healthcheck` **0** · `depends_on` 셋 다 `condition:` 없음(§2-c 비용 1). **후보 A 의 재개 조건 ⓑ 가 이 항목이다** |
| **79** | 「실패를 삼키는 모양」의 전수 | architect + qa | **열림 → 이 사이클이 진다.** §1-b 가 걸음 1(표기 축)을 만들고 작업 1 이 게이트를 세운다. **닫히지 않는 것은 §1-b ⓐ 의 ⑦**(표기 아닌 기전) |
| 80 | 이월을 「상태 표」로 바꾼 규약 | architect | **열림. 표본이 6 이 된다**(§3) |
| 86 | 큐를 연속으로 밀었다 | architect | **열림. 네 사이클째다**(§2-a) |
| 87 | 짓고 안 짓는 결정이 워크플로 주석에만 남는다 | qa + architect | **열림. 관측 0 그대로** |
| 88 | 스택이 뜨는가를 도는 주체가 사용자뿐 | qa + architect | **열림. 이 사이클이 후보로 심사하고 이월했다**(§2-c) — 재개 조건 ⓐⓑ |
| 90 · 97 · 98 · 99 | 데몬이 있어야 재는 자리들 | qa | **열린 채**(§확인하지 않은 것 표) |
| 93 | 재빌드 경로를 도는 주체가 없다 | qa + architect | **열림. 표본 셋, 전부 콜드**(계획 0017 §N-5) |
| 94 | 이미지 **안**을 보는 게이트가 없다 | qa | **열림. 관측 1회 그대로** |
| 95 | `.env.example` 머리말의 한정어 | 소유 없음 → 배정 필요 | **열림. 이 사이클도 배정하지 않는다** |
| 100 | 러너의 두 전송값 | qa | **ⓐ 닫힘 · ⓑ 갈림**(계획 0017 §N-3). 미측정으로 남은 것: **42바이트 차이 · 잔차 약 30KB** |
| 63 · 50 · 38 · 25 · 69 · 71 · 82 · 83 · 84 · 85 | §6-5 커밋의 몫 | architect | **열림. 이 사이클도 CLAUDE.md 를 만지지 않는다**(§2-b) |
| 2 · 3 · 6 · 11 · 23 · 24 | 큐 여섯 | 각 소유 | **열림. 값은 §2-a 에 있다** — 코드 쪽 전부 무변 |

---

## 이 계획이 확인하지 않은 것

**번호는 계획 0017 §3-6 이 정한 하나의 공간을 잇는다**(마지막이 100 이었다).

101. **[재지 않았다] 워크플로의 `uses:` 액션이 실패를 삼키는가**(qa). 이 계획의 조건절은
     **`run:` 스크립트**에 걸린다(§1-d 셋째 행) — 액션 안에서 rc 가 어떻게 다뤄지는지는 이 축 밖이고,
     그 워크플로들이 부르는 액션의 목록조차 이 계획이 세지 않았다. *닫힘의 조건*: 그 목록이 서고
     각 액션의 실패 전파가 값으로 적힐 때.
102. **[재지 않았다] `except Exception` 39 자리 중 판정 경로가 몇인가**(각 소유). 저장소 전체 히트만
     쟀고(N=1) **로깅인지 판정인지 가르지 않았다.** 이 저장소의 지배적 실패 모드가 *"조용히 죽는 것"*
     이므로 이 축은 §1-b ⓐ 의 ⑦ 중 가장 큰 칸일 수 있다. *닫힘의 조건*: 그 39 를 **경로별로** 가른
     표가 설 때.
103. **[재지 않았다] `docker compose up --wait` 가 러너의 compose 판에서 어떻게 죽는가**(qa).
     §2-c 의 재개 조건 ⓑ 가 그 명령 위에 서는데, **이 환경은 데몬 rc 1** 이라 재지 못했고 러너에서도
     아무도 돌리지 않았다. *한정*: **`--wait` 는 `healthcheck` 가 없으면 「running」만 본다** —
     그래서 ⓑ 는 「명령을 쓴다」가 아니라 **「compose 에 계약을 둔다」**가 먼저다.
104. **[재지 않았다] `Makefile` 의 `-` 접두 밖의 삼킴**(qa). 이 계획이 조건으로 주는 것은 `-` 접두
     하나이고, `$(MAKE) -k` · 레시피 안 서브셸의 `||` · `; true` 는 **세지 않았다**(§1-d 마지막 행).
105. **[재지 않았다] 이 사이클의 게이트가 러너에서 무엇을 하는가**(qa). 작업 1 의 불변식은
     `make test` 의 invariants 갈래로 CI 에서도 돌지만, **그 잡 로그를 이 사이클이 읽을지는 정해지지
     않았다** — 계획 0017 이 그 값을 세션 밖에서 받은 것과 같은 자리다(§후속 62).

---

## 다음에 부를 에이전트

```
@qa   계획 0018 작업 1 — rc 를 성공으로 바꾸는 모양을 워크플로 전부·Makefile 에서 붙드는 불변식.
      완료 조건 여덟을 전부 지고, 변이 표와 「서기 전」 표와 거짓 양성 음성 대조군 표를 파일 머리에.
@qa   계획 0018 작업 2 — 두 게이트의 경계. 작업 1 이 선 뒤에만 쓸 수 있는 문장이다.
@architect  작업 3 — 범위 밖으로 민 축 · 후보 A 의 재개 조건.
@architect  작업 4 — 마감.
@reviewer   5체크 + §6-1·§6-3·§6-4 준수 + 이 계획의 §1-d 역방향 확인 표가 실행값으로 채워졌는지.
```
