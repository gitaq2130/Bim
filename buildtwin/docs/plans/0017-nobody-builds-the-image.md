# 계획 0017 — 이미지를 짓는 자리가 **저장소에 하나**이고, 그것은 **사용자의 손**이다

> **이 문서가 여는 것**(계획 0015 §후속 66 의 조항 후보 둘째 — *"커밋 제목·본문은 그 커밋이 무엇을
> 여는지 한 줄로 적는다"*, 문서 자신에도 같은 것을 건다): 사이클 0017 을 연다. 주제는 **첫 실행의
> 이미지를 사용자보다 먼저 짓는 자리를 저장소에 두는 것** 하나이고, 이 계획이 닫으려는 것은
> **`Dockerfile` 의 rc 를 아무도 읽지 않는다**는 오늘의 사실이다(§1-c).
> **이 커밋이 담는 것은 이 계획 문서와 계획 0016 의 재심사 마감(§M-13) 둘뿐이다** — 작업 1~4 의
> 산출물은 각 소유의 커밋이 담는다.

---

## 0. 이 문서의 실측이 나온 자리 (재현 방법)

**작업 트리** `/home/user/Bim/buildtwin`, **저장소 루트 `/home/user/Bim`**, 브랜치
`claude/buildtwin-initial-setup-ubulzb`, **트리 `12a2d34`**(루트 `git status --porcelain` 은 이 문서를
여는 시점에 **빈 출력**, 커밋 직전에 **이 문서와 계획 0016 두 줄**).

**이 문서의 모든 `파일:줄`·커밋 참조는 트리 `12a2d34` 의 것이다**(CLAUDE.md §3-13 첫째 갈래).
**못박음을 좁히지 않는다 — 전체를 못박고 예외를 이름으로 적는다.** 예외 둘:

- **㉮ 다른 기록물을 인용하는 줄** — 계획 0016 은 그 문서의 §0·§M-0·§M-13-0 이 못박은 트리들
  (`a03f13d` · `ddf6bb9` · `12a2d34`). 그 자리에서 트리를 적는다.
- **㉯ CLAUDE.md 를 가리키는 줄** — §3-13 **둘째** 갈래(계속 편집되는 자리)라 줄 번호가 조용히 낡는다.
  **줄 번호로 부르지 않고 그 자리에서 도는 `grep` 으로 적는다.**

**명령의 못박음**(§후속 **65** 확장 항 ㉡ — 이 문서가 그 조항의 **셋째** 준수 사례다). 이 문서에 적는
모든 `git` 명령은 **트리 인자를 명령 안에 담고 `--all` 을 쓰지 않는다**: `git grep <패턴> 12a2d34 -- .` ·
`git show <트리>:<경로>` · `git log --oneline <A>..<B> -- <경로>` · `git ls-tree --name-only 12a2d34 <경로>`.
**트리 인자를 받지 못하는 명령**(`make` · `pytest` · `npx` · `docker` · `ps` · `ls` · `grep` · `python3`)은
전부 이 §0 이 못박은 **`12a2d34` 의 작업 트리**에서 돌린 것이고, 그 사실을 여기 한 번 적어 각 자리에서
되풀이하지 않는다.

**시점을 부르는 말**(65 의 원래 항 ㉠). 「오늘」·「지금」·「HEAD」는 **이 §0 안에서만** 쓴다.
§1 이하는 커밋 해시로 부른다.

**방법 · N · 표본 간격**(§후속 **34** · 27). 아래 값은 **2026-09-08 한 세션 안**(18:16~18:45 UTC,
`date -u` 로 앞뒤를 찍었다)에 잰 것이고, **따로 적지 않으면 N=1**, 서로 몇 분 간격이다.
**「없다」·「히트 0」·「죽었다」에도 N 을 적는다.**

**게이트**(이 계획을 여는 시점에 **내가 다시 돌린 값**, 각 **N=3** — 계획 0016 §M-13-1 이 같은 표를
싣고 그 절이 방법·시각을 진다):
`make test` **rc 0** · unit **492** · invariants **118** · regression **8** · integration **232** ·
vitest **310**(31 files) · `make lint` **rc 0** · `make e2e` **12 passed · rc 0** ·
고아 `vite preview` **전 0 → 후 0** · `.e2e-preview.*` 잔여 **0**.

**자원**: `make e2e` 가 잡은 포트는 그 명령이 반납했다 · postgres 클러스터 **0회** · **심은 변이 0** ·
**docker 데몬 0회**(`docker info` → **rc 1**, `/proc/sys/net/ipv4/ip_forward` → **0**, 각 N=1) ·
저장소 루트에도 `buildtwin/` 에도 **`.env` 를 만들지 않았다**(`ls -a <각 디렉터리> | grep -c '^\.env$'`
→ **0**, 각 N=1, 이 문서의 앞과 뒤 둘 다).

---

## 1. 분석 — **이미지를 짓는 자리**와 **그것을 도는 주체**

### 1-a. 사이클 0016 이 남긴 자리

사이클 0016 은 `Dockerfile` 의 폴백을 지웠다(`c82cf47`). 그 트리의 설치 줄은 이렇다(**코드 인용 · 발췌**,
`git show 12a2d34:buildtwin/Dockerfile`):

```dockerfile
COPY pyproject.toml ./
# 설치의 정본은 pyproject.toml 하나다 — 의존성 이름을 여기 두 번째로 적지 않는다.
# 폴백을 두지 않는다 — 실패를 삼키고 두 번째 설치를 잇는 셸 OR 도, 조용한 기본값도.
# 이 설치가 실패하면 빌드는 rc 0 으로 끝나지 않는다(ADR 0020 §2-1 항 1).
RUN pip install --no-cache-dir -e .
COPY . .
```

**그 주석의 마지막 줄이 이 계획의 표적이다.** 「빌드는 rc 0 으로 끝나지 않는다」는 참이지만,
**그 rc 를 읽는 자리가 저장소에 없다.** 계획 0016 의 조건절은 「실패가 **소리내어** 죽는가」였고
그 계획이 정의한 「소리」는 **네 관측자 중 최소 하나에서 보이는 것**인데, 문 3 의 rc 는
**아무도 듣지 않는 소리**다 — 계획 0016 §확인하지 않은 것 **72·79** 가 둘 다 *"[재야 한다 → 사용자
또는 CI]"* 로 열린 채인 것이 그 값이다.

### 1-b. 전수 — 「이미지를 짓는 자리」 (§6-1 ①②③)

**① 생성 기준.** 저장소 루트에서, **이미지를 짓는 명령을 이름으로 부르는 줄**을 센다 — 표기 집합은
`docker compose build` · `docker compose up`(`--build` 를 달고) · `docker build` · `--build` 넷이고,
**경로로 먼저 좁히지 않으며 자기 커밋도 센다**(§53). 값(각 N=1, 트리 `12a2d34`):

| 명령 | 값 |
|---|---|
| `git grep -nIE 'docker[ -]compose (build\|up)' 12a2d34 -- .` | **33줄** |
| 같은 명령의 `buildtwin/docs/` **밖** 히트 | `buildtwin/Makefile:40` · `buildtwin/README.md:38` · `buildtwin/tests/invariants/test_demo_stack_can_stand.py:53` · `buildtwin/tests/invariants/test_the_first_run_names_its_doors.py` 의 `:35`·`:71`·`:73`·`:77`·`:85` |
| `git grep -nIE 'docker build' 12a2d34 -- .` | **0줄** |
| `git grep -nIE 'docker[ -]compose\|docker build\|--build' 12a2d34 -- .github` | **rc 1 = 히트 0** |

**그 히트들을 읽으면 갈래가 둘이다**(히트 수가 아니라 각 히트를 읽는다 — §6-1):
**실제로 명령을 부르는 자리는 `Makefile:40` 하나**(`docker compose up --build`)이고, 나머지는
**문서의 안내**(`README.md:38`)이거나 **테스트가 문자열로 붙드는 상수**
(`test_the_first_run_names_its_doors.py:85` 의 `DOOR_OPENING_COMMAND = "docker compose up"`)다.

**② 이 기준이 놓치는 것**(§6-1 ②).
- **ⓐ 짓지 않으면서 그 이미지에 기대는 자리** — 이 기준은 「짓는다」만 센다.
- **ⓑ 저장소 밖에서 짓는 자리**(사용자의 손 · 남의 CI · 배포 파이프라인). **사용자의 손이 그 자리다.**
- **ⓒ 보간·줄바꿈** — §6-1 역방향 확인 ③④ 그대로. 표기가 변수로 조립되면 문자열이 소스에 없다.
- **ⓓ 짓기의 *결과*에만 기대는 자리**(이미지 안에 무엇이 들어갔는가) — `openpyxl` 축이 그 모양이었다
  (계획 0016 §1-c 문 3).

**③ 블라인드 스팟을 한 건 태웠다 — ⓐ 를 태우니 자리가 났다.**
`git grep -nIE 'docker compose [a-z]+' 12a2d34 -- .` 의 `docs/` 밖 히트를 읽으면
`buildtwin/Makefile` 의 `seed-compose` 레시피가 **`docker compose exec -T api python -m services.api.seed`**
다(코드 인용). **그 명령은 이미지가 이미 있고 컨테이너가 떠 있다고 가정한다** — 짓는 명령이 아니라서
①의 목록에는 영원히 들어오지 않는데, **그 가정이 참인지 아무도 재지 않는다.**
(`tests/invariants/test_demo_stack_can_stand.py` 는 그 레시피의 **배선 이름**만 읽는다 — 그 파일의
머리 주석이 *"5 는 배선의 이름(어느 서비스·어느 명령)만 읽는다"* 라고 스스로 적는다, **인용 · 발췌**.)

### 1-c. 곱 표 — (이미지를 짓는 일 × 그것을 도는 주체) (§6-1: 관계를 세는 목록은 **곱**으로)

축은 **주체 넷**이다: **사용자**(첫 실행자) · **CI**(`buildtwin-ci.yml`) · **로컬 게이트**
(`make test`/`make lint`/`make e2e`) · **리뷰어**. ✔ = 그 주체가 실제로 돈다, ✘ = 돌지 않는다.
**각 칸은 실행값이거나 코드 인용이고 다른 절 참조가 아니다.**

| 일 | 사용자 | CI | 로컬 게이트 | 리뷰어 | 값 (각 N=1) |
|---|---|---|---|---|---|
| `docker compose config`(파일 해석) | ✔ | ✘ | **✔** | ✘ | `tests/invariants/test_demo_stack_can_stand.py` 가 `.env` 없이 rc≠0, `make env` 뒤 rc 0 을 붙든다(그 파일, 내가 읽었다) |
| **이미지를 짓는다**(`pip install -e .`) | **✔ — 유일하다** | **✘** | **✘** | **✘** | `.github` 에서 표기 집합 히트 **0**(§1-b) · 로컬은 데몬 rc 1 · `ip_forward` 0 |
| 스택이 뜬다(`up`) | ✔ | ✘ | ✘ | ✘ | 같은 값 |
| 시드가 그 스택에 닿는다(`exec`) | ✔ | ✘ | **△** | ✘ | 로컬 게이트는 **레시피의 이름**만 읽는다(§1-b ③) |
| 웹 번들을 짓는다(`npm run build`) | ✔ | **✔** | **✔** | ✘ | CI `:250` `run: npm run build` · `Makefile:92` `cd apps/web && npm run build`(`e2e` 가 의존한다) |

**곱이 만든 칸이 이 계획의 범위를 정한다.** **사용자만 ✔ 인 행**은 「이미지를 짓는다」·「스택이 뜬다」·
「시드가 닿는다」이고, 그중 **가장 앞의 문**이 「짓는다」다 — 짓지 못하면 나머지는 시작하지도
못한다. **그리고 마지막 행이 대조군이다**: 같은 저장소가 **웹 번들은 CI 와 로컬 게이트 둘 다에서
짓는다.** 파이썬 이미지만 사용자에게 남아 있다.

### 1-d. 한정어 역방향 확인 (§6-3 — **새로 쓰는 조건절도 예외가 아니다**)

이 계획의 조건절은 **「첫 실행의 이미지를 **사용자보다 먼저** 짓는 자리를 저장소에 둔다」** 다.
한정어마다 한 줄씩 답한다.

| 한정어 | 빼면 무엇이 더 들어오나 | 있어서 무엇이 빠지나 | 이 계획의 처분 |
|---|---|---|---|
| **첫 실행의** | 재빌드·캐시 무효화·업그레이드 경로가 들어온다 | **두 번째 빌드부터의 실패**(레이어 캐시가 감추는 것) | 범위 밖으로 **명시**한다(§1-e). CI 는 캐시 없는 러너에서 도므로 이 축은 오히려 CI 가 낫다 |
| **이미지를** | 스택 기동·헬스체크·마이그레이션까지 들어온다 | **뜨는가**(`build` ≠ `up`) — 컨테이너가 실제로 서는지는 이 조건절 밖이다 | 범위 밖. **§후속 77**(준비 상태 계약)과 한 자리이고, 이 계획은 그것을 닫지 않는다 |
| **짓는** | `config` 축·lint 축까지 들어온다 | 이미 붙들려 있는 것 — `docker compose config` 의 rc 는 불변식이 본다 | 잃는 것 없음(§1-c 첫 행이 ✔ 다) |
| **사용자보다 먼저** | 「언젠가 짓는다」가 들어온다 | **사용자가 이미 친 뒤의 재현** | 그것은 §확인하지 않은 것 **79** 이고 이 계획이 닫지 않는다 |
| **저장소에 둔다** | 남의 CI·개인 스크립트가 들어온다 | **저장소가 아닌 자리에서 도는 빌드** — 그것은 저장소가 붙들 수 없다 | 게이트를 **저장소 안**(불변식)에 둔다. 그 이유가 §1-e ⓑ 다 |

**옛 조건이 잡던 것도 묻는다**(§6-3). 계획 0016 의 조건절은 **「첫 실행 경로의 실패가 소리내어
죽는가」** 였고 그것이 잡던 것은 **문 1·2·3·6 의 관측 가능성**이다. 이 계획의 조건절로 바꾸면 그
자리들은 **전부 「통과」로 넘어간다** — 문 1·2 는 불변식이, 문 3 은 `Dockerfile` 의 rc 가, 문 6 은
vitest 가 붙든다. **잃는 것은 하나다**: 계획 0016 은 「소리」를 **네 관측자 중 최소 하나**로 정의했는데
문 3 의 관측자는 **rc 뿐**이고, 이 계획이 그 rc 를 **읽는 주체**를 만들지 못하면 그 정의는 문 3 에서
공허하게 참이다. **그러므로 이 계획은 계획 0016 의 조건절을 대체하지 않고 그것의 미완을 잇는다.**

### 1-e. 이 사이클의 범위

**닫는다**: ⓐ **CI 가 이미지를 짓는다**(사용자보다 먼저 도는 주체를 하나 만든다) ⓑ **저장소가 그
사실을 붙든다**(워크플로 선언을 읽는 불변식 — 그 잡이 사라지거나 조용히 건너뛰어지면 빨개진다).

**닫지 않는다**: 스택 기동(`up`)·헬스체크(§후속 **77**) · 시드가 실제로 닿는가 · 컨테이너 안의 프록시 ·
재빌드 경로. 근거 셋: ⓐ **곱 표의 가장 앞 문이 「짓는다」**이고 뒤의 것은 그것 없이 서지 못한다
ⓑ 기동을 CI 에 두면 **서비스 여섯**이 들어와 잡의 실패 원인이 갈리지 않는다(§6-2 1: 결함이 있어도
값이 같은 시나리오를 만들지 않는다) ⓒ 이 환경에서 **데몬이 없어**(rc 1) 기동 축의 실행값을 이 계획이
낼 수 없다. **셋 다 §후속으로 이름을 남긴다.**

---

## 2. 판정 — 큐 여섯 · CLAUDE.md 대기열 · 경쟁 후보 중 **무엇을 이 사이클로 삼는가**

**결론: 경쟁 후보(이미지를 짓는 주체)를 삼는다.** 큐 여섯도, CLAUDE.md 대기열도 삼지 않는다.
**그리고 여섯을 「재측정하지 않았다」로 넘기지 않는다** — 아래가 `12a2d34` 에서 **내가 다시 잰 값**이다
(§6-1 12회차에 대한 이 계획의 답: 목록을 옮기는 것과 상태를 아는 것은 다른 일이다).

| # | 문안(계획 0016 §M-8 에서 **인용 · 발췌**) | **`12a2d34` 에서 다시 잰 값**(각 N=1) | 삼지 않는 근거 |
|---|---|---|---|
| **2** | *"`"rejected"` 값 리터럴의 전수 감사"*(qa) | `git grep -nI '"rejected"' 12a2d34 -- .` → **187줄**, 그중 `/docs/` 밖 **117줄**. 계획 0016 이 `a03f13d` 에서 잰 186 / 117 에서 **문서 쪽만 하나 늘었다** | 이 경로 밖. **117줄을 읽는 일**이고, 이 사이클의 qa 예산은 작업 1·2 가 쓴다 |
| **3** | *"`apps/web/src/api/client.ts:12` 의 TODO"*(qa) | `git grep -n "TODO(round-6" 12a2d34 -- buildtwin/apps/web/src/api/client.ts` → **`:12`** 그대로 | 이 경로 밖이고 **자리도 겹치지 않는다**(이 사이클은 `apps/web/` 를 만지지 않는다) |
| **6** | *"`docs/api.md` 오류 절의 예외 열거와 `internal_error`"*(api) | `git grep -c "MappingDecisionNotCancellableError\|MappingDecisionCancelReasonRequiredError" 12a2d34 -- buildtwin/services/api/scripts/gen_api_doc.py` → **rc 1 = 히트 0**(BRE 라 `\|` 가 교대다) — 계획 0016 과 같다 | **생성기 사이클**의 일이고 이 사이클은 아니다 |
| **11** | *"확정 축의 `expert_review_logs` 도 무보호"*(qa) | `git grep -nI "expert_review_logs" 12a2d34 -- .` → **40줄**. **변이를 심지 않았다** | **로그** 경로이고 판정·인증 경로가 아니다. 그리고 **이미지가 서지 않으면 그 로그를 만들 사용자가 없다** |
| **23** | *"`postgres.measured.json` 의 `server_version`"*(qa + architect). 재개 조건: **단언·비교가 하나라도 생기는 순간** | `git grep -nI "server_version" 12a2d34 -- .` → **65줄**, `/docs/` 밖 **15줄**(계획 0016 과 같다). **잰 값에 대한 단언·비교는 여전히 0건** | **재개 조건이 성립하지 않는다.** 이월 |
| **24** | *"PostGIS 가 로드된 DB 에서 무엇이 달라지는가"*(qa) | `git grep -nIi "postgis" 12a2d34 -- .` 의 `/docs/` 밖 **13줄**. 이 환경은 **데몬 rc 1 · `ip_forward` 0** 이라 컨테이너 네트워킹이 없다 | 이월. **닫힘은 여전히 §확인하지 않은 것 76(`--network host` 갈래)와 한 자리다** |

**CLAUDE.md 대기열도 삼지 않는다 — 그러나 그 판정의 근거가 이 사이클에 바뀌었다.**
대기열은 **§6-5(압축) → §2(19 무소유)** 순서이고, 둘 다 값으로 다시 쟀다(계획 0016 §M-13-7, **내가 잰 값**).

- **§6-5 압축**: 문턱은 넘겨 있다(근거 표면 **1,090자** = 못박은 분모 대비 **7.20%** · §6 실측 분모
  21,128 대비 **5.16%**). **그러나 「문턱을 넘겼다」는 「압축하라」가 아니다**(§후속 63 의 *닫힘의 모양*).
  **그리고 이 사이클이 값 하나를 더 얻었다**: §후속 **50** 의 세 칸(§6-1·§6-3·§6-5 자신의 근거 표면)은
  `**규칙.**` 줄로 갈리지 않는다 — §6-1 은 그 줄 **뒤**가, §6-3 은 그 줄 **앞**이 규칙·근거 섞임이고,
  §6-5 에는 그 줄이 **없다**(계획 0016 §M-13-7 ⓑ 의 표). **그러므로 §6-5 사이클의 첫 일은 압축이
  아니라 「무엇이 규칙인가」를 사람이 한 줄씩 가르는 일**이고, 그것만으로 한 사이클이다.
- **§2(19) 무소유**: 계획 0016 §M-13-7 ⓐ 가 `buildtwin/` 최상위를 전수했고, §2 트리 블록이 소유를
  적지 않는 자리는 **`Dockerfile` · `docker-compose.yml` · `pyproject.toml` · `README.md` ·
  `.env.example` · `.gitignore` · `scripts/`** 다. **이 사이클의 표적인 `Dockerfile` 이 그 목록에 있다** —
  그래서 **작업 분배가 그것을 「배정」으로 다루고 소유는 정하지 않는다**(계획 0016 과 같은 처분).

**그리고 그 커밋이 져야 할 항목이 또 늘었다**: 25 + 27·34·38 + 46 + 63 + 65 + 69·71 에 더해
이 사이클이 인계받는 **79 의 닫힘 조건 정정** · **82·83·84·85**. **그 항목들을 한 커밋에 담는 일은
그 자체로 한 사이클**이고, 이 사이클의 architect 예산은 작업 3·4 가 쓴다.

**이 경로를 이 사이클로 삼는 근거 넷 — 전부 값이다.**

1. **곱 표에서 「사용자만 ✔」인 가장 앞의 문이 그것이다**(§1-c). 같은 저장소가 **웹 번들은 CI 와 로컬
   게이트 둘 다에서 짓는다**(CI `:250` · `Makefile:92`) — **파이썬 이미지만 사용자에게 남아 있다.**
2. **사이클 0016 이 그 위험을 사용자에게 보냈다.** `c82cf47` 이 폴백을 지웠으므로 그 설치가 실패하면
   빌드가 **rc≠0** 으로 죽는데, 계획 0016 §리스크 1 이 적은 대로 **그 rc 를 처음 보는 사람이 사용자**다.
   이 계획은 그 위험을 **CI 로 옮긴다** — 없애는 것이 아니라 **먼저 받게** 한다.
3. **이 저장소가 그 실패를 이미 이름으로 갖고 있다.** `.github/workflows/buildtwin-ci.yml:6` 은
   *"…CI 가 그 축을 안 돌리면 **아무도 돌리지 않는 축**이 된다(ADR 0017 §6 대안 5 가 기각한 것이
   그것이다)"* 라고 적는다(**인용 · 발췌**, 내가 읽었다). **같은 문장이 오늘 이미지 축에 그대로 걸린다.**
4. **여기서 잴 수 없는 것을 재려 들지 않는다 — 그런데 잴 수 있는 절반이 있다.** 데몬이 없어
   **빌드의 결과**는 이 환경이 못 낸다. 그러나 **잡의 선언**(워크플로 YAML)은 데몬 없이 읽히고,
   그것을 붙드는 불변식도 데몬 없이 선다. **그래서 이 계획은 완료 조건을 두 층으로 나눈다** —
   저장소가 붙드는 층(로컬에서 값이 난다)과 **CI 가 답하는 층**(이 사이클이 재지 못하고 이름으로 남긴다).

*역방향 확인 — 이 판정이 놓치는 것(§6-1 ②)*.
- **ⓐ 큐를 세 사이클 연속 민다.** 2·3·6·11 은 계획 0006~0008 에서 열려 **열 사이클째**다. 계획 0016
  §2 역방향 ⓐ 가 *"대기열을 한 번 밀면 다음에도 밀 수 있다"* 를 적고 자신이 첫 실례라 했는데,
  **이 계획이 둘째 실례다.** 할 수 있는 것은 위 표처럼 **상태를 갱신해 넘기는 것**뿐이고 그 사실을
  여기 적는다. **그리고 계획 0016 §리스크 4 의 처방을 이행한다** — 그 자체를 §후속 **86** 으로 연다.
- **ⓑ 「주체 넷」이라는 축은 내가 골랐다**(§1-c). 다섯째 주체(예: 다른 개발자의 로컬 · 배포 파이프라인)
  에서만 도는 자리는 이 곱에 칸이 없다.
- **ⓒ CI 가 도는 것이 「사용자의 환경」은 아니다.** ubuntu-latest 러너의 빌드가 통과해도 사용자의
  기계에서 통과한다는 뜻이 아니다 — 이 계획이 만드는 것은 **보증이 아니라 관측자**다.
- **ⓓ 이 판정은 「사용자가 첫 실행자」라는 사실 위에 선다.** 그 사실이 바뀌면(누군가 이미 돌렸다면)
  근거 1·2 의 무게가 줄고 근거 3·4 만 남는다 — **그 둘로도 선다고 나는 판단하지만, 그 판단은 여기
  적어야 검증된다.**

---

## 3. 이월 규약 판정 — **계획 0016 과 같다: 「상태 표」로 옮긴다**

**근거는 값이다**(계획 0016 §M-13-5, N=1): 그 규약의 표본이 **셋**이 됐고(§후속 → §M-8 → §M-13-5),
**그 표가 갈림을 감췄는가**의 답은 **감추지 않았다**이다 — 심사 0016 재심사가 잡은 둘(N2·N3)은
상태 표가 아니라 **축 표의 라벨**과 **근거 문안**에서 났다.

**그러므로 이 계획의 §후속도 이렇게 옮긴다.**
- **정본은 못박은 트리의 계획 0016 이다**: `git show 12a2d34:buildtwin/docs/plans/0016-the-first-run-must-fail-out-loud.md`
  의 §후속 + §M-8 + §M-13-5. **이 계획은 그 산문을 다시 쓰지 않는다.**
- **이 계획이 싣는 것은 「번호 · 한 줄 문안 · 소유 · 상태」 표**이고, **상태 칸은 세 자리를 겹쳐 읽은
  결과 하나**다.
- **이 계획이 지는 항목**(작업의 완료 조건에 이름으로 들어가는 것)만 **문안을 그 자리에 적는다.**

*역방향 확인 — 이 규약이 놓치는 것.* ⓐ **한 줄 문안은 발췌다** — 항목을 **닫으려면** 정본을 열어
그 조건을 읽어야 하고, **이 표만 보고 닫지 않는다.** ⓑ **상태 칸은 내가 겹쳐 읽은 판정이다** — 갈리면
내 판정이 하나의 답을 내고, 그 판정이 틀리면 **표가 갈림을 감춘다.** ⓒ 표본 **3**(계획 0015 → 0016 →
이 계획). §후속 80 이 그 축이고 이 계획이 넷째 표본을 만든다.

---

## 작업 분배

**소유는 CLAUDE.md §2 가 정본이다.** 아래 「소유」 열이 그것이고, 「배정」은 이 사이클의 것이지
소유가 아니다(§2: *"계획의 작업 분배 행은 그 사이클의 배정이지 소유가 아니다"*, **인용 · 발췌**).
확인(각 N=1, 트리 `12a2d34`): `.github/workflows/` 의 소유는 **`qa`** 다 — CLAUDE.md §4 의 에이전트 표가
`qa | sonnet | tests/, .github/workflows/, Makefile` 이라 적는다(그 자리에서 도는 참조:
`grep -n "github/workflows" CLAUDE.md`). **`Dockerfile` 은 소유가 없다**(§후속 19 — 계획 0016
§M-13-7 ⓐ 의 전수) — 이 계획은 그것을 **읽기만** 하고 만지지 않는다.

| # | 작업 | 소유 | 만지는 트리 | 의존 |
|---|---|---|---|---|
| 1 | **CI 가 이미지를 짓는다** — `buildtwin-ci.yml` 에 빌드 잡을 더한다 | `qa` | `.github/workflows/buildtwin-ci.yml` | — |
| 2 | **저장소가 그 잡을 붙든다** — 워크플로 선언을 읽는 불변식(잡의 존재 · 짓는 명령 · **삼키는 열쇠의 부재**) | `qa` | `tests/invariants/` | 1 |
| 3 | **범위 밖으로 민 축을 이름으로 남긴다** — §후속·§확인하지 않은 것의 신규 항목 문안 | `architect` | `docs/plans/` | 1·2 |
| 4 | **마감** — 게이트 재측정 · 교차 확인 표 · 상태 표 | `architect` | `docs/` | 전부 |

**작업 1·2 를 한 소유가 지는 것이 이 사이클의 축이다**(계획 0016 은 세 소유가 같은 문자열 계약을
나눠 써서 ADR 이 필요했다). 여기서는 **계약을 나눠 쓰는 소유가 없다** → **ADR 을 열지 않는다**(아래).

### 완료 조건

**형식(§후속 55).** 각 인용에 **ⓐ**(그 자리에서 도는 참조)와 **ⓑ**(인용/재서술 · 전체/발췌)를 붙인다.
**그리고 각 상자는 「이 작업이 거짓으로 만드는 *남의* 자리」 행을 이름으로 갖는다**
(§후속 **72·74·76** 의 공통 처방 — 계획 0016 이 첫 이행이었고 **76 은 첫 시험을 통과했다**;
이 계획이 **둘째 이행**이다).

**작업 1 (qa · `.github/workflows/buildtwin-ci.yml`)**
1. **잡이 이미지를 짓는다.** `docker compose build`(또는 그와 같은 일을 하는 명령)를 부르고,
   **그 rc 가 잡의 rc 가 된다.** 확인: 그 스텝이 `continue-on-error` 를 갖지 않는다.
2. **짓기 전에 `.env` 를 만든다.** compose 의 `api`·`worker` 는 둘 다 `env_file: [.env]` 이고
   (**ⓐ** `grep -n "env_file" docker-compose.yml`, **ⓑ 코드 인용 · 발췌**), `.env` 가 없으면
   **파일 해석 단계에서** 죽는다(그 사실은 `tests/invariants/test_demo_stack_can_stand.py` 가 이미
   붙든다). **저장소에 `.env` 를 커밋하지 않는다** — 잡 안에서 `make env` 를 부른다.
3. **조용히 건너뛰지 않는다.** `if:` 로 그 잡을 끄는 조건을 두지 않고, `paths` 필터는 이미 워크플로
   전체에 걸려 있는 것을 그대로 쓴다(**ⓐ** `grep -n "paths:" ../.github/workflows/buildtwin-ci.yml`).
4. **다른 잡의 초록을 이 잡이 가리지 않는다.** 기존 넷(`lint`·`unit`·`integration`·`e2e`)의
   `needs` 를 바꾸지 않는다 — 빌드가 빨개져도 **다른 축의 값은 그대로 보여야 한다**(§6-2: 하나의
   실패가 다른 축을 삼키면 그 축은 관측되지 않는다).
5. **빨개지면 그것이 값이다.** 이 잡이 처음부터 빨간 채로 오면 그것은 회귀가 아니라 **계획 0016
   §리스크 1 이 예고한 발견**이다 — 그때 고칠 자리는 `pyproject.toml` 이지 이 잡이 아니고,
   **그 판정과 값을 커밋 본문에 이름으로 적는다.**
6. **거짓으로 만드는 남의 자리**: ⓧ `buildtwin/README.md` 의 §빠른 시작(소유 **없음**, §후속 19) 이
   *"빌드는 당신이 처음 돌린다"* 는 뜻의 문장을 갖는지 ⓨ `buildtwin/Makefile` 의 `dev:` 선행 안내
   (소유 `qa` — 자기 소유) ⓩ `buildtwin/docs/adr/0020-*.md` §2-1 **항 2** — *"rc 를 갖지 않는 자리
   … 의 실패는 … **관측자 넷**(rc · stderr/로그 · 게이트 · 화면) 중 **최소 하나에서 「실패라고 말하는
   형태로」** 보인다"*(**ⓑ 인용 · 발췌**) — 가 문 3 에서 **관측자를 얻는다**(오늘 문 3 의 관측자는
   rc 뿐이고 그 rc 를 읽는 주체가 없다). 소유 `architect`. **셋을 각각 읽고 값으로 판정해 커밋 본문에 적는다** — 거짓이 되면 ⓩ 는 **같은 사이클 안에서 architect 가 고쳐야 완료**다
   (§후속 76). **히트를 읽고 0 이면 「0 을 읽었다」를 커밋 본문에 적는다** — 안 적으면 안 읽은 것과
   구별되지 않는다.

**작업 2 (qa · `tests/invariants/`)**
1. **잡의 선언을 읽는 불변식**이 셋을 단언한다: ⓘ 그 이름의 잡이 워크플로에 **있다** ⓙ 그 잡의 어느
   스텝이 **이미지를 짓는 명령**을 부른다 ⓚ 그 스텝·잡에 **삼키는 열쇠가 없다**
   (`continue-on-error: true` · 그 잡을 끄는 `if:`).
2. **양성 케이스가 결함 코드에서 값이 갈리는지 태운다**(§6-2 1). 변이는 **한 자리씩** 심고
   **심기 직전의 작업 트리 사본과 `diff`** 로 적용을 확인한 뒤 재고 그 사본으로 원복한다
   (§6-2 규칙 5 — **같은 커밋이 처음 넣는 줄을 지우는 변이는 `git diff` 가 침묵한다**).
   최소한 이 셋을 심는다: 잡을 통째로 지운다 · 짓는 명령을 다른 명령으로 바꾼다 ·
   `continue-on-error: true` 를 단다. **각 변이가 죽이는 단언의 이름과 값을 표로 남긴다.**
3. **탐침 자신을 태운다**(§6-2 5 — `test_the_first_run_names_its_doors.py` 의 Y5 가 그 선례다):
   워크플로를 읽는 경로가 틀렸을 때 **초록으로 지나가지 않는지**를 단언 하나로 붙든다.
4. **개수를 세지 않는다**(§6-1 9회차) — *"잡이 다섯이다"* 같은 문장을 쓰지 않고, 세는 대신
   **이름**과 **부재**를 단언한다.
5. **테스트를 skip·비활성·격리하지 않는다.** **데몬을 요구하는 단언을 두지 않는다**(§0) —
   이 불변식이 보는 것은 **YAML 의 선언**이지 빌드의 결과가 아니고, **그 한계를 그 파일의 머리
   주석이 값으로 적는다**(그 파일이 보는 것 ↔ 보지 못하는 것).
6. **거짓으로 만드는 남의 자리**: `tests/invariants/test_demo_stack_can_stand.py` 머리의
   **§보지 못하는 것** 첫 불릿 — *"**컨테이너가 실제로 뜨는가.** `config` 는 파일 해석까지다.
   `docker compose up --build` 는 이 사이클의 누구도 돌리지 않았다"*(**ⓑ 인용 · 전체**, 같은 소유).
   새 잡이 그 문장의 **뒷줄**을 낡게 만드는지(「누구도 돌리지 않았다」 ↔ CI 가 짓는다) 읽고,
   낡으면 **같은 커밋에서** 고친다. *한정*: 그 불릿은 `up` 을 말하고 이 사이클은 `build` 만 더한다 —
   **낡는지 아닌지를 값으로 판정하고 커밋 본문에 적는다.**

**작업 3 (architect · `docs/plans/`)**
1. **범위 밖으로 민 축을 §후속으로 연다** — 기동(`up`)·시드가 닿는가·재빌드 경로. 각 항목에
   **묻는 것 · 닫힘의 조건 · 한정**을 적는다.
2. **`Dockerfile` 의 소유 없음을 다시 적지 않는다** — 정본은 §후속 19 이고 계획 0016 §M-13-7 ⓐ 가
   전수를 실었다. **이 계획은 그것을 가리킬 뿐 자기 축을 세우지 않는다**(§2·§3-13 의 판단과 같다).
3. **거짓으로 만드는 남의 자리**: **없다고 판단했다.** 근거: 이 작업이 만지는 것은 이 계획 문서
   하나이고, **다른 소유의 문구를 인용만 한다.** 인용마다 ⓐⓑ 를 붙였는지 마감이 대조한다.

**작업 4 (architect · `docs/`)**
1. 게이트 재측정(아래 §게이트), **방법 · N · 표본 간격**과 함께.
2. **교차 확인 표** — 이 사이클이 **두 번 이상 적은 값**(§6-3 11회차). **갈리지 않은 칸도 적는다.**
   **표에 넣을 값의 목록도 §6-1 ①②③ 을 받는다** — *"계획이 미리 이름 붙인 값"* 이 아니라
   *"이 사이클의 어느 자리든 두 번 이상 적힌 값 — **실행이 만들어 낸 값 자신**을 포함해서"*.
3. §후속 · §확인하지 않은 것 갱신은 **§3 의 규약대로 상태 표**로.
4. **CLAUDE.md 를 만지지 않는다** — 승격은 §6-5 대기열의 순서 안에서 한다(§2).
5. **CI 가 낸 값을 이 문서로 옮긴다**(§후속 62): 잡이 초록인지 빨간지, 빨갛다면 **어느 스텝이
   무엇으로** 죽었는지. **그 값은 저장소 밖(CI 로그)이므로 출처를 표시한다.**

---

## 게이트

- **커밋 전 `make test` 통과**(§3-1). 문서만 고쳤어도 **직접 돌리고 전후 `git status --porcelain` 을 본다.**
- `make lint` **rc 0**, `make e2e` **12 passed**(고아 `vite preview` 전 0 → 후 0 · `.e2e-preview.*` 잔여 0).
- **바닥값(`tests/metrics.json` · `postgres.floor.json`)을 이 사이클이 올리지 않는다.**
- **unit 492 · invariants 118 · regression 8 · integration 232 · vitest 310 이 유지되는지를 수가 아니라
  「어느 것이 바뀌는지」로 커밋 본문에 적는다.** 작업 2 는 invariants 를 늘리므로 **늘어난 것의 이름과
  따로 실행한 값**을 적는다.
- **포트를 잡았으면 반납한다. 저장소 루트에도 `buildtwin/` 에도 `.env` 를 만들지 않는다.**
- **어떤 로컬 완료 조건도 docker 데몬을 요구하지 않는다**(§2 근거 4). **빌드의 결과는 CI 가 답한다.**
- **소유 경계가 원자적 변경을 가르면**(§후속 73) 커밋 경계는 소유로, 원자성은 PR 로, **빨간 이유는
  어느 테스트가 왜 빨간지를 이름과 값으로** 커밋 본문에.

## 리스크

1. **새 잡이 처음부터 빨갈 수 있다.** `Dockerfile` 의 `RUN pip install --no-cache-dir -e .` 가 통과하는지
   **아무도 재지 않았다**(계획 0016 §확인하지 않은 것 79). **그것이 이 작업의 목적이다** — 빨개지면
   그때 고칠 자리는 `pyproject.toml` 이고, **그 발견은 사용자가 아니라 CI 가 한다.** 다만 이 위험은
   **PR 의 초록 개수를 바꾼다** → 작업 1 조건 4·5 가 그것을 다룬다.
2. **CI 러너에 시간·용량이 든다.** 이미지 빌드는 `apt-get` 과 `pip` 를 탄다. 잡이 오래 걸리면 다른
   잡의 피드백이 늦어진다 → 작업 1 조건 4(다른 잡의 `needs` 를 바꾸지 않는다)가 그것을 막는다.
3. **불변식이 YAML 의 선언만 본다.** 잡이 있는데 **실제로는 아무것도 짓지 않는** 경우를 그 불변식은
   구별하지 못할 수 있다 → 작업 2 조건 2 의 변이(짓는 명령을 다른 명령으로 바꾼다)가 그 축을 태운다.
   **그래도 남는 미지**는 §확인하지 않은 것으로 이름을 남긴다.
4. **이 계획이 큐를 세 사이클 연속 민다**(§2 역방향 ⓐ). **그 자체를 §후속 86 으로 연다** —
   미루는 것과 안 하기로 하는 것은 다르다.

## ADR 필요 여부 — **열지 않는다**

근거 셋(각 N=1):
1. **계약을 나눠 쓰는 소유가 없다.** 계획 0016 이 ADR 0020 을 연 근거는 *"세 소유(architect 배정 ·
   frontend · qa)가 **같은 문자열 계약**을 각자 만든다"* 였다(**ⓑ 인용 · 발췌**). 이 사이클의 산출물은
   **`qa` 하나의 트리 둘**(워크플로 · 불변식)이고, 그 둘은 같은 소유의 같은 사이클이 쓴다.
2. **이 축의 판단은 이미 저장소에 ADR 로 있다.** `.github/workflows/buildtwin-ci.yml:6` 이 ADR 0017
   §6 대안 5 를 가리키고, `docs/adr/0017-*.md` 가 *"**아무도 돌리지 않는 축**이 된다"* 를 적는다
   (**ⓐ** `git grep -nI "아무도 돌리지 않는 축" 12a2d34 -- .` → **그 둘**, N=1). **새 ADR 은 그
   판단을 되풀이할 뿐이다.**
3. **CLAUDE.md 승격도 이 사이클이 하지 않는다** — §6-5 대기열의 순서 안이다(§2).

*역방향 확인 — 이 판정이 놓치는 것.* **ADR 을 열지 않으면 「무엇을 짓고 무엇을 안 짓는가」의 결정이
워크플로 주석에만 남는다.** 그 주석은 소유가 `qa` 이고 CI 파일과 함께 낡는다 — **그 위험을 이름으로
남긴다**(§후속 87). 관측이 둘 이상 쌓이면 그때 ADR 을 연다.

## 후속 — 이월 (§3 의 규약: **상태 표**로 옮긴다)

**정본**: `git show 12a2d34:buildtwin/docs/plans/0016-the-first-run-must-fail-out-loud.md` 의
§후속 + §M-8 + §M-13-5.

**이 계획이 지는 항목**(완료 조건 상자에 이름으로 들어간 것) — 문안을 여기 적는다:
- **19** *"소유가 §2 에 없는 파일들"*(architect). **이 계획은 실례를 더하지 않는다** — 계획 0016
  §M-13-7 ⓐ 가 `buildtwin/` 최상위를 **전수**했고 이 계획은 그것을 가리킨다.
- **72 · 74 · 76** — 세 항목의 처방이 하나다: *"계획의 완료 조건 상자에 **이 작업이 거짓으로 만드는
  남의 자리** 행을 이름으로 넣는다"*(**ⓑ 인용 · 발췌**). **이 계획의 완료 조건 상자 넷이 그 행을
  갖는다 — 그 처방의 둘째 이행이다.** 셋은 **닫히지 않는다**.
- **77** *"compose 에 준비 상태 계약이 없다"* — **이 계획이 닫지 않는다**(§1-e ⓑⓒ). 기동 축이다.
- **79** *"「실패를 삼키는 모양」의 전수"*(architect + qa) — **닫히지 않는다.** 계획 0016 §M-8 79-2 가
  **네 모양 각각의 전수**로 닫힘 조건을 고쳐 적었고, 모양 4 의 세는 법(걸음 1~4)까지 정했다.
  **남은 것은 걸음 1 의 목록**(사용자에게 값을 문자열로 말하는 자리)이고 **이 사이클은 그것을 만들지
  않는다** — 이 사이클의 축은 「짓는 주체」이지 「삼키는 모양」이 아니다.
- **65 · 46 · 34 · 62** — 이 계획의 §0 과 측정 방법이 그 조항들을 진다.
- **63 · 50 · 38 · 25 · 27 · 69 · 71 · 82 · 83 · 84 · 85** — **§6-5 커밋**의 몫. 이 사이클은 만지지 않는다(§2).

**신규(이 계획이 연다):**

86. **큐 여섯을 세 사이클 연속 밀었다 — 미루는 것과 안 하기로 하는 것을 가른다**(architect).
    계획 0016 §리스크 4 의 처방이다(*"다음 계획이 또 밀면 그 자체를 §후속 항목으로 연다"*,
    **ⓑ 인용 · 전체**). 값: 2·3·6·11 은 계획 0006~0008 에서 열려 **열 사이클째**이고, 23·24 는
    **재개 조건이 성립하지 않는다**(§2 표, 내가 다시 잰 값). *묻는 것*: 여섯 각각을 **① 다음 사이클의
    주제로 삼는다 ② 재개 조건을 명시하고 잠근다 ③ 닫지 않기로 하고 이유를 적는다** 중 어디에 두는가.
    *닫힘의 조건*: 어느 계획이 여섯 각각에 그 셋 중 하나를 값과 함께 배정할 때. *한정*: **이 항목은
    큐를 대신 처리하지 않는다** — 미루기를 **보이게** 만들 뿐이다.
87. **「무엇을 짓고 무엇을 안 짓는가」의 결정이 워크플로 주석에만 남는다**(qa + architect).
    이 계획이 **ADR 을 열지 않기로** 판정한 것의 대가다(§ADR 필요 여부의 역방향 확인). *묻는 것*:
    그 결정(왜 `build` 만이고 `up` 은 아닌가 · 왜 `api` 이미지인가)이 **CI 파일과 함께 낡을 때**
    누가 그것을 보는가. *닫힘의 조건*: 그 결정이 **어느 게이트에도 걸리지 않는다**는 것이 관측
    **2회** 이상이 되거나, 한 사이클이 그것을 ADR 로 올릴 때. *한정*: **관측 0회** — 오늘은 판정이지
    관측이 아니다.
88. **스택이 뜨는가 · 시드가 닿는가를 도는 주체가 여전히 사용자뿐이다**(qa + architect).
    §1-c 곱 표에서 **사용자만 ✔ 인 행 중 이 계획이 닫지 않는 둘**이다. *묻는 것*: CI 가 `up` 까지
    가야 하는가(서비스 여섯 · 헬스체크 — §후속 77 과 한 자리). *닫힘의 조건*: 그 잡의 실패 원인이
    **갈리는** 모양으로 설계되고(§6-2 1), 그 값이 어느 계획에 적힐 때. *한정*: 이 환경은 그 축의
    실행값을 낼 수 없다(데몬 rc 1).

## 이 계획이 확인하지 않은 것

**정본**: 계획 0016 의 §확인하지 않은 것 + §M-9 + §M-13-6. **번호를 잇는다.**

87. **[재지 않았다] 새 잡이 초록인지 빨간지.** 이 환경은 데몬이 없어(`docker info` rc 1,
    `ip_forward` 0) **빌드를 돌릴 수 없다.** 이 계획이 아는 것은 ⓘ 저장소에 짓는 자리가
    `Makefile:40` 하나다 ⓙ `.github` 에 그 표기가 **히트 0** 이다 ⓚ 설치 줄이 폴백을 갖지 않는다
    셋뿐이다. **[재야 한다 → CI · 작업 1 이 머지된 뒤 첫 실행]**
88. **[재지 않았다] 러너에서 빌드가 얼마나 걸리는가.** 리스크 2 의 값이 없다.
    **[재야 한다 → qa · 첫 실행의 잡 로그]**
89. **[재지 않았다] 불변식이 「잡이 있는데 아무것도 짓지 않는」 경우를 가르는가.** 작업 2 조건 2 의
    변이가 그것을 태우기로 했지만 **이 계획은 변이를 하나도 심지 않았다**(자원 0).
    **[재야 한다 → qa · 작업 2 를 만들 때]**
90. **[재지 않았다] `docker compose build` 와 `docker compose up --build` 가 같은 것을 짓는가.**
    이 계획은 **명령의 이름**만 읽었다. **[재야 한다 → qa · 데몬이 있는 자리]**
91. **[재지 않았다] `.env` 를 CI 에서 만드는 것의 부작용.** `make env` 는 난수 `JWT_SECRET` 을
    만든다(`Makefile` 의 `env:` 레시피, 내가 읽었다) — 그 값이 잡 로그·아티팩트에 새는지 재지 않았다.
    **[재야 한다 → qa · 작업 1 을 만들 때]**

## 다음에 부를 에이전트

```
@qa         작업 1 — buildtwin-ci.yml 에 이미지 빌드 잡을 더한다(.env 는 make env 로, 삼키는 열쇠 없이, 다른 잡의 needs 를 바꾸지 않는다).
@qa         작업 2 — 그 잡의 선언을 붙드는 불변식 + 변이로 값이 갈리는 것을 보인다(탐침 자신도 태운다).
@architect  작업 3 — 범위 밖으로 민 축(기동·시드·재빌드)을 §후속으로 연다.
@qa         작업 5 — buildtwin/.dockerignore + 그것을 붙드는 불변식(작업 3 이 더한 배정 — §3-1-6). 작업 4 보다 앞이다.
@architect  작업 4 — 마감(게이트 재측정 · 교차 확인 표 · 상태 표 · CI 가 낸 값의 이관).
@reviewer   전체 리뷰 — 5체크 + 계획이 §6-1·§6-3 을 지켰는지 + 새 잡이 §6-2 를 지켰는지(결함이 있어도 초록인 잡이 아닌지).
```

---

## 작업 3 (architect · `docs/plans/`) — 범위 밖으로 민 축 · 인계받은 판정 · **`.dockerignore` 배정** · **CI 첫 실행의 값**

> **이 절이 이 작업의 산출물이다.** 위 §0~§다음에 부를 에이전트는 트리 `12a2d34` 의 기록물이고
> **이 절은 그것을 고치지 않는다** — 더할 뿐이다(§3-13 첫째 갈래). 이 절이 위 문서를 낡게 만드는
> 자리는 §3-2-4 · §3-5 가 자리마다 판정한다.

### 3-0. 이 절의 실측이 나온 자리 (재현 방법)

**작업 트리** `/home/user/Bim/buildtwin`, **저장소 루트 `/home/user/Bim`**, **트리 `1e8fb19`**
(작업 1·2 가 머지된 뒤. 이 절을 여는 시점의 루트 `git status --porcelain` 은 **빈 출력**, 커밋 직전에
**이 문서 한 줄**). **이 절의 모든 `파일:줄`·커밋 참조는 `1e8fb19` 의 것이다**; 예외는 §0 의 ㉮㉯ 와 같다
(다른 기록물을 인용하는 줄은 그 자리에서 트리를 적고, CLAUDE.md 는 **그 자리에서 도는 `grep`** 으로 부른다).

**명령의 못박음**(§후속 **65** 확장 항 ㉡ — 이 문서가 그 조항을 두 번째로 이행한다): 이 절의 모든 `git`
명령은 **트리 인자를 명령 안에 담고 `--all` 을 쓰지 않는다**(`git grep <패턴> 1e8fb19 -- .` ·
`git log --oneline 1e8fb19 -- <경로>` · `git log --oneline -S <문자열> 1e8fb19 -- <경로>`).
트리 인자를 받지 못하는 명령(`ls` · `du` · `grep` · `python3` · `pytest` · `make`)은 전부 위 작업 트리에서
돌린 것이다.

**방법 · N · 표본 간격**(§후속 **34** · 27). 아래 값은 **2026-09-08 한 세션 안**(19:15~19:24 UTC,
`date -u` 로 앞뒤를 찍었다)에 잰 것이고, **따로 적지 않으면 N=1**, 서로 몇 분 간격이다.
**「없다」·「히트 0」에도 N 을 적는다.**

**저장소 밖 값의 출처**(§후속 **62**). 이 절은 **CI 잡 로그**의 값을 싣는다 — 그것은 저장소 밖이고
내가 이 작업 트리에서 재지 못하는 값이다. 출처를 한 자리에 적고 각 자리에서 되풀이하지 않는다:
**PR #2 의 `1e8fb19`, 잡 `image (docker compose build — 사용자보다 먼저 짓는다)` = success,
19:14:39→19:16:00 UTC.** 오케스트레이터가 내려받은 **로그 사본**(1005줄)을 내가 직접 읽었고
(`grep`·`sed`, 각 N=1), 그 사본은 **세션 스크래치패드에 있고 사라진다** — 저장소에 들어오지 않는다.
**로그 줄 번호는 그 사본의 것이다.**

**자원**: **docker 데몬 0회**(이 절은 데몬을 요구하는 값을 하나도 내지 않는다 — 러너의 값은 읽기만 했다) ·
postgres 클러스터 **0회** · **심은 변이 0** · 저장소 루트에도 `buildtwin/` 에도 **`.env` 를 만들지 않았다**
(`ls -a <각 디렉터리> | grep -c '^\.env$'` → **0**, 각 N=1, 이 절의 앞과 뒤 둘 다) · 잡은 포트 없음.

### 3-1. ㉠ **`.dockerignore` 가 없다 — `make env` 의 시크릿이 이미지 레이어로 흐른다**

#### 3-1-1. 값

**㉮ 이 작업 트리에서 잰 것**(전부 정적 판독 — 데몬 0회):

| # | 물음 | 명령 · 자리 | 값 |
|---|---|---|---|
| ㄱ | `.dockerignore` 가 있는가 | `ls -a /home/user/Bim \| grep -c '^\.dockerignore$'` · 같은 명령의 `buildtwin` | **0 · 0**(각 **N=3**, 19:21:16·:18·:20 UTC) |
| ㄴ | 저장소가 그것을 가진 적이 있는가 | `git log --oneline 1e8fb19 -- buildtwin/.dockerignore` | **빈 출력 = 그 경로의 커밋 0**(N=1) |
| ㄷ | 빌드 컨텍스트가 어디인가 | `grep -n "build:" docker-compose.yml` → `:35`·`:47` `build: { context: ., dockerfile: Dockerfile }`(**ⓑ 코드 인용 · 발췌**) | 컨텍스트 = **`buildtwin/` 자신** — `make env` 가 `.env` 를 쓰는 바로 그 디렉터리 |
| ㄹ | `Dockerfile` 이 무엇을 넣는가 | `git show 1e8fb19:buildtwin/Dockerfile` 의 `COPY pyproject.toml ./` 와 마지막 줄 **`COPY . .`**(**ⓑ 코드 인용 · 발췌**) | 이름으로 COPY 하는 것은 `pyproject.toml` 하나, 나머지는 **컨텍스트 전부** |
| ㅁ | 그 모양이 언제부터인가 | `git log --oneline -S 'COPY . .' 1e8fb19 -- buildtwin/Dockerfile` · `git log --oneline -S 'docker compose up --build' 1e8fb19 -- buildtwin/Makefile` | **`6149e17`**(Dockerfile 첫 커밋) · **`6e48e4c`**(저장소 초기 셋업). **이 사이클이 만든 모양이 아니다** |
| ㅂ | `.env` 에 무엇이 들어가는가 | `Makefile` 의 `env:` 레시피(**ⓐ** `grep -n "^env:" Makefile`, **ⓑ 재서술**) | `umask 077` 뒤 `JWT_SECRET=<난수>` 한 줄. **값은 출력하지 않는다** |
| ㅅ | 컨텍스트가 실제로 무엇을 담는가 | `du -sh .` · 같은 명령에 `--exclude` 를 건 것 | **2.7G** → 파생물(`.venv` **2.4G** · `apps/web/node_modules` **231M** · `.mypy_cache` **43M** · `.pytest_cache` · `.ruff_cache` · `*.egg-info` · `apps/web/dist`)을 빼면 **9.3M**(각 N=1) |
| ㅇ | `.git` 이 컨텍스트에 있는가 | `ls -a buildtwin \| grep -c '^\.git$'` | **0** — 워크플로는 저장소 루트에 있고 `.git` 도 그 위다(N=1) |

**ㅅ 이 왜 값인가**: README 의 ① 갈래가 `make setup`(= `.venv` + `npm install`)을 먼저 시키고
(**ⓐ** `grep -n "make setup" README.md`, **ⓑ 재서술**) ② 갈래가 `make env` → `make dev` 다.
**둘을 순서대로 따른 기계에서 컨텍스트는 파생물이 대부분이다** — 이 작업 트리가 그 상태이고 그 값이 위다.

**㉯ 러너가 잰 것**(저장소 밖 · 출처는 §3-0. 각 N=1, 로그 사본을 내가 읽었다):

| # | 로그 줄 | 값 | 무엇을 말하는가 |
|---|---|---|---|
| ㅈ | `:159`·`:160` | `#5 [worker internal] load .dockerignore` / `#5 transferring context: **2B** done` | **부재를 러너가 스스로 잰 값이다** — 뺄 것을 선언한 파일이 없다 |
| ㅊ | `:135` | `created .env — JWT_SECRET(난수)만 담았다. 값은 출력하지 않는다.`(19:14:42.14) | `.env` 가 **컨텍스트 디렉터리에** 만들어졌다 |
| ㅋ | `:168` | `#7 transferring context: **5.78MB** 0.1s done`(19:14:43.55) | 그 **뒤에** 컨텍스트가 전송됐다 — ㅊ 보다 늦다 |
| ㅌ | `:461`·`:966` | `#10 [worker 4/6] COPY pyproject.toml ./` · `#12 [api 6/6] **COPY . .**` | `COPY . .` 가 **돌았다** |
| ㅍ | `:975`·`:981` | `#13 naming to docker.io/library/**buildtwin-worker**` · `#14 naming to docker.io/library/**buildtwin-api**` | 짓기의 산출물은 **이름 붙은 로컬 이미지**다 |

**그러므로 「들어갈 수 있다」가 아니라 「그 경로가 실제로 돌았다」** — `.env` 생성(ㅊ) → 컨텍스트 전송(ㅋ)
→ `COPY . .`(ㅌ)가 **시각 순으로 확인된다.**
***한정 — 여기서 멈춘다***: **레이어 안에 그 파일이 실제로 있는지는 아무도 열어 보지 않았다.**
로그가 보이는 것은 `COPY . .` 가 돌았다는 것까지이고, 이미지를 여는 것은 데몬을 요구한다
(이 환경 `docker info` rc 1) → **§확인하지 않은 것 97.**

#### 3-1-2. 거짓인가 위험인가 — CLAUDE.md §3-4 의 **한정어 역방향 확인**(§6-3)

판정 대상 문장(**ⓐ** 그 자리에서 도는 참조 `grep -n "외부 API 키" CLAUDE.md`, **ⓑ 인용 · 발췌**):
*"**외부 API 키·시크릿은 `.env`에만** 둔다. 코드·YAML·테스트 픽스처에 하드코딩 금지.
`.env.example`에 키 이름만 나열한다."*

| 한정어 | 빼면 무엇이 더 들어오나 | 있어서 무엇이 빠지나 | 판정 |
|---|---|---|---|
| **외부 API 키·시크릿** | 시크릿이 아닌 설정값까지 | — | `JWT_SECRET` 은 **시크릿**이다(서명 키) → **이 문장 안이다** |
| **`.env`에만** | 값을 여러 파일에 적어도 된다 | 「그 파일 말고 **다른 곳에 그 값이 존재해서는 안 된다**」로 읽으면 — **compose 의 `env_file:` 자신이 위반이 된다**(`docker-compose.yml:37`·`:49`, **ⓑ 코드 인용 · 발췌**). 프로세스 환경 · `docker inspect` · 바인드 마운트된 `/app/.env` 도 같다 | **그 읽기는 자기가 처방한 기전을 위반으로 만든다** → 「에만」이 지는 것은 **값을 적는 자리**(사람이 쓰는 소스 표면)이지 **값이 흘러가는 자리**가 아니다 |
| **둔다** | 「흘러간다」까지 | 사람이 쓰지 않고 **빌드가 복사하는** 자리 | 이미지 레이어는 아무도 「두지」 않는다 — **빌드가 옮긴 것**이다 |
| **코드·YAML·테스트 픽스처에 하드코딩 금지** | — | 이 열거는 전부 **저장소와 함께 이동하는 소스 표면**이다 | 축이 「소스 표면」임을 이 열거가 다시 말한다 |

**판정: 거짓이 아니다. §6-4 1 이 걸리지 않는다.** §3-4 는 **소스 표면**의 규칙이고 이미지 레이어는 그
표면이 아니다. **그리고 그렇게 읽지 않으면 그 규칙은 같은 저장소가 처방한 `env_file:` 과 정면으로
갈린다** — 규칙과 그 규칙이 처방한 기전이 갈리면 틀린 것은 읽기다.

**그러나 위험이다. 그 위험의 이름은 「지속」과 「이동」이다** — §3-4 가 재지 않는 축들이다.

| 축 | `.env` 파일 | 이미지 레이어 |
|---|---|---|
| **수명** | 지우면 없어진다. 사용자가 통제한다 | **`.env` 를 지워도 남는다** · `docker compose down -v` 로도 지워지지 않는다(이미지는 볼륨이 아니다) |
| **접근 제어** | `make env` 가 `umask 077` 로 만든다 — 그 파일시스템의 모드가 진다 | **그 모드가 아니라 「이미지에 닿을 수 있는가」가 진다** — 그 둘이 같은지 이 사이클은 재지 못했다(§확인하지 않은 것 97) |
| **이동** | 사람이 복사해야 나간다 | `docker save` · 레지스트리 푸시 · 기계 공유로 **레이어째** 나갈 수 있다 |
| **관측** | `ls`·`cat` 로 그 자리에서 보인다 | **그 안에 무엇이 들어갔는지 저장소의 어떤 게이트도 보지 않는다** — `tests/invariants/test_ci_builds_the_image.py` 머리의 「보지 못하는 것」 마지막 불릿이 그 부재를 스스로 적는다(**ⓑ 인용 · 발췌**: *"…담게 되는 것은 두 파일 어디에도 없다"*. **문장 전체로는 히트 0 이다** — 그 파일이 *"그 이미지가 실제로 / 담게 되는 것은…"* 으로 줄을 바꾼다: §6-1 역방향 확인 ④ 의 모양이라 **한 줄에 들어가는 조각**으로 인용했다, N=1) |

#### 3-1-3. qa 의 「CI 잡에 대해서는 손해 0」 판정 — **확인했다. 로그도 그것과 맞다**

qa 가 워크플로 주석에 적은 문장(**ⓐ** `grep -n "러너와 함께 사라진다" ../.github/workflows/buildtwin-ci.yml`,
**ⓑ 인용 · 발췌**): *"**이 잡은 이미지를 푸시하지 않으므로** 그 레이어는 러너와 함께 사라진다."*

**㉮ 내가 그 YAML 을 다시 읽은 값**(`python3` 으로 파싱, N=1):

| 물음 | 값 |
|---|---|
| `image` 잡의 스텝 | `actions/checkout@v4` · `run: make env` · `run: docker compose build` — **그뿐이다** |
| 그 잡의 최상위 열쇠 | `name` · `runs-on` · `steps` · `timeout-minutes` — **`permissions` 도 `outputs` 도 없다** |
| 아티팩트로 나가는 자리 | 그 잡에 `upload-artifact` **없다**(있는 자리는 `unit`·`integration` 의 metrics 뿐) |
| 레지스트리로 나가는 자리 | 그 잡에 `login`·`push`·`docker save` **없다** |
| 워크플로 권한 | `permissions: contents: read`(`:27-28`, **ⓑ 코드 인용 · 발췌**) — **`packages: write` 가 없어 푸시할 권한 자체가 없다** |

**㉯ 로그가 같은 것을 말한다**(저장소 밖 · 출처 §3-0, 각 N=1): `grep -nEi "docker (push|login|save)|##\[group\]Run "` 의
히트는 **`Run actions/checkout@v4` · `Run make env` · `Run docker compose build`** 뿐이고
**push·login·save 는 히트 0** 이다. 산출물은 로컬 이름뿐이다(위 ㅍ).

**판정: qa 가 맞다.** 그리고 그 판정을 **더 싸게 만드는 축**이 하나 더 있다: 그 러너의 `JWT_SECRET` 은
**그 잡이 그 자리에서 만든 난수**이고 **아무것도 서명하지 않는다** — 사용자의 토큰도, 어떤 지속 DB 도
그 값에 매여 있지 않다. 즉 그 레이어는 **새지 않을 뿐 아니라 새더라도 아무것도 열지 못한다.**
두 축이 독립이라 하나가 틀려도 다른 하나가 남는다.

**그리고 「잡 로그로도 새지 않는다」를 값으로 쟀다**(§확인하지 않은 것 **91** 이 그 물음이다 —
*"`make env` 가 만든 난수 `JWT_SECRET` 이 잡 로그·아티팩트에 새는지 재지 않았다"*, **ⓑ 인용 · 발췌**):

| 축 | 명령(로그 사본, N=1) | 값 |
|---|---|---|
| 이름으로 | `grep -n "JWT_SECRET"` | **히트 하나** — `:135` 의 *"값은 출력하지 않는다"* 그 줄이다 |
| `.env` 를 읽는 줄 | `grep -n "\.env"` | **같은 한 줄뿐** — `cat`·`echo` 로 그 파일을 찍는 자리가 없다 |
| **값의 모양으로** | `grep -oE '[A-Za-z0-9_-]{43,}' \| sort -u` | 히트를 **전부 읽었다**: URL 슬러그 하나와 나머지는 **16진 다이제스트**다. `secrets.token_urlsafe(32)` 가 내는 **43자 base64url 모양은 없다** |
| 아티팩트 | 위 YAML 표 | 그 잡은 **아무것도 업로드하지 않는다** |

***한정***: 마지막 축은 **모양**으로 판정한 것이다 — 값 자신을 모르므로 그것을 직접 grep 할 수 없다.
「그 모양의 문자열이 로그에 없다」까지가 이 값이고, 「어떤 인코딩으로도 없다」는 아니다.

*역방향 확인 — 이 「손해 0」이 놓치는 것.* ⓐ **잡 로그는 러너와 함께 사라지지 않는다**(GitHub 이
보관한다) — 위 표가 그 축을 값으로 답했다. ⓑ **캐시**: 이 잡은 빌드 캐시를 저장·복원하지 않는다
(`cache` 열쇠 없음) — 캐시를 붙이는 편집이 오면 이 판정은 다시 재야 한다.

#### 3-1-4. **사용자의 기계에서는 그 이미지가 남는다** — 이것이 더 큰 축이다

경로는 README ② 다(**ⓑ 재서술**): `make env`(난수 `JWT_SECRET` 을 `buildtwin/.env` 에 쓴다) →
`make dev`(= `Makefile:40` `docker compose up --build`). **그 `--build` 가 같은 컨텍스트를 같은
`Dockerfile` 로 짓는다.** 러너에서 그 짓기의 산출물이 **`buildtwin-api`·`buildtwin-worker` 라는 이름의
로컬 이미지**였고(ㅍ), 러너에서는 그것이 기계와 함께 사라진다 — **사용자의 기계에서는 사라지지 않는다.**

**그리고 그 레이어는 실행에 쓰이지도 않는다.** `api`·`worker` 는 `volumes: [".:/app", …]` 로
**호스트 디렉터리를 `/app` 위에 덮는다**(`docker-compose.yml:43`·`:55` `volumes: [".:/app", "storage:/data/storage"]`, **ⓑ 코드 인용 · 전체**) —
그래서 `COPY . .` 가 넣은 `/app` 의 내용은 **뜬 컨테이너가 읽지도 않는다.**
**얻는 것 없이 시크릿만 굽는다**는 것이 이 자리의 값이다.

***한정 — 이 축은 아무도 재지 못했다***: 위 문단에서 러너가 답한 것은 **「짓기가 이름 붙은 로컬
이미지를 낸다」** 까지다. **사용자의 기계에서 그것이 남는가**(그리고 얼마나 오래)는 이 사이클이 재지
못했다 → **§확인하지 않은 것 98.** 판정이 갈리는 자리가 정확히 여기다.

#### 3-1-5. **`.dockerignore` 를 이 커밋이 만들지 않는다 — 배정한다**(근거는 값이다)

1. **소유가 없는 자리다**(§후속 **19**). 이 계획 자신이 같은 부류의 `Dockerfile` 에 대해
   *"이 계획은 그것을 **읽기만** 하고 만지지 않는다"* 라고 적었다(§작업 분배 머리말, **ⓑ 인용 · 발췌**).
   **같은 부류를 다르게 처분할 근거가 없다.**
2. **작업 3 의 만지는 트리는 `docs/plans/` 다**(§작업 분배 표). CLAUDE.md §2: *"밖을 고쳐야 하면
   `architect` 에게 먼저 제안하고, `architect` 가 **계획에 명시한 경우에만** 수정한다"*
   (**ⓐ** `grep -n "계획에 명시한 경우에만" CLAUDE.md`, **ⓑ 인용 · 발췌**). **명시하는 것이 내 일이고,
   그것이 아래 작업 5 다.**
3. **결정적 근거 — 게이트를 내가 쓸 수 없다.** `tests/` 의 소유는 `qa` 다. 내가 `.dockerignore` 만
   만들면 그것은 **게이트 없는 계약**이 되고, 그 실패 모양은 이 저장소가 이미 값으로 갖고 있다:
   *"계약이 있는데 게이트가 없으면 다음 편집이 그것을 조용히 지운다"*
   (`tests/invariants/test_ci_builds_the_image.py` 머리, **ⓑ 인용 · 발췌**) — 그 파일이 든 값이
   *"안내를 지워도 `123 passed` 로 무변화였다"* 이고, 같은 파일의 「이 파일이 서기 전」 표가
   *"`image` 잡을 통째로 지운다 → **126 passed**(무변화)"* 다. **`.dockerignore` 의 `.env` 한 줄도
   똑같이 조용히 지워질 수 있다.**
4. **한 소유가 파일과 게이트를 함께 지는 것이 이 사이클의 축이다** — §작업 분배가
   *"작업 1·2 를 한 소유가 지는 것이 이 사이클의 축이다"* 라고 적었고(**ⓑ 인용 · 발췌**),
   작업 5 는 같은 모양이다.

*역방향 확인 — 이 판정이 놓치는 것.* ⓐ **배정은 실행이 아니다.** 작업 5 가 돌기 전에 사용자가
`make dev` 를 치면 그 기계에는 시크릿이 구워진 이미지가 남는다 — **그 창을 이 판정이 연다.**
그 창을 닫는 것은 순서뿐이므로 **작업 5 를 작업 4(마감)보다 앞에 둔다**(§다음에 부를 에이전트에
그 순서로 적었다). ⓑ 이것을 §후속으로 미루지 **않는 것**이 그 창에 대한 답이다 — §후속 **86** 이
이름 붙인 갈래에서 이것은 **① 이 사이클의 작업으로 삼는다** 쪽이다. ⓒ **위험의 크기는 내가
판단했다** — 「시크릿이 굽히는 것」을 「파생물이 통째로 전송되는 것」보다 앞에 뒀고, 그 순서를 뒤집을
값은 이 절에 없다. ⓓ **러너에서는 손해가 0 이므로**(3-1-3) 이 배정이 고치는 것은 **CI 가 아니라
사용자의 기계**다 — 러너의 컨텍스트는 이미 작다(5.78MB, ㅋ).

#### 3-1-6. **작업 5 (qa · `buildtwin/.dockerignore` + `tests/invariants/`)** — 이 절이 더하는 배정

> **소유가 아니라 배정이다**(§2). `.dockerignore` 는 소유가 없는 자리(§후속 19)이고, 이 사이클이
> `qa` 에게 **배정**한다. 근거는 3-1-5 의 3·4 다.

**완료 조건**

1. **`buildtwin/.dockerignore` 를 만든다.** 자리는 **빌드 컨텍스트의 뿌리**여야 한다 — 컨텍스트가
   `buildtwin/` 이므로(ㄷ) 저장소 루트에 두면 **아무 효과가 없다.** 그 자리가 맞는지는 러너가 이미
   이름으로 말했다(ㅈ: `load .dockerignore` 가 `worker`·`api` 의 **internal** 단계다).
2. **빼는 것을 이름으로 정한다**(개수를 적지 않는다 — §6-1 9회차). 축은 하나다:
   **「커밋하지 않는 것은 이미지에도 넣지 않는다」** — `.gitignore` 와 같은 축이고, 그래서 그
   목록이 이 목록의 뼈대다(**ⓐ** `git show 1e8fb19:buildtwin/.gitignore`).
   **반드시 들어가야 하는 이름**(각 이유와 함께):
   - `.env` · `.env.*` — **이 배정의 이유 그 자체**(3-1-2·3-1-4). `.env.example` 은 예외로 남긴다
     (`!.env.example` — `.gitignore` 와 같은 처분).
   - `.venv/` — 호스트의 가상환경. **2.4G**(ㅅ)이고 컨테이너의 인터프리터와 무관하다.
   - `node_modules/` · `apps/web/dist/` · `dist/` — 웹 파생물(**231M**). `api`·`worker` 는 읽지 않고
     `web` 컨테이너는 `node:22-alpine` **이미지**를 쓴다(`docker-compose.yml:58`, **ⓑ 코드 인용 · 발췌**).
   - `.mypy_cache/` · `.pytest_cache/` · `.ruff_cache/` · `__pycache__/` · `*.pyc` · `*.egg-info/` —
     호스트 도구의 캐시(**43M** 이상).
   - `storage/` · `*.db` — **런타임 데이터**. 오늘 이 트리엔 없지만(`ls` → absent, N=1) 사용자의
     기계에는 업로드 원본과 로컬 sqlite 가 그 이름으로 생긴다(`Makefile` 의 `db-reset` 이
     `rm -f *.db` 다, **ⓑ 코드 인용 · 발췌**).
   - `apps/web/.e2e-preview*.config.mts` — e2e 가 잠시 만드는 설정(`.gitignore` 마지막 줄).
   **이 목록 밖의 것은 컨텍스트에 그대로 남는다** — 그것이 이 목록이 기대는 **부재**이고, 특히
   `services/` · `packages/` · `config/` · `rules/` · `pyproject.toml` 은 **빠지지 않는다.**
3. **빌드를 깨뜨리지 않는지 정적으로 확인한다**(데몬 없이). `Dockerfile` 이 이름으로 `COPY` 하는 것은
   **`pyproject.toml` 하나**이고(ㄹ · 러너 로그 ㅌ 가 그 스텝을 이름으로 싣는다) 위 목록의 어느
   항목도 그 이름에 걸리지 않는다. `COPY . .` 는 남는 것을 넣는다 — 컨텍스트를 통째로 비우는
   패턴(`*`)을 쓰지 않으므로 빈 COPY 가 되지 않는다. **`pip install -e .` 은 `COPY . .` 보다
   앞이라**(러너 로그: `#10 … COPY pyproject.toml ./` 가 `#12 … COPY . .` 보다 앞) **이 목록의
   영향을 받지 않는다.**
4. **게이트를 함께 만든다**(3-1-5 의 3). 불변식이 **파일을 읽어** 단언한다: ⓛ `.dockerignore` 가
   **빌드 컨텍스트의 뿌리에 있다** ⓜ 그 안에 **`.env` 를 빼는 줄이 있다** ⓝ **`Dockerfile` 이 이름으로
   `COPY` 하는 것을 빼는 줄이 없다**. **데몬을 요구하는 단언을 두지 않는다**(§0).
5. **변이로 값이 갈리는 것을 보인다**(§6-2 1, 작업 2 와 같은 규율): 최소한 `.env` 줄을 지운다 ·
   파일을 통째로 지운다 · `pyproject.toml` 을 빼는 줄을 넣는다. **한 자리씩 심고, 심기 직전의 작업
   트리 사본과 `diff` 로 적용을 확인하고, 그 사본으로 원복한다**(§6-2 규칙 5 — **이 커밋이 처음 넣는
   파일이라 `git diff` 는 지우는 변이에서 침묵한다**). 각 변이가 죽이는 단언의 이름과 값을 표로 남긴다.
6. **개수를 세지 않는다**(§6-1 9회차) — **이름**과 **부재**(위 2 의 마지막 줄)만 적는다.
7. **`Dockerfile` 을 만지지 않는다** — 소유가 없는 다른 자리이고 이 배정 밖이다.
   **`.gitignore` 도 만지지 않는다.**
8. **거짓으로 만드는 남의 자리**: ⓧ `.github/workflows/buildtwin-ci.yml` 의 `make env` 스텝 주석 —
   *"다만 이 저장소에 `.dockerignore` 가 없어 … 그 파일은 빌드 컨텍스트에 들어가고 `Dockerfile` 의
   `COPY . .` 가 이미지 레이어에 넣는다"*(**ⓑ 인용 · 발췌**, 소유 `qa` — **자기 소유**). 작업 5 가
   그 문장을 **거짓으로 만든다** → **같은 커밋에서 고친다**(§후속 76 · §6-4 1). ⓨ
   `tests/invariants/test_ci_builds_the_image.py` 머리의 「보지 못하는 것」 마지막 불릿(*"…담게 되는 것은 두 파일 어디에도 없다"* — 줄바꿈 한정은 §3-1-2 표) — **낡는가를 값으로 판정한다**: 새 불변식은
   **컨텍스트에서 빠지는 것의 선언**을 보지 **레이어에 담기는 것**을 보지 않으므로 나는 **낡지 않는다**고
   읽지만, 그 판정은 그 파일을 여는 사람이 값으로 다시 낸다(그 축이 §후속 94 다). ⓩ **그 밖에는 없다** —
   `README.md` 와 `Makefile` 에 `.dockerignore` 를 말하는 줄이 **하나도 없다**
   (**저장소 루트 `/home/user/Bim` 에서** `git grep -nI "dockerignore" 1e8fb19 -- .`, N=1 — `docs/` 밖
   히트는 위 ⓧ 그 주석뿐이고 **히트를 읽었다**. **같은 명령을 `buildtwin/` 에서 돌리면 히트 0 이다** —
   경로 지정자가 CWD 기준이다, N=1 · §후속 96). **그 값을 커밋 본문에 적는다.**

### 3-2. ㉡ qa 가 넘긴 것 — 자리마다 판정

#### 3-2-1. §확인하지 않은 것 **79** — **짓는 축은 값으로 닫혔다. 서는 축은 열린 채다**

정본(**ⓐ** `git show 12a2d34:buildtwin/docs/plans/0016-the-first-run-must-fail-out-loud.md` 의
§확인하지 않은 것 79, **ⓑ 인용 · 발췌**): *"**[재지 않았다] `c82cf47` 뒤의 `Dockerfile` 로
`docker compose up --build` 가 서는가.** … **[재야 한다 → 사용자 또는 CI · 데몬과 컨테이너 네트워킹이
있는 자리]**"*

**한정어 역방향 확인**(§6-3) — 그 항목의 주어는 **`up --build`** 이고 새 잡이 부르는 것은 **`build`** 다.

| 그 항목이 묻는 것 | 새 잡이 답하는가 | 값 (저장소 밖 · 출처 §3-0) | 판정 |
|---|---|---|---|
| **짓는가**(`c82cf47` 가 폴백을 지운 뒤의 설치가 통과하는가) | **답했다 — 사용자보다 먼저** | `#11 19.51 Building editable for buildtwin (pyproject.toml): finished with status 'done'` · `#11 55.55 Successfully installed … **openpyxl-3.1.5** … ifcopenshell-0.8.5 open3d-0.19.0 ortools-9.15.6755 … buildtwin-0.1.0` · `#11 DONE 59.7s` · 잡 결론 **success** | **닫힌다 — 「짓는가」에 대해서만.** 그리고 `openpyxl` 이 그 목록에 있다: 계획 0016 이 지운 폴백이 빠뜨렸던 바로 그 이름이고, 그것을 `services/progress/importers/document_register.py` 가 모듈 최상단에서 import 한다(**ⓐ** `grep -n "^import openpyxl" services/progress/importers/document_register.py` → `:32`) |
| **서는가**(컨테이너가 실제로 뜨는가) | **답하지 않는다** — 잡은 `up` 을 부르지 않는다(3-1-3 스텝 표) | — | **닫히지 않는다. 여전히 사용자다** — 그 자리가 §후속 **88** 이다 |

**그러므로 79 는 이렇게 갈라 읽는다**: **짓는 축 = 닫혔다(CI, `1e8fb19` 의 첫 실행) · 서는 축 =
열린 채(사용자 또는 §후속 88 이 만드는 주체)**. **그리고 짓는 축의 닫힘에도 한정이 있다**:
그 값은 `docker compose build` 의 것이고 `up --build` 의 것이 아니다 — 둘이 같은 것을 짓는지는
**§확인하지 않은 것 90 이 그대로 산다.**

#### 3-2-2. **ADR 0020 §2-1 항 2 는 거짓이 되지 않는다** — 확인했다

항 2 의 문안(**ⓐ** `grep -n "관측자 넷" docs/adr/0020-*.md`, **ⓑ 인용 · 발췌**): *"rc 를 갖지 않는
자리(컨테이너 안 · 브라우저 안)의 실패는 … **관측자 넷**(rc · stderr/로그 · 게이트 · 화면) 중
**최소 하나에서 「실패라고 말하는 형태로」** 보인다."*

**판정: 거짓이 되지 않는다. 근거는 그 문장의 형태다** — 항 2 는 **하한 주장**(「최소 하나」)이다.
관측자를 **더하는** 변경은 하한 주장을 강하게만 하고 결코 거짓으로 만들지 못한다.
새 잡이 한 일이 정확히 그것이다: 문 3 의 rc 를 **읽는 주체**를 하나 만들었고(그전에는 그 rc 를 처음
보는 사람이 사용자였다 — §1-a), **첫 실행에서 실제로 읽었다**(잡 결론 success, §3-2-1).

*역방향 확인 — 그러면 무엇이 낡는가.* 낡을 수 있는 것은 항 2 가 아니라 **「문 3 의 rc 는 아무도 듣지
않는 소리다」** 라고 적은 자리다. 전수는 **저장소 루트에서** 했다(§6-1):
`git grep -nI "아무도 듣지 않는" 1e8fb19 -- .`(N=1) → **히트 둘이고 둘 다 읽었다.**

| 히트 | 그 자리가 말하는 것 | 낡는가 |
|---|---|---|
| 이 계획 `:72` | *"문 3 의 rc 는 **아무도 듣지 않는 소리**다"* — 트리 `12a2d34` 를 잰 값 | **낡았다. 그러나 기록물이라 고치지 않는다**(§3-13 첫째 갈래) |
| `tests/invariants/test_ci_builds_the_image.py:254` | 셸 OR 단언의 **실패 문구**: *"앞 절이 실패해도 뒤 절이 rc 0 을 내고, 그 빌드의 실패는 **다시** 아무도 듣지 않는 소리가 된다"*(소유 `qa`, **ⓑ 인용 · 전체**) | **낡지 않는다** — 그 문장은 **조건절 안**이다(그 열쇠가 걸렸을 때 무엇이 되는가). 오늘을 말하지 않으므로 관측자가 생겨도 참이다. 「다시」가 그 한정어다 |

***이 전수를 한 번 틀렸고 그것을 값으로 적는다***: 같은 명령을 **`buildtwin/` 에서** 돌리면
`-- .` 의 경로 지정자가 **CWD 기준**이라 저장소 루트의 `.github/` 가 통째로 빠진다. 그 모양을
아래 §후속 96 로 연다.

#### 3-2-3. §확인하지 않은 것 **90** 과 `test_demo_stack_can_stand.py` 의 그 불릿 — **낡지 않았다. 확인했다**

그 불릿(**ⓐ** `grep -n "컨테이너가 실제로 뜨는가" tests/invariants/test_demo_stack_can_stand.py`,
**ⓑ 인용 · 전체**): *"**컨테이너가 실제로 뜨는가.** `config` 는 파일 해석까지다.
`docker compose up --build` 는 이 사이클의 누구도 돌리지 않았다(계획 0015 §확인하지 않은 것 43·60 —
데몬 부재)."*

**qa 의 판정이 맞다. 그것을 지지하는 축들이 서로 독립이다**(하나가 틀려도 나머지가 남는다):

| # | 축 | 값 |
|---|---|---|
| ㄱ | **주어가 다르다** | 불릿의 주어는 `up --build`, 새 잡의 명령은 `docker compose build`(3-1-3 스텝 표). 둘이 같은 것을 짓는지는 **아무도 재지 않았다** — §확인하지 않은 것 **90** |
| ㄴ | **표제가 다르다** | 불릿의 표제는 **「컨테이너가 실제로 뜨는가」** 이고 `build` 는 컨테이너를 만들지 않는다. 첫 실행이 **초록**이어도(§3-2-1) 이 물음은 그대로다 — 러너 로그의 마지막은 `naming to …`(ㅍ)이지 기동이 아니다 |
| ㄷ | **시점이 못박혀 있다** | *"**이 사이클의** 누구도"* — 그 문장은 계획 0015 사이클을 가리키는 **기록물의 한정어**다. 다음 사이클이 돌려도 그 문장은 거짓이 되지 않는다 |

**그러므로 고치지 않는다**(§3-13 첫째 갈래). **90 도 닫히지 않는다** — 이 사이클은 명령의 **이름**만
읽었고 첫 실행도 그 축을 가르지 않았다.

#### 3-2-4. **계획의 근거 하나가 거짓이었다** — 자리마다 판정한다(§M-12 의 네 축을 쓴다)

**qa 실측**(저장소 밖 사본, 각 **N=3**, compose **v5.1.1**; **저장소 밖 값이라 나는 다시 재지 못했다** —
§후속 62 대로 커밋 본문이 아니라 **그 값을 그 자리에 적은 워크플로 주석**을 읽었다,
**ⓐ** `grep -n "데몬 오류로 죽고" ../.github/workflows/buildtwin-ci.yml`):
`.env` 없이 `docker compose build` 는 `env file … not found` 가 **아니라 데몬 오류로** 죽고,
같은 자리 같은 시각의 `docker compose config` 는 `env file … not found` 로 죽는다.
**즉 `build` 는 `env_file` 을 읽지 않는다.**

**전수 — 그 단정을 적은 자리**(§6-1 ①: 저장소 루트에서, 경로로 먼저 좁히지 않고 자기 커밋도 센다.
표기 집합은 `파일 해석` · `env file`):
`git grep -nI "파일 해석" 1e8fb19 -- .` · `git grep -nI "env file" 1e8fb19 -- .`(각 N=1). **히트를 다 읽었다.**
***이 기준이 놓치는 것***(§6-1 ②): **한국어 재서술** — 같은 사실을 그 두 낱말 없이 적는 자리
(예: *"먼저 `make env`"*)는 이 목록 밖이다. 보간·줄바꿈 축은 이 두 낱말이 **한 줄에 들어가는 조각**이라
이 목록에서는 밖이 없다.

| 자리 | 그 자리의 주어 | 종류(§M-12) | 참인가 | 처분 |
|---|---|---|---|---|
| `buildtwin/Makefile` 의 `dev:` 첫 `@echo` | `make dev` = **`up --build`** | 처방(사용자가 읽고 친다) | **참이다** — 계획 0016 §M 의 재측정이 그 값을 싣는다: 데몬 없이 `make dev` 의 stderr 가 `env file /home/user/Bim/buildtwin/.env not found: …`(그 문서 `12a2d34`, **ⓑ 인용 · 발췌**) | **고치지 않는다** |
| `buildtwin/README.md` §빠른 시작 ②의 「걸음을 건너뛰면」 | 걸음 2 = `make dev` | 처방 | **참이다**(같은 값) | **고치지 않는다** |
| `buildtwin/.env.example` 머리말 | *"compose 가"*(한정 없음) | 처방 | **`config`·`up` 갈래에서 참, `build` 갈래에서 거짓.** 그 파일이 말하는 경로는 사용자 갈래(`up`)다 | **고치지 않는다 — 그러나 한정어가 없다.** 소유 없는 자리(§후속 19)이고 이 사이클의 배정 밖이므로 **§후속 95 로 이름을 남긴다** |
| `tests/invariants/test_the_first_run_names_its_doors.py` 의 실패 문구 | `dev` 의 선행 안내 | 처방(게이트) | **참이다** | **고치지 않는다** |
| 계획 0015 · 0016 의 표 | `docker compose config` 를 **이름으로** 적는다 | 그때 잰 값 | **참이다** | **고치지 않는다**(§3-13 첫째 갈래) |
| **이 계획 §완료 조건 작업 1 조건 2** | *"compose 의 `api`·`worker` 는 둘 다 `env_file: [.env]` 이고 … `.env` 가 없으면 **파일 해석 단계에서** 죽는다"* — 그 조건이 처방하는 스텝은 **`build`** 다 | **처방이었으나 그 사이클의 실행이 이미 소진했다** | **처방으로서 거짓이었다** | **고치지 않는다** — §M-12 의 **넷째 축**. 근거는 아래 |
| `.github/workflows/buildtwin-ci.yml` 의 `make env` 스텝 주석 | `build` | **살아 있는 처방** | **참이다 — qa 가 그 자리에 정정을 적었다** | **이미 고쳐져 있다. 정본은 여기다** |

**마지막에서 두 번째 행의 근거**(§M-12 넷째 축의 닫는 조건을 그대로 판정한다):
ⓐ **그 상자를 실행한 커밋이 있다** — `1e8fb19`(작업 1)이 `make env` 스텝을 넣었다.
ⓑ **그 결과를 마감이 진다** — 작업 4 의 교차 확인 표가 그 칸을 진다.
ⓒ **읽고 만들 사람이 남아 있지 않다** — 그 조건 2 는 소진됐고 **살아 있는 정본은 워크플로 주석**이다
(맨 아래 행). **그래서 다음 구현자는 거짓을 읽지 않는다** — §6-4 1 이 막으려는 것이 정확히 그것이고,
그 위험이 여기서는 **이미 닫혀 있다.**
*그리고 §M-12 ⓑ 의 한정이 걸린다*: 「소진됐다」는 **이 사이클 안에서만** 참이다. 다음 계획이 이 상자를
인용해 새 작업을 세우면 그 문장은 다시 처방이 되고 **고쳐야 한다**(§후속 **85** 가 그 이동을 본다).

*역방향 확인 — 「고치지 않는다」가 놓치는 것.* 이 처분은 **그 문장이 조용히 인용될 위험**을 남긴다.
그래서 이 절이 그것을 **이름으로** 적었고, 작업 4 의 교차 확인 표가 같은 값을 한 번 더 진다.
**두 자리가 서로를 가리키게 두지 않았다** — 위 표의 각 칸은 실행값이거나 코드 인용이다(§6-3).
***그리고 첫 실행은 이 축을 가르지 못했다***: 러너는 `make env` 를 먼저 돌아 **`.env` 를 가진 채**
빌드했으므로, **「러너의 compose 판이 `.env` 없이 어떻게 죽는가」의 값은 여전히 없다**
→ **§확인하지 않은 것 99.**

#### 3-2-5. **C7 사각지대** — 새 번호를 열지 않는다. §후속 **79** 의 실례로 싣는다

값(qa 실측, 그 파일의 변이 표 — **ⓐ** `grep -n "C7" tests/invariants/test_ci_builds_the_image.py`,
**ⓑ 인용 · 발췌**): *"C7 | 짓는 스텝의 `run` 을 `set +e` + 빌드 + `exit 0` 로 바꾼다 | **4 passed**"*.
그 파일의 「보지 못하는 것」이 같은 사실을 이름으로 적는다: *"`set +e`, `;` 로 이은 명령, 실패해도
rc 0 을 내는 래퍼 스크립트, 워크플로 자체를 끄는 `paths` 축소는 **전부 이 단언을 지나간다**"*.

**판정: 새 항목을 만들지 않는다.**
1. **그 물음은 이미 §후속 79 다** — *"「실패를 삼키는 모양」의 전수"*(architect + qa). C7 은 그
   전수가 아직 없다는 것의 **실례**이지 새로운 종류가 아니다. 새 번호를 열면 §6-1 **12회차**가 이름
   붙인 모양(목록이 상태를 싣지 못해 같은 것이 두 번 열린다)을 내가 만든다.
2. **「재지 않았다」가 아니다** — C7 은 **쟀고 값이 있다**(4 passed). §확인하지 않은 것의 자리가 아니다.

**그러나 79 는 이 사이클에 무엇이 달라졌는지를 실어야 한다.** 79 의 전수 범위에 **워크플로의 `run:`
블록**이라는 자리가 새로 생겼고(그전 자리들은 `Dockerfile`·`Makefile`·파이썬 코드였다),
**그 자리에서 삼키는 모양이 값으로 관측됐다**(C7 = 4 passed). **79 의 닫힘 조건은 그 자리를 포함한다** —
아래 §후속 갱신이 그것을 적는다.

### 3-3. 작업 3 조건 1 — 범위 밖으로 민 축

§1-e 가 민 것은 **기동(`up`) · 시드가 닿는가 · 재빌드 경로**다. 앞의 둘은 **§후속 88 이 이미 진다**
(*"스택이 뜨는가 · 시드가 닿는가를 도는 주체가 여전히 사용자뿐이다"*, **ⓑ 인용 · 발췌**) — **다시
열지 않는다**(§6-1 12회차). 남는 것은 **재빌드 경로**이고 그것을 §후속 **93** 으로 연다.

### 3-4. ㉢ **CI 첫 실행의 값** — 초록이고, 진짜로 지었다

**출처는 §3-0**(저장소 밖 · PR #2 의 `1e8fb19` · 잡 `image` = **success** · 19:14:39→19:16:00 UTC ·
로그 사본을 내가 읽었다, 각 N=1). **이 사이클은 이 값을 재지 못하는 자리에 있었다** — 옮겨 적을 뿐이다.

| 물음 | 값 |
|---|---|
| 잡의 결론 | **success** |
| 설치 | `Building editable for buildtwin (pyproject.toml): finished with status 'done'` · `Successfully installed … openpyxl-3.1.5 ifcopenshell-0.8.5 open3d-0.19.0 ortools-9.15.6755 … buildtwin-0.1.0` |
| 그 스텝의 소요 | `#11 DONE **59.7s**`(pip 설치 레이어). 잡 전체는 로그 첫 줄 19:14:40.50 → 마지막 19:15:59.60 |
| 산출물 | `naming to docker.io/library/**buildtwin-worker**` · `…/**buildtwin-api**`(로컬 이름뿐) |
| 컨텍스트 | `#7 transferring context: **5.78MB**` — 러너는 신선한 체크아웃이라 파생물이 없다(이 작업 트리의 **2.7G** 와 대조: ㅅ) |
| `.dockerignore` | `#5 transferring context: **2B** done` — **부재를 러너가 잰 값**(ㅈ) |

**닫히는 것**(값과 함께):

| 항목 | 처분 | 남는 것 |
|---|---|---|
| §확인하지 않은 것 **87** *"새 잡이 초록인지 빨간지"* | **닫힌다 — 초록** | 그 초록은 **`build` 에 대한** 것이다 |
| §확인하지 않은 것 **88** *"러너에서 빌드가 얼마나 걸리는가"*(§리스크 2 의 값) | **닫힌다** — 설치 레이어 59.7s · 잡 전체 ~79s(로그 앞뒤). **다른 잡의 피드백을 늦추지 않는다**(그 잡은 `needs` 를 갖지 않는다) | 캐시 없는 첫 실행의 값이다 — 재빌드 축은 §후속 93 |
| §확인하지 않은 것 **89** *"불변식이 「잡이 있는데 아무것도 짓지 않는」 경우를 가르는가"* | **닫힌다** — 작업 2 의 변이 C4(`echo "docker compose build"`)에서 **2 failed, 2 passed** | — |
| §확인하지 않은 것 **91** *"`.env` 를 CI 에서 만드는 것의 부작용"* | **닫힌다** — 3-1-3 의 표(이름·`.env` 언급·값의 모양·아티팩트) | **모양으로 판정했다**는 한정(3-1-3) |
| 계획 0016 §확인하지 않은 것 **79** | **짓는 축만 닫힌다**(§3-2-1) | **서는 축은 열린 채** |

**열린 채로 남는 것**(초록이 닫지 **못하는** 것 — 이 표가 이 절의 요점이다):
ⓐ **§확인하지 않은 것 90**(`build` ↔ `up --build`) — 초록은 `build` 의 값이다.
ⓑ **§후속 88**(스택이 뜨는가 · 시드가 닿는가) — 러너 로그의 마지막은 `naming to …` 다.
ⓒ **§2 역방향 ⓒ** — *"CI 가 도는 것이 「사용자의 환경」은 아니다"*(**ⓑ 인용 · 발췌**). ubuntu 러너의
초록은 사용자 기계의 초록이 아니다.
ⓓ **§확인하지 않은 것 97·98·99** — 레이어 **안**을 아무도 열지 않았고(97), 사용자의 기계에서 그
이미지가 남는가를 아무도 재지 않았고(98), 러너의 compose 가 `.env` **없이** 어떻게 죽는지도 값이 없다(99).

**그리고 빨강이었다면 어디로 갔을지도 적어 둔다**(이 표가 초록에만 맞는 표가 되지 않게 —
§6-2 1 의 축을 문서에 건다): 어느 스텝이 무엇으로 죽었는지는 **작업 4 조건 5** 가 지고, 고칠 자리는
`pyproject.toml`(**소유 없는 자리** — 고치려면 배정이 필요했고 그 배정은 architect 가 냈을 것이다),
그리고 그것은 회귀가 아니라 §리스크 1 이 예고한 **발견**으로 기록됐을 것이다.

### 3-5. 이 작업이 **거짓으로 만드는 남의 자리** (§후속 72·74·76)

§작업 분배 작업 3 조건 3 은 *"**없다고 판단했다**"* 라고 적었다(**ⓑ 인용 · 발췌**). **그 판단을 이
절에서 다시 재고, 하나를 고친다.**

| 자리 | 소유 | 이 절이 낡게 만드는가 | 처분 |
|---|---|---|---|
| §작업 분배 표(작업 넷) | architect(내 것) | **예 — 작업 5 가 늘었다** | **표를 고치지 않고 §3-1-6 이 행을 더한다**(기록물 · §3-13 첫째 갈래) |
| §다음에 부를 에이전트 | architect(내 것) | **예** | **살아 있는 처방**이라 줄을 더했다(아래) — 순서는 **작업 5 → 작업 4**(3-1-5 역방향 ⓐ) |
| §확인하지 않은 것 87·88·89·91 | architect(내 것) | **예 — 첫 실행이 답했다** | **원 항목을 고치지 않고** §3-4 의 표가 상태를 싣는다(§3 의 이월 규약과 같은 모양) |
| `.github/workflows/buildtwin-ci.yml` 의 `.dockerignore` 주석 | qa | **아니다 — 이 절은 읽기만 한다.** 그것을 거짓으로 만드는 것은 **작업 5** 이고 그 처분은 §3-1-6 조건 8 ⓧ 가 진다 | — |
| `tests/invariants/*.py` | qa | **아니다** — 인용만 한다 | — |
| CLAUDE.md | architect(내 것) | **아니다 — 만지지 않았다**(§2 · §6-5 대기열) | — |

### 3-6. §후속 · §확인하지 않은 것 — **이 절이 여는 것**

> **번호 공간을 하나로 합친다.** 이 계획은 §후속 신규를 86·87·88 로, §확인하지 않은 것 신규를
> 87~91 로 열었다 — **87 과 88 이 각각 두 항목을 가리킨다**(**ⓐ** `grep -n "^8[678]\. "
> docs/plans/0017-nobody-builds-the-image.md`, N=1). 그것이 §6-3 **11회차**가 이름 붙인 모양(같은 이름이 두 뜻)이고, 실제로
> 이 사이클의 인계에서 **§확인하지 않은 것 79 를 「§후속 79」로 부르는 혼동**이 났다
> (**저장소 밖 값** — 이 사이클의 인계 문서, N=1).
> **이 절부터 두 목록은 하나의 번호 공간을 쓰고, 다음 번호는 두 목록의 최댓값 다음이다**(= 92).
> 옛 87·88 은 **소속을 함께 적어** 부른다. 그 자체를 아래 92 로 연다.

**후속(신규):**

92. **두 목록이 같은 번호를 두 항목에 준다**(architect). 값: 이 계획의 §후속 신규 86·87·88 과
    §확인하지 않은 것 신규 87~91 이 **87·88 에서 겹친다**(N=1). *묻는 것*: 이월 표가 그 번호를 옮길 때
    **어느 목록의 것인지**를 무엇이 싣는가. *닫힘의 조건*: 어느 계획이 두 목록의 번호를 **하나의
    공간**으로 잇거나, 번호마다 소속을 싣는 규약을 값과 함께 세울 때. *한정*: **이 절은 규약을
    바꾸지 않는다** — 이 절부터의 새 번호에만 하나의 공간을 쓴다. 옛 번호는 그대로 두고 부를 때
    소속을 붙인다.
93. **재빌드 경로를 도는 주체가 없다**(qa + architect). §1-e 가 민 셋 중 §후속 88 이 지지 않는 것이다.
    값: 새 잡은 **캐시 없는 첫 빌드**만 돈다(`#11 DONE 59.7s` = 러너에서 처음부터 설치, 저장소 밖).
    *묻는 것*: 레이어 캐시가 있는 두 번째 빌드부터의 실패(의존성 갱신 · 베이스 이미지 이동)를 누가
    먼저 보는가. *닫힘의 조건*: 그 축을 도는 주체가 저장소 안에 생기고 그 값이 어느 계획에 적힐 때.
    *한정*: §1-d 가 **범위 밖으로 명시**한 축이다 — 이 항목은 그것을 되돌리지 않고 **이름만** 남긴다.
94. **이미지 안에 무엇이 들어갔는지 보는 게이트가 없다**(qa). 값:
    `tests/invariants/test_ci_builds_the_image.py` 가 스스로 적는다 — *"…담게 되는 것은 두 파일 어디에도 없다"*(**ⓑ 인용 · 발췌** — 줄바꿈 한정은 §3-1-2 표). **작업 5 도 그것을 닫지 않는다** — 작업 5 의
    불변식이 보는 것은 **빼겠다는 선언**이지 **담긴 것**이 아니다. *묻는 것*: 레이어의 내용을 보는
    게이트가 있어야 하는가(그 게이트는 데몬을 요구한다 — 이 환경 밖이다). *닫힘의 조건*: 데몬이 있는
    자리에서 그 대조가 값으로 서고 어느 계획이 그것을 배정할 때. *한정*: **관측 1회**(이 사이클의
    `.env` 축)뿐이다.
95. **`.env.example` 머리말의 「compose 가」에 한정어가 없다**(소유 없음 → architect 가 배정할 자리).
    값: 그 문장은 `config`·`up` 갈래에서 참이고 `build` 갈래에서 거짓이다(§3-2-4 표). *묻는 것*:
    그 파일이 말하는 「compose」가 어느 명령인지를 적을 것인가. *닫힘의 조건*: 어느 계획이 그 자리를
    배정하고 한정어가 들어갈 때. *한정*: **오늘 그 문장이 사용자를 잘못 이끄는 값은 없다** — 그 파일이
    말하는 경로는 사용자 갈래(`up`)이고 거기서는 참이다.

96. **`git grep <패턴> <트리> -- .` 의 경로 지정자가 CWD 기준이라 저장소 루트의 `.github/` 가
    조용히 빠진다**(architect + qa). **이 절이 그 함정에 실제로 빠졌다** — 값(각 N=1):
    `git grep -nI "dockerignore" 1e8fb19 -- .` 는 `/home/user/Bim` 에서 워크플로의 그 주석 줄들을 내고
    `/home/user/Bim/buildtwin` 에서 **히트 0** 이다. **같은 자리에서 두 번 물렸다**: 워크플로 인용
    `git grep -c "이 잡은 이미지를 푸시하지 않으므로" 1e8fb19 -- .` 도 `buildtwin/` 에서 **히트 0**,
    루트에서 **1** 이다(각 N=1). **예외도 던지지 않고 「없다」로 보인다** — 이 저장소의
    지배적 실패 모드 그 자체다. 그리고 **이 사이클의 ⓐ 참조 다수가 그 형태**다. *묻는 것*: ⓐ 참조가
    **어느 디렉터리에서 도는 명령인지**를 무엇이 싣는가(§후속 55 의 ⓐ 는 「그 자리에서 도는 참조」라고만
    적는다). *닫힘의 조건*: ⓐ 형식이 CWD 를 싣거나(예: 루트 고정 `-- :/`), 그것을 붙드는 게이트가 설 때.
    *한정*: **관측 1회**(이 절)다. §6-1 이 요구하는 *"목록은 저장소 루트에서 만든다"* 는 이미 있고,
    없는 것은 **그 명령이 루트에서 돌았음을 읽는 사람이 아는 방법**이다.

**확인하지 않은 것(신규):**

97. **[재지 않았다] 이미지 레이어 안에 `.env` 가 실제로 있는가.** 러너 로그가 보인 것은
    `.env` 생성 → 컨텍스트 전송 → `COPY . .` 까지다(§3-1-1 ㉯). **레이어를 연 사람이 없다** —
    여는 것은 데몬을 요구하고 이 환경은 `docker info` rc 1 이다(N=1).
    **[재야 한다 → qa · 데몬이 있는 자리에서 `docker run --rm <이미지> ls -l /app/.env` 또는
    `docker save` 의 레이어를 읽어]**
98. **[재지 않았다] 사용자의 기계에서 그 이미지가 남는가 · 얼마나.** 러너에서 잰 것은
    `naming to docker.io/library/buildtwin-{api,worker}` 까지이고 러너는 그 뒤 사라진다.
    **`.env` 를 지운 뒤에도 · `docker compose down -v` 뒤에도 그 이미지가 남는가**는 아무도 재지 않았다.
    **이 축이 ㉠ 판정의 무게가 걸린 자리다**(§3-1-4). **[재야 한다 → qa 또는 사용자 · 데몬이 있는 자리]**
99. **[재지 않았다] 러너의 compose 가 `.env` **없이** 어떻게 죽는가.** 첫 실행은 `make env` 를 먼저
    돌아 `.env` 를 가진 채 빌드했으므로 이 축을 가르지 못했다(§3-2-4 끝). 저장소 밖 사본에서 잰 값
    (compose **v5.1.1**: `build` 는 데몬 오류로, `config` 는 `env file … not found` 로 죽는다)이
    **러너의 판에서도 같은지** 모른다. **[재야 한다 → qa · `make env` 스텝을 잠시 뺀 실행 또는 러너의
    `docker compose version`]**

---

## M. 마감 (작업 4, architect) — 게이트 재측정 · 교차 확인 표 · 상태 표 · CI 값의 이관

> **이 절이 이 작업의 산출물이다.** 위 §0~§3-6 은 트리 `12a2d34`·`1e8fb19` 의 기록물이고 **이 절은
> 그것을 고치지 않는다** — 더할 뿐이다(CLAUDE.md §3-13 첫째 갈래). 이 절이 위 문서를 낡게 만드는
> 자리는 §M-7 이 자리마다 판정한다.

### M-0. 이 절의 트리 · 방법 · 자원

**작업 트리** `/home/user/Bim/buildtwin`, **저장소 루트 `/home/user/Bim`**, **트리 `d7ad6f7`**
(작업 5 까지 머지된 뒤. 이 절을 여는 시점의 루트 `git status --porcelain` 은 **빈 출력**, 커밋 직전에
**이 문서 한 줄**). **이 절의 모든 `파일:줄`·커밋 참조는 `d7ad6f7` 의 것이다**; 예외는 §0 의 ㉮㉯ 와
같다(다른 기록물을 인용하는 줄은 그 자리에서 트리를 적고, CLAUDE.md 는 **그 자리에서 도는 `grep`**
으로 부른다).

**명령의 못박음**(§후속 **65** 확장 항 ㉡ — **이 절도 그 조항을 진다**): 이 절의 모든
`git` 명령은 **트리 인자를 명령 안에 담고 `--all` 을 쓰지 않는다**(`git grep <패턴> d7ad6f7 -- .` ·
`git ls-tree -r --name-only <트리> -- buildtwin` · `git log --oneline <A>..<B> -- <경로>`).
트리 인자를 받지 못하는 명령(`make` · `pytest` · `du` · `ls` · `stat` · `pgrep` · `python3` · `diff`)은
전부 위 작업 트리에서 돌린 것이다. **`git diff` 는 예외로 다룬다** — 그 명령의 **경로 지정자가 CWD
기준**이라 이 절이 §후속 96 에 두 번째로 물렸고, 그 값이 §M-6 이다.

**방법 · N · 표본 간격**(§후속 **34** · 27). 아래 값은 **2026-09-08 한 세션 안**(20:07~20:20 UTC,
`date -u` 로 앞뒤를 찍었다)에 잰 것이고, **따로 적지 않으면 N=1**, 서로 몇 분 간격이다.
**「없다」·「히트 0」·「죽었다」에도 N 을 적는다.**

**저장소 밖 값의 출처**(§후속 **62**). 이 절이 싣는 저장소 밖 값은 셋에서 온다 — ⓐ **PR #2 의 CI**
(내가 **GitHub Actions API 로 직접 읽은 잡·스텝의 결론과 시각**: 실행 `34272768384` = `d7ad6f7`
푸시 실행) ⓑ **이 사이클의 인계**(오케스트레이터가 내려받은 `1e8fb19` 잡 로그 사본을 §3 이 읽었고,
`94ed3ec` 은 **결론만** 넘어왔다 — 그 사본은 저장소에 들어오지 않는다) ⓒ **qa 의 커밋 본문**
(`d7ad6f7`). **커밋 본문은 근거의 정본이 아니다** — 아래 표들의 **「내가 다시 쟀는가」 칸**이 그 조항의
이행이고, 다시 재지 못한 칸은 그렇게 적었다.

**자원**: **docker 데몬 0회**(`docker info` → **rc 1**, `/proc/sys/net/ipv4/ip_forward` → **0**, 각 N=1) ·
postgres 클러스터 **0회**(로컬 `make test` 는 sqlite 갈래다) · **심은 변이 하나**(D2 재현 — §M-6, 매번
사본으로 원복하고 루트 `git status --porcelain` 을 확인했다, 마지막 확인 20:13:07 UTC 빈 출력) ·
`make e2e` 가 잡은 포트는 그 명령이 반납했다(§M-1 의 고아 표) · **저장소 루트에도 `buildtwin/` 에도
`.env` 를 만들지 않았다**(`ls -a <각 디렉터리> | grep -c '^\.env$'` → **0**, 각 N=2, 이 절의 앞과 뒤).
***예외를 이름으로 적는다***: `make -n env` 가 인쇄한 레시피를 **세션 스크래치패드에서** 돌려 그
산출물의 크기·모드를 쟀고(§M-3 의 664 행) **그 파일은 그 자리에서 지웠다** — 저장소 두 자리의 값은
그 앞뒤로 **0** 이다. **CLAUDE.md 를 만지지 않았다**(작업 4 조건 4 · §2 · §6-5 대기열).

### M-1. 게이트 — **내가 다시 돌린 값** (완료 조건 1)

| 게이트 | 명령 | 값 | 시각(UTC) |
|---|---|---|---|
| `make test` | `make test` | **rc 0** — unit **492** · invariants **126** · regression **8** · integration **232** · vitest **310**(31 files). **N=2 · 두 실행이 같은 값**(둘째는 이 절을 쓴 뒤 커밋 직전) | 20:07:44 → 20:10:54 · 20:20:18 → 20:22:16 |
| `make lint` | `make lint` | **rc 0**(ruff `All checks passed!` · mypy · eslint · tsc) | 20:11:11 → 20:11:19 |
| `make e2e` | `make e2e` | **12 passed · rc 0** | 20:11:19 → 20:11:48 |
| 고아 | `pgrep -fc '[v]ite preview'` · `'[u]vicorn'` · `ls apps/web \| grep -c '^\.e2e-preview'` | **0 · 0 · 0**(각 rc 1 = 히트 없음) | 20:12:14 |
| 바닥값 | `git log --oneline f9c6265^..d7ad6f7 -- buildtwin/tests/postgres.floor.json buildtwin/tests/metrics.json` | **빈 출력 = 그 경로들의 커밋 0** — `min_tests_on_postgres` 는 `tests/integration` **187** · `tests/unit` **252** 그대로 | 20:12:00 |
| 작업 트리 | 루트 `git status --porcelain` | **빈 출력**(첫 `make test` 앞 20:07:44 · 뒤 20:10:56 · 변이 원복 뒤 20:13:07). 둘째 실행의 앞뒤는 **이 문서 한 줄**뿐이다(20:20:18 · 20:22:16) | 위 행들의 앞뒤 |

**「어느 것이 바뀌는지」 — 수가 아니라 이름으로**(§게이트의 요구). **바뀐 것은 invariants 하나뿐이다.**

| 트리 | invariants | 무엇이 더해졌나 |
|---|---|---|
| `12a2d34`(계획 §0 이 잰 값) | **118** | — |
| `1e8fb19`(작업 1·2) | **122** | `tests/invariants/test_ci_builds_the_image.py` 의 ⓘⓙⓚ + 탐침 |
| `d7ad6f7`(작업 5) | **126** | `tests/invariants/test_the_build_context_excludes_the_secret.py` 의 ⓛⓜⓝ + 탐침 |

**나머지 넷은 이름으로 불변이다**: unit 492 · regression 8 · integration 232 · vitest 310. 그 넷을
움직이는 파일이 이 사이클의 커밋들에 **없다**(`git log --oneline --stat f9c6265^..d7ad6f7` 의 경로를
읽었다 — 만진 것은 워크플로 · `tests/invariants/` 둘 · `buildtwin/.dockerignore` · `docs/plans/`, N=1).

***이 마감이 자기 탐침에서 한 번 틀렸고 그것을 값으로 적는다*(§후속 46).** 고아를 처음 셀 때
`pgrep -fc 'vite preview'` 를 **탐침 명령 자신의 명령줄 안에서** 불러 `2 · 2` 를 얻었다(20:11:11 과
20:11:48, 각 N=1) — `make e2e` 앞뒤가 **같은 값**이라 남은 프로세스가 아니라 **자기-계수**임이
그 자리에서 보였고, §후속 46 이 정본으로 정한 **대괄호 표기**로 다시 재니 **0 · 0** 이다(20:12:14).
`.e2e-preview.*` 잔여는 두 방법 모두 **0** 이다.

### M-2. **CI 가 낸 값의 이관** (완료 조건 5 · §후속 62)

**`image` 잡의 결론은 아래 세 트리에서 전부 success 다**(N=3 — 이 마감의 커밋이 만드는 실행은 이 표에
없다). **마지막 것은 내가 직접 읽었다.**

| 트리 | 잡 결론 | 짓는 스텝 | 출처 · 내가 다시 쟀는가 |
|---|---|---|---|
| `1e8fb19` | **success** | `#11 DONE 59.7s`(pip 설치 레이어) · 잡 전체 19:14:39→19:16:00 | 인계된 **로그 사본**을 §3 이 읽었다. **이 마감은 다시 재지 못했다**(사본이 세션 밖) |
| `94ed3ec` | **success** | — | 인계값 · **결론만** 넘어왔다. **로그를 읽은 사람이 없다** |
| `d7ad6f7` | **success** | `docker compose build` 스텝 **20:06:01→20:07:20**(=79초) · 잡 20:05:59→20:07:21 | **내가 쟀다** — GitHub Actions API 의 잡·스텝 결론(실행 `34272768384`, N=1). 같은 실행의 다른 잡(`lint`·`unit`·`integration`·`e2e`)도 전부 success |

**이 표가 닫는 것과 닫지 못하는 것.**
- **`.dockerignore` 가 빌드를 깨뜨리지 않는다** — 작업 5 는 그것을 **정적으로만** 판정했다(`Dockerfile`
  이 이름으로 `COPY` 하는 것 · 추적 파일 전수 · `COPY . .` 보다 앞선 설치). **`d7ad6f7` 의 러너가
  그 판정을 실행으로 받았다**: 그 파일이 선 뒤의 첫 빌드가 **success** 다. 이것이 이 마감이 더하는
  값이고, 계획 §3-1-6 조건 3 의 「정적 확인」이 실행값 하나를 얻은 자리다.
- **재빌드 축은 여전히 없다**(§후속 93). `d7ad6f7` 의 짓는 스텝은 **79초**로 `1e8fb19` 의 잡 전체와
  같은 자리수다 — 앞선 실행이 같은 이미지를 지었는데도 그렇다. **러너는 매번 캐시 없이 짓는다**가
  그 값의 읽기이고, 93 이 묻는 「레이어 캐시가 있는 두 번째 빌드」는 이 사이클의 어느 실행도 아니다.
- **로그를 읽지 않았다.** 내가 읽은 것은 **잡·스텝의 결론과 시각**이다 — `transferring context` 도
  `load .dockerignore` 도 이 마감의 값이 아니다. 그 자리가 아래 **§확인하지 않은 것 100** 이다.
- **`build` 는 `up --build` 가 아니다**(§확인하지 않은 것 **90** 이 그대로 산다) · **컨테이너는 서지
  않았다**(§후속 88) · **ubuntu 러너의 초록은 사용자 기계의 초록이 아니다**(§2 역방향 ⓒ).

### M-3. 교차 확인 표 — 이 사이클이 **두 번 이상 적은 값** (완료 조건 2 · §6-3 11회차)

**① 생성 기준.** *"이 사이클(`f9c6265`~`d7ad6f7`)의 어느 자리든 두 번 이상 적힌 값"* 이고,
**계획이 미리 이름 붙인 값에 한정하지 않는다** — **작업 1·2·5 의 실행이 만들어 낸 값 자신**과
**이 마감이 만든 값**을 포함한다(§6-1 13회차가 이름 붙인 방향). 자리를 찾는 명령은 **저장소 루트**
에서 돌렸고 경로로 먼저 좁히지 않았다(§53 — 자기 커밋도 센다): `git grep -nI -F <값> d7ad6f7 -- .`.

**② 이 기준이 놓치는 것.** ⓐ **한 자리에만 적힌 값** — 특히 **커밋 본문에만** 있는 값은 이 기준으로는
표에 들어올 자격이 없는데, 그것이야말로 §후속 62 가 경계하는 자리다. 그래서 **그런 값도 표에 넣고
「자리 하나」로 표시**했다. ⓑ **표기 변종·보간·줄바꿈**(§6-1 역방향 확인 ①~④) — 수를 `du` 의 접미사로
적는 자리(`2.7G`)와 산문으로 적는 자리는 문자열이 다르다. ⓒ **값이 아니라 판정**(「거짓이 아니라
위험이다」 같은 문장)은 이 기준 밖이고, 그 대조는 §M-7 이 진다.

**③ 블라인드 스팟을 하나 태웠다.** ⓐ 의 실례로 **`664바이트 · 모드 600`** 을 골랐다 — 저장소 히트
**0**(`git grep -nI -F "664" d7ad6f7 -- .` 의 히트는 전부 픽스처, N=1)이고 qa 의 커밋 본문에만 있는
값이다. **다시 쟀다**(아래 표 그 행).

| # | 값 | 이 사이클이 적은 자리 | 갈리는가 | 내가 다시 쟀는가 |
|---|---|---|---|---|
| 1 | invariants **126** | `d7ad6f7` 본문 · 인계 · §M-1 | **갈리지 않는다** | **쟀다** — `make test` 의 `pytest tests/invariants -q` → **126 passed**(20:08~20:09) |
| 2 | **「126 passed」** | `test_ci_builds_the_image.py` 「서기 전」 표 · `test_the_build_context_excludes_the_secret.py` 머리 · §3-1-5 3 · `d7ad6f7` 본문 | **갈리지 않는다 — 그러나 같은 수가 두 뜻이다** | **쟀다 · §M-4 ㄴ** |
| 3 | 파생물을 뺀 컨텍스트 **9.3M** ↔ **15M** | §3-1-1 ㅅ(9.3M) · `d7ad6f7` 본문(15M) | **갈린다** | **쟀다 · §M-4 ㄱ** |
| 4 | `.dockerignore` 를 건 컨텍스트 **6.7M** | `d7ad6f7` 본문 **하나뿐**(저장소 히트 0, N=1) | 대조할 둘째 자리가 없다 | **쟀다** — 그 목록의 이름을 `du --exclude` 로 건 값 **6.7M**(20:11:42). **재현된다** |
| 5 | 컨텍스트 전체 **2.7G** | §3-1-1 ㅅ · §3-4 · `d7ad6f7` 본문 | **갈리지 않는다** | **쟀다** — `du -sh .` → **2.7G**(20:11:25) |
| 6 | `.env` **664바이트 · 모드 600** | `d7ad6f7` 본문 **하나뿐**(저장소 히트 0) | 대조할 둘째 자리가 없다 | **쟀다** — `make -n env` 가 인쇄한 레시피를 스크래치패드에서 돌려 `stat` → **664 bytes · mode 600**(20:08~20:11 사이 · **20:19:59 재확인**, N=2). **재현된다** |
| 7 | 추적 파일 전수에서 **걸리는 것은 `.env.example` 하나** | 테스트 파일 머리 · `d7ad6f7` 본문 · §3-1-6 2 | **갈리지 않는다** | **쟀다** — `git ls-tree -r --name-only <트리> -- buildtwin` 을 그 목록의 패턴으로 거른 값: **`.env.example` 하나**(`94ed3ec` **421경로** · `d7ad6f7` **423경로**, 각 N=1) |
| 8 | 변이 표 D1~D10 | 테스트 파일 머리 표 · `d7ad6f7` 본문 | **갈리지 않는다** — 두 자리를 칸마다 대조했다(D1 4 failed · D4 ⓝ · D6 ⓜ 초록 · D8 4 failed 포함) | **한 칸만 쟀다** — D2 재현 → **1 failed, 3 passed**(§M-6). 나머지는 **다시 심지 않았다** |
| 9 | `git diff \| grep -c '^-[^-]'` 의 **0**(침묵) | 테스트 파일 머리 표 · `d7ad6f7` 본문 | **갈리지 않는다 — 그러나 `d7ad6f7` 에서는 재현되지 않는다** | **쟀다 · §M-6** — 같은 변이가 이 트리에서는 **1** 이다 |
| 10 | `image` 잡 **success** | §3-0 · §3-4(`1e8fb19`) · 인계(`94ed3ec`) · §M-2(`d7ad6f7`) | **갈리지 않는다**(N=3) | **셋째만 쟀다**(§M-2) |
| 11 | 설치 **59.7s** · `openpyxl-3.1.5` 등 | §3-2-1 · §3-4 · §후속 93 | **갈리지 않는다** | **못 쟀다** — 로그 사본이 세션 밖이다(출처 §M-0 ⓑ) |
| 12 | `transferring context: 5.78MB` · `load .dockerignore: 2B` | §3-1-1 ㅈㅋ · §3-4 · 테스트 파일 머리 · `d7ad6f7` 본문(「이 커밋으로도 바뀌지 않는다」) | **갈리지 않는다** | **못 쟀다** — 결론만 읽었다(§M-2). **그리고 2B 는 이 커밋 뒤 낡는다** → §확인하지 않은 것 **100** |
| 13 | compose **v5.1.1** 의 갈래(`build` 는 데몬 오류 · `config` 는 `env file … not found`) | 워크플로 `make env` 스텝 주석 · §3-2-4 | **갈리지 않는다** | **못 쟀다** — 데몬 0회. 러너의 판은 §확인하지 않은 것 **99** |
| 14 | 「`.dockerignore` 가 없다」 | §3-1 제목·ㄱㄴ(기록물) · 테스트 파일 머리(**「없었다」** — 과거형) · 워크플로 주석(작업 5 가 **「들어가지 않는다」로 정정**) | **갈리지 않는다 — 시제가 자리마다 갈려 있다** | **쟀다** — 루트 `git grep -nI -F "dockerignore" d7ad6f7 -- .` 의 `docs/plans/0017` 밖 히트를 다 읽었다(N=1): 워크플로 주석과 새 파일 둘뿐이고 **현재형으로 부재를 말하는 자리가 없다** |
| 15 | 바닥값 **187 · 252** | `postgres.floor.json` · `d7ad6f7` 본문 | **갈리지 않는다** | **쟀다**(§M-1 바닥값 행) |

### M-4. 갈린 칸 — **실행으로 가른다**

**ㄱ. 「파생물을 뺀 컨텍스트」가 9.3M ↔ 15M 으로 갈렸다 — 갈린 것은 값이 아니라 *제외 집합*이다.**
같은 작업 트리에서 세 집합을 나란히 쟀다(각 N=1, 20:11:42 UTC):

| 제외 집합 | 값 |
|---|---|
| §3-1-1 ㅅ 이 이름으로 적은 것(`.venv` · `apps/web/node_modules` · `.mypy_cache` · `.pytest_cache` · `.ruff_cache` · `*.egg-info` · `apps/web/dist`) | **16M** |
| 그 집합 **+ 깊은 `__pycache__` · `*.pyc`** | **9.4M** |
| `.dockerignore` 의 이름 전부 | **6.7M** |
| (참고) 깊은 `__pycache__` 합계 · `tests/fixtures` | **5.7M** · **2.9M**(그중 `sample.ply` **2.7M**) |

**판정: 둘 다 참이고 둘 다 다른 것을 쟀다.** `d7ad6f7` 본문의 **15M** 은 첫 행의 축(**16M**, 오늘의
트리)이고, §3-1-1 ㅅ 의 **9.3M** 은 둘째 행의 축(**9.4M**)이다 — **§3-1-1 ㅅ 이 이름으로 적은 목록에
깊은 `__pycache__` 가 없는데 그 값은 그것을 뺀 값이었다.** 그 차이가 **5.7M** 이고, 6.7M 과 9.4M 의
차이는 대부분 `tests/fixtures` 의 `sample.ply`(2.7M)다. **낡음이 아니라 기준의 누락**이므로 기록물인
§3-1-1 은 고치지 않고(§3-13 첫째 갈래) **이 표가 그 목록을 이름으로 보충한다.**

**ㄴ. 「126 passed」가 두 뜻이다.** 한쪽은 `test_ci_builds_the_image.py` 의 「이 파일이 서기 전」 표
(= 그 트리에서 `pytest tests/invariants tests/regression -q`)이고, 다른 쪽은 이 마감의 **invariants
126**(트리 `d7ad6f7`)이다. **실행으로 가른다**: 같은 명령을 `d7ad6f7` 에서 돌리면 **134 passed** 다
(20:12:21, N=1 — invariants 126 + regression 8). 즉 그 표의 126 은 **118 + 8** 이고 오늘의 126 과
**같은 수의 다른 값**이다. **어느 자리도 거짓이 아니므로 고치지 않는다** — 그러나 이것이 §6-3 11회차가
이름 붙인 모양(같은 이름이 두 뜻)의 실례이고, §후속 92 가 번호에 대해 여는 물음이 **값에 대해서도**
난 것이다. 아래 §후속 표의 92 행이 그것을 싣는다.

### M-5. **ⓓ 처리 — 컨텍스트 값이 어느 축의 것인가** (qa 의 정정을 받는다)

qa 가 `d7ad6f7` 본문에 적은 정정: *"`transferring context: 5.78MB` 는 이 커밋으로도 바뀌지 않는다 …
줄어드는 것은 러너가 아니라 **사용자의 기계**다"*(**ⓑ 인용 · 발췌**, 저장소 밖 값).

**판정: 내가 §3-1 에 적은 컨텍스트 값은 그 축으로 읽히지 않는다. 고칠 자리가 없다.** 근거는 그
문서의 자기 표기다(내가 다시 읽었다, 각 N=1):
- §3-1-1 은 표를 **㉮ 이 작업 트리에서 잰 것**과 **㉯ 러너가 잰 것**으로 갈라 놓았고, `2.7G`·`9.3M` 은
  ㉮ 에, `5.78MB` 는 ㉯ 에 있다.
- §3-4 의 컨텍스트 행이 그 둘을 한 줄에서 대조한다: *"러너는 신선한 체크아웃이라 파생물이 없다
  (이 작업 트리의 **2.7G** 와 대조: ㅅ)"*(**ⓑ 인용 · 발췌**).
- §3-1-5 역방향 ⓓ 가 결론을 이미 그 축으로 적었다: *"이 배정이 고치는 것은 **CI 가 아니라 사용자의
  기계**다 — 러너의 컨텍스트는 이미 작다(5.78MB, ㅋ)"*(**ⓑ 인용 · 발췌**).

**그러므로 qa 의 정정은 내 문장을 거짓으로 만들지 않고 같은 축을 더 좁게 다시 말한다** — 러너에서
이 목록이 실제로 빼는 것은 **`make env` 가 만드는 `.env` 하나**이고, 그 크기를 이 마감이 다시 쟀다
(**664바이트 · 모드 600**, §M-3 6행). **0016 §M-12 의 넷째 축(「기록물로 처분한다」)을 쓸 자리가
아니다** — 그 축은 *거짓이 된 처방*을 위한 것이고 여기서는 거짓이 없다.
***한정***: 「5.78MB 가 바뀌지 않는다」는 **아무도 재지 않았다.** 664바이트가 5.78MB 의 표시 자리수를
움직이지 못한다는 것은 산술이지만, **그 커밋 뒤 러너가 실제로 무엇을 전송했는지는 로그가 답하고 이
마감은 그것을 읽지 않았다**(§M-2) → **§확인하지 않은 것 100.**

### M-6. **§6-2 규칙 5 를 반대 방향에서 태웠다 — 그리고 §후속 96 이 두 번째로 물었다**

D2(`.dockerignore` 에서 `.env` 줄을 지운다)를 **한 자리만** 다시 심었다(사본과 `diff` 로 적용 확인 →
`26d25 < .env`, 잰 뒤 그 사본으로 원복, 루트 `git status --porcelain` 빈 출력, 20:12:53~20:13:07 UTC).

| 물음 | `1e8fb19`/`d7ad6f7` 작업 시점(qa) | **`d7ad6f7`(이 마감)** |
|---|---|---|
| 그 변이가 죽이는 것 | **1 failed, 3 passed**(ⓜ) | **1 failed, 3 passed** — 재현된다 |
| `git diff \| grep -c '^-[^-]'` | **0**(침묵) — 그 커밋이 **처음 넣는 파일**이라 대-HEAD diff 가 「지웠다」를 못 보인다 | **1** — 파일이 이제 **추적 파일**이라 같은 관측이 말한다 |

**그러므로 §6-2 규칙 5 의 「침묵하는 것은 변이가 아니라 기준선」이 양방향으로 값이 됐다**: 같은 변이가
기준선이 바뀌자 0 에서 1 로 갔다. **다음 사이클이 이 파일에 변이를 심을 때 사본 `diff` 가 아니라
`git diff` 로 확인해도 된다** — 다만 그 판단의 근거는 **파일이 추적된다는 사실**이지 명령의 이름이
아니다.

***그리고 이 마감이 그 값을 처음 재면서 「0」을 얻었는데, 그 0 은 침묵이 아니라 §후속 96 이었다.***
`buildtwin/` 에서 `git diff -- buildtwin/.dockerignore` 를 돌렸고 **경로 지정자가 CWD 기준**이라
`buildtwin/buildtwin/.dockerignore` 를 찾아 **빈 diff** 를 냈다. 같은 변이 하나에 대해 CWD × 경로
지정자를 곱으로 다시 쟀다(각 N=1, 20:13:07 UTC):

| CWD | 경로 지정자 | `-` 줄 |
|---|---|---|
| 저장소 루트 | 없음 | **1** |
| 저장소 루트 | `buildtwin/.dockerignore` | **1** |
| `buildtwin/` | `buildtwin/.dockerignore` | **0** ← **함정** |
| `buildtwin/` | `.dockerignore` | **1** |
| `buildtwin/` | `:/buildtwin/.dockerignore`(루트 고정) | **1** |

**이것이 96 의 둘째 관측이고, 첫째보다 나쁘다**: `git grep` 의 히트 0 은 「없다」로 **읽히기라도**
하지만, 여기서는 그 0 이 **§6-2 규칙 5 가 말하는 정당한 침묵과 글자 그대로 같은 값**이다 — 같은
0 이 「기준선이 침묵한다」와 「경로를 잘못 짚었다」 둘 중 어느 것인지 **그 자리에서 갈리지 않는다.**
그리고 96 이 닫힘 조건의 후보로 적어 둔 **루트 고정 `-- :/`** 가 이 표의 마지막 행에서 **작동한다**
(잘못된 CWD 에서도 1 을 낸다). 96 은 그 값을 얻었고 **닫히지는 않는다** — 없는 것은 여전히
「그 명령이 어디서 돌았는지를 읽는 사람이 아는 방법」이다.

### M-7. 이 마감이 **거짓으로 만드는 남의 자리** (§후속 72·74·76)

| 자리 | 소유 | 이 절이 낡게 만드는가 | 처분 |
|---|---|---|---|
| §3-1-1 ㅅ 의 제외 집합 | architect(내 것) | **아니다 — 값은 참이다.** 그 목록이 이름으로 적지 않은 것(깊은 `__pycache__`)이 있을 뿐 | **기록물이라 고치지 않는다**(§3-13). §M-4 ㄱ 이 그 목록을 보충한다 |
| §3-4 의 「닫히는 것」 표 | architect(내 것) | **아니다** — 이 마감이 상태를 **더** 싣는다(§M-2) | 원 표를 고치지 않는다 |
| §3-6 의 §후속 96 *"관측 1회"* | architect(내 것) | **예 — 관측이 둘이 됐다** | **기록물이라 고치지 않고** §M-8 의 96 행이 상태를 싣는다 |
| `.github/workflows/buildtwin-ci.yml` 의 주석 | qa | **아니다** — 이 절은 그 잡의 **결론**을 읽었을 뿐이고, 주석의 단정(`.dockerignore` 가 `.env` 를 뺀다 · 이 잡은 푸시하지 않는다)을 건드리지 않는다 | — |
| `tests/invariants/*.py` 머리의 표 | qa | **아니다 — 한 칸을 재현했고 갈리지 않았다**(§M-6). 「서기 전」 표의 **126 passed** 도 거짓이 아니다(§M-4 ㄴ) | — |
| `tests/postgres.floor.json` · `tests/metrics.json` | qa | **아니다 — 만지지 않았다**(§M-1 바닥값 행) | — |
| CLAUDE.md | architect(내 것) | **아니다 — 만지지 않았다** | §6-5 대기열의 순서 안에서(§2) |

### M-8. §후속 — **최종 상태 표** (§3 의 규약: 상태 표로 옮긴다)

**정본**: `git show 12a2d34:buildtwin/docs/plans/0016-the-first-run-must-fail-out-loud.md` 의 §후속 +
§M-8 + §M-13-5, 그리고 이 계획 §후속 · §3-6. **한 줄 문안은 발췌이므로 항목을 닫으려면 정본을 연다**
(§3 역방향 ⓐ). **아래는 이 사이클이 상태를 만지는 항목이고, 자구는 고치지 않는다.**

| # | 한 줄 문안(발췌) | 소유 | 이 사이클 뒤 상태 |
|---|---|---|---|
| 19 | 소유가 §2 에 없는 파일들 | architect | **열림. 실례가 는다 — `buildtwin/.dockerignore`**(이 사이클이 만들었고 **소유가 아니라 배정**으로 `qa` 가 졌다, §3-1-6). 계획 0016 §M-13-7 ⓐ 의 전수를 이 사이클은 다시 만들지 않았다 |
| 34 · 27 | 값에는 **방법 · N · 표본 간격** | architect + qa | **열림. 이 사이클의 네 절(§0 · §3-0 · 커밋 본문 · §M-0)이 그 형식을 졌다** |
| 46 | 환경 탐침의 자기-계수 | architect + qa | **열림. 실례 하나 더 — 이 마감의 고아 탐침이 자기를 셌다**(2 · 2 → 대괄호로 0 · 0, §M-1) |
| 62 | 근거 값의 정본은 커밋 본문이 아니다 | architect | **열림. 이 마감이 그 조항을 자리마다 이행했다** — §M-2·§M-3 의 **「내가 다시 쟀는가」 칸**, 그리고 **커밋 본문에만 있던 값 둘**(6.7M · 664바이트)을 **다시 재서** 이 문서로 옮겼다 |
| 65 | 기록물의 `git` 명령은 트리 인자를 명령 안에, `--all` 을 뺀다 | architect | **열림. 준수 사례가 는다**(§M-0). **그리고 그 조항이 덮지 못하는 자리가 값으로 났다** — `git diff` 는 트리 인자가 아니라 **CWD** 가 답을 바꾼다(§M-6) |
| 72 · 74 · 76 | 완료 조건 상자에 **「거짓으로 만드는 남의 자리」** 행 | architect + 각 소유 | **열림. 이 계획이 둘째 이행이었고 이 마감이 그 행을 자기에게도 걸었다**(§M-7). **작업 5 에서 시험이 한 번 더 났고 통과했다** — 워크플로 주석을 거짓으로 만든 커밋이 **같은 커밋에서** 고쳤다(`d7ad6f7`) |
| 77 | compose 에 준비 상태 계약이 없다 | 배정 미정 | **열림**(이 사이클이 닫지 않기로 한 것, §1-e) |
| 79 | 「실패를 삼키는 모양」의 전수 | architect + qa | **열림. 자리가 하나 늘었다 — 워크플로의 `run:` 블록**이고 그 자리에서 삼키는 모양이 값으로 관측됐다(C7 = 4 passed, §3-2-5). 걸음 1 의 목록은 이 사이클이 만들지 않았다 |
| 80 | 이월을 「상태 표」로 바꾼 규약 | architect | **열림. 표본이 4 가 된다**(계획 0015 → 0016 → 0017 §후속 → 이 표). **그 표가 갈림을 감췄는가 → 감추지 않았다**: 이 사이클에서 갈린 값 둘(§M-4)은 상태 표가 아니라 **측정의 제외 집합**과 **같은 수의 두 뜻**에서 났다 |
| 86 | 큐 여섯을 세 사이클 연속 밀었다 | architect | **열림. 이 사이클도 밀었다 — 그러나 「보이게」는 됐다**: 이 계획이 그 항목을 열었고 다음 계획이 여섯 각각에 ①②③ 을 배정하지 않으면 **네 사이클째**가 된다 |
| 87 | 짓고 안 짓는 결정이 워크플로 주석에만 남는다 | qa + architect | **열림. 관측 여전히 0** — 이 사이클은 그 주석을 **읽었고**(§M-2·§M-7) 어느 게이트도 그것을 읽지 않았다는 사실은 바뀌지 않았다 |
| 88 | 스택이 뜨는가 · 시드가 닿는가를 도는 주체가 사용자뿐 | qa + architect | **열림. 값이 하나 더 붙는다** — `d7ad6f7` 의 `image` 잡도 `build` 에서 끝난다(스텝 표, §M-2) |
| 92 | 두 목록이 같은 번호를 두 항목에 준다 | architect | **열림. 이 마감이 새 번호를 하나의 공간에서 뽑았다**(=100). **그리고 같은 모양이 번호가 아니라 *값*에서 났다** — 「126 passed」가 두 뜻이다(§M-4 ㄴ) |
| 93 | 재빌드 경로를 도는 주체가 없다 | qa + architect | **열림. 값이 하나 붙는다** — `d7ad6f7` 의 짓는 스텝이 **79초**로 첫 실행과 같은 자리수다(§M-2). **러너는 매번 캐시 없이 짓는다** |
| 94 | 이미지 **안**에 무엇이 들어갔는지 보는 게이트가 없다 | qa | **열림. 작업 5 도 닫지 않았다** — 그 불변식이 보는 것은 **빼겠다는 선언**이다. 관측 1회 그대로 |
| 95 | `.env.example` 머리말의 「compose 가」에 한정어가 없다 | 소유 없음 → 배정 필요 | **열림. 이 사이클이 배정하지 않았다** |
| 96 | `git <명령> … -- .` 의 경로 지정자가 CWD 기준이라 조용히 빠진다 | architect + qa | **열림. 관측이 2회가 됐다**(§M-6) — 그리고 둘째는 `git grep` 이 아니라 **`git diff`** 이고, 그 0 이 **§6-2 규칙 5 의 정당한 침묵과 같은 값**이다. 닫힘 조건의 후보 **루트 고정 `-- :/`** 는 값으로 작동한다 |
| 63 · 50 · 38 · 25 · 27 · 69 · 71 · 82 · 83 · 84 · 85 | §6-5 커밋의 몫 | architect | **열림. 이 사이클은 만지지 않았다**(§2 — CLAUDE.md 무접촉) |
| 2 · 3 · 6 · 11 · 23 · 24 | 큐 여섯 | 각 소유 | **열림. 이 사이클도 밀었다** — 상태는 86 이 진다 |

### M-9. §확인하지 않은 것 — **최종 상태 표**

**정본**: 계획 0016 의 §확인하지 않은 것 + §M-9 + §M-13-6, 그리고 이 계획 §확인하지 않은 것 · §3-6.
**번호는 §3-6 이 정한 하나의 공간을 쓴다**(옛 번호는 소속을 붙여 부른다).

| # | 한 줄 문안(발췌) | 이 사이클 뒤 상태 |
|---|---|---|
| **계획 0016 의 79** | `c82cf47` 뒤의 `Dockerfile` 로 `up --build` 가 서는가 | **짓는 축은 닫힌다 · 서는 축은 열린 채**(§3-2-1). 짓는 축의 값이 **N=3** 이 됐다(§M-2) |
| 87(§확인하지 않은 것) | 새 잡이 초록인지 빨간지 | **닫힌 채 — 초록이다**(N=3, §M-2). 한정: **`build` 에 대한** 초록이다 |
| 88(§확인하지 않은 것) | 러너에서 빌드가 얼마나 걸리는가 | **닫힌 채. 값이 하나 더 붙는다** — `d7ad6f7` 의 짓는 스텝 **79초**(§M-2) |
| 89 | 불변식이 「잡이 있는데 아무것도 짓지 않는」 경우를 가르는가 | **닫힌 채**(C4 = 2 failed, 2 passed) |
| 90 | `docker compose build` 와 `up --build` 가 같은 것을 짓는가 | **열린 채.** 이 마감도 명령의 **이름**만 읽었다. **[재야 한다 → qa · 데몬이 있는 자리]** |
| 91 | `.env` 를 CI 에서 만드는 것의 부작용 | **닫힌 채**(§3-1-3). 한정: **모양으로** 판정했다 |
| 97 | 이미지 레이어 **안**에 `.env` 가 실제로 있는가 | **열린 채.** 이 마감이 읽은 것은 잡·스텝의 **결론**이다(§M-2) — 레이어를 연 사람이 없다. ***한정을 하나 더 적는다***: 작업 5 의 매처는 **도커가 아니다**(그 파일 머리의 한정) — 그러므로 이 항목은 「레이어에 있는가」뿐 아니라 **「선언과 도커의 판정이 같은가」**도 함께 묻는다. **[재야 한다 → qa · 데몬이 있는 자리]** |
| 98 | 사용자의 기계에서 그 이미지가 남는가 · 얼마나 | **열린 채.** 그리고 **이미 구워진 레이어를 이 사이클의 무엇도 지우지 못한다**(`d7ad6f7` 본문이 같은 것을 적는다). **[재야 한다 → qa 또는 사용자]** |
| 99 | 러너의 compose 가 `.env` **없이** 어떻게 죽는가 | **열린 채.** 세 실행 모두 `make env` 를 먼저 돌았다(§M-2 의 스텝 표) — 이 축을 가른 실행이 **0** 이다(N=1, 내가 스텝 목록을 읽었다) |

**신규(이 마감이 연다):**

100. **[재지 않았다] `.dockerignore` 가 선 뒤 러너의 두 전송값이 무엇이 되는가**(qa). 값의 자리는
     잡 로그의 `#5 [worker internal] load .dockerignore` 와 `#7 transferring context: …` 이고,
     `1e8fb19` 에서 그것은 **2B** 와 **5.78MB** 였다(저장소 밖 · §3-1-1 ㉯). **`d7ad6f7` 의 실행은
     초록이지만 그 두 줄을 아무도 읽지 않았다** — 이 마감이 읽은 것은 잡·스텝의 결론과 시각뿐이다
     (§M-2). *묻는 것*: ⓐ `load .dockerignore` 가 이제 그 파일의 크기를 싣는가(= 러너가 그 선언을
     실제로 읽었는가) ⓑ `transferring context` 가 **5.78MB 그대로인가**(qa 의 정정이 예측한 것 —
     §M-5). *닫힘의 조건*: 그 두 줄의 값이 어느 문서에 출처와 함께 적힐 때. *한정*: **ⓐ 가 초록이어도
     레이어 안은 여전히 아무도 보지 않는다**(§후속 94 · §확인하지 않은 것 97) — 이 항목은
     **선언이 읽혔는가**까지이고 **담겼는가**가 아니다.

### M-10. 이 마감이 **반증한 것 · 재지 않은 것**

**반증한 것**(전부 값이다):
- **「파생물을 뺀 컨텍스트」의 두 값이 갈린 이유가 낡음이 아니다** — 제외 집합이 다르고, §3-1-1 ㅅ 이
  이름으로 적은 목록에 **깊은 `__pycache__`(5.7M)가 없다**(§M-4 ㄱ).
- **`d7ad6f7` 에서 D2 의 `git diff` 침묵이 재현되지 않는다** — 같은 변이가 **1** 을 낸다(§M-6).
- **이 마감의 첫 고아 값 `2 · 2` 는 자기-계수였다**(§M-1).
- **이 마감의 첫 `git diff` 값 `0` 은 침묵이 아니라 경로 지정자였다**(§M-6).

**재지 않은 것**(누가 재야 하는지와 함께 — *"놓칠 수 있다"* 는 커버리지가 아니다):
- **잡 로그의 본문**(`transferring context` · `load .dockerignore` · 설치 목록) — 결론만 읽었다.
  **[→ qa · §확인하지 않은 것 100]**
- **레이어 안**(97) · **사용자의 기계**(98) · **`.env` 없는 러너**(99) · **`up --build`**(90) ·
  **컨테이너 기동**(§후속 88).
- **변이 표 D1~D10 중 아홉 칸을 다시 심지 않았다.** 값의 정본은 저장소 안의 파일 머리 표이고, 이
  마감은 그것을 `d7ad6f7` 본문과 **칸마다 대조**했을 뿐이다(§M-3 8행).
- **`1e8fb19`·`94ed3ec` 의 잡 값을 다시 재지 못했다** — 로그 사본이 세션 밖이고, `94ed3ec` 은 결론만
  넘어왔다(§M-2).

### M-11. 사이클 0017 의 마감 판정

**작업 1·2·3·5 의 산출물이 저장소에 있고, 게이트는 이 트리에서 다시 초록이다**(§M-1).
이 사이클이 닫은 것은 **「이미지를 짓는 자리가 저장소에 하나이고 그것은 사용자의 손이다」** 라는
사실 하나다 — 이제 그 rc 를 **CI 가 사용자보다 먼저 읽고**(N=3), 그 컨텍스트에서 **시크릿이 빠지며**,
그 두 선언(짓는 잡 · 빠지는 시크릿)을 **저장소 안의 게이트가** 붙든다 —
`tests/invariants/test_ci_builds_the_image.py` 와 `tests/invariants/test_the_build_context_excludes_the_secret.py` 다.
**닫지 못한 것은 열림으로 남았다**: 레이어 안(94 · 97) · 사용자의 기계(98) · 기동(88) ·
재빌드(93) · `up --build`(90) · 러너의 로그 본문(100).
**다음 사이클이 받을 자리는 §M-8 · §M-9 의 두 표이고, 큐를 미룬 사실은 86 이 진다.**

---

## N. 마감 확장 (심사 0017 = APPROVE 뒤) — **리뷰어가 낸 값의 이관 · 100 의 처분 · 최종 상태 표**

> **이 절이 사이클 0017 의 마지막 산출물이다.** 위 §0~§M 은 트리 `12a2d34`·`1e8fb19`·`d7ad6f7` 의
> 기록물이고 **이 절은 그것을 고치지 않는다** — 더할 뿐이다(CLAUDE.md §3-13 첫째 갈래).
> 이 절이 남의 자리를 낡게 만드는지는 §N-9 가 자리마다 판정한다.

### N-0. 이 절의 트리 · 방법 · N · 표본 간격 · 자원

**작업 트리** `/home/user/Bim/buildtwin`, **저장소 루트 `/home/user/Bim`**, **트리 `580d3f6`**
(§M 이 커밋된 뒤. 이 절을 여는 시점의 루트 `git status --porcelain` 은 **빈 출력**, 커밋 직전에
이 문서와 계획 0018 두 줄).

**명령의 못박음**(§후속 **65** ㉡ · §후속 **96**). 이 절의 모든 `git` 명령은 **트리 인자를 명령 안에
담고 `--all` 을 쓰지 않는다**. **그리고 전수와 `git diff`·`git ls-tree` 는 전부 저장소 루트에서
돌렸다** — 96 의 함정이 CWD 이기 때문이다. 트리 인자를 받지 못하는 명령(`make` · `pytest` · `wc` ·
`ls` · `pgrep` · `grep` · `python3`)은 전부 위 작업 트리에서 돌린 것이다.

**방법 · N · 표본 간격**(§후속 **34** · 27). 아래 **내가 잰** 값은 **2026-09-08 한 세션 안**
(20:36~20:43 UTC, `date -u` 로 앞뒤를 찍었다)의 것이고, **따로 적지 않으면 N=1**, 서로 몇 분 간격이다.
**「없다」·「히트 0」·「죽었다」에도 N 을 적는다.** **리뷰어가 낸 값은 내가 잰 값이 아니다** —
§N-2 가 그것을 출처와 함께 분리해 싣는다.

**자원**: **docker 데몬 0회**(`docker info` **rc 1** · `/proc/sys/net/ipv4/ip_forward` **0**, 각 N=1) ·
postgres 클러스터 0회(로컬 `make test` 는 sqlite 갈래다) · **심은 변이 0** · **저장소 두 자리에
`.env` 를 만들지 않았다**(`ls -a <각 디렉터리> | grep -c '^\.env$'` → **0 · 0**, 각 N=1) ·
`make e2e` 가 잡은 포트는 그 명령이 반납했다(§N-1 의 고아 행).

### N-1. 게이트 — **내가 다시 돌린 값** (트리 `580d3f6`)

| 게이트 | 값 | 방법 · N · 간격 |
|---|---|---|
| `make test` | **rc 0** · unit **492** · invariants **126** · regression **8** · integration **232** · vitest **310**(31 files) | `make test`, **N=2**(20:38:57→20:40:44 · 20:40:44→20:42:35). 첫 실행은 rc 0 과 vitest 310 을 읽었고 둘째 실행이 다섯 수를 전부 냈다 |
| `make lint` | **rc 0** | N=1, 20:42:42→20:42:50 |
| `make e2e` | **12 passed · rc 0** | N=1, 20:42:54→20:43:24 |
| 고아 | `pgrep -fc '[u]vicorn'` **0** · `pgrep -fc '[v]ite'` **0** · `.e2e-preview.*` **0** | 각 N=1, e2e 뒤. **자기-계수는 §N-6 ㄴ** |
| 바닥값 | `git log --oneline f9c6265^..580d3f6 -- buildtwin/tests/postgres.floor.json buildtwin/tests/metrics.json` **빈 출력**, `min_tests_on_postgres` 통합 **187** · 단위 **252** | N=1, 저장소 루트 |
| CLAUDE.md | **무접촉** — `git log --oneline f9c6265^..580d3f6 -- buildtwin/CLAUDE.md` **빈 출력**. §6 는 **21,128자 · 277줄**(전체 32,222자 · 491줄 대비 **65.6%**) | N=1, 저장소 루트 + `python3` |

**심사가 잰 값과 갈리는 칸은 없다.** 이 표는 심사의 재현이 아니라 **셋째 측정**이다(qa `d7ad6f7` →
§M-1 `d7ad6f7` N=2 → 심사 → 이 절 `580d3f6` N=2).

### N-2. **리뷰어가 낸 값의 이관** (§후속 **62** — 출처를 적고, 근거로 쓰려면 다시 잰다)

**아래는 전부 저장소 밖의 값이고 내가 재지 못했다.** 출처는 하나다 — **리뷰어가 `d7ad6f7` 의 잡 로그
본문(105KB)을 읽었다**. 이 표의 어느 줄도 내가 실행해서 얻은 것이 아니므로, **다음 사이클이 이 값을
근거로 삼으려면 그 자리에서 다시 재야 한다.**

| 자리 | 리뷰어가 읽은 값 | 내가 다시 쟀는가 |
|---|---|---|
| `#5 [worker internal] load .dockerignore` | `transferring context: **2.74kB** done` | **아니다**(저장소 밖). 대조값은 §N-3 ⓐ |
| `#7 transferring context` | **5.85MB** | **아니다**(저장소 밖). 대조값은 §N-4 |
| `#11 DONE` | **61.2s** | **아니다**(저장소 밖) |
| 설치 목록 | `Successfully installed … openpyxl-3.1.5 …` | **아니다**(저장소 밖) |
| 태그 | `naming to docker.io/library/buildtwin-{api,worker}` | **아니다**(저장소 밖) |
| `docker push`\|`login`\|`save` | **히트 0** | **아니다**(저장소 밖). 저장소 안의 대응 단정은 워크플로 주석 *"이 잡은 이미지를 푸시하지 않는다"* 이고 그것은 **선언**이다 |
| `JWT_SECRET` | **히트 1** — 그 *"값은 출력하지 않는다"* 줄 | **아니다**(저장소 밖) |
| `580d3f6` 의 푸시 실행 | **success** | **아니다**(저장소 밖) |
| 변이 다섯(C7·D2·D6·D7·D8) 재현 | **계획의 표와 전부 일치** | **아니다 — 이 절은 변이를 하나도 심지 않았다**. §M 이 심은 것은 D2 하나다 |
| 추적 전수 **423 경로** → 최종 제외 **0** | — | **아니다** |

### N-3. **§확인하지 않은 것 100 의 처분** — ⓐ 는 닫힌다 · ⓑ 는 갈린다

**ⓐ 닫힌다.** 100 이 물은 것은 *"`load .dockerignore` 가 이제 그 파일의 크기를 싣는가(= 러너가 그
선언을 실제로 읽었는가)"* 이고, 그 자리의 값이 **2B → 2.74kB** 로 바뀌었다(출처 §N-2).
**저장소 안의 대조값은 내가 쟀다**: `wc -c buildtwin/.dockerignore` → **2698**(N=1, 20:36 UTC).
**자리수가 맞고, 그러므로 러너는 그 선언을 읽었다.**

- ***한정 하나를 적는다 — 두 수는 같지 않다.*** 2,698 B 와 2.74 kB 는 **42바이트 차이**이고
  **나는 그 차이를 재지 않았다**(전송 프레이밍인지, 리뷰어가 읽은 반올림 표기인지 갈리지 않는다).
  **ⓐ 를 닫는 것은 등식이 아니라 자리수다** — 「2B(= 빈 선언)」와 「그 파일 크기」는 세 자리수가
  다르고, 그 차이만이 이 물음이 필요로 한 값이다. 42바이트는 **§N-11 의 100 행에 미측정으로 남긴다.**
- ***한정 둘 — 100 이 원래 적어 둔 것을 지운다는 뜻이 아니다.*** **ⓐ 가 닫혀도 레이어 안은 여전히
  아무도 보지 않는다**(§후속 94 · §확인하지 않은 것 97). 이 항목이 닫는 것은 **선언이 읽혔는가**
  까지이고 **담겼는가**가 아니다.

**ⓑ 갈린다.** 100 이 함께 물은 것은 *"`transferring context` 가 **5.78MB 그대로인가**(qa 의 정정이
예측한 것 — §M-5)"* 였고, 러너가 낸 값은 **5.85MB** 다. **그 예측은 자리수에서 재현되지 않는다.**

| qa 의 정정(§M-5) | 러너의 값 | 판정 |
|---|---|---|
| **방향**: 「러너에서 컨텍스트가 줄지 않는다」 | 5.78MB → **5.85MB**(줄지 않았다) | **참이다** |
| **값**: 「5.78MB 그대로」 | **5.85MB** | **거짓이다** — 늘었다 |

**늘어난 원인은 `.dockerignore` 가 아니라 이 사이클 자신이 더한 추적 파일이고, `.env` 664B 의 제거는
그 아래다.** 기전은 §N-4 가 값으로 낸다.

### N-4. **컨텍스트 두 값의 차이 — 저장소 안에서 잰 대조값** (내가 쟀다)

러너가 두 실행에서 잰 컨텍스트는 서로 다른 두 트리의 것이다(`1e8fb19` 와 `d7ad6f7`). 그 두 트리의
**추적 바이트**를 저장소 루트에서 쟀다 — 명령은 `git ls-tree -r -l <트리> -- buildtwin` 의 크기 열
합계이고, **트리 인자를 담고 `--all` 을 뺐으며 CWD 는 저장소 루트다**(§후속 65 ㉡ · 96). 각 N=1.

| 트리 | 추적 바이트(`buildtwin/`) | 러너가 낸 `transferring context`(§N-2 · 저장소 밖) |
|---|---|---|
| `1e8fb19` | **5,749,428** = 5.75 MB | **5.78MB** |
| `d7ad6f7` | **5,822,950** = 5.82 MB | **5.85MB** |
| **차이** | **+73,522 B = +0.0735 MB** | **+0.07 MB** |

**차이가 맞는다.** 그 73,522 B 의 내역도 같은 명령이 낸다(각 N=1):
`buildtwin/.dockerignore` **+2,698**(새 파일) · `tests/invariants/test_the_build_context_excludes_the_secret.py`
**+22,060**(새 파일) · **이 계획 문서 자신 +48,764**(같은 구간의 `git diff --name-status 1e8fb19 d7ad6f7 -- buildtwin`
가 그 셋만 낸다).

**그러므로 §N-3 ⓑ 의 원인은 값으로 선다**: 러너의 컨텍스트를 늘린 것은 **이 사이클이 커밋한 추적
파일**이고, 그중 **가장 큰 것은 이 계획 문서**다. `.env` 의 664 B 는 러너가 보고하는 해상도
(0.01MB = 10,000 B)보다 **작아서 그 값에 보이지 않는다.**

***역방향 확인 — 이 대조가 놓치는 것.***
- **추적 바이트는 빌드 컨텍스트가 아니다.** 컨텍스트는 **작업 트리**에서 `.dockerignore` 를 뺀 것이고,
  러너의 갓 체크아웃한 트리가 추적 파일에 가까울 뿐이다. 두 축의 잔차 **약 30KB**(5.78−5.75,
  5.85−5.82)는 **내가 재지 않았다** — 체크아웃이 만드는 미추적 파일인지 tar 프레이밍인지 갈리지 않는다.
  **잔차가 두 실행에서 같은 자리수라는 것만 값이다.**
- **이 대조는 「러너가 무엇을 뺐는가」를 말하지 않는다.** `.dockerignore` 가 실제로 무엇을 걸렀는지는
  잡 로그의 그 줄 하나가 아니라 레이어가 답한다(§확인하지 않은 것 97).
- **§3-1-1 ㅅ 의 큰 값들(9.4M · 6.7M)은 이 축에서 재현되지 않는다** — 그 값들이 재는 제외 집합은
  **이 기계의 파생물**(깊은 `__pycache__` 5.7M · `node_modules` 등)이고 **러너의 갓 체크아웃한 트리에는
  그것이 없다.** 그러므로 **`.dockerignore` 의 러너에서의 이득은 `.env` 664 B 이고, 이 기계에서의
  이득과 자리수가 다르다.** 이 사실은 §3-1-1 을 거짓으로 만들지 않는다(그 절은 **이 기계**를 쟀다) —
  **두 값이 다른 것을 재고 있었다**는 것이 여기서 처음 이름을 얻는다.

### N-5. **짓는 축의 표본이 넷이 됐다 — 그리고 매번 콜드다**

| 트리 | 짓는 잡의 결론 | 짓는 스텝 | 잡 전체 | 출처 |
|---|---|---|---|---|
| `1e8fb19` | success | (재지 않았다) | **59.7s** | §3-1-1 ㉯(저장소 밖, 이 사이클의 인계) |
| `94ed3ec` | success | (넘어오지 않았다) | (넘어오지 않았다) | §M-2(결론만) |
| `d7ad6f7` | success | **61.2s** | **79s** | 리뷰어(§N-2) · §M-2 |
| **`580d3f6`** | **success** | (재지 않았다) | **84s** | **리뷰어**(§N-2) |

**§M-8 의 88·93 행이 *"이 마감의 N=3 표가 자기 커밋의 실행을 담지 못한다"* 고 스스로 적었는데
그 칸이 채워졌다** — 마감을 담은 커밋의 푸시 실행도 초록이다.

**그리고 §후속 93(재빌드 경로를 도는 주체가 없다)의 표본이 셋이 됐다**: 79 · 84 · 61.2 초는 전부
**콜드 빌드의 자리수**이고, 러너 사이에 레이어 캐시가 넘어가지 않는다는 사실이 세 실행에서 같다.
***한정***: **이것은 「캐시가 없다」의 관측이지 「캐시를 두면 얼마가 되는가」가 아니다** — 후자는
아무도 재지 않았고 93 은 그 축에서 열린 채다.

### N-6. **이 절이 이름으로 남기는 두 자리**

**ㄱ. C7 사각지대는 저장소의 열린 구멍이고, 그것을 지는 것은 §후속 79 다.**
리뷰어가 다시 심었다: 짓는 스텝의 `run` 을 `set +e` + 빌드 + `exit 0` 으로 바꾸면
`test_ci_builds_the_image.py` 가 **4 passed**(= 무변화)다. **0017 이 세운 관측자 자신이, 셸 안에서
rc 를 0 으로 만드는 편집에 대해 침묵한다.**

**이 사실에 이름을 붙인다**: **「워크플로 `run:` 축」은 값만 있고 게이트가 없다.** 값은 있다 —
C7 의 4 passed 가 그것이고, 관측은 **1회**(qa 가 심었고 리뷰어가 재현했으니 같은 변이의 관측은
둘이지만, **그 축을 전수한 목록은 0회**다). 없는 것은 **§후속 79 의 걸음 1 — 그 축에서 「실패를
삼키는 모양」이 무엇인지의 전수**이고, 그것이 서기 전까지 이 구멍은 이름만 있고 닫히지 않는다.
**이 사이클은 그것을 닫지 않았다.** 계획 0018 이 그것을 삼는다.

**ㄴ. §후속 46 의 실례가 하나 더 났고, 기전이 다르다.**
이 절의 첫 고아 탐침이 `1 · 1` 을 냈다. 대괄호(`[u]vicorn`)는 **pgrep 자신**을 거르지만, 그 호출이
같은 셸 줄에 `echo "uvicorn rc=$?"` 를 담고 있어 **부모 셸의 cmdline 에 그 낱말이 맨 채로 들어갔고**
`pgrep -f` 가 그것을 셌다. **별도 호출로 다시 재니 `0 · 0`**(각 N=1, §N-1).
**그러므로 46 의 처방은 「대괄호」가 아니라 「별도 호출」이다** — 대괄호만으로는 같은 줄의 다른
낱말을 못 막는다. 이것이 46 의 **셋째** 기전이다(앞의 둘: pgrep 이 자기를 셈 · `pkill -f` 가 부모
셸을 죽임).

### N-7. **§후속 96 의 처리** — 곱 표는 재현됐고, D 표의 0 은 구조로 설명되며, **그래도 갈리지 않는다**

리뷰어가 §M-6 의 곱 표(CWD × 경로 지정자)를 다시 쟀고 **다섯 칸이 계획과 일치**한다 —
`buildtwin/` + `buildtwin/.dockerignore` = **0**(함정), 나머지 넷 = **1**. 그리고 **빈 저장소에서
기전으로 갈랐다**: 추적되지 않는 파일의 줄을 지우면 대-HEAD `git diff` 의 `-` 는 **0**, 추적 파일이면
**1**.

**그러므로 `test_the_build_context_excludes_the_secret.py` 의 D 표에서 D1~D7·D9 의 0 은
「기준선이 추적 밖」으로 구조적으로 설명되고, 함정(CWD)이 아니다.** 그 표에서 D8 만 **2** 인 것도
같은 기전이다 — 그 변이가 만지는 `docker-compose.yml` 은 **추적 파일**이다.

***그런데 96 이 말하는 것은 정확히 그다음이다: 값 하나만으로는 여전히 안 갈린다.***
「기준선이 추적 밖이라 침묵한 0」과 「CWD 를 잘못 짚어 나온 0」은 **글자 그대로 같은 값**이고,
그 0 을 읽는 사람은 그 자리에서 둘을 가르지 못한다. **위 판정을 가능하게 한 것은 그 0 자신이 아니라
바깥에서 세운 기전 실험(빈 저장소)과 「그 파일이 그 시점에 추적됐는가」라는 별도 사실**이다.
**그러므로 96 은 값을 하나 더 얻었고 닫히지 않는다.** 닫힘의 후보는 여전히 **루트 고정 `-- :/`**
(§M-6 마지막 행에서 작동한다)이고, 없는 것은 **그 명령이 어디서 돌았는지를 읽는 사람이 아는 방법**이다.

### N-8. **리뷰어도 재지 못한 것** (누가 재야 하는지와 함께 — *"놓칠 수 있다"* 는 커버리지가 아니다)

- **레이어 안**(§확인하지 않은 것 **97**) · **사용자의 기계에 남는 이미지**(**98**) · **`.env` 없는
  러너의 compose**(**99**) · **`up --build`**(**90**) · **컨테이너 기동**(§후속 **88**) — 전부
  **데몬 rc 1** 인 자리에서는 잴 수 없다. **[재야 한다 → qa · 데몬이 있는 자리]**
- **`1e8fb19` 의 잡 로그 본문**(59.7s · 2B · 5.78MB) — **그 사본이 세션 밖이라 리뷰어도 읽지 못했다.**
  그가 낸 것은 **`d7ad6f7` 의 대응값**이다. 그러므로 §N-3 ⓐ 의 「2B → 2.74kB」는 **두 트리에 걸친
  비교**이고 한 로그 안의 두 줄이 아니다.
- **변이 C1~C6·C8 · D1·D3·D4·D5·D9·D10 은 다시 심지 않았다.** 리뷰어가 심은 것은 C7·D2·D6·D7·D8 다.
  나머지 칸의 정본은 저장소 안의 파일 머리 표이고, **이 절은 그것을 다시 재지 않았다.**
- **`1e8fb19` 의 invariants 122 는 다시 재지 않았다** — 126 = 118 + 8 의 산술과 134 로만 대조했다.
  **[재야 한다 → 그 트리를 체크아웃하는 사람]**

### N-9. 이 절이 **거짓으로 만드는 남의 자리** (§후속 72·74·76)

| 자리 | 소유 | 이 절이 낡게 만드는가 | 처분 |
|---|---|---|---|
| §M-9 의 100 행(*"두 줄을 아무도 읽지 않았다"*) | architect(내 것) | **예 — 읽혔다** | **기록물이라 고치지 않고** §N-11 의 100 행이 상태를 싣는다 |
| §M-5 의 예측(*"5.78MB 그대로"*) | architect(내 것) | **예 — 값이 갈렸다**(방향은 참, 값은 거짓) | **고치지 않는다.** §N-3 ⓑ 가 판정을 싣고 §N-4 가 기전을 낸다 |
| §M-8 의 88·93 행(*"자기 커밋의 실행을 담지 못한다"*) | architect(내 것) | **예 — 그 칸이 채워졌다** | **고치지 않는다.** §N-5 가 넷째 표본을 싣고 §N-10 이 상태를 갱신한다 |
| §3-1-1 ㅅ 의 제외 집합 값(9.4M · 6.7M) | architect(내 것) | **아니다 — 값은 참이다**(그 절은 **이 기계**를 쟀다) | §N-4 역방향 셋째가 **두 값이 다른 것을 잰다**는 사실을 이름으로 남긴다 |
| `tests/invariants/test_ci_builds_the_image.py` 의 C7 행 | qa | **아니다 — 재현됐다**(4 passed) | §N-6 ㄱ 이 그 사각지대를 §후속 79 에 매단다. 파일은 만지지 않는다 |
| `tests/invariants/test_the_build_context_excludes_the_secret.py` 의 D 표 | qa | **아니다 — 다섯 칸이 재현됐고 나머지는 다시 재지 않았다** | §N-7 이 그 0 들의 기전을 싣는다. 파일은 만지지 않는다 |
| `.github/workflows/buildtwin-ci.yml` 의 주석 | qa | **아니다** — 이 절은 그 잡의 로그 값을 실을 뿐 주석의 단정을 건드리지 않는다 | — |
| `tests/postgres.floor.json` · `tests/metrics.json` | qa | **아니다 — 만지지 않았다**(§N-1) | — |
| CLAUDE.md | architect(내 것) | **아니다 — 만지지 않았다**(§N-1) | 계획 0018 §2 가 대기열의 순서를 다시 판정한다 |

### N-10. §후속 — **사이클 0017 최종 상태 표** (§3 의 규약: 상태 표로 옮긴다)

**정본**: `git show 12a2d34:buildtwin/docs/plans/0016-the-first-run-must-fail-out-loud.md` 의 §후속 +
§M-8 + §M-13-5, 그리고 이 계획 §후속 · §3-6 · §M-8. **한 줄 문안은 발췌이므로 항목을 닫으려면 정본을
연다**(§3 역방향 ⓐ). **아래는 §M-8 을 상태만 갱신한 것이고 자구는 고치지 않는다** — 갱신되지 않은
항목은 §M-8 의 상태 그대로다.

| # | 한 줄 문안(발췌) | 소유 | **사이클 0017 이 닫힌 뒤 상태** |
|---|---|---|---|
| 19 | 소유가 §2 에 없는 파일들 | architect | **열림. 실례가 하나 늘었다** — §2 트리 블록이 소유를 적지 않는 자리에 **`.dockerignore`** 가 더해졌다(그 자리에서 도는 전수: `git ls-tree --name-only 580d3f6 -- buildtwin/` 의 각 이름을 §2 트리 블록과 대조한다 — 이 절은 그 목록의 **길이를 적지 않는다**, §6-1 9회차)(이 사이클이 만들었고 **명시 배정**으로 `qa` 가 졌다). **그 선례가 축을 바꿨는지는 계획 0018 §2 가 값으로 판정한다** |
| 34 · 27 | 값에는 **방법 · N · 표본 간격** | architect + qa | **열림. 이 절이 다섯째 이행이다**(§0 · §3-0 · 커밋 본문 · §M-0 · §N-0) |
| 46 | 환경 탐침의 자기-계수 | architect + qa | **열림. 기전이 셋이 됐다**(§N-6 ㄴ) — pgrep 이 자기를 셈 · `pkill -f` 가 부모 셸을 죽임 · **같은 셸 줄의 다른 낱말이 부모 cmdline 에 들어감**. **처방은 대괄호가 아니라 별도 호출이다** |
| 62 | 근거 값의 정본은 커밋 본문이 아니다 | architect | **열림. 축이 하나 늘었다** — 이 절이 이관한 값의 출처는 커밋 본문이 아니라 **세션 밖의 잡 로그**이고, §N-2 의 **「내가 다시 쟀는가」 칸이 전부 「아니다」**다. **그 값을 근거로 쓰려면 다시 재야 한다** |
| 65 | 기록물의 `git` 명령은 트리 인자를 담고 `--all` 을 뺀다 | architect | **열림. 준수 사례가 는다**(§N-0 · §N-4) |
| 72 · 74 · 76 | 완료 조건 상자에 **「거짓으로 만드는 남의 자리」** 행 | architect + 각 소유 | **열림. 이 절이 셋째 이행이다**(§N-9). **그리고 이 절에서 그 행이 실제로 값을 냈다** — 내 자리 셋이 낡았다고 판정했고 셋 다 **고치지 않고 상태로** 옮겼다 |
| 77 | compose 에 준비 상태 계약이 없다 | 배정 미정 | **열림. 값이 붙는다** — `grep -c healthcheck buildtwin/docker-compose.yml` **0**, `depends_on` 셋 다 `condition:` 없음(각 N=1). **계획 0018 §2 가 이것을 경쟁 후보의 비용으로 읽는다** |
| 79 | 「실패를 삼키는 모양」의 전수 | architect + qa | **열림 — 그러나 다음 사이클이 진다.** 축의 이름이 섰다(**워크플로 `run:`**, §N-6 ㄱ)이고 값도 있다(C7 = 4 passed, 관측 재현됨). **걸음 1 의 목록은 아직 0회**다 → **계획 0018 의 주제** |
| 80 | 이월을 「상태 표」로 바꾼 규약 | architect | **열림. 표본이 5 가 된다**(0015 → 0016 → 0017 §후속 → §M-8 → 이 표). **그 표가 갈림을 감췄는가 → 감추지 않았다**: 이 절에서 갈린 값(§N-3 ⓑ)은 상태 표가 아니라 **예측과 실측** 사이에서 났다 |
| 86 | 큐 여섯을 세 사이클 연속 밀었다 | architect | **열림. 네 사이클째가 된다** — 계획 0018 도 여섯을 삼지 않는다(그 판정과 재측정값은 0018 §2) |
| 87 | 짓고 안 짓는 결정이 워크플로 주석에만 남는다 | qa + architect | **열림. 관측 여전히 0** — 어느 게이트도 그 주석을 읽지 않는다 |
| 88 | 스택이 뜨는가 · 시드가 닿는가를 도는 주체가 사용자뿐 | qa + architect | **열림. 사용자는 여전히 첫 실행자다** — 넷째 실행도 `build` 에서 끝난다(§N-5). **계획 0018 이 이것을 경쟁 후보로 심사하고 이월한다**(근거는 0018 §2) |
| 92 | 두 목록이 같은 번호를 두 항목에 준다 | architect | **열림. 이 절은 새 번호를 열지 않았다** |
| 93 | 재빌드 경로를 도는 주체가 없다 | qa + architect | **열림. 표본이 셋이 됐다** — 61.2s · 79s · 84s 가 전부 **콜드**다(§N-5). **한정: 「캐시가 없다」의 관측이지 「캐시를 두면 얼마인가」가 아니다** |
| 94 | 이미지 **안**에 무엇이 들어갔는지 보는 게이트가 없다 | qa | **열림. 관측 1회 그대로** — ⓐ 가 닫혀도 레이어는 아무도 보지 않았다(§N-3 한정 둘) |
| 95 | `.env.example` 머리말의 「compose 가」에 한정어가 없다 | 소유 없음 → 배정 필요 | **열림. 이 사이클은 배정하지 않았다** |
| 96 | `git <명령> … -- .` 의 경로 지정자가 CWD 기준이라 조용히 빠진다 | architect + qa | **열림. 관측 2회 그대로이고, 기전이 갈렸다**(§N-7): D 표의 0 들은 **함정이 아니라 「기준선이 추적 밖」**이다. **그래도 값 하나로는 안 갈린다** — 가른 것은 값이 아니라 **바깥의 기전 실험**이다 |
| 63 · 50 · 38 · 25 · 27 · 69 · 71 · 82 · 83 · 84 · 85 | §6-5 커밋의 몫 | architect | **열림. 이 사이클은 CLAUDE.md 를 만지지 않았다**(§N-1) |
| 2 · 3 · 6 · 11 · 23 · 24 | 큐 여섯 | 각 소유 | **열림. 계획 0018 이 값을 다시 쟀고 그 표는 0018 §2 에 있다** — 상태는 86 이 진다 |

### N-11. §확인하지 않은 것 — **사이클 0017 최종 상태 표**

**정본**: 계획 0016 의 §확인하지 않은 것 + §M-9 + §M-13-6, 그리고 이 계획 §확인하지 않은 것 ·
§3-6 · §M-9. **번호는 §3-6 이 정한 하나의 공간을 쓴다.**

| # | 한 줄 문안(발췌) | **사이클 0017 이 닫힌 뒤 상태** |
|---|---|---|
| **계획 0016 의 79** | `c82cf47` 뒤의 `Dockerfile` 로 `up --build` 가 서는가 | **짓는 축은 닫힌다**(표본 **N=4**, §N-5) · **서는 축은 열린 채** |
| 87 | 새 잡이 초록인지 빨간지 | **닫힌 채 — 초록이다**(N=4). 한정: **`build` 에 대한** 초록이다 |
| 88 | 러너에서 빌드가 얼마나 걸리는가 | **닫힌 채. 표본이 넷** — 59.7s · (94ed3ec 미전달) · 79s · 84s, 짓는 스텝은 61.2s(§N-5) |
| 89 | 불변식이 「잡이 있는데 아무것도 짓지 않는」 경우를 가르는가 | **닫힌 채**(C4 = 2 failed, 2 passed — 리뷰어가 재현하지 않은 칸이다) |
| 90 | `docker compose build` 와 `up --build` 가 같은 것을 짓는가 | **열린 채.** **[재야 한다 → qa · 데몬이 있는 자리]**. **계획 0018 §2 가 이것을 경쟁 후보로 심사하고 이월했다** |
| 91 | `.env` 를 CI 에서 만드는 것의 부작용 | **닫힌 채**(§3-1-3). **그리고 로그가 그것과 맞는다**(§N-2: `push`\|`login`\|`save` 히트 0 · `JWT_SECRET` 히트 1). 한정: **저장소 밖의 값이라 내가 재지 않았다** |
| 97 | 이미지 레이어 **안**에 `.env` 가 실제로 있는가 | **열린 채.** ⓐ 가 닫혀도 레이어를 연 사람은 없다. **[재야 한다 → qa · 데몬이 있는 자리]** |
| 98 | 사용자의 기계에서 그 이미지가 남는가 · 얼마나 | **열린 채. [재야 한다 → qa 또는 사용자]** |
| 99 | 러너의 compose 가 `.env` **없이** 어떻게 죽는가 | **열린 채.** 네 실행 모두 `make env` 를 먼저 돌았다 — 이 축을 가른 실행이 **0** 이다 |
| **100** | `.dockerignore` 가 선 뒤 러너의 두 전송값이 무엇이 되는가 | **ⓐ 닫힌다**(2B → **2.74kB**, 저장소 안 대조 `wc -c` **2698** — §N-3) · **ⓑ 갈린다**(예측 5.78MB, 실측 **5.85MB**; 방향은 참, 값은 거짓 — 기전은 §N-4). ***미측정으로 남는 것***: **2,698 B 와 2.74 kB 의 42바이트 차이**, 그리고 두 축의 **잔차 약 30KB**(§N-4 역방향 첫째) |

**신규(이 절이 여는 것): 없다.** 이 절은 값을 이관하고 상태를 갱신했을 뿐, 새로 잰 축을 열지 않았다.
**다음 사이클이 여는 자리는 계획 0018 이다.**

### N-12. 사이클 0017 최종 판정

**이 사이클이 닫은 것은 하나다** — 「이미지를 짓는 자리가 저장소에 하나이고 그것은 사용자의 손이다」가
더는 참이 아니다. 이제 그 rc 를 **CI 가 사용자보다 먼저 읽고**(초록 **N=4**, 매번 **콜드**), 그
컨텍스트에서 **시크릿이 빠지며**(러너가 그 선언을 **실제로 읽었다** — §N-3 ⓐ), 그 두 선언을 저장소
안의 게이트가 붙든다.

**닫지 못한 것은 열림으로 남았고, 그중 하나는 이 사이클이 세운 관측자 자신의 구멍이다** —
`set +e` + `exit 0` 은 그 게이트를 지나간다(§N-6 ㄱ). **다음 사이클은 새 관측자를 하나 더 세우기
전에 그 축을 붙든다**: 계획 0018 의 주제가 그것이고, 판정의 근거는 §N-6 ㄱ 과 §N-10 의 77 · 79 · 88
행이다.
