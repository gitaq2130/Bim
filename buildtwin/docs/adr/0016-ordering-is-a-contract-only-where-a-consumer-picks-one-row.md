# ADR 0016 — 순서는 **소비자가 한 행을 고르는 자리에서만** 계약이다

- 상태: Accepted
- 작성: architect
- 날짜: 2026-09-07
- 관련: `docs/adr/0015-heap-placement-is-not-a-test-contract.md` §2-1 · §Deferred 3(이 ADR 이 여는 자리),
  `docs/adr/0014-database-as-a-test-parameter.md` §5-3(`[ORD]` 677),
  `docs/plans/0012-the-order-of-rows-is-a-contract-or-it-is-not.md`(이 사이클의 계획),
  `docs/plans/0009-postgres-coverage-gap.md` §후속 17,
  CLAUDE.md §3-13 · §6-1 · §6-2 · §6-3 · §6-4

---

## 0. 이 문서의 실측이 나온 자리 (재현 방법)

**작업 트리** `/home/user/Bim/buildtwin`, **저장소 루트 `/home/user/Bim`**, 브랜치
`claude/buildtwin-initial-setup-ubulzb`, **HEAD `08238c9`**, 루트 `git status --porcelain` 빈 출력.
DB: 로컬 `PostgreSQL 16.13 (Ubuntu 16.13-0ubuntu0.24.04.1)`, **포트 55438**, `autovacuum=on`.

**이 문서의 모든 `파일:줄`·커밋 참조는 HEAD `08238c9` 트리의 것이다**(CLAUDE.md §3-13 첫째 갈래).
**범위를 좁히지 않고 예외를 이름으로 적는다**(ADR 0015 §0 이 겪은 것 — 계획 0011 §후속 36).
**예외는 둘뿐이다:**

- **㉮ 다른 문서를 인용하는 줄** — ADR 0014 는 `743bcb9`, ADR 0015 는 `743bcb9`, 계획 0009 는 `5beb954`,
  계획 0011 은 `711e12f`(본문)·`3dddf58`(마감)을 못박았고, 그 값을 그대로 말한다.
- **㉯ §1-2 의 0009 트리 재계수** — `git archive 5beb954` 로 꺼낸 트리의 값이다. 두 트리의 값을 나란히
  싣고 **칸마다 어느 트리인지 적는다**(계획 0009 §M-0 의 형식).
- **㉰ §2-3 의 정정 상자와 그것이 낳은 §3 2 · §5 6행 · §6 7·8 · §Deferred 8** — 이 사이클의 작업
  3·4·5·7 이 끝난 뒤(**`cac4559`**) 실측이 초판을 반증해 고친 자리다. **그 상자 안에서 트리를 적고,
  초판 문장을 지우지 않고 그대로 인용한다**(무엇이 왜 틀렸는지가 이 ADR 의 값이다).
- **㉱ 리뷰 라운드 2 의 정정** — `[정정 — 리뷰 라운드 2]` 로 표시한 자리(§2-1 역방향 확인 · §4
  한정 ④ · §5 2행 · §6 대안 7)는 **잰 트리 `f4758f0`** 의 값이다. **㉰ 과 같은 규율을 따른다**:
  초판을 지우지 않고 취소선·대괄호로만 고친다. *예외를 늘렸으므로 적어 둔다 — 예외가 늘어난다는
  것은 이 문서가 「한 트리에 못박힌 기록물」에서 멀어진다는 뜻이고, 그 대가는 §Deferred 로 넘기지
  않고 여기서 이름 붙인다.*
- **㉲ 리뷰 라운드 3 의 정정** — `[정정 — 리뷰 라운드 3]` 로 표시한 자리(§1-3 · §2-3 정정 상자
  안의 재측정 블록 · §3 2 · §4 · §5 6행과 교차 확인 표 · §6 대안 7 ② · §Deferred 8)는
  **잰 트리 `3a816c4`** 의 값이다. `qa` 가 20칸을 다시 재고 `EXPLAIN` 을 **그 칸이 관측된 통합
  세션 안에서** 잡았으며(`tests/integration/test_22_row_order_contract.py` docstring 이 정본),
  architect 가 그중 `[ORD]` 재계수를 **자기 포트 55445 의 임시 클러스터**에서 독립 재현하고
  **옛 트리 `08238c9` 를 `git archive` 로 꺼내 같은 플러그인으로 다시 재** 분해했다(§1-3).
  **㉰·㉱ 과 같은 규율을 따른다.** *예외가 셋이 됐다 — 이 문서는 이제 「한 트리에 못박힌
  기록물」이 아니라 **트리를 칸마다 적는 기록물**이고, 그 대가는 여기서 이름 붙인다: 어느 줄이
  어느 트리의 값인지는 그 줄 옆의 표시로만 갈린다(§3-13 첫째 갈래가 요구하는 못박음의 최소형).*
- **㉳ 리뷰 재심사 라운드 4 의 정정** — `[한정 — 리뷰 재심사 라운드 4]`·`[정정 — 리뷰 재심사
  라운드 4]` 로 표시한 자리(**§3 의 축 분리 상자** · **§Deferred 8**)는 **잰 트리 `e3037df`** 의
  것이다. **새 측정은 없다** — 이 라운드는 클러스터를 띄우지 않았고, 고친 것은 **문안과
  포함관계** 둘뿐이다(계획 0012 §M-8-0·§M-8-1). **㉰·㉱·㉲ 와 같은 규율을 따른다.**

**낱말 "오늘"을 쓰지 않는다** — 트리를 붙여도 현재형 낱말이 옆에 있으면 다음 커밋이 그 문장을 낡게
만든다(ADR 0015 §0 ㉯ 가 그 실례다).

---

## 1. Context

### 1-1. 무엇이 물어졌나

ADR 0014 §5-3(`743bcb9`)이 실측을 하나 남겼다: postgres 통합 실행에서 **`ORDER BY` 없이 2행 이상을
돌려준 조회가 677회** 실행되고 **어느 단언도 그 순서를 붙들지 않는다.** 그리고 계획 0009 §후속 17 이
서버 트리의 *"정렬 미지정 42자리"* 를 열어 두었다. ADR 0015 는 §Deferred 3 에서 그 자리를 **열지 않고**
이름만 붙였다 — *"기댐의 다른 얼굴"*.

ADR 0015 §2-1 이 확정한 것은 **축의 부정형**이다: 기댈 것은 **`created_at` 값**이지 물리 배치가 아니다.
남은 물음은 **긍정형**이다 — *그러면 42(→35)자리에서 무엇을 해야 하는가.* 두 답이 다 틀렸다:

- **"전부 붙인다"** — 계획 0009 §후속 17 이 이미 기각했다(*"계약이 없는 자리에 정렬을 붙이면 그 정렬이
  다시 무보호가 된다"*). 그리고 붙이기만 하면 **677 이 678 이 될 뿐이다**(CLAUDE.md §6-2).
- **"아무것도 안 한다"** — 그러면 §후속 16 이 `tests/unit` 을 postgres 로 옮길 때 순서 의존 실패가
  한꺼번에 뜨는데 **판정할 계약이 없다.**

### 1-2. 42 가 아니라 **35** 다 (실측)

계획 0012 §1-a 가 저장소 루트에서 다시 셌다. `select(` 를 **텍스트가 아니라 AST 호출**로 세고,
`order_by` 를 **같은 줄이 아니라 체인 + 함수 범위**로 본다.

| 값 | `5beb954`(계획 0009 트리) | `08238c9`(이 트리) |
|---|---|---|
| 루트 grep `select(` 히트(`services/`+`packages/`) | **63** | **63** |
| AST `select(...)` 호출 | **62** | **62** |
| 즉시 체인 `order_by` | **21** | **21** |
| 함수 범위 안의 `<대상>.order_by(` | **6** | **6** |
| **정렬 미지정** | **35** | **35** |

**63 ↔ 62 의 차이는 `services/progress/persistence.py:223` 의 docstring** 안에 있는 *"이 `select()` 경로는
조용히 틀린다"* 한 건이다. **42 는 줄 단위 휴리스틱의 상한이었고**(계획 0009 자신이 그렇게 적었다)
**두 트리 모두에서 참값은 35 다** — 이 축에서 여덟 사이클 동안 바뀐 자리가 없다.

### 1-3. 무정렬 다중행 실행은 **679** 다 (호출 자리별)

계획 0012 의 탐침(저장소 밖 pytest 플러그인)이 같은 것을 **테이블이 아니라 호출 자리로** 귀속한다.
실행값(`08238c9`, 포트 55438, `tests/integration` **216 passed**):

```
[ORD] select_exec=12131  noorder_exec=11125  noorder_multirow_exec=679
```

**ADR 0014 의 677 과 갈린다** — 그 값은 `743bcb9` 트리·통합 205건의 것이고 이 값은 216건의 것이다.
**그 귀속은 실행으로 태우지 않았다**(계획 0012 §확인하지 않은 것 3).

> **[정정 — 리뷰 라운드 3] 이 사이클이 끝난 트리에서 그 값은 542 다. 그리고 679 → 542 는
> 「분해되지 않는 일치」가 아니라 실측된 분해다** (잰 트리 `3a816c4`, architect 포트 55445 의 임시
> 클러스터, 명령 `pytest -q tests/integration` **222 passed**, 계획 §1-c 와 같은 정의의 저장소 밖
> 플러그인, **N=2 두 실행이 한 자리도 다르지 않다**):
>
> ```
> [ORD] select_exec=12217  noorder_exec=10695  noorder_multirow_exec=542      (3a816c4, 222 passed)
> [ORD] select_exec=12131  noorder_exec=11125  noorder_multirow_exec=679      (08238c9, 216 passed)
> ```
>
> **아래 줄은 옛 트리를 `git archive 08238c9` 로 꺼내 같은 플러그인으로 다시 잰 값이고, 계획 §1-c 의
> 세 수와 한 자리도 다르지 않다** — 그래서 두 값을 자리별로 뺄 수 있다:
>
> | 몫 | `08238c9` | `3a816c4` |
> |---|---|---|
> | 서비스 자리 **열둘**(200·92·81·68·40·35·5·3·2·2·1·1) | **530** | **530** — 한 건도 안 움직였다 |
> | ⓐ 넷(A3 113 · A2 9 · A5 8 · A4 7) | **137** | **0** — `ORDER BY` 가 붙었다 |
> | 운영 코드 프레임이 스택에 없는 실행(테스트 코드 프레임, 자리 아홉) | **12** | **12** |
> | 합 | **679** | **542** |
>
> **그러므로 137 은 우연의 일치가 아니라 ⓐ 넷 그 자체다.** 계획 §1-c 표가 그 12회를 `4` 한 칸으로만
> 실은 것은 그 표의 축이 **(자리 × 테이블)** 이기 때문이고, **표 밖 8회(679−671)는 같은 테스트
> 프레임들의 나머지 칸이다** — 그 표의 서비스 16행 합이 정확히 **667**(= 530+137)이라는 것이 그것을
> 산술로 닫는다. *반증한 것*: `qa` 는 *"테스트 코드 프레임이 4 → 12(+8) 로 늘어 두 몫이 상쇄됐다"*
> 라고 적었는데(커밋 `3a816c4` 본문), **옛 트리에서 그 값은 이미 12 였다** — 늘어난 것이 아니라
> 계획의 표가 4만 실었다. 옛 트리를 재지 않고 쓴 문장이다.
> *남는 칸*: 계획 §확인하지 않은 것 3(**677 ↔ 679**)은 **여전히 열려 있다** — 여기서 재현한 것은
> `08238c9` 이지 `743bcb9` 가 아니다. **다만 그 항목은 이제 싸다**: `git archive <트리> | tar -x` +
> 같은 플러그인이 옛 트리의 값을 **그대로** 낸다는 것이 이 표의 아랫줄이다.

---

## 2. Decision

### 2-1. 결정 1 — **순서가 계약인 자리는 「소비자가 다중행 결과에서 한 원소를 위치로 고르는 자리」다**

> ⓐ **`[0]` · `[-1]` · `.first()` · 루프 안의 첫 매치 `return` · `max()`/`find()` 처럼 동률에서 첫
> 원소가 이기는 연산.** → **`ORDER BY` 를 명시한다.**
> ⓑ **집합·사전·합계·계수로만 쓰거나 호출자가 다시 정렬한다.** → **정렬을 붙이지 않는다.**
> ⓒ **한 원소를 고르는데 「행이 하나뿐이다」가 스키마가 아니라 산문 불변식에 걸려 있다.**
> → **`ORDER BY` 는 답이 아니다**(§2-4).

`08238c9` 에서 **ⓐ 5 · ⓑ 24 · ⓒ 6**. 자리별 전수 목록은 **계획 0012 §1-b 의 35행 표가 정본이고
이 ADR 은 그것을 다시 싣지 않는다** — 같은 표를 두 자리에 두면 갈린다(§6-3 8회차). 다시 세는 명령:

```
$ grep -rn "select(" . --include='*.py' \
    --exclude-dir=.git --exclude-dir=.venv --exclude-dir=node_modules --exclude-dir=__pycache__ \
    --exclude-dir=.mypy_cache --exclude-dir=.pytest_cache \
    --exclude-dir=android --exclude-dir=app --exclude-dir=artifact --exclude-dir=components \
    --exclude-dir=lib --exclude-dir=public | grep -v '/design_handoff_'
```
그 뒤 **AST 로** 호출을 가르고 `order_by` 를 체인 + **함수 범위**에서 찾는다(파일 전역 정규식은
`stmt` 같은 흔한 이름 때문에 **과대 계상한다** — 계획 0012 §1-a 상자가 6 을 12 로 부풀린 실측을 싣는다).

*역방향 확인 — 이 결정이 금지하지 않는 것.* **표시 순서가 흔들리는 것**은 이 결정 밖이다. 목록을
화면에 그리는 순서가 실행마다 달라지는 것은 보이지만 **CM 이 고르는 다음 행동을 바꾸지 않는다**
(CLAUDE.md §6-4 가 지키는 것은 후자다). 실행값으로 가른 예: `apps/web/src/pages/SummaryPage.tsx:145`
가 `b.related_ids.map((id, i) => …)` 로 **인덱스를 React key 로만** 쓴다 → ⓑ.
**이것이 이 결정의 가장 큰 임의성이고, 그 대가는 §Deferred 2 다.**

*역방향 확인 — "다중행"이 미는 것.* 인자가 **한 개 키**여서 구조적으로 최대 1행인 조회는 `[0]` 을
읽어도 ⓑ 다. 실행값: `services/progress/verification.py:92` 가 `obj[0].quantity` 를 읽지만 그 `obj` 는
`db.load_objects_by_ids(session, project_id, [global_id])` 이고 `BimObjectRow` 의 PK 는
`(project_id, global_id)` 다(`grep -n "class BimObjectRow" -A 4 packages/core/models/orm.py`).
**[정정 — 리뷰 라운드 2] 이 한정어를 서버 자리에만 걸었고, 그래서 클라이언트에서 오판이 났다.**
「구조적으로 최대 1행」은 **인자가 한 개 키일 때만** 성립하는 것이 아니다 — **가드가 그것을
강제하는 자리**도 같다. 실례: `apps/web/src/sync/broker.ts:76` `swallow(viewer3d.flyTo(globalIds[0]))`
는 `pushTo3d` 의 `single` 가드 안이고(`:69-77`, `globalIds.length === 0` 은 `:71` 에서 조기 return),
`single = globalIds.length === 1 || (globalIds.length === 0 && entityHandles.length === 1)`(`:83`) 이다 —
**`flyTo` 가 도는 순간 길이는 언제나 1이므로 `[0]` 은 유일 원소다 → ⓑ.** 계획 0012 §1-d A5 칸이
이 자리를 ⓐ 로 실었다가 정정했다(그 계획 §M-3-3). **같은 파일의 `:66` `panTo(handles[0])` 는
반대다** — `handlesFor(gid)` 는 `setMappings` 가 서버 순서대로 쌓은 배열이고(`:116-127`)
`select3d`/`selectFromPanel` 에서 `single=true` 인 채 **≥2 일 수 있다** → ⓐ.
**판정 축은 「인자가 한 개 키인가」가 아니라 「그 줄이 도는 순간 결과가 몇 행인가」다.**
→ 나머지 자리를 이 축에서 다시 보는 것은 계획 0012 §후속 49.

### 2-2. 결정 2 — **정렬 키에는 두 종류가 있다: 「의미의 축」과 「결정성의 축」**

ADR 0015 §2-1 은 *"기구의 축은 `created_at` 값이다"* 라고 적었다. **그 문장은 「가장 최근」을 고르는
자리에 대한 것이고, 모든 ⓐ 가 그런 자리는 아니다.** 이 ADR 이 둘로 나눈다.

| | **의미의 축** | **결정성의 축** |
|---|---|---|
| 소비자가 묻는 것 | *"가장 최근/가장 오래된 것"* | *"매번 같은 것"* |
| 키 | **`created_at`**(또는 도메인 시간 컬럼) | **PK 또는 PK 접두사** |
| 실례(이 트리) | `find_document_mapping_review`(`persistence.py:464` `created_at.desc()`) · `latest_model`(`queries.py:35` `version.desc()`) | `project_objects`(`queries.py:46` `global_id`) · `load_documents`(`persistence.py:321` `doc_id`) |
| 키가 없으면 | **컬럼을 만들어야 한다**(스키마 변경) | **만들지 않는다** — PK 는 언제나 있다 |

**실행값이 이 구별을 필요하게 만들었다.** `DrawingRow` 에는 시간 컬럼이 **없다** — 그 클래스의 컬럼은
`drawing_id`(PK)·`project_id`·`file_id`·`level`·`coordinate_system`·`alignment`·`svg_uri`·`stats` 뿐이다
(`grep -n "class DrawingRow" -A 9 packages/core/models/orm.py`). 그런데 그 목록의 `[0]` 을 읽는
소비자(`apps/web/src/pages/ViewerPage.tsx:41`)가 묻는 것은 *"가장 최근 도면"* 이 아니다 —
의미 있는 선택은 그 **앞줄**이 한다(`list.find((d) => d.level === level)`). `[0]` 은 **폴백**이고
폴백에 필요한 것은 결정성뿐이다. **그래서 `created_at` 컬럼을 만들지 않는다.**

*역방향 확인 — 이 구별이 미는 것.* *"결정성만 필요하다"* 는 판정은 **소비자가 지금 무엇을 묻는가**에
달려 있다. 소비자가 나중에 *"가장 최근 도면을 기본으로 열자"* 로 바뀌면 **키가 틀린 것이 되고, 그때
`ORDER BY drawing_id` 는 조용히 틀린 답을 준다**(문자열 정렬이 시간 순서가 아니다). 이 위험은
**없앨 수 없고 이름만 붙일 수 있다** — 그래서 결정 3 이 회귀를 요구한다: 소비자가 바뀌면 그 회귀가
**깨지는 자리에서** 이 판정을 다시 보게 된다.

*역방향 확인 — ADR 0015 를 뒤집는가.* **아니다.** 0015 가 금지한 것은 **물리 배치**(`ctid`·"스캔 맨 뒤")에
기대는 것이고, PK 정렬은 물리 배치가 아니라 **값**이다. 0015 §2-1 의 *"`created_at` 값이지 물리 배치가
아니다"* 에서 이 ADR 이 넓히는 것은 **왼쪽 항**(어떤 값인가)이지 오른쪽 항이 아니다.

### 2-3. 결정 3 — **`ORDER BY` 는 그것을 지우면 죽는 회귀와 같은 사이클에 온다**

**정렬을 붙이는 커밋 하나로는 완료가 아니다.** 근거는 이 사이클의 실측이다 — 계획 0012 가 ⓐ 다섯
자리에 **역방향 정렬을 주입하고** 스위트를 태웠다:

| 변이 | 실행값 | 기준선 |
|---|---|---|
| A1~A5 각각 단독(자리당 N=1) | **832 passed**, `FAILED` 0 | 832 |
| 다섯 동시 | **832 passed**, `FAILED` 0 | 832 |
| 다섯 동시 · postgres 축(`tests/integration`) | **216 passed**, `FAILED` 0 | 216 |

**다섯 자리의 순서를 전부 뒤집어도 죽는 것이 없다.** 그러므로 정렬만 붙이면 `[ORD]` 가 679 → 680 이
될 뿐이다. **요구되는 산출물은 정렬 다섯 줄이 아니라 「그 다섯 줄을 지우면 죽는 회귀 다섯」이다.**

*역방향 확인 — 왜 「지움」과 「역방향」 둘 다인가.* 계획 0008 §과제 1 이 잰 대로 sqlite 의 스캔 순서는
삽입 순서이므로, **삭제 변이가 우연히 옳은 답을 낼 수 있다.** `services/progress/persistence.py:441` 이
그 성질을 그 자리에 적는다(*"SQLite 에서 관측되는 것은 삽입 순서다"*). **그래서 삭제 변이는 postgres
축에서도 태운다**(계획 0012 작업 7 완료 조건 2).
**이 문단의 처방은 방향이 반쪽이었다 — 아래 정정 상자가 값으로 고친다**(postgres 가 관대한 쪽인
자리가 있다).

> ### 정정 — **"삭제 변이가 안 죽으면 그 회귀는 장식이다"는 거짓이다** (2026-09-07, 잰 트리 `cac4559`)
>
> **초판의 요구를 그대로 인용한다**(지우지 않는다): 계획 0012 작업 7 완료 조건 2 —
> *"각 회귀에 대해 역방향 변이와 삭제 변이 **둘 다** 태우고 … **삭제 변이가 안 죽으면 그 회귀는
> 장식이다**(§6-2)"*, 그리고 §3 2 초판 — *"그 `ORDER BY` 를 **지우거나 뒤집으면** 빨개지는 테스트가
> 있고"*.
>
> **그 문장은 서로 다른 둘을 하나로 묶었다.**
> ① **회귀가 그 원소를 보지 않는다** — 테스트의 결함.
> ② **엔진의 기본 계획이 그 정렬과 같은 순서를 낸다** — 엔진의 성질.
> **①만 회귀를 장식으로 만든다. ②는 회귀에 대해 아무것도 말하지 않는다.**
>
> **계약을 재는 변이는 「역방향」이다.** 역방향은 엔진이 어떤 계획을 고르든 **다른 답을 강제**하므로,
> 그것이 안 죽으면 회귀가 그 원소를 안 보는 것이다. → **역방향 변이가 안 죽으면 그 회귀는 장식이다.**
> **「삭제」 변이는 테스트가 아니라 엔진을 잰다.** 그 사망률은 *"이 엔진·이 계획·이 행 수에서
> 무정렬 결과가 정렬 결과와 얼마나 자주 다른가"* 의 추정치이고, **회귀의 품질 척도가 아니다.**
> 그리고 **안 죽는 것은 정렬을 지울 근거가 아니라 정렬이 있어야 하는 근거다** — 그 "같다"는
> **세션마다**(ADR 0015 §2-1: 같은 문장이 세션마다 다른 btree 를 고르고 비용이 동률이다) 그리고
> **행 수마다**(ADR 0014 §4-1 `[PG-bulk-plan]`: 200행에서 Index Scan → Seq Scan) 깨진다.
>
> **삭제 변이를 없애지 않는다. 요구를 바꾼다.**
> - 죽으면 그 실행값을 적는다(그대로).
> - **안 죽으면 「왜 안 죽는지」를 `EXPLAIN`(sqlite 는 `EXPLAIN QUERY PLAN`)으로 이름 붙여 적는다.**
>   이름 붙이지 못하면 그 자리는 **재지 않은 것**이고 닫히지 않는다.
>   **[정정 — 리뷰 라운드 3] 「이름」의 두 한정이 빠져 있었고 실측이 둘 다 반증했다** — 그 이름은
>   ① 그 칸이 관측된 **통합 세션 안에서**, ② **칸마다 하나가 아니라 계획마다 하나**여야 한다
>   (아래 재측정 블록). 저장소 밖 2~3행 탐침이 붙인 이름은 두 칸 다 세션의 결과를 **틀리게
>   예측했고**, 「안 죽는 칸」 안에도 **죽는 계획이 섞여 있다** — 칸을 한 이름으로 부르면 그 섞임이
>   사라진다. 초판이 정확히 그렇게 했다.
> - **면제는 「PK 접두사 키」라는 *이름*이 주지 않는다. 주는 것은 *"그 계획에서 무정렬 결과가 정렬
>   결과와 같다"* 는 실행값이다.** 이름으로 면제하면 **픽스처가 망가진 자리까지 같은 이름으로
>   빠져나간다** — CLAUDE.md §6-3 이 열 번 이름 붙인 모양이다.
>
> **이 정정을 강제한 실측 — 20칸**(qa `cac4559` 커밋 본문, 자리·축·변이마다 pytest 세션 단위).
> **N 의 단위는 「pytest 세션」**이다(세션마다 새 스키마·새 커넥션 — ADR 0015 §2-1 이 인덱스 선택이
> 갈리는 것을 잰 그 단위). 두 부정 단정은 **N=10**, 나머지 열여덟 칸은 N=1.
>
> | 자리 | 역방향 · sqlite | 역방향 · pg | 삭제 · sqlite | 삭제 · pg |
> |---|---|---|---|---|
> | A1 | 1 failed | 1 failed | 1 failed | 1 failed |
> | A2 | 1 failed | 1 failed | 1 failed | 1 failed |
> | **A3** | 2 failed | 2 failed | 2 failed | **죽은 세션 5/10** |
> | A4 | 1 failed | 1 failed | 1 failed | 1 failed |
> | **A5** | 1 failed | 1 failed | **죽은 세션 0/10** | 1 failed |
>
> **architect 독립 재현**(같은 트리 `cac4559`, **내 포트 55442** · 다른 클러스터, 명령
> `pytest -q tests/integration`): 네 칸을 다시 쟀다 —
> **A5 삭제·sqlite `0/5`** · **A5 삭제·postgres `2/5`** · **A3 삭제·postgres `4/10`** ·
> **A3 삭제·sqlite `2/2`**(전부 죽음). qa 의 두 부정 단정(0/10 · 5/10)은 재현되고,
> **qa 가 N=1 로 `1 failed` 라 적은 A5 삭제·postgres 칸은 사실 세션 의존이다**(2/5).
> **그래서 삭제·postgres 두 칸이 모두 세션마다 갈린다** — 이 표에서 *"1 failed"* 로 적힌 칸은
> N=1 의 값이고 안정된 값이 아니다.
>
> **결정적인 칸은 A5 의 삭제 변이 둘이다.** *같은 테스트·같은 픽스처·같은 변이인데* **sqlite 에서는
> 안 죽고 postgres 에서는 죽는다.** 갈리는 변수는 **엔진**이지 회귀가 아니다 — 그러므로 그 칸은
> 회귀에 대해 아무것도 말하지 못한다. **A3 은 같은 것을 한 엔진 안에서 보인다**: postgres 세션마다
> 갈린다. `qa` 가 기전에 이름을 붙였다(저장소 밖 탐침, 세션 12회) — A3 무정렬은
> `Index Scan using activity_object_mappings_pkey` **12/12** 로 첫 행이 정답과 같고, A5 무정렬은
> postgres 에서 `Bitmap Heap Scan` **12/12**(삽입 순서 → 죽는다)인데 SQLite 는
> `SEARCH … USING INDEX sqlite_autoindex_entity_object_mappings_1 (drawing_id=?)` 라 첫 행이 정답과
> 같다. **붙인 키가 「필터를 뺀 PK 나머지」= 결정 2 의 결정성의 축이라서** 그 계획에서는 무정렬이
> 공짜로 그 순서를 낸다.
>
> > #### [정정 — 리뷰 라운드 3] 위 20칸과 위 이름을 **다시 쟀다**. 표도 이름도 바뀐다 (잰 트리 `3a816c4`)
> >
> > **정본은 `tests/integration/test_22_row_order_contract.py` 의 docstring 이다**(소유 `qa`) —
> > 이 ADR 은 판정에 필요한 값만 옮기고 표를 두 번 싣지 않는다(§6-3 8회차). 아래 수는 architect 가
> > 그 docstring 에서 직접 읽어 확인한 것이다.
> >
> > **① 20칸 재측정**(칸마다 N 이 붙는다. 명령은 **두 축 모두** `pytest -q tests/integration`,
> > 기준선 **222 passed** — **초판 표의 sqlite 칸은 루트 `pytest -q` 였으므로 같은 명령이 아니다**):
> >
> > | 자리 | 역방향·sqlite | 역방향·pg | 삭제·sqlite | 삭제·pg |
> > |---|---|---|---|---|
> > | A1·A2·A4 | 4/4 | 4/4 | 4/4 | 4/4 |
> > | **A3** | 4/4 | 4/4 | **10/10** | **세션 의존 6/10** |
> > | **A5** | 4/4 | 4/4 | **0/10** | **세션 의존 2/20** |
> >
> > **역방향 열 칸은 전부 죽는다. 삭제 열 칸은 셋으로 갈린다** — 결정적으로 죽는 것 **일곱**
> > (A1·A2·A4 두 축 여섯 **+ A3·sqlite**), 세션 의존 **둘**, 안 죽는 것 **하나**(A5·sqlite).
> > **초판 표의 "18 + 2"가 거짓인 이유는 둘이다**: ① A5·pg 를 **N=1** 값으로 「죽는다」에 넣었다
> > ② **「죽는다 / 안 죽는다」라는 이분법 자체가 세션 의존 칸을 담지 못한다.** 위 초판 표에서
> > `1 failed`·`2 failed` 로 적힌 칸은 **N=1 의 값이다.**
> >
> > **② 이름은 저장소 밖 탐침이 붙일 수 없다 — 그 탐침이 세션을 틀리게 예측한다:**
> >
> > | 자리·축 | 저장소 밖 탐침(2~3행, 세션 12회) | 그 이름의 예측 | 통합 세션 안 실측 |
> > |---|---|---|---|
> > | A3·pg | `Index Scan … _pkey` **12/12** | *"안 죽는다"* | pkey **4/10** · `ix_…_project_id` **6/10** |
> > | A5·pg | `Bitmap Heap Scan` **12/12** | *"언제나 죽는다"* | pkey **18/20** · `Bitmap Heap Scan` **2/20** |
> >
> > **③ 세션 안 계획이 그 세션의 결과를 완전히 예측한다 — 38 세션 38/38, 「같은 계획인데 결과가
> > 갈린」 세션은 0 이다.** 무정렬 첫 행까지 같은 커넥션에서 잡았다: A3·pg 는 pkey → `ORD-A100`
> > (정답, **산다** 4세션) ↔ `ix_…_project_id` → `ORD-A900`(**죽는다** 6세션); A5·pg 는 pkey →
> > `H-aaa/ORD-OBJ-A5`(정답, **산다** 18세션) ↔ `Bitmap Heap Scan` → `H-zzz/…`(**죽는다** 2세션);
> > A3·sqlite 는 `SEARCH … ix_activity_object_mappings_project_id` 로 **죽고**(4세션), A5·sqlite 는
> > `SEARCH … sqlite_autoindex_entity_object_mappings_1` 로 **산다**(4세션).
> >
> > **그래서 이 게이트는 「세션 안에서 재는 한」 작동하고 「이름으로 면제」와 갈린다** — 위 요구의
> > *이름* 은 **계획마다 하나**다. 리뷰어가 지적한 투영 문제(2컬럼으로 쏘면 A3·sqlite 가
> > `COVERING INDEX` 라 반대를 예측한다)는 이 형태에는 **없다** — 계측이 **ORM 이 실제로 쏘는 문장
> > 그대로**를 `EXPLAIN` 에 넘긴다.
> > **재지 않은 것 둘**: ⓐ **무엇이 세션마다 계획을 뒤집는가**(행 수는 세션마다 같다 — 통계·
> > autovacuum 타이밍을 의심할 뿐 태우지 않았다. 관측 하나: 두 병렬 스트림에서 **같은 회차 번호**의
> > 세션이 함께 죽었다) → **계획 0012 §후속 52.** ⓑ **정렬이 있을 때의 계획**(위는 전부 무정렬 쪽)
> > → §후속 43.
> > **계측의 자리**: 이 값을 낸 pytest 플러그인은 **저장소 밖이고 커밋되지 않았다** — 그러면 이
> > 게이트는 다음 사이클에서 **재현 불가**다. 처분은 **계획 0012 §후속 51**(둔다, 배정과 조건 포함).
>
>
> *역방향 확인 — 이 정정이 여는 구멍.* *"안 죽는 이유를 EXPLAIN 으로 적으면 된다"* 는 **면죄부로
> 쓰일 수 있다**: 행을 하나만 넣은 픽스처도 무정렬과 정렬이 같은 답을 내고 `EXPLAIN` 은 그것을
> 설명한다. **그래서 순서가 있다 — 역방향 변이가 죽는 것이 먼저다**(그것이 ≥2행과 소비자 관측을
> 동시에 증명한다). `EXPLAIN` 은 그 다음이고, 역방향이 죽지 않는 자리에서는 **아무것도 면제하지
> 않는다.**
>
> *역방향 확인 — 결정 2 를 바꾸지 않는 이유.* §6 대안 7·8 에 값으로 적었다.
>
> *이 실패의 모양.* **반박은 이 결정 자신의 역방향 확인에 이미 있었다** — 바로 위 문단이
> *"삭제 변이가 우연히 옳은 답을 낼 수 있다"* 라고 적어 놓고, 같은 ADR 의 §3 2 와 계획의 완료 조건 2
> 는 *"지우거나 뒤집으면 빨개져야 한다"* 를 **한정어 없이** 요구했다. CLAUDE.md §6-3 이 이름 붙인
> *"근거는 문서 안에 다 있었고 결론이 근거를 따라가지 못했다"* 가 **한 결정과 그 결정의 역방향 확인
> 사이에서** 난 것이고, **이 사이클이 §6-3 에 더한 11회차 규칙**(같은 사이클의 문서·코드에 두 번 이상
> 적은 값을 표로 모아 태운다)**을 이 값에 걸었으면 커밋 전에 잡혔다.** 잡은 것은 `qa` 다.

*역방향 확인 — 이 결정이 만드는 비용.* ⓐ 자리마다 **≥2행을 만드는 픽스처**가 필요하다. 그것을 만들 수
없는 자리가 있다 — 계획 0012 §열린 질문 1 이 A4(`matcher.py:158` 의 부동소수 동률)를 그렇게 이름
붙였다. **만들지 못하면 그 자리는 닫지 않고 되돌린다**(작업 7 완료 조건 4) — *"붙였으니 됐다"* 로
닫는 것이 이 결정이 막는 바로 그것이다.

### 2-4. 결정 4 — **ⓒ 에 `ORDER BY` 를 붙여 덮지 않는다**

ⓒ 여섯은 전부 한 모양이다: **스키마가 유일성을 강제하지 않는데 코드가 유일하다고 믿는다.**
`.first()` 나 첫 매치 `return` 에 정렬을 붙이면 **결정적으로 고르게 되지만, 두 행이 있다는 사실 자체가
가려진다** — 이 저장소의 지배적 실패 모드(*"조용히 죽는 것"*)를 정확히 한 겹 더 두르는 일이다.

**답은 셋 중 하나이고 자리마다 다르다:** ① **유일 제약을 세운다** ② **둘 이상이면 그 자리에서
죽인다**(ADR 0015 결정 2 와 같은 판단) ③ **정렬을 붙여 결정적으로 고른다 — 다만 그것이 도메인적으로
옳을 때만.** **이 ADR 은 그 선택을 하지 않고 §Deferred 1 로 넘긴다.**

가장 급한 자리를 이름으로 적는다: **`services/api/auth/router.py:23`** 이 `func.lower(UserRow.email)` 로
조회하는데 유일 제약은 **원문 `email`** 에 걸려 있다(`email: Mapped[str] = mapped_column(String, unique=True)`).
대소문자만 다른 두 행을 스키마가 막지 않고, 막는 것은 `register`(`:37` `email = str(body.email).lower()`)
뿐이다. **잘못 고르면 다른 계정으로 인증된다.**

---

## 3. 구현은 소유자의 판단이다 — 요구되는 것은 완료 조건

이 ADR 은 **키와 회귀의 계약**만 정한다. 어느 줄에 어떻게 쓸지는 소유자(`api`·`progress-engine`·
`sync-2d3d`·`qa`)의 판단이고, 완료 조건 표는 **계획 0012 §작업 분배** 아래 두 상자에 있다
(작업 3·4·5 다섯 칸 / 작업 7 여섯 칸). **여기에 다시 적지 않는다** — 같은 조건을 두 자리에 두면 갈린다.

> **[한정 — 리뷰 재심사 라운드 4] 「정본이 둘」이 아니다 — 이 문단이 넘기는 것과 아래 셋은 다른
> 종류의 문장이다.** 라운드 3 이 아래 **2** 만 좁히고 계획 0012 **작업 7 완료 조건 2**·**§M-2-4 ②**
> 를 초판 문안 그대로 두어, **이 문단 자신이 경고한 갈림이 실제로 났다.** 그래서 축을 갈라 각각
> 한 자리에 못박는다(CLAUDE.md **§2** 의 소유 축 · **§3-13** 의 `file:line` 축을 한 줄로 모은 것과
> 같은 판단이고, 판정의 근거는 계획 0012 **§M-8-1** 에 값으로 있다).
>
> - **완료 조건 표** — *누가 무엇을 **몇 칸** 만족해야 하는가*(작업 3·4·5 다섯 칸 / 작업 7 여섯 칸,
>   소유·개수·커밋 본문에 적을 것). **이 사이클의 배정**이고 사이클과 함께 닫힌다.
>   **정본은 계획 0012 이고, 이 ADR 은 여기에 다시 적지 않는다**(위 문단 그대로).
> - **계약 조항** — *한 자리가 **무엇으로** 닫히는가*(아래 1·2·3 의 문안). **사이클보다 오래
>   산다** — §Deferred 1(ⓒ 여섯 = 계획 0012 §후속 40)이 **같은 조항을 물려받고**, 그때 계획 0012 는
>   닫힌 문서다. **정본은 이 절이다.** 계획은 그 문안을 **그대로 싣고**, 고칠 때는 **여기를 먼저
>   고친다. 두 문안이 갈리면 이 절이 이긴다.**
>
> *역방향 확인 — 「그대로 싣는다」가 미는 것.* 복사는 갈림의 원인이고 이번에 난 것이 그것이다.
> 그런데 **가리킴만 두면 실행하는 소유자가 게이트를 열지 않고 지나간다** — 완료 조건은 작업하는
> 사람이 **그 자리에서 읽는 목록**이라 링크로 대체되지 않는다. 그리고 CLAUDE.md §6-1 이 금지하는
> 것은 *"다른 절을 가리켜 **확인**을 갈음하는 것"* 이지 *"규칙의 **정본**을 한 자리에 두는 것"* 이
> 아니다(§2·§3-13 이 이미 후자를 택했다). **그래서 싣되 정본을 이름으로 못박는다.**
> *대가도 적는다*: **두 문안의 일치를 어떤 기계도 보지 않는다.** 이번 반려가 그 실례이고,
> **닫지 않고 한계로 적는다**(계획 0012 §후속 54 의 역방향 확인 — 소유가 `architect` 이고 대상이
> 산문이라 `qa` 항목에 넣지 않았다).

이 ADR 이 계약으로 붙드는 것은 셋뿐이다:

1. **ⓐ 로 판정한 자리에는 `ORDER BY` 가 있다.**
2. **그 `ORDER BY` 를 「뒤집으면」 빨개지는 테스트가 있고, 그 이름과 개수가 커밋 본문에 실행값으로
   적혀 있다.** 없으면 그 자리는 **닫히지 않았다.** 「지우면」도 함께 태우되, 안 죽는 칸은
   ~~`EXPLAIN` 으로 왜 안 죽는지를 이름 붙여 적는다~~ — **[정정 — 리뷰 라운드 3] 그 칸이 관측된
   통합 세션 안에서, 계획마다 하나씩** `EXPLAIN`(sqlite 는 `EXPLAIN QUERY PLAN`)으로 이름을 붙여
   적는다. 계측은 **ORM 이 실제로 쏘는 문장 그대로**를 그 **세션의 같은 커넥션**에 넘기고, 각 계획을
   **그 세션의 사망 여부와 짝지어** 적는다. **이름 붙이지 못한 칸은 재지 않은 것이다**
   (§2-3 정정 상자와 그 안의 라운드 3 재측정 블록. 초판은 *"지우거나 뒤집으면"* 을 한정어 없이
   요구했고, 그 정정이 새로 쓴 *"`EXPLAIN` 으로 이름 붙인다"* 도 **두 한정이 빠져 실측에 반증됐다**:
   저장소 밖 탐침의 이름은 두 칸 다 세션을 틀리게 예측했고, **한 칸 안에 계획이 둘 이상 섞인다**).
   **한 칸을 한 이름으로 부르는 것은 이 조항이 금지하는 「이름으로 면제」다.**
3. **ⓑ 로 판정한 자리에는 `ORDER BY` 를 붙이지 않는다.** 붙이려면 그 자리가 ⓐ 가 된 근거
   (새 소비자의 코드 인용)를 함께 낸다.

---

## 4. Consequences

- **`[ORD]` 의 679 는 이 사이클로 크게 줄지 않는다.** ⓐ 다섯 중 postgres 통합에서 다중행으로 관측된
  것은 넷이고 그 합은 **113 + 9 + 8 + 7 = 137** 이다(계획 0012 §1-b 표의 `실행` 칸). 나머지 542 는
  ⓑ·ⓒ 이거나 테스트 코드 프레임이다. **줄어드는 수가 이 사이클의 성과가 아니다** — 성과는
  **679 중 무엇이 계약이고 무엇이 아닌지 갈렸다**는 것이다.
  **[정정 — 리뷰 라운드 3] 이 줄은 예측이었고, 재계수가 그것을 태웠다 — 값은 정확히 542 다**
  (`3a816c4`, N=2). 두 트리를 자리별로 뺀 표는 §1-3 의 라운드 3 상자에 있다: 서비스 자리 열둘
  **530 은 한 건도 안 움직였고**, 테스트 코드 프레임은 **두 트리 모두 12**, 차이 137 은 **ⓐ 넷
  그 자체**다. **그러므로 이 줄에서 「예측」이었던 것은 이제 「실측」이고, 「크게 줄지 않는다」는
  20%(137/679)를 뜻한다** — 남은 542 중 **530 이 ⓑ·ⓒ, 12 가 테스트 코드 프레임**이라는 분류도
  자리별로 확인됐다.
- **§후속 16(`tests/unit` → postgres)이 이 판정의 반증 장치가 된다.** 수백 건이 새 축으로 갈 때
  ⓑ 로 판정한 24 자리에서 실패가 나면 **이 ADR 의 판정이 틀린 것**이고, 계획 0012 §1-b 표가
  반증 가능한 형태로 남아 있다.
- **스키마는 바뀌지 않는다.** 새 컬럼도 새 인덱스도 만들지 않는다(결정 2).
- **정렬을 붙이면 플래너의 선택이 바뀔 수 있다.** ADR 0015 §2-1 이 배역에서 두 btree 의 **비용 동률**과
  세션마다 갈리는 선택을 쟀다. 정렬 뒤 그 동률이 어떻게 되는지 **이 사이클은 재지 않았다**
  (계획 0012 §후속 43).
- **ⓐ 다섯이 닫힌다 — 새 게이트(역방향 변이)를 다섯 자리가 두 축에서 모두 만족한다**(§2-3 정정 표의
  왼쪽 두 열: 스물 칸 중 열 칸이 전부 죽는다). ~~**닫힘의 한정 셋**~~ **닫힘의 한정 넷
  [정정 — 리뷰 라운드 2: ④ 를 더한다]**: ① A3·A5 의 **삭제 변이 두 칸은
  「죽지 않음」이 기록됐고 기전이 `EXPLAIN` 으로 이름 붙었다** — 면제가 아니라 **엔진 관측**이다.
  ② 그 관측이 깨지는 조건(행 수·통계·세션)은 **재지 않았다**. ③ **화면 축(판정 경로 ②)은 A5 의
  브로커 하나뿐**이고(`apps/web/src/sync/broker.test.ts`) A1·A5 의 `ViewerPage` 축은 **없다** —
  그 파일이 저장소에 없다. ④ **[정정 — 리뷰 라운드 2] 회귀는 「순서」를 붙들지만 「그 `ORDER BY`
  줄의 존재」는 A3·sqlite 한 축에서만 붙든다.** ①이 *"죽지 않음이 기록됐다"* 까지만 적고 **그
  결과를 이름 붙이지 않았다** — 이름은 이것이다: **A5 의 정렬 줄을 지우는 커밋은 두 축 어디서도
  결정적으로 빨개지지 않고, A3 의 그것은 postgres 에서 세션 의존이다**(A3·sqlite 만 관측된 세
  세션 전부에서 죽는다 — `qa` `2 failed`(N=1) + architect `2/2`). 그러므로 이 다섯 자리의 **정렬
  줄은 회귀가 아니라 이 ADR 의 문서가 지킨다.** 닫는 후보는 §6 대안 3 의 기계 감사이고 계획 0012
  §후속 42·48 에 있다.
  - **[정정 — 리뷰 라운드 3] 네 한정을 재측정에 비추어 다시 판정한다**(잰 트리 `3a816c4`).
    **① 은 통과하지 못했다.** ①이 가리킨 *"이름 붙은 기전"* 은 **저장소 밖 2~3행 탐침의 이름**
    이었고, 그 이름은 **두 칸 다 통합 세션을 틀리게 예측한다**(A3·pg 탐침 `Index Scan … _pkey`
    12/12 → *"안 죽는다"* ↔ 세션 안 6/10 죽음 / A5·pg 탐침 `Bitmap Heap Scan` 12/12 →
    *"언제나 죽는다"* ↔ 세션 안 2/20 죽음). **그러므로 ①이 「엔진 관측」이라 부른 것은 그 칸에서
    실제로 도는 계획의 관측이 아니었다** — 새 기준(§3 2, 라운드 3)으로는 **미측정**이었다.
    **다시 통과시킨 것은 세션 안 측정이고, 통과의 모양이 바뀐다**: 그 칸의 이름은 하나가 아니라
    **둘**이고(사는 계획 · 죽는 계획), **세션 안 계획이 결과를 38/38 예측한다.** 즉 A3·pg 와
    A5·pg 는 *"안 죽는 칸"* 이 아니라 **「대부분의 세션에서 그 순서를 공짜로 내는 계획이 뽑히고,
    나머지 세션에서는 죽는 계획이 뽑히는 칸」** 이다.
    **② 는 반만 통과한다.** **세션 축은 이제 값이 있다** — A3·pg **6/10**, A5·pg **2/20**, 그리고
    계획이 결과를 **완전히 예측**한다(38/38). **행 수·통계 축은 여전히 재지 않았다**(§Deferred 8 ·
    계획 §후속 16). **무엇이 세션마다 그 계획을 뒤집는지도 재지 않았다** → 계획 §후속 **52**.
    **③ 은 그대로다**(화면 축은 브로커 하나 — 계획 §후속 44).
    **④ 는 값이 바뀐다.** A3·sqlite 는 *"관측된 세 세션"* 이 아니라 **재측정 10/10 + 리뷰어 4/4 +
    architect 2/2 + qa 초판 `2 failed`(N=1)** 이고, A5·pg 는 *"결정적으로 안 죽는다"* 가 아니라
    **세션 의존 2/20** 이다. **④ 의 결론(정렬 줄의 존재를 붙드는 축은 A3·sqlite 하나)은 그대로다.**
    **그리고 이 한정들이 기대는 계측이 저장소 밖에 있다** — 그것이 §후속 **51** 이다.
- **삭제 변이의 사망률이 새 관측량이 된다.** *"이 정렬이 얼마나 자주 답을 바꾸는가"* 를 재는 값이고,
  §후속 16 이 `tests/unit` 을 postgres 로 옮겨 행 수를 키울 때 **그 값이 올라가야 한다**(계획이
  바뀌면 우연이 깨진다). 올라가지 않으면 이 ADR 의 기전 설명이 틀린 것이다 — **반증 가능한 형태로
  남긴다.**

---

## 5. 한정어 역방향 확인 표 (CLAUDE.md §6-3 — 각 칸은 **실행값 또는 코드 인용**이고 다른 절 참조가 아니다)

| # | 한정어 | 빼면 무엇이 더 들어오나 / 때문에 무엇이 빠지나 | 태운 값 |
|---|---|---|---|
| 1 | "**소비자가**" (결정 1) | 뺀다 = 무정렬 조회 전부(35). 때문에 빠지는 것: 조회만 보면 같은 모양인데 소비자가 달라 판정이 갈리는 자리 | 코드 인용: `services/progress/persistence.py:409`(ⓒ, 첫 매치 `return`) ↔ `:484`(ⓑ, 호출자가 `sorted`) — **같은 파일·같은 테이블·같은 술어인데 판정이 다르다** |
| 2 | "**다중행**" (결정 1) | 뺀다 = `session.get()` 류 1행 조회 1,418+1,127회가 들어온다. 때문에 빠지는 것: 한 개 키 조회의 `[0]` **[정정 — 리뷰 라운드 2] 그리고 「가드가 1행을 강제하는」 클라이언트 자리** — 이 칸이 서버 실례만 들어서 `broker.ts:76` 이 ⓐ 로 들어왔다 | 실행값: `services/api/deps.py:55` **1,418회 전부 ≤1행** / 코드 인용: `verification.py:92` `obj[0]` 의 인자가 `[global_id]` 하나 / **`apps/web/src/sync/broker.ts:76` 의 `flyTo(globalIds[0])` 는 `single` 가드(`:83`)와 `:71` 의 조기 return 때문에 언제나 `length===1`(→ ⓑ), 같은 파일 `:66` `panTo(handles[0])` 는 ≥2 가능(→ ⓐ)** |
| 3 | "**위치로**" (결정 1) | 뺀다 = 표시 순서가 흔들리는 자리 전부. 때문에 빠지는 것: 사용자에게 보이는 목록 순서 | 코드 인용: `apps/web/src/pages/SummaryPage.tsx:145` `related_ids.map((id, i) => …)` — `i` 는 React key 로만 쓰인다 |
| 4 | "**결정성의 축**" (결정 2) | 뺀다 = 모든 ⓐ 가 `created_at` 을 요구한다 → `DrawingRow` 에 컬럼 추가(스키마 변경). 때문에 빠지는 것: *"가장 최근"* 이 정말 필요한 자리와의 구별 | 코드 인용: `class DrawingRow` 컬럼 여덟에 시간 컬럼 **없음**; `ViewerPage.tsx:41` 의 `[0]` 앞에 `find((d) => d.level === level)` 가 이미 있다 |
| 5 | "**지우면 죽는**" (결정 3) | 뺀다 = 정렬 다섯 줄만으로 닫힌다. 때문에 빠지는 것: 붙였는데 아무도 안 보는 상태 | 실행값: 역방향 변이 다섯 + 동시 + postgres 축 = **전부 초록**(§2-3 표) |
| 6 | "**뒤집으면**"(역방향) vs "**지우면**"(삭제) — **초판은 둘을 같은 요구로 묶었다. 실측이 갈랐다**(§2-3 정정) | 뺀다(=둘을 묶는다) = 엔진이 관대한 자리에서 **옳은 회귀가 「장식」으로 오판된다**. 때문에 빠지는 것(=역방향만 재면) = *"이 정렬이 실제로 얼마나 자주 필요한가"* 라는 엔진 관측 | 실행값: ~~**A5 삭제 변이가 sqlite 0/10 · postgres 1 failed**~~ **[정정 — 리뷰 라운드 3, `3a816c4`] A5 삭제 변이는 sqlite 0/10 · postgres 세션 의존 2/20** — `1 failed` 는 N=1 값이었다. 같은 테스트·같은 픽스처인데 엔진이 갈리는 것은 그대로다. **A3 삭제 변이는 postgres 안에서 세션마다 갈리고**(~~5/10~~ **재측정 6/10**) **sqlite 에서는 결정적으로 죽는다**(**10/10** — 초판은 이 칸도 N=1 이었다). 초판이 *"postgres 축에서도 태운다"* 로 처방한 것은 **postgres 가 엄격한 쪽이라는 전제**였는데 A3 이 그 전제를 반증한다 |
| 7 | "**스키마가 강제하지 않는데**" (결정 4) | 뺀다 = `.first()` 전부가 ⓒ. 때문에 빠지는 것: 복합 PK 가 유일성을 **강제하는** `.first()` | 코드 인용: `EntityObjectMappingRow` PK = `(drawing_id, entity_handle, **global_id**)` — 앞 둘만 거는 `queries.py:105`·`review_queue.py:98` 은 ⓒ 이고, 세 컬럼을 다 거는 조회는 없다 |

### 같은 사이클의 문서·코드와 교차 확인 (계획 0011 §후속 32 를 손으로 이행한다)

| 값 | 이 ADR | 다른 자리 | 갈리는가 |
|---|---|---|---|
| 정렬 미지정 자리 수 | **35** | ADR 0015 §Deferred 3 · 계획 0009 §후속 17: **42** | **갈린다** — 42 는 줄 단위 휴리스틱 상한 |
| 무정렬 다중행 실행 | **679** | ADR 0014 §5-3(`743bcb9`): **677** | **갈린다** — 트리·통합 건수(205 → 216) |
| **[라운드 3 이 더한다] 무정렬 다중행 실행(사이클 뒤)** | §1-3 라운드 3 상자 · §4 1행: **542** | `qa` 커밋 `3a816c4` 본문: **542**(13 자리) · §4 초판의 예측 *"나머지 542"* | **갈리지 않는다.** 세 자리가 같은 값이고, architect 가 자기 클러스터에서 N=2 로 재고 **옛 트리 `08238c9` 를 다시 재 분해**했다(679 = 530 + 137 + 12). **다만 그 분해의 설명은 갈렸다** — `qa` 본문의 *"테스트 코드 프레임 4 → 12(+8) 로 늘어 상쇄"* 는 **반증됐다**(두 트리 모두 12) |
| **[라운드 3 이 더한다] 20칸 사망률** | §2-3 정정 상자의 20칸 표(초판, N=1 칸 포함) → **그 안의 라운드 3 재측정 블록** | `test_22` docstring(**정본**) · 계획 0012 §M-1 표 7행·§M-2-2·§M-3-4·§M-4·§M-5 47·§M-5 48·§M-6 2·§M-6 34 · 커밋 본문 **넷**(`cac4559`(제목 포함) · `94751c5` · `f5044b5` · `3a816c4`) | **갈린다 — 그리고 이 사이클이 그 값을 어느 교차 확인 표에도 넣지 않았다.** 초판 *"18칸이 죽고 두 칸이 안 죽는다"* ↔ 재측정 *"결정적 사망 일곱 · 세션 의존 둘 · 안 죽는 것 하나"*. `1 failed`·`2 failed` 로 적힌 열여덟 칸은 **N=1** 이었다. **커밋 본문은 고칠 수 없다** — 네 커밋의 값은 그 트리의 기록으로 남고, 문서 자리는 이 라운드가 정정한다 |
| **[라운드 3 이 더한다] 무정렬 계획의 이름(`EXPLAIN`)** | §2-3 정정 상자: 저장소 밖 탐침 **12/12** 둘 | 계획 0012 §M-2-2·§M-2-3 · §6 대안 7 ② · `test_22` docstring(세션 안 값) | **갈린다** — 같은 두 칸에 대해 탐침은 `12/12`(한 계획), 통합 세션 안은 **계획이 둘씩 섞인다**(A3·pg 4/10 ↔ 6/10, A5·pg 18/20 ↔ 2/20). **탐침의 이름이 세션의 결과를 틀리게 예측한다** |
| 순서의 축 | `created_at` **또는** PK | ADR 0015 §2-1: *"`created_at` 값"* | **넓힌다**(결정 2). 오른쪽 항(*물리 배치가 아니다*)은 그대로 |
| 이 자리를 여는 주체 | **이 ADR** | ADR 0015 §Deferred 3: *"이 ADR 은 그 축을 열지 않는다"* | 갈리지 않는다 — 0015 가 넘긴 것을 여기서 받는다 |
| ⓐ 다섯의 커밋 수 | **셋**(`api`·`progress-engine`·`sync-2d3d`) | 계획 0009 §후속 17 의 배정: *"architect + progress-engine + api"* | **갈린다** — `services/sync` 가 배정에 없었다(A5) |

---

## 6. Alternatives (기각한 것들)

| # | 대안 | 기각 근거(실행값 또는 코드 인용) |
|---|---|---|
| 1 | **35 자리 전부에 `ORDER BY` 를 붙인다** | 계획 0009 §후속 17 이 이미 기각했다(*"계약이 없는 자리에 정렬을 붙이면 그 정렬이 다시 무보호가 된다"*). 실측이 그 말을 잇는다: ⓐ 다섯에 역방향을 넣어도 **832 passed** — 회귀 없이 붙이는 것은 무보호를 35개 만드는 일이다 |
| 2 | **자리마다 *"순서 무관"* 주석을 단다** | `services/` 는 계속 편집되는 자리라 주석이 소비자 변경에 **조용히 낡는다**(CLAUDE.md §3-13 둘째 갈래). 그리고 **열거는 길이가 곧 개수다**(§6-1 9회차) — 25번째 자리가 생겨도 24개 주석 중 어느 것도 거짓이 되지 않는다 |
| 3 | **`make lint` 로 기계 감사를 만든다**(무정렬 조회 + `[0]` 소비자 = 실패) | **정적으로 판정되지 않는다** — 소비자가 다른 파일·다른 언어에 있다(A1 의 소비자는 `apps/web/src/pages/ViewerPage.tsx` 다). 기각이 아니라 **미측정**이므로 계획 0012 §후속 42 로 연다 |
| 4 | **`DrawingRow` 에 `created_at` 을 추가한다** | 소비자가 묻는 것이 *"가장 최근"* 이 아니다 — `ViewerPage.tsx:41` 에서 `[0]` 은 `find(… level)` 뒤의 **폴백**이다. 스키마를 바꿔 답할 물음이 아니다(결정 2) |
| 5 | **소비자를 고친다**(`[0]` 을 없앤다) | 다섯 중 셋의 소비자가 `apps/web`(소유 `frontend`)이고, `[0]` 자체는 **폴백으로서 옳다**. 고칠 것은 그 앞의 순서다. 그리고 소비자를 고쳐도 **서버가 여전히 임의 순서를 준다** — 다음 소비자가 같은 함정에 빠진다 |
| 6 | **ⓒ 여섯에도 `ORDER BY` 를 붙여 함께 닫는다** | 결정 4. 정렬은 **두 행이 있다는 사실을 가린다** — *"조용히 죽는 것"* 을 한 겹 더 두른다. 그리고 여섯의 답이 서로 달라 한 사이클이 아니다(계획 0012 §1-g) |
| 7 | **삭제 변이가 죽도록 결정 2 의 키 규칙을 바꾼다**(PK 나머지가 **아닌** 키를 고른다) | **테스트가 개를 흔든다.** ① 키의 근거가 *"소비자가 무엇을 묻는가"* 에서 *"변이가 죽게 하려고"* 로 바뀐다 — 결정 2 가 금지하는 바로 그 종류의 근거다. ② PK 접두사는 인덱스 순서가 **공짜**인데(qa 실측: A3 무정렬이 `Index Scan using activity_object_mappings_pkey` **12/12**) 다른 키는 플래너에게 **실제 정렬을 시킨다** — 검증 편의를 위해 운영 비용을 낸다. **[정정 — 리뷰 라운드 2] ②는 한 자리에서 일반화됐다 — 「공짜」는 A3 에서만 관측됐다.** 같은 「필터를 뺀 PK 나머지」 키인데 **A5 무정렬은 postgres 에서 `Bitmap Heap Scan` 12/12**(§2-3 정정 상자에 이미 실린 qa 의 값)이고 Bitmap Heap Scan 은 인덱스 순서를 잃으므로, **그 자리에 `ORDER BY` 를 붙이면 플래너가 실제로 Sort 를 한다.** 두 자리는 **키 모양이 같다**(A3 PK `(project_id, activity_id, global_id)`·필터 `project_id`·키 `(activity_id, global_id)` / A5 PK `(drawing_id, entity_handle, global_id)`·필터 `drawing_id`·키 `(entity_handle, global_id)` — `packages/core/models/orm.py`). **모양이 같은데 계획이 다르므로 「PK 접두사」라는 *이름* 은 비용을 예측하지 못한다** — §2-3 정정 상자 ③(*"면제는 이름이 주지 않는다, 주는 것은 실행값이다"*)을 이 근거 자신에 걸지 않았다. **②는 A3 에 한정해서만 쓴다.** **[정정 — 리뷰 라운드 3] A3 에도 한정 없이 쓸 수 없다** — 그 `12/12` 는 저장소 밖 2~3행 탐침의 값이고, **통합 세션 안에서 A3·pg 의 무정렬 계획은 pkey 4/10 · `ix_…_project_id` 6/10 으로 갈린다**(`test_22` docstring, `3a816c4`). 「인덱스 순서가 공짜」는 **그 자리의 성질이 아니라 그 세션에서 뽑힌 계획의 성질**이다. **②는 「A3 의 어떤 계획에서는 공짜다」로만 쓴다.** ~~③ 그 선택은 **무정렬 결과와 정렬 결과가 더 자주 달라지도록** 키를 고르는 것인데, 그 divergence 는 이 ADR 이 줄이려는 위험 자체다~~ **[정정 — 리뷰 라운드 2] ③은 순환이라 근거로 쓰지 않는다** — `ORDER BY` 가 붙은 뒤 그 divergence 는 위험이 아니라 **정렬이 실제로 하는 일의 크기**이고, 그런 키를 피해서 줄어드는 것은 위험이 아니라 **관측 가능성**이다. **기각은 ①만으로 성립한다.** 원문은 지우지 않고 취소선으로 남긴다 |
| 8 | **행을 늘려 플래너가 계획을 바꾸게 만든다**(ADR 0014 §4-1 `[PG-bulk-plan]` 은 200행에서 Index Scan → Seq Scan) | 그러면 그 회귀가 **플래너의 계획에 고정된다** — ADR 0015 결정 1 이 금지한 *"저장 엔진의 배치를 테스트의 전제로 쓰는 것"* 의 다른 얼굴이다. 그리고 그 임계는 통계에 달려 **세션마다 갈린다**(0015 §2-1). `qa` 도 같은 결론에 실행으로 닿았다(*"어떤 픽스처도 그 계획에서는 결함 코드와 옳은 코드를 가르지 못한다"*) |

---

## 7. Deferred — 이 ADR 이 보고 결정하지 않는 것

1. **ⓒ 여섯의 처분**(계획 0012 §후속 40). 유일 제약 / 둘이면 실패 / 결정적 선택 중 무엇인지는 자리마다
   다른 도메인 판단이다. **`auth/router.py:23`(로그인)이 가장 급하다.**
2. **표시 순서.** 결정 1 이 ⓑ 로 놓아 준 자리에서 목록 순서가 실행마다 달라지는 것은 그대로 남는다.
   CM 이 고르는 다음 행동을 바꾸지 않는다는 판정에 **선다** — 그러나 그 판정을 **사용자에게 태워
   확인하지 않았다.**
3. **정렬이 있는데 타이가 남는 자리**(계획 0012 §후속 41). 이 ADR 의 축은 `order_by` 의 **유무**다.
   알려진 자리: `services/progress/persistence.py:458` 이 스스로 *"같은 `created_at` 을 갖는 두 행은
   이 정렬로 갈리지 않는다"* 라고 적는다. **전수는 세지 않았다.**
4. **`tests/` 트리의 무정렬 조회.** 루트 grep 실행값으로 `buildtwin/tests/` 에 `select(` **33 히트**가
   있다. 세는 대상을 운영 코드로 한정했다 — 그 축은 §후속 16 이 연다.
5. **CI 환경에서의 재측정.** 이 문서의 모든 값은 **PostGIS 없는 로컬 16.13** 의 것이다.
   CI 는 `postgis/postgis:16-3.4` 다 — 계획 0009 §후속 24 의 자리.
6. **정렬을 붙인 뒤 플래너가 무엇을 고르는가**(계획 0012 §후속 43). ADR 0015 §Deferred 6 이 연
   비용 동률 문제와 같은 자리다.
7. **파이썬 `dict` 의 삽입 순서에 기대는 자리**(계획 0012 §확인하지 않은 것 32).
   `{r.doc_id: r for r in rows}` 는 같은 키가 둘이면 **뒤에 온 것이 이긴다** — 이 ADR 은 그런 자리를
   ⓑ 로 놓았고, 그 판정은 *"키가 유일하다"* 는 **결정 4 와 같은 종류의 믿음** 위에 서 있다.
8. **삭제 변이가 안 죽는 두 칸이 언제 죽기 시작하는가**(§2-3 정정). 기전은 이름 붙었지만
   **그 우연이 깨지는 임계**(행 수·통계·`ANALYZE`·세션)는 재지 않았다. 재는 자리는 §후속 16 이
   힙을 키울 때이고, 그때 사망률이 오르지 않으면 이 ADR 의 기전 설명이 틀린 것이다.
   **[정정 — 리뷰 라운드 3] 네 축 중 「세션」은 재졌다** — 같은 픽스처·같은 행 수에서 A3·pg 는
   **6/10**, A5·pg 는 **2/20** 이 죽고, **그 세션의 계획이 결과를 38/38 예측한다**. 그러므로
   *"안 죽는 두 칸"* 이라는 이 항목의 제목은 **A5·sqlite 한 칸에만** 맞고, 두 pg 칸은
   *"대부분의 세션에서 안 죽는 칸"* 이다. **남는 축은 행 수·통계·`ANALYZE` 셋**이고, ~~거기에
   **「무엇이 세션마다 계획을 뒤집는가」**(계획 §후속 52)가 더해진다~~ — 행 수가 고정된 채로도
   계획이 갈리므로, 이 항목이 전제한 *"임계"* 는 행 수 축 하나가 아니다.
   **[정정 — 리뷰 재심사 라운드 4] 취소선 안의 *"더해진다"* 는 52 를 8 의 **부분**으로 읽게 하는데,
   계획 §후속 52 는 반대로 *"8 과 같은 자리가 아니다"*(= 분리)라고 적었다 — 두 문장이 포함관계를
   서로 반대로 말했다.** 맞춘 문안은 하나이고 양쪽에 같은 것을 싣는다:
   **물음은 다르고 축은 겹친다.**
   ① **물음이 다르다** — 8 은 **임계**(*"행 수를 키우면 언제부터 죽는가"*), 52 는 **원인**
   (*"행 수가 고정된 채로도 왜 세션마다 갈리는가"*). 산출물이 다르므로 **번호를 합치지 않는다.**
   ② **축이 겹친다** — 위에 적은 남는 축 셋 중 **「통계」와 그것을 움직이는 `ANALYZE`·autovacuum 은
   계획 §후속 52 가 의심한다고 적은 바로 그 축이다.** 겹치는 것은 **한 축**이고 행 수는 8 에만,
   *"무엇이 뒤집는가"* 라는 물음은 52 에만 있다.
   ③ 그러므로 **그 축을 태우는 커밋은 두 번호를 함께 적는다** — 한 번 태우면 두 항목에 함께 값이
   온다. (근거와 재계수는 계획 0012 §M-8-3 5.)
